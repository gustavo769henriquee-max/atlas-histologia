===== ATLAS DE HISTOLOGIA — INVENTÁRIO =====

===== ARQUIVOS DO PROJETO =====
.env
.env.example
.gitignore
aplicar-categoria-id.ps1
atualizar-editor.ps1
combo3.cjs
corrigir-admin.cjs
corrigir-combo3.cjs
corrigir-editor-lamina.cjs
corrigir-escopo-real.cjs
diagnostico-combo3.cjs
index.html
INVENTARIO_ATLAS.txt
package-lock.json
package.json
parte4-categorias.cjs
reparar-atlas.ps1
supabase-categorias.sql
.vscode\extensions.json
.vscode\settings.json
backup-editor-20260819-210237\main.js
backup-editor-20260819-210237\nova-lamina.js
backup-editor-20260819-210237\style.css
backup-reparador-completo-20260819-212337\.env
backup-reparador-completo-20260819-212337\src\admin.css
backup-reparador-completo-20260819-212337\src\main.backup-admin-20260819-165208.js
backup-reparador-completo-20260819-212337\src\main.backup-admin-route-final.js
backup-reparador-completo-20260819-212337\src\main.backup-auth-session-final.js
backup-reparador-completo-20260819-212337\src\main.backup-clique-20260819-172639.js
backup-reparador-completo-20260819-212337\src\main.backup-clique-20260819-173237.js
backup-reparador-completo-20260819-212337\src\main.backup-clique-20260819-173303.js
backup-reparador-completo-20260819-212337\src\main.backup-clique-direto-20260819-173141.js
backup-reparador-completo-20260819-212337\src\main.backup-clique-direto-20260819-173410.js
backup-reparador-completo-20260819-212337\src\main.backup-clique-final-20260819-172753.js
backup-reparador-completo-20260819-212337\src\main.backup-clique-final-20260819-175706.js
backup-reparador-completo-20260819-212337\src\main.backup-clique-lateral-20260819-175013.js
backup-reparador-completo-20260819-212337\src\main.backup-clique-OSD-20260819-180616.js
backup-reparador-completo-20260819-212337\src\main.backup-clique-robusto-20260819-175100.js
backup-reparador-completo-20260819-212337\src\main.backup-conexao-final-20260819-172939.js
backup-reparador-completo-20260819-212337\src\main.backup-corrige-clique-20260819-174410.js
backup-reparador-completo-20260819-212337\src\main.backup-diagnostico-20260819-170054.js
backup-reparador-completo-20260819-212337\src\main.backup-final-20260819-172150.js
backup-reparador-completo-20260819-212337\src\main.backup-funcao-clique-20260819-173807.js
backup-reparador-completo-20260819-212337\src\main.backup-login-session.js
backup-reparador-completo-20260819-212337\src\main.backup-marcadores-20260819-170509.js
backup-reparador-completo-20260819-212337\src\main.backup-marcadores-css-20260819-172103.js
backup-reparador-completo-20260819-212337\src\main.backup-session-final-v2.js
backup-reparador-completo-20260819-212337\src\main.backup-session-safe.js
backup-reparador-completo-20260819-212337\src\main.backup.js
backup-reparador-completo-20260819-212337\src\main.js
backup-reparador-completo-20260819-212337\src\main.js.backup
backup-reparador-completo-20260819-212337\src\main.js.backup-20260819-002235
backup-reparador-completo-20260819-212337\src\main.js.backup-20260819-002242
backup-reparador-completo-20260819-212337\src\main.js.backup-antes-botoes-final.js
backup-reparador-completo-20260819-212337\src\main.js.backup-antes-conexao-final.js
backup-reparador-completo-20260819-212337\src\main.js.backup-clique-final-20260819-133709.js
backup-reparador-completo-20260819-212337\src\main.js.backup-clique-lista-20260819-144217.js
backup-reparador-completo-20260819-212337\src\main.js.backup-duplicata-mostrarTextoEstrutura.js
backup-reparador-completo-20260819-212337\src\main.js.backup-fix-zoom-home-20260819-152415.js
backup-reparador-completo-20260819-212337\src\main.js.backup-indice-botoes-20260819-144503.js
backup-reparador-completo-20260819-212337\src\main.js.backup-indice-correto-20260819-144535.js
backup-reparador-completo-20260819-212337\src\main.js.backup-info-simples-20260819-134257.js
backup-reparador-completo-20260819-212337\src\main.js.backup-limpeza-lista-20260819-145306.js
backup-reparador-completo-20260819-212337\src\main.js.backup-lista-info-20260819-140846.js
backup-reparador-completo-20260819-212337\src\main.js.backup-lista-info-20260819-140848.js
backup-reparador-completo-20260819-212337\src\main.js.backup-marcacoes
backup-reparador-completo-20260819-212337\src\main.js.backup-recolocar-home-20260819-151210
backup-reparador-completo-20260819-212337\src\main.js.backup-recolocar-home-20260819-151214
backup-reparador-completo-20260819-212337\src\main.js.backup-recolocar-home-20260819-151232
backup-reparador-completo-20260819-212337\src\main.js.backup-recolocar-zoom-home-20260819-151146
backup-reparador-completo-20260819-212337\src\main.js.backup-refino-painel-20260819-145526.js
backup-reparador-completo-20260819-212337\src\main.js.backup-remover-botoes-reset-20260819-150956
backup-reparador-completo-20260819-212337\src\main.js.backup-seguranca-20260819-133801.js
backup-reparador-completo-20260819-212337\src\main.js.backup-texto-20260819-134401.js
backup-reparador-completo-20260819-212337\src\main.js.backup-texto-20260819-134408.js
backup-reparador-completo-20260819-212337\src\main.js.backup-visual-estudo-20260819-145758.js
backup-reparador-completo-20260819-212337\src\main.js.backup-webgl
backup-reparador-completo-20260819-212337\src\main.js.backup2
backup-reparador-completo-20260819-212337\src\main.js.backup4
backup-reparador-completo-20260819-212337\src\main.js.backup_20260819_110021
backup-reparador-completo-20260819-212337\src\main.js.backup_20260819_110035
backup-reparador-completo-20260819-212337\src\main.js.backup_combo5_20260819_110206
backup-reparador-completo-20260819-212337\src\main.js.backup_combo5_20260819_110231
backup-reparador-completo-20260819-212337\src\main.js.backup_combo5_20260819_110323
backup-reparador-completo-20260819-212337\src\main.js.backup_combo6_20260819_111112
backup-reparador-completo-20260819-212337\src\main.js.backup_combo7_20260819_111428
backup-reparador-completo-20260819-212337\src\main.js.backup_combo7_20260819_111449
backup-reparador-completo-20260819-212337\src\main.js.backup_combo7_20260819_111934
backup-reparador-completo-20260819-212337\src\main.js.backup_combo7_click_20260819_112345
backup-reparador-completo-20260819-212337\src\main.js.backup_combo7_click_20260819_112347
backup-reparador-completo-20260819-212337\src\main.js.backup_combo7_delegacao
backup-reparador-completo-20260819-212337\src\main.js.backup_combo7_fix
backup-reparador-completo-20260819-212337\src\main.js.backup_combo7_reparo_20260819_113017
backup-reparador-completo-20260819-212337\src\main.js.backup_correcao_combo7_20260819_112144
backup-reparador-completo-20260819-212337\src\main.js.combo10-backup.js
backup-reparador-completo-20260819-212337\src\main.js.combo12-backup.js
backup-reparador-completo-20260819-212337\src\main.js.combo13-backup.js
backup-reparador-completo-20260819-212337\src\main.js.combo14-backup.js
backup-reparador-completo-20260819-212337\src\main.js.combo15-backup.js
backup-reparador-completo-20260819-212337\src\main.js.combo7-3cliques-backup.js
backup-reparador-completo-20260819-212337\src\main.js.combo7-backup.js
backup-reparador-completo-20260819-212337\src\main.js.combo7-clique-backup.js
backup-reparador-completo-20260819-212337\src\main.js.combo7-clique-final-backup.js
backup-reparador-completo-20260819-212337\src\main.js.combo7-clique2-backup.js
backup-reparador-completo-20260819-212337\src\main.js.combo7-correcao-nome-backup.js
backup-reparador-completo-20260819-212337\src\main.js.combo7-final-backup.js
backup-reparador-completo-20260819-212337\src\main.js.combo7-limpeza-backup.js
backup-reparador-completo-20260819-212337\src\main.js.combo7-listener-backup.js
backup-reparador-completo-20260819-212337\src\main.js.combo7-seguranca-backup.js
backup-reparador-completo-20260819-212337\src\main.js.combo8-1-backup.js
backup-reparador-completo-20260819-212337\src\main.js.combo8-2-backup.js
backup-reparador-completo-20260819-212337\src\main.js.combo8-backup.js
backup-reparador-completo-20260819-212337\src\main.js.combo9-backup.js
backup-reparador-completo-20260819-212337\src\style.css
backup-reparador-completo-20260819-212337\src\style.css.backup
backup-reparador-completo-20260819-212337\src\style.css.backup-20260819-002235
backup-reparador-completo-20260819-212337\src\style.css.backup-20260819-002242
backup-reparador-completo-20260819-212337\src\style.css.backup-marcacoes
backup-reparador-completo-20260819-212337\src\style.css.backup-modo-estudo-20260819-145850.css
backup-reparador-completo-20260819-212337\src\style.css.backup-painel-final-20260819-150052.css
backup-reparador-completo-20260819-212337\src\style.css.backup_20260819_110021
backup-reparador-completo-20260819-212337\src\style.css.backup_20260819_110035
backup-reparador-completo-20260819-212337\src\style.css.backup_combo5_20260819_110206
backup-reparador-completo-20260819-212337\src\style.css.backup_combo5_20260819_110231
backup-reparador-completo-20260819-212337\src\style.css.backup_combo5_20260819_110323
backup-reparador-completo-20260819-212337\src\style.css.backup_combo6_20260819_111112
backup-reparador-completo-20260819-212337\src\style.css.backup_combo7_20260819_111428
backup-reparador-completo-20260819-212337\src\style.css.backup_combo7_20260819_111449
backup-reparador-completo-20260819-212337\src\style.css.backup_combo7_20260819_111934
backup-reparador-completo-20260819-212337\src\assets\hero.png
backup-reparador-completo-20260819-212337\src\assets\vite.svg
backup-reparador-completo-20260819-212337\src\data\laminas.js
backup-reparador-completo-20260819-212337\src\lib\supabase.backup-admin-20260819-165208.js
backup-reparador-completo-20260819-212337\src\lib\supabase.js
backup-reparador-completo-20260819-212337\src\pages\admin-style-import.js
backup-reparador-completo-20260819-212337\src\pages\admin.backup-admin-20260819-165208.js
backup-reparador-completo-20260819-212337\src\pages\admin.css
backup-reparador-completo-20260819-212337\src\pages\admin.js
backup-reparador-completo-20260819-212337\src\pages\admin.js.backup-parte4-1787174572949
backup-reparador-completo-20260819-212337\src\pages\aparencia.js
backup-reparador-completo-20260819-212337\src\pages\catalogo.js
backup-reparador-completo-20260819-212337\src\pages\catalogo.js.backup-categoria-id-20260819-185416.js
backup-reparador-completo-20260819-212337\src\pages\catalogo.js.backup-combo1-20260819-185011
backup-reparador-completo-20260819-212337\src\pages\catalogo.js.backup-combo2-20260819-185814.js
backup-reparador-completo-20260819-212337\src\pages\catalogo.js.backup-combo3-1787180084637.js
backup-reparador-completo-20260819-212337\src\pages\catalogo.js.backup-escopo-real-1787180937384.js
backup-reparador-completo-20260819-212337\src\pages\login.js
backup-reparador-completo-20260819-212337\src\pages\nova-lamina.backup-fix-estruturas.js
backup-reparador-completo-20260819-212337\src\pages\nova-lamina.backup-remove-estrutura-loader.js
backup-reparador-completo-20260819-212337\src\pages\nova-lamina.backup.js
backup-reparador-completo-20260819-212337\src\pages\nova-lamina.js
backup-reparador-completo-20260819-212337\src\pages\nova-lamina.js.backup
backup-reparador-completo-20260819-212337\src\pages\nova-lamina.js.backup-1787174424459
backup-reparador-completo-20260819-212337\src\pages\nova-lamina.js.backup-20260819-002235
backup-reparador-completo-20260819-212337\src\pages\nova-lamina.js.backup-20260819-002242
backup-reparador-completo-20260819-212337\src\pages\nova-lamina.js.backup-20260819-203508
backup-reparador-completo-20260819-212337\src\pages\nova-lamina.js.backup-editor-visual
backup-reparador-completo-20260819-212337\src\pages\nova-lamina.js.backup-parte4-1787174572950
backup-reparador-completo-20260819-212337\src\pages\nova-lamina.js.bak
backup-reparo-20260819-212048\main.js
backup-reparo-20260819-212048\nova-lamina.js
backup-reparo-20260819-212048\backup-editor-20260819-210237\style.css
backup-reparo-20260819-212048\src\admin.css
backup-reparo-20260819-212048\src\style.css
backup-reparo-20260819-212048\src\style.css.backup-modo-estudo-20260819-145850.css
backup-reparo-20260819-212048\src\style.css.backup-painel-final-20260819-150052.css
backup-reparo-20260819-212048\src\pages\admin.css
public\favicon.svg
public\icons.svg
public\images\laminas\teste\lamina-teste.png
src\admin.css
src\main.backup-admin-20260819-165208.js
src\main.backup-admin-route-final.js
src\main.backup-auth-session-final.js
src\main.backup-clique-20260819-172639.js
src\main.backup-clique-20260819-173237.js
src\main.backup-clique-20260819-173303.js
src\main.backup-clique-direto-20260819-173141.js
src\main.backup-clique-direto-20260819-173410.js
src\main.backup-clique-final-20260819-172753.js
src\main.backup-clique-final-20260819-175706.js
src\main.backup-clique-lateral-20260819-175013.js
src\main.backup-clique-OSD-20260819-180616.js
src\main.backup-clique-robusto-20260819-175100.js
src\main.backup-conexao-final-20260819-172939.js
src\main.backup-corrige-clique-20260819-174410.js
src\main.backup-diagnostico-20260819-170054.js
src\main.backup-final-20260819-172150.js
src\main.backup-funcao-clique-20260819-173807.js
src\main.backup-login-session.js
src\main.backup-marcadores-20260819-170509.js
src\main.backup-marcadores-css-20260819-172103.js
src\main.backup-session-final-v2.js
src\main.backup-session-safe.js
src\main.backup.js
src\main.js
src\main.js.backup
src\main.js.backup-20260819-002235
src\main.js.backup-20260819-002242
src\main.js.backup-antes-botoes-final.js
src\main.js.backup-antes-conexao-final.js
src\main.js.backup-clique-final-20260819-133709.js
src\main.js.backup-clique-lista-20260819-144217.js
src\main.js.backup-duplicata-mostrarTextoEstrutura.js
src\main.js.backup-fix-zoom-home-20260819-152415.js
src\main.js.backup-indice-botoes-20260819-144503.js
src\main.js.backup-indice-correto-20260819-144535.js
src\main.js.backup-info-simples-20260819-134257.js
src\main.js.backup-limpeza-lista-20260819-145306.js
src\main.js.backup-lista-info-20260819-140846.js
src\main.js.backup-lista-info-20260819-140848.js
src\main.js.backup-marcacoes
src\main.js.backup-recolocar-home-20260819-151210
src\main.js.backup-recolocar-home-20260819-151214
src\main.js.backup-recolocar-home-20260819-151232
src\main.js.backup-recolocar-zoom-home-20260819-151146
src\main.js.backup-refino-painel-20260819-145526.js
src\main.js.backup-remover-botoes-reset-20260819-150956
src\main.js.backup-seguranca-20260819-133801.js
src\main.js.backup-texto-20260819-134401.js
src\main.js.backup-texto-20260819-134408.js
src\main.js.backup-visual-estudo-20260819-145758.js
src\main.js.backup-webgl
src\main.js.backup2
src\main.js.backup4
src\main.js.backup_20260819_110021
src\main.js.backup_20260819_110035
src\main.js.backup_combo5_20260819_110206
src\main.js.backup_combo5_20260819_110231
src\main.js.backup_combo5_20260819_110323
src\main.js.backup_combo6_20260819_111112
src\main.js.backup_combo7_20260819_111428
src\main.js.backup_combo7_20260819_111449
src\main.js.backup_combo7_20260819_111934
src\main.js.backup_combo7_click_20260819_112345
src\main.js.backup_combo7_click_20260819_112347
src\main.js.backup_combo7_delegacao
src\main.js.backup_combo7_fix
src\main.js.backup_combo7_reparo_20260819_113017
src\main.js.backup_correcao_combo7_20260819_112144
src\main.js.combo10-backup.js
src\main.js.combo12-backup.js
src\main.js.combo13-backup.js
src\main.js.combo14-backup.js
src\main.js.combo15-backup.js
src\main.js.combo7-3cliques-backup.js
src\main.js.combo7-backup.js
src\main.js.combo7-clique-backup.js
src\main.js.combo7-clique-final-backup.js
src\main.js.combo7-clique2-backup.js
src\main.js.combo7-correcao-nome-backup.js
src\main.js.combo7-final-backup.js
src\main.js.combo7-limpeza-backup.js
src\main.js.combo7-listener-backup.js
src\main.js.combo7-seguranca-backup.js
src\main.js.combo8-1-backup.js
src\main.js.combo8-2-backup.js
src\main.js.combo8-backup.js
src\main.js.combo9-backup.js
src\style.css
src\style.css.backup
src\style.css.backup-20260819-002235
src\style.css.backup-20260819-002242
src\style.css.backup-marcacoes
src\style.css.backup-modo-estudo-20260819-145850.css
src\style.css.backup-painel-final-20260819-150052.css
src\style.css.backup_20260819_110021
src\style.css.backup_20260819_110035
src\style.css.backup_combo5_20260819_110206
src\style.css.backup_combo5_20260819_110231
src\style.css.backup_combo5_20260819_110323
src\style.css.backup_combo6_20260819_111112
src\style.css.backup_combo7_20260819_111428
src\style.css.backup_combo7_20260819_111449
src\style.css.backup_combo7_20260819_111934
src\assets\hero.png
src\assets\vite.svg
src\data\laminas.js
src\lib\supabase.backup-admin-20260819-165208.js
src\lib\supabase.js
src\pages\admin-style-import.js
src\pages\admin.backup-admin-20260819-165208.js
src\pages\admin.css
src\pages\admin.js
src\pages\admin.js.backup-parte4-1787174572949
src\pages\aparencia.js
src\pages\catalogo.js
src\pages\catalogo.js.backup-categoria-id-20260819-185416.js
src\pages\catalogo.js.backup-combo1-20260819-185011
src\pages\catalogo.js.backup-combo2-20260819-185814.js
src\pages\catalogo.js.backup-combo3-1787180084637.js
src\pages\catalogo.js.backup-escopo-real-1787180937384.js
src\pages\login.js
src\pages\nova-lamina.backup-fix-estruturas.js
src\pages\nova-lamina.backup-remove-estrutura-loader.js
src\pages\nova-lamina.backup.js
src\pages\nova-lamina.js
src\pages\nova-lamina.js.backup
src\pages\nova-lamina.js.backup-1787174424459
src\pages\nova-lamina.js.backup-20260819-002235
src\pages\nova-lamina.js.backup-20260819-002242
src\pages\nova-lamina.js.backup-20260819-203508
src\pages\nova-lamina.js.backup-20260820-105741
src\pages\nova-lamina.js.backup-antes-correcao-20260820-184038
src\pages\nova-lamina.js.backup-antes-restauracao-20260820-183821
src\pages\nova-lamina.js.backup-antes-restauracao-20260820-183828
src\pages\nova-lamina.js.backup-before-form-fix-20260820-121021
src\pages\nova-lamina.js.backup-before-form-link-20260820-115431
src\pages\nova-lamina.js.backup-before-form-link-20260820-115704
src\pages\nova-lamina.js.backup-editor-20260820-123613
src\pages\nova-lamina.js.backup-editor-call-20260820-183658
src\pages\nova-lamina.js.backup-editor-visual
src\pages\nova-lamina.js.backup-encoding-20260820-184435
src\pages\nova-lamina.js.backup-estrutura-editor-20260820-183236
src\pages\nova-lamina.js.backup-estrutura-editor-20260820-183332
src\pages\nova-lamina.js.backup-estrutura-editor-20260820-183336
src\pages\nova-lamina.js.backup-estruturas-20260820-184622
src\pages\nova-lamina.js.backup-estruturas-final-20260820-184711
src\pages\nova-lamina.js.backup-estruturas-limpeza-20260820-184803
src\pages\nova-lamina.js.backup-parte4-1787174572950
src\pages\nova-lamina.js.backup-ponto-definitivo-20260820-191911
src\pages\nova-lamina.js.backup-ponto-editor-20260820-185811
src\pages\nova-lamina.js.backup-ponto-seguro-20260820-191939
src\pages\nova-lamina.js.backup-posicao-estrutura-20260820-190519
src\pages\nova-lamina.js.bak
supabase\.temp\cli-latest
supabase\.temp\gotrue-version
supabase\.temp\linked-project.json
supabase\.temp\pooler-url
supabase\.temp\postgres-version
supabase\.temp\project-ref
supabase\.temp\rest-version
supabase\.temp\storage-migration
supabase\.temp\storage-version
supabase\migrations\20260818152138_create_laminas_storage.sql
supabase\migrations\20260818152247_fix_laminas_rls.sql

===== PACKAGE.JSON =====
{
  "name": "atlas-histologia",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.2.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "openseadragon": "^6.1.0"
  }
}


===== index.html =====
<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Atlas Histolï¿½gico Interativo" />
    <title>Atlas Histolï¿½gico</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.js"></script>
  </body>
</html>


===== backup-editor-20260819-210237\main.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'


const app = document.querySelector('#app')


/* ============================================================
   CONFIGURAÇÕES VISUAIS
   ============================================================ */

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}


async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {

    console.error(
      'Erro ao carregar configurações do site:',
      error
    )

    return {}
  }

  return data || {}

}


/* ============================================================
   ROTAS
   ============================================================ */

function getRoute() {

  const hash =
    window.location.hash

  if (hash === '#login') {
    return 'login'
  }

  if (hash === '#admin') {
    return 'admin'
  }

  if (hash.startsWith('#nova-lamina')) {
    return 'nova-lamina'
  }

  if (hash === '#laminas') {
    return 'catalogo'
  }

  if (hash.startsWith('#lamina/')) {
    return 'lamina'
  }

  return 'home'

}


/* ============================================================
   HEADER
   ============================================================ */

function renderHeader(config = {}) {

  return `
    <header class="header">

      <div class="container header-content">

        <a href="#inicio" class="brand">

          <span
            class="brand-icon"
            style="
              width: ${Number(config.logo_tamanho || 100) * 0.42}px;
              height: ${Number(config.logo_tamanho || 100) * 0.42}px;
            "
          >

            ${
              config.logo_url
                ? `
                  <img
                    src="${escapeHtml(config.logo_url)}"
                    alt="Logo"
                    style="
                      width:100%;
                      height:100%;
                      object-fit:contain;
                    "
                  >
                `
                : '🔬'
            }

          </span>

          <span>

            <strong>
              ${escapeHtml(config.nome_site || 'Atlas')}
            </strong>

            <small>
              ${escapeHtml(config.subtitulo || 'Histológico')}
            </small>

          </span>

        </a>


        <nav class="nav">

          <a href="#inicio">
            Início
          </a>

          <a href="#laminas">
            Lâminas
          </a>

          <a href="#sobre">
            Sobre
          </a>

          <a href="#login">
            Administração
          </a>

        </nav>

      </div>

    </header>
  `

}


/* ============================================================
   HOME
   ============================================================ */

function renderHome(config = {}) {

  return `
    ${renderHeader(config)}

    <main>

      <section
        class="hero"
        id="inicio"
      >

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(
                config.titulo_inicio ||
                'Explore o mundo microscópico.'
              )}
            </h1>

            <p>
              ${escapeHtml(
                config.descricao_inicio ||
                'Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.'
              )}
            </p>

            <div class="hero-actions">

              <a
                href="#laminas"
                class="button primary"
              >
                ${escapeHtml(
                  config.texto_botao_principal ||
                  'Explorar ' +
                  (config.nome_site || 'Atlas')
                )}

                <span>
                  →
                </span>

              </a>

              <a
                href="#sobre"
                class="button secondary"
              >
                ${escapeHtml(
                  config.texto_botao_secundario ||
                  'Conheça o projeto'
                )}
              </a>

            </div>

          </div>


          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section
        class="section"
        id="sobre"
      >

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>


          <div class="about-text">

            <p>
              ${escapeHtml(
                config.texto_sobre ||
                (
                  'O ' +
                  (config.nome_site || 'Atlas') +
                  ' ' +
                  (config.subtitulo || 'Histológico') +
                  ' foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa.'
                )
              )}
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>
          🔬
          ${escapeHtml(config.nome_site || 'Atlas')}
          ${escapeHtml(config.subtitulo || 'Histológico')}
        </span>

        <span>
          ${escapeHtml(
            config.texto_rodape ||
            'Projeto acadêmico'
          )}
        </span>

      </div>

    </footer>
  `

}


/* ============================================================
   ESCAPE HTML
   ============================================================ */

function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


/* ============================================================
   RENDER DA LÂMINA
   ============================================================ */

function renderLamina(lamina, config = {}) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a
            href="#laminas"
            class="back-link"
          >
            ← Voltar para as lâminas
          </a>


          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(
                  lamina.categoria ||
                  'Histologia'
                )}
              </span>

              <h1>
                ${escapeHtml(
                  lamina.nome ||
                  'Lâmina'
                )}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>


            <div class="slide-meta">

              ${
                lamina.tecnica
                  ? `
                    <span>
                      🔬
                      ${escapeHtml(lamina.tecnica)}
                    </span>
                  `
                  : ''
              }

              ${
                lamina.coloracao
                  ? `
                    <span>
                      🧫
                      ${escapeHtml(lamina.coloracao)}
                    </span>
                  `
                  : ''
              }

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">


          <div class="viewer-container">


            <!-- PAINEL DE ESTUDO -->

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>


              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>


              <p id="estrutura-estudo-descricao"></p>

            </div>


            <!-- VIEWER -->

            <div
              id="openseadragon"
              class="viewer"
            ></div>


            <div class="estrutura-navegacao"></div>


            <!-- CONTROLES -->

            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>


              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >
                  −
                </button>


                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >
                  ⌂
                </button>


                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >
                  +
                </button>


                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >
                  ⛶
                </button>


                <span
                  id="zoom-value"
                  class="zoom-value"
                >
                  100%
                </span>

              </div>

            </div>

          </div>


          <!-- PAINEL LATERAL -->

          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>


            <h2>
              ${escapeHtml(
                lamina.nome ||
                'Lâmina'
              )}
            </h2>


            <p class="description">
              ${escapeHtml(
                lamina.descricao ||
                ''
              )}
            </p>


            ${
              estruturas.length
                ? `
                  <div class="info-section">

                    <h3>
                      🔎 O que observar
                    </h3>


                    <div class="structure-list">

                      ${
                        estruturas
                          .map(
                            (estrutura, index) => `
                              <button
                                class="structure"
                                data-estrutura-index="${index}"
                                type="button"
                              >

                                <span class="structure-dot"></span>

                                <span>

                                  <strong>
                                    ${escapeHtml(
                                      estrutura.nome ||
                                      ''
                                    )}
                                  </strong>

                                  <small>
                                    ${escapeHtml(
                                      estrutura.descricao ||
                                      ''
                                    )}
                                  </small>

                                </span>

                              </button>
                            `
                          )
                          .join('')
                      }

                    </div>

                  </div>
                `
                : ''
            }

          </aside>

        </div>

      </section>

    </main>
  `

}


/* ============================================================
   BUSCAR LÂMINA
   ============================================================ */

async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data

}


/* ============================================================
   PAINEL DE ESTUDO
   ============================================================ */

function mostrarTextoEstrutura(
  index,
  lamina
) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []


  const indice =
    Number(index)


  const estrutura =
    estruturas[indice]


  if (!estrutura) {

    console.warn(
      'Estrutura não encontrada:',
      index
    )

    return

  }


  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )


  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )


  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )


  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo não encontrado.'
    )

    return

  }


  nome.textContent =
    estrutura.nome ||
    `Estrutura ${indice + 1}`


  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'


  panel.hidden = false


  panel.dataset.estruturaIndex =
    String(indice)


  document
    .querySelectorAll('.estrutura-overlay')
    .forEach(overlay => {

      overlay.classList.toggle(
        'estrutura-selecionada',
        overlay.dataset.estruturaIndex ===
        String(indice)
      )

    })


  document
    .querySelectorAll('.structure')
    .forEach((botao, botaoIndex) => {

      botao.classList.toggle(
        'estrutura-selecionada',
        botaoIndex === indice
      )

    })


  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}


/* ============================================================
   DESENHAR ESTRUTURAS
   ============================================================ */

function desenharEstruturas(
  viewer,
  lamina
) {

  if (!viewer || !lamina) {

    console.warn(
      'Viewer ou lâmina ausente.'
    )

    return

  }


  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  console.log(
    '========================================'
  )

  console.log(
    'DESENHAR ESTRUTURAS'
  )

  console.log(
    'Estruturas:',
    estruturas
  )

  console.log(
    'Quantidade:',
    estruturas.length
  )

  console.log(
    '========================================'
  )


  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return

  }


  const tiledImage =
    viewer.world.getItemAt(0)


  console.log(
    'TILED IMAGE:',
    tiledImage
  )


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return

  }


  const imageSize =
    tiledImage.getContentSize()


  console.log(
    'IMAGE SIZE:',
    imageSize
  )


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !Number.isFinite(imageWidth) ||
    !Number.isFinite(imageHeight) ||
    imageWidth <= 0 ||
    imageHeight <= 0
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return

  }


  /*
   * Remove somente os marcadores antigos.
   * Não remove overlays de outros tipos.
   */

  viewer
    .currentOverlays
    .slice()
    .forEach(overlay => {

      const elemento =
        overlay?.element


      if (
        elemento?.classList?.contains(
          'estrutura-overlay'
        )
      ) {

        try {

          viewer.removeOverlay(
            elemento
          )

        } catch (erro) {

          console.warn(
            'Erro removendo marcador antigo:',
            erro
          )

        }

      }

    })


  estruturas.forEach(
    (estrutura, index) => {

      console.log(
        'CRIANDO MARCADOR:',
        index,
        estrutura
      )


      console.log(
        'COORDENADAS ORIGINAIS:',
        estrutura.x,
        estrutura.y,
        estrutura.largura,
        estrutura.altura
      )


      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao ||
          ''
        )


      /*
       * Coordenadas normalizadas:
       * 0 = início da imagem
       * 1 = final da imagem
       */

      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return

      }


      /*
       * Limita as coordenadas ao intervalo 0–1.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        )


      y =
        Math.max(
          0,
          Math.min(1, y)
        )


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        )


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        )


      /*
       * Converte normalizado -> pixels.
       */

      const imageX =
        x * imageWidth


      const imageY =
        y * imageHeight


      const imageWidthEstrutura =
        largura * imageWidth


      const imageHeightEstrutura =
        altura * imageHeight


      /*
       * Converte pixels -> viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          imageX,
          imageY,
          imageWidthEstrutura,
          imageHeightEstrutura
        )


      if (!rect) {

        console.warn(
          'Não foi possível criar retângulo:',
          estrutura
        )

        return

      }


      /*
       * Cria elemento visual.
       */

      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)


      overlay.dataset.imageX =
        String(imageX)


      overlay.dataset.imageY =
        String(imageY)


      overlay.dataset.imageWidth =
        String(imageWidthEstrutura)


      overlay.dataset.imageHeight =
        String(imageHeightEstrutura)


      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      overlay.title =
        descricao
          ? `${nome}: ${descricao}`
          : nome


      overlay.style.cursor =
        'pointer'


      /*
       * Clique diretamente no marcador.
       */

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          mostrarTextoEstrutura(
            index,
            lamina
          )


          centralizarEstrutura(
            viewer,
            overlay,
            true
          )

        }
      )


      /*
       * Adiciona o marcador ao OpenSeadragon.
       */

      viewer.addOverlay({

        element: overlay,

        location: rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

      })


      console.log(
        'MARCADOR CRIADO:',
        index,
        rect
      )

    }
  )


  console.log(
    'Total de marcadores criados:',
    document.querySelectorAll(
      '.estrutura-overlay'
    ).length
  )

}


/* ============================================================
   CENTRALIZAR ESTRUTURA
   ============================================================ */

function centralizarEstrutura(
  viewer,
  overlay,
  aplicarZoom = true
) {

  if (!viewer || !overlay) {
    return
  }


  const x =
    Number(
      overlay.dataset.imageX
    )


  const y =
    Number(
      overlay.dataset.imageY
    )


  const largura =
    Number(
      overlay.dataset.imageWidth
    )


  const altura =
    Number(
      overlay.dataset.imageHeight
    )


  if (
    !Number.isFinite(x) ||
    !Number.isFinite(y)
  ) {

    return

  }


  const centroX =
    x +
    (
      Number.isFinite(largura)
        ? largura / 2
        : 0
    )


  const centroY =
    y +
    (
      Number.isFinite(altura)
        ? altura / 2
        : 0
    )


  const ponto =
    viewer.viewport.imageToViewportCoordinates(
      centroX,
      centroY
    )


  viewer.viewport.panTo(
    ponto,
    true
  )


  if (!aplicarZoom) {
    return
  }


  const zoomAtual =
    viewer.viewport.getZoom()


  const zoomMaximo =
    viewer.viewport.getMaxZoom()


  const zoomDesejado =
    Math.min(
      zoomAtual * 1.8,
      zoomMaximo
    )


  viewer.viewport.zoomTo(
    zoomDesejado,
    ponto,
    true
  )

}


/* ============================================================
   MODO ESTUDO
   ============================================================ */

function configurarModoEstudo(
  viewer,
  lamina
) {

  if (!viewer || !lamina) {
    return
  }


  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )


  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )


  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )


  const fechar =
    document.querySelector(
      '#estrutura-estudo-fechar'
    )


  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo não encontrado.'
    )

    return

  }


  let indiceAtual =
    -1


  function limparSelecao() {

    document
      .querySelectorAll(
        '.estrutura-overlay'
      )
      .forEach(
        overlay => {

          overlay.classList.remove(
            'estrutura-selecionada'
          )

        }
      )


    document
      .querySelectorAll(
        '.structure'
      )
      .forEach(
        botao => {

          botao.classList.remove(
            'estrutura-selecionada'
          )

        }
      )

  }


  function selecionarVisual(index) {

    const indice =
      String(index)


    document
      .querySelectorAll(
        '.estrutura-overlay'
      )
      .forEach(
        overlay => {

          overlay.classList.toggle(
            'estrutura-selecionada',
            overlay.dataset.estruturaIndex ===
            indice
          )

        }
      )


    document
      .querySelectorAll(
        '.structure'
      )
      .forEach(
        (botao, botaoIndex) => {

          botao.classList.toggle(
            'estrutura-selecionada',
            String(botaoIndex) === indice
          )

        }
      )

  }


  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      Number(index)


    const estrutura =
      estruturas[indice]


    if (!estrutura) {
      return
    }


    indiceAtual =
      indice


    nome.textContent =
      estrutura.nome ||
      `Estrutura ${indice + 1}`


    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'


    panel.hidden =
      false


    panel.dataset.estruturaIndex =
      String(indice)


    selecionarVisual(
      indice
    )


    if (centralizar) {

      const overlay =
        document.querySelector(
          `.estrutura-overlay[data-estrutura-index="${indice}"]`
        )


      if (overlay) {

        centralizarEstrutura(
          viewer,
          overlay,
          true
        )

      }

    }

  }


  function fecharPainel() {

    panel.hidden =
      true


    indiceAtual =
      -1


    limparSelecao()

  }


  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )


  /*
   * Clique na lista.
   *
   * IMPORTANTE:
   * Este é o único controlador de clique
   * necessário para os botões .structure.
   */

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, index) => {

        botao.dataset.estruturaIndex =
          String(index)


        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()


            mostrarEstrutura(
              index,
              true
            )

          }
        )

      }
    )


  /*
   * Teclado.
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }


      if (
        panel.hidden ||
        !estruturas.length
      ) {

        return

      }


      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {

        return

      }


      event.preventDefault()


      if (indiceAtual < 0) {

        mostrarEstrutura(
          0,
          true
        )

        return

      }


      let novoIndice =
        indiceAtual


      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1


        if (
          novoIndice >=
          estruturas.length
        ) {

          novoIndice =
            0

        }

      } else {

        novoIndice =
          indiceAtual - 1


        if (novoIndice < 0) {

          novoIndice =
            estruturas.length - 1

        }

      }


      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )


  console.log(
    'Modo estudo conectado:',
    estruturas.length,
    'estruturas.'
  )

}


/* ============================================================
   VIEWER
   ============================================================ */

function iniciarViewer(lamina) {

  const elemento =
    document.querySelector(
      '#openseadragon'
    )


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return

  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `

      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">

        Imagem da lâmina não encontrada.

      </div>

    `

    return

  }


  /*
   * Criação do OpenSeadragon.
   */

  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url:
          lamina.imagem_url

      },

      showNavigationControl:
        false,

      animationTime:
        0.8,

      zoomPerScroll:
        1.5,

      maxZoomPixelRatio:
        8,

      visibilityRatio:
        1,

      constrainDuringPan:
        true

    })


  /*
   * Zoom.
   */

  const zoomValue =
    document.querySelector(
      '#zoom-value'
    )


  function atualizarZoom() {

    if (!zoomValue) {
      return
    }


    const zoom =
      viewer.viewport.getZoom()


    zoomValue.textContent =
      Math.round(
        zoom * 100
      ) + '%'

  }


  document
    .querySelector(
      '#zoom-in'
    )
    ?.addEventListener(
      'click',
      event => {

        event.preventDefault()

        viewer.viewport.zoomBy(
          1.5
        )

        viewer.viewport.applyConstraints()

        atualizarZoom()

      }
    )


  document
    .querySelector(
      '#zoom-out'
    )
    ?.addEventListener(
      'click',
      event => {

        event.preventDefault()

        viewer.viewport.zoomBy(
          0.67
        )

        viewer.viewport.applyConstraints()

        atualizarZoom()

      }
    )


  document
    .querySelector(
      '#zoom-home'
    )
    ?.addEventListener(
      'click',
      event => {

        event.preventDefault()

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document
    .querySelector(
      '#zoom-fullscreen'
    )
    ?.addEventListener(
      'click',
      event => {

        event.preventDefault()


        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            true
          )

        } else {

          viewer.setFullPage(
            true
          )

        }

      }
    )


  /*
   * Atualização do zoom.
   */

  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * ABERTURA DA IMAGEM
   *
   * Aqui existe APENAS UM handler.
   */

  viewer.addOnceHandler(
    'open',
    () => {

      console.log(
        '========================================'
      )

      console.log(
        'DIAGNÓSTICO DA LÂMINA NO OPEN'
      )

      console.log(
        'LÂMINA:',
        lamina
      )

      console.log(
        'ESTRUTURAS:',
        lamina?.estruturas
      )

      console.log(
        'TIPO:',
        typeof lamina?.estruturas
      )

      console.log(
        'É ARRAY:',
        Array.isArray(
          lamina?.estruturas
        )
      )

      console.log(
        'QUANTIDADE:',
        Array.isArray(
          lamina?.estruturas
        )
          ? lamina.estruturas.length
          : 'NÃO É ARRAY'
      )

      console.log(
        '========================================'
      )


      /*
       * Primeiro desenha os marcadores.
       */

      desenharEstruturas(
        viewer,
        lamina
      )


      /*
       * Depois conecta o modo estudo.
       */

      configurarModoEstudo(
        viewer,
        lamina
      )


      atualizarZoom()

    }
  )


  /*
   * Atalhos de teclado do viewer.
   */

  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {

        return

      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(
          1.25
        )

        viewer.viewport.applyConstraints()

        atualizarZoom()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(
          0.8
        )

        viewer.viewport.applyConstraints()

        atualizarZoom()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }

    }
  )


  atualizarZoom()


  /*
   * Guarda referência para diagnóstico.
   */

  window.__atlasViewer =
    viewer


  return viewer

}


/* ============================================================
   LIMPAR ESTRUTURAS
   ============================================================ */

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const overlays =
    viewer.currentOverlays || []


  overlays
    .slice()
    .forEach(
      overlay => {

        const elemento =
          overlay?.element


        if (
          elemento?.classList?.contains(
            'estrutura-overlay'
          )
        ) {

          try {

            viewer.removeOverlay(
              elemento
            )

          } catch (erro) {

            console.warn(
              'Erro ao remover estrutura:',
              erro
            )

          }

        }

      }
    )

}


/* ============================================================
   INFORMAÇÃO SIMPLES
   ============================================================ */

function mostrarInfoEstruturaSimples(
  index,
  lamina
) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []


  const indice =
    Number(index)


  const estrutura =
    estruturas[indice]


  if (!estrutura) {

    console.warn(
      'Estrutura não encontrada:',
      index
    )

    return

  }


  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )


  if (!area) {

    area =
      document.createElement(
        'div'
      )


    area.id =
      'estrutura-info-simples'


    area.style.cssText = `

      margin-top:16px;

      padding:20px;

      background:#ffffff;

      border:1px solid #dcdcdc;

      border-radius:12px;

      box-shadow:
        0 4px 16px
        rgba(0,0,0,0.08);

      position:relative;

      z-index:20;

    `


    const titulo =
      document.createElement(
        'h3'
      )


    titulo.id =
      'estrutura-info-simples-nome'


    titulo.style.cssText = `

      margin:
        0 35px 8px 0;

      font-size:20px;

    `


    const descricao =
      document.createElement(
        'p'
      )


    descricao.id =
      'estrutura-info-simples-descricao'


    descricao.style.cssText = `

      margin:0;

      line-height:1.6;

      color:#555;

    `


    const fechar =
      document.createElement(
        'button'
      )


    fechar.type =
      'button'


    fechar.textContent =
      '×'


    fechar.title =
      'Fechar'


    fechar.style.cssText = `

      position:absolute;

      top:10px;

      right:12px;

      border:none;

      background:transparent;

      font-size:26px;

      cursor:pointer;

      color:#777;

      line-height:1;

    `


    fechar.addEventListener(
      'click',
      () => {

        area.hidden =
          true

      }
    )


    area.appendChild(
      fechar
    )


    area.appendChild(
      titulo
    )


    area.appendChild(
      descricao
    )


    const viewer =
      document.querySelector(
        '#openseadragon'
      )


    if (
      viewer?.parentElement
    ) {

      viewer.parentElement.appendChild(
        area
      )

    } else {

      document.body.appendChild(
        area
      )

    }

  }


  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )


  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )


  if (
    !titulo ||
    !descricao
  ) {

    return

  }


  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${indice + 1}`


  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'


  area.hidden =
    false


  area.dataset.estruturaIndex =
    String(indice)


  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   INFORMAÇÃO DA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(
  index,
  lamina
) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []


  const indice =
    Number(index)


  const estrutura =
    estruturas[indice]


  if (!estrutura) {

    console.warn(
      'Estrutura não encontrada:',
      index
    )

    return

  }


  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )


  if (!area) {

    const layout =
      document.querySelector(
        '.viewer-layout'
      )


    if (!layout) {

      console.warn(
        'viewer-layout não encontrado.'
      )

      return

    }


    area =
      document.createElement(
        'div'
      )


    area.id =
      'estrutura-info-lista'


    area.style.cssText = `

      grid-column:1 / -1;

      margin-top:18px;

      padding:22px;

      background:white;

      border:1px solid #ddd;

      border-radius:14px;

      box-shadow:
        0 4px 15px
        rgba(0,0,0,.08);

    `


    layout.appendChild(
      area
    )

  }


  const nome =
    estrutura.nome ||
    `Estrutura ${indice + 1}`


  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'


  area.innerHTML = `

    <div style="
      font-size:11px;
      font-weight:700;
      letter-spacing:.08em;
      text-transform:uppercase;
      color:#777;
      margin-bottom:6px;
    ">
      Estrutura selecionada
    </div>


    <h3 style="
      margin:0 0 8px 0;
      font-size:21px;
    ">
      ${escapeHtml(nome)}
    </h3>


    <p style="
      margin:0;
      font-size:15px;
      line-height:1.6;
      color:#555;
    ">
      ${escapeHtml(descricao)}
    </p>

  `


  area.hidden =
    false


  console.log(
    'INFORMAÇÃO DA LISTA EXIBIDA:',
    nome
  )

}


/* ============================================================
   NAVEGAÇÃO DAS ESTRUTURAS
   ============================================================ */

function configurarNavegacaoEstruturas(
  viewer,
  lamina
) {

  if (!viewer || !lamina) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []


  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botões /',
    estruturas.length,
    'estruturas'
  )

}


/* ============================================================
   CONECTAR INFO DOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }


  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples ===
        '1'
      ) {

        return

      }


      overlay.dataset.infoSimples =
        '1'


      overlay.style.cursor =
        'pointer'


      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }
      )

    }
  )


  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}


/* ============================================================
   INSTALAR CLIQUE DA LISTA
   ============================================================ */

function instalarCliqueListaEstruturas(
  lamina
) {

  if (!lamina) {

    console.warn(
      'Clique lista: lâmina não encontrada.'
    )

    return

  }


  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, index) => {

        botao.dataset.estruturaIndex =
          String(index)

      }
    )


  console.log(
    'Índices da lista instalados:',
    document.querySelectorAll(
      '.structure'
    ).length
  )

}


/* ============================================================
   RENDER PRINCIPAL
   ============================================================ */

async function render() {

  try {

    /*
     * Configurações.
     */

    const config =
      await carregarConfiguracoesSite()


    /*
     * Aplica imediatamente.
     */

    aplicarConfiguracoesVisuais(
      config
    )


    /*
     * Rota atual.
     */

    const route =
      getRoute()


    console.log(
      'ROTA:',
      route
    )


    /* ========================================================
       LOGIN
       ======================================================== */

    if (
      route === 'login'
    ) {

      const {
        data: {
          session
        },
        error
      } =
        await supabase.auth.getSession()


      if (
        !error &&
        session?.user
      ) {

        window.location.hash =
          '#admin'

        return

      }


      app.innerHTML =
        renderLogin()


      setupLogin()


      return

    }


    /* ========================================================
       ADMIN
       ======================================================== */

    if (
      route === 'admin'
    ) {

      const autorizado =
        await isAdmin()


      if (!autorizado) {

        window.location.hash =
          '#login'

        return

      }


      app.innerHTML =
        await renderAdmin()


      setupAdmin()


      return

    }


    /* ========================================================
       NOVA LÂMINA
       ======================================================== */

    if (
      route === 'nova-lamina'
    ) {

      const autorizado =
        await isAdmin()


      if (!autorizado) {

        window.location.hash =
          '#login'

        return

      }


      app.innerHTML =
        renderNovaLamina()


      setupNovaLamina()


      return

    }


    /* ========================================================
       CATÁLOGO
       ======================================================== */

    if (
      route === 'catalogo'
    ) {

      await renderCatalogo()

      return

    }


    /* ========================================================
       LÂMINA
       ======================================================== */

    if (
      route === 'lamina'
    ) {

      const id =
        window.location.hash
          .replace(
            '#lamina/',
            ''
          )


      if (!id) {

        window.location.hash =
          '#laminas'

        return

      }


      app.innerHTML = `

        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
        ">

          <div>

            <h1>
              Carregando lâmina...
            </h1>

          </div>

        </main>

      `


      const lamina =
        await buscarLamina(id)


      if (!lamina) {

        app.innerHTML = `

          ${renderHeader(config)}

          <main style="
            min-height:60vh;
            display:flex;
            align-items:center;
            justify-content:center;
            text-align:center;
          ">

            <div>

              <h1>
                Lâmina não encontrada
              </h1>


              <p>
                Não foi possível carregar esta lâmina.
              </p>


              <a
                href="#laminas"
                class="button primary"
              >
                Voltar para lâminas
              </a>

            </div>

          </main>

        `

        return

      }


      /*
       * Renderiza a página.
       */

      app.innerHTML =
        renderLamina(
          lamina,
          config
        )


      /*
       * Inicia o viewer.
       */

      iniciarViewer(
        lamina
      )


      return

    }


    /* ========================================================
       HOME
       ======================================================== */

    app.innerHTML =
      renderHome(
        config
      )

  } catch (erro) {

    console.error(
      'ERRO FATAL NO RENDER:',
      erro
    )


    /*
     * Não deixa o site simplesmente
     * ficar branco se alguma função falhar.
     */

    if (app) {

      app.innerHTML = `

        <main style="
          min-height:70vh;
          display:flex;
          align-items:center;
          justify-content:center;
          padding:30px;
          font-family:system-ui,sans-serif;
        ">

          <div style="
            max-width:700px;
            width:100%;
            padding:30px;
            border:1px solid #ddd;
            border-radius:16px;
            background:white;
          ">

            <h1>
              Ocorreu um erro ao carregar o Atlas.
            </h1>


            <p>
              Abra o console do navegador
              para ver os detalhes.
            </p>


            <button
              type="button"
              onclick="location.reload()"
              style="
                padding:10px 18px;
                border:none;
                border-radius:8px;
                cursor:pointer;
              "
            >
              Recarregar
            </button>

          </div>

        </main>

      `

    }

  }

}


/* ============================================================
   HASHCHANGE
   ============================================================ */

window.addEventListener(
  'hashchange',
  render
)


/* ============================================================
   INICIALIZAÇÃO
   ============================================================ */

render()


===== backup-editor-20260819-210237\nova-lamina.js =====
import { supabase } from '../lib/supabase.js'

export function renderNovaLamina() {

  const params =
    new URLSearchParams(
      window.location.hash.split('?')[1] || ''
    )

  const editarId =
    params.get('editar')


  return `
    <header class="header">

      <div class="container header-content">

        <a href="#inicio" class="brand">

          <span class="brand-icon">
            🔬
          </span>

          <span>
            <strong>Atlas</strong>
            <small>Histológico</small>
          </span>

        </a>


        <nav class="nav">

          <a href="#inicio">
            Início
          </a>

          <a href="#laminas">
            Lâminas
          </a>

          <a href="#sobre">
            Sobre
          </a>

          <a href="#admin">
            Administração
          </a>

        </nav>

      </div>

    </header>


    <main>

      <section class="form-page">

        <div class="container">

          <a
            href="#admin"
            class="back-link"
          >
            ← Voltar para administração
          </a>


          <div class="form-header">

            <span class="eyebrow">
              ${editarId ? 'EDIÇÃO' : 'CADASTRO'}
            </span>

            <h1>
              ${editarId
                ? 'Editar lâmina'
                : 'Nova lâmina'}
            </h1>

            <p>
              ${
                editarId
                  ? 'Altere as informações desta lâmina.'
                  : 'Cadastre uma nova lâmina no Atlas Histológico.'
              }
            </p>

          </div>


          <div
            id="lamina-form-status"
            class="form-status"
          ></div>


          <form
            id="lamina-form"
            class="lamina-form"
            data-editar-id="${editarId || ''}"
          >

            <div class="form-card">

              <div class="form-card-title">

                <span>
                  📋
                </span>

                <div>

                  <h2>
                    Informações
                  </h2>

                  <p>
                    Dados básicos da lâmina.
                  </p>

                </div>

              </div>


              <div class="form-grid">


                <div class="form-field full">

                  <label for="nome">
                    Nome da lâmina *
                  </label>

                  <input
                    id="nome"
                    name="nome"
                    type="text"
                    placeholder="Ex.: Epitélio intestinal"
                    required
                  >

                </div>


                <div class="form-field full">

                  <label for="descricao">
                    Descrição
                  </label>

                  <textarea
                    id="descricao"
                    name="descricao"
                    rows="5"
                    placeholder="Descreva o que pode ser observado nesta lâmina..."
                  ></textarea>

                </div>


                
<div class="form-field">

  <label for="categoria">
    Categoria
  </label>

  <select
    id="categoria"
    name="categoria"
  >

    <option value="">
      Selecione uma categoria
    </option>

  </select>

</div>
<div class="form-field">

                  <label for="tecnica">
                    Técnica
                  </label>

                  <input
                    id="tecnica"
                    name="tecnica"
                    type="text"
                    placeholder="Ex.: Corte histológico"
                  >

                </div>


                <div class="form-field">

                  <label for="coloracao">
                    Coloração
                  </label>

                  <input
                    id="coloracao"
                    name="coloracao"
                    type="text"
                    placeholder="Ex.: Hematoxilina e eosina"
                  >

                </div>


                <div class="form-field">

                  <label for="publicado">
                    Visibilidade
                  </label>

                  <select
                    id="publicado"
                    name="publicado"
                  >

                    <option value="true">
                      🟢 Publicada
                    </option>

                    <option value="false">
                      ⚪ Oculta
                    </option>

                  </select>

                </div>

              </div>

            </div>


            <div class="form-card">

              <div class="form-card-title">

                <span>
                  🖼️
                </span>

                <div>

                  <h2>
                    Imagem
                  </h2>

                  <p>
                    Imagem utilizada para visualizar a lâmina.
                  </p>

                </div>

              </div>


              <div
                id="current-image"
                class="current-image"
              ></div>


              <div class="upload-area">

                <input
                  id="imagem"
                  name="imagem"
                  type="file"
                  accept="image/*"
                >

                <label for="imagem">

                  <span class="upload-icon">
                    📤
                  </span>

                  <strong>
                    Escolher imagem
                  </strong>

                  <small>
                    PNG, JPG ou WEBP
                  </small>

                </label>

              </div>

            </div>


            <div class="form-card estruturas-editor-card">

              <div class="form-card-title">

                <span>
                  🔬
                </span>

                <div>

                  <h2>
                    Estruturas da lâmina
                  </h2>

                  <p>
                    Cadastre as estruturas que poderão ser identificadas na lâmina.
                  </p>

                </div>

              </div>


              <div
                id="estruturas-lista"
                class="estruturas-lista"
              ></div>


              <div class="estrutura-editor">

                <div class="form-grid">

                  <div class="form-field">

                    <label for="estrutura-nome">
                      Nome da estrutura
                    </label>

                    <input
                      id="estrutura-nome"
                      type="text"
                      placeholder="Ex.: Núcleo"
                    >

                  </div>


                  <div class="form-field">

                    <label for="estrutura-descricao">
                      Descrição
                    </label>

                    <input
                      id="estrutura-descricao"
                      type="text"
                      placeholder="Ex.: Região central da célula"
                    >

                  </div>


                  <div class="form-field">

                    <label for="estrutura-x">
                      Posição X
                    </label>

                    <input
                      id="estrutura-x"
                      type="number"
                      min="0"
                      max="1"
                      step="0.01"
                      value="0.5"
                    >

                  </div>


                  <div class="form-field">

                    <label for="estrutura-y">
                      Posição Y
                    </label>

                    <input
                      id="estrutura-y"
                      type="number"
                      min="0"
                      max="1"
                      step="0.01"
                      value="0.5"
                    >

                  </div>


                  <div class="form-field">

                    <label for="estrutura-largura">
                      Largura
                    </label>

                    <input
                      id="estrutura-largura"
                      type="number"
                      min="0.01"
                      max="1"
                      step="0.01"
                      value="0.08"
                    >

                  </div>


                  <div class="form-field">

                    <label for="estrutura-altura">
                      Altura
                    </label>

                    <input
                      id="estrutura-altura"
                      type="number"
                      min="0.01"
                      max="1"
                      step="0.01"
                      value="0.08"
                    >

                  </div>

                </div>


                <button
                  type="button"
                  class="button secondary"
                  id="adicionar-estrutura"
                >
                  + Adicionar estrutura
                </button>

              </div>

            </div>

            <div class="form-actions">

              <a
                href="#admin"
                class="button secondary"
              >
                Cancelar
              </a>

              <button
                type="submit"
                class="button primary"
                id="save-lamina"
              >
                ${
                  editarId
                    ? 'Salvar alterações'
                    : 'Cadastrar lâmina'
                }
              </button>

            </div>

          </form>

        </div>

      </section>

    </main>
  `
}


export async function setupNovaLamina() {

  await carregarCategoriasLamina()


  let estruturas = []


  const form =
    document.querySelector('#lamina-form')

  if (!form) return


  const editarId =
    form.dataset.editarId || null


  /*
   * ============================================================
   * TODAS AS ESTRUTURAS DA LÂMINA
   * ============================================================
   */
  const listaEstruturas =
    document.querySelector('#estruturas-lista')


  function renderizarEstruturas() {

    if (!listaEstruturas) return


    if (!estruturas.length) {

      listaEstruturas.innerHTML = `
        <div class="estrutura-vazia">
          Nenhuma estrutura adicionada ainda.
        </div>
      `

      return
    }


    listaEstruturas.innerHTML =
      estruturas
        .map(
          (estrutura, index) => `

            <div class="estrutura-item">

              <div class="estrutura-item-info">

                <strong>
                  ${escapeHtml(
                    estrutura.nome || 'Estrutura'
                  )}
                </strong>


                ${
                  estrutura.descricao
                    ? `
                      <small>
                        ${escapeHtml(
                          estrutura.descricao
                        )}
                      </small>
                    `
                    : ''
                }


                <small>
                  X: ${estrutura.x}
                  • Y: ${estrutura.y}
                  • L: ${estrutura.largura}
                  • A: ${estrutura.altura}
                </small>

              </div>


              <button
                type="button"
                class="button secondary"
                data-remover-estrutura="${index}"
              >
                Remover
              </button>

            </div>
          `
        )
        .join('')


    listaEstruturas
      .querySelectorAll(
        '[data-remover-estrutura]'
      )
      .forEach(
        botao => {

          botao.addEventListener(
            'click',
            () => {

              const index =
                Number(
                  botao.dataset
                    .removerEstrutura
                )


              if (
                Number.isNaN(index)
              ) {
                return
              }


              estruturas.splice(
                index,
                1
              )


              renderizarEstruturas()

            }
          )

        }
      )

  }


  /*
   * ============================================================
   * BOTÃO ADICIONAR ESTRUTURA
   * ============================================================
   */

  const adicionarEstrutura =
    document.querySelector(
      '#adicionar-estrutura'
    )


  adicionarEstrutura?.addEventListener(
    'click',
    () => {

      const nomeInput =
        document.querySelector(
          '#estrutura-nome'
        )


      const descricaoInput =
        document.querySelector(
          '#estrutura-descricao'
        )


      const xInput =
        document.querySelector(
          '#estrutura-x'
        )


      const yInput =
        document.querySelector(
          '#estrutura-y'
        )


      const larguraInput =
        document.querySelector(
          '#estrutura-largura'
        )


      const alturaInput =
        document.querySelector(
          '#estrutura-altura'
        )


      const nome =
        nomeInput?.value.trim() || ''


      const descricao =
        descricaoInput?.value.trim() || ''


      const x =
        Number(
          xInput?.value || 0.5
        )


      const y =
        Number(
          yInput?.value || 0.5
        )


      const largura =
        Number(
          larguraInput?.value || 0.08
        )


      const altura =
        Number(
          alturaInput?.value || 0.08
        )


      if (!nome) {

        mostrarStatus(
          'Digite o nome da estrutura.',
          'error'
        )

        nomeInput?.focus()

        return
      }


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        mostrarStatus(
          'Verifique as posições e dimensões da estrutura.',
          'error'
        )

        return
      }


      estruturas.push({

        nome,

        descricao,

        x: Math.max(
          0,
          Math.min(1, x)
        ),

        y: Math.max(
          0,
          Math.min(1, y)
        ),

        largura: Math.max(
          0.01,
          Math.min(1, largura)
        ),

        altura: Math.max(
          0.01,
          Math.min(1, altura)
        )

      })


      renderizarEstruturas()


      /*
       * Limpa os campos de texto.
       */

      if (nomeInput) {
        nomeInput.value = ''
      }


      if (descricaoInput) {
        descricaoInput.value = ''
      }


      if (xInput) {
        xInput.value = '0.5'
      }


      if (yInput) {
        yInput.value = '0.5'
      }


      if (larguraInput) {
        larguraInput.value = '0.08'
      }


      if (alturaInput) {
        alturaInput.value = '0.08'
      }


      nomeInput?.focus()


      mostrarStatus(
        'Estrutura adicionada.',
        'success'
      )

    }
  )


  /*
   * ============================================================
   * CARREGA LÂMINA QUANDO ESTÁ EDITANDO
   * ============================================================
   */

  if (editarId) {

    const carregada =
      await carregarLamina(
        editarId
      )


    if (
      carregada &&
      Array.isArray(
        carregada.estruturas
      )
    ) {

      estruturas =
        Array.isArray(carregada.estruturas)
          ? carregada.estruturas.map(normalizarEstrutura)
          : []

      renderizarEstruturas()

    }

  }


  /*
   * ============================================================
   * SALVAR
   * ============================================================
   */

  form.addEventListener(
    'submit',
    async event => {

      event.preventDefault()


      await salvarLamina(
        form,
        editarId,
        estruturas
      )

    }
  )


  renderizarEstruturas()

}


function normalizarEstrutura(
  estrutura = {}
) {

  return {

    nome:
      String(
        estrutura.nome || ''
      ).trim(),

    descricao:
      String(
        estrutura.descricao || ''
      ).trim(),

    x:
      Math.max(
        0,
        Math.min(
          1,
          Number(
            estrutura.x ?? 0.5
          )
        )
      ),

    y:
      Math.max(
        0,
        Math.min(
          1,
          Number(
            estrutura.y ?? 0.5
          )
        )
      ),

    largura:
      Math.max(
        0.01,
        Math.min(
          1,
          Number(
            estrutura.largura ?? 0.08
          )
        )
      ),

    altura:
      Math.max(
        0.01,
        Math.min(
          1,
          Number(
            estrutura.altura ?? 0.08
          )
        )
      )

  }

}

async function carregarLamina(id) {

  mostrarStatus(
    'Carregando informações da lâmina...',
    'loading'
  )


  const {
    data,
    error
  } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .single()


  if (error || !data) {

    console.error(
      'Erro ao carregar lâmina:',
      error
    )


    mostrarStatus(
      'Não foi possível carregar esta lâmina.',
      'error'
    )

    return
  }


  document.querySelector('#nome').value =
    data.nome || ''


  document.querySelector('#descricao').value =
    data.descricao || ''


  await carregarCategoriasLamina(
    data.categoria_id || '',
    data.categoria || ''
  )


  document.querySelector('#tecnica').value =
    data.tecnica || ''


  document.querySelector('#coloracao').value =
    data.coloracao || ''


  document.querySelector('#publicado').value =
    data.publicado === false
      ? 'false'
      : 'true'



  if (data.imagem_url) {

    document.querySelector(
      '#current-image'
    ).innerHTML = `

      <div class="current-image-preview">

        <span>
          Imagem atual
        </span>

        <img
          src="${escapeHtml(data.imagem_url)}"
          alt="Imagem atual da lâmina"
        >

      </div>

    `
  }


  mostrarStatus(
    '',
    ''
  )

  return data

}


async function salvarLamina(
  form,
  editarId,
  estruturas = []
) {

  const button =
    document.querySelector('#save-lamina')


  button.disabled = true


  button.textContent =
    editarId
      ? 'Salvando alterações...'
      : 'Cadastrando...'


  mostrarStatus(
    'Salvando informações...',
    'loading'
  )


  try {

    const formData =
      new FormData(form)


    const nome =
      String(
        formData.get('nome') || ''
      ).trim()


    if (!nome) {

      throw new Error(
        'Digite o nome da lâmina.'
      )

    }


    const descricao =
      String(
        formData.get('descricao') || ''
      ).trim()


    const categoriaId =
      String(
        formData.get('categoria') || ''
      ).trim()


    const tecnica =
      String(
        formData.get('tecnica') || ''
      ).trim()


    const coloracao =
      String(
        formData.get('coloracao') || ''
      ).trim()


    const publicado =
      formData.get('publicado') === 'true'


    const arquivo =
      formData.get('imagem')

    if (
      arquivo &&
      arquivo instanceof File &&
      arquivo.size > 0
    ) {

      if (
        !arquivo.type.startsWith('image/')
      ) {

        throw new Error(
          'O arquivo selecionado não é uma imagem válida.'
        )

      }

      const tamanhoMaximo =
        20 * 1024 * 1024

      if (
        arquivo.size > tamanhoMaximo
      ) {

        throw new Error(
          'A imagem deve ter no máximo 20 MB.'
        )

      }

    }


    let imagemUrl =
      null


    /*
     * Se estiver editando e não escolher
     * uma nova imagem, mantém a imagem atual.
     */

    if (editarId) {

      const {
        data: atual
      } =
        await supabase
          .from('laminas')
          .select('imagem_url')
          .eq('id', editarId)
          .single()


      imagemUrl =
        atual?.imagem_url || null

    }


    /*
     * Upload de uma nova imagem
     */

    if (
      arquivo &&
      arquivo instanceof File &&
      arquivo.size > 0
    ) {

      const extensao =
        arquivo.name
          .split('.')
          .pop()
          .toLowerCase()


      const nomeArquivo =
        `${crypto.randomUUID()}.${extensao}`


      const caminho =
        `laminas/${nomeArquivo}`


      const {
        error: uploadError
      } =
        await supabase
          .storage
          .from('laminas')
          .upload(
            caminho,
            arquivo,
            {
              cacheControl: '3600',
              upsert: false
            }
          )


      if (uploadError) {

        console.error(
          'Erro no upload:',
          uploadError
        )

        throw new Error(
          `Erro ao enviar a imagem: ${uploadError.message}`
        )

      }


      const {
        data: publicUrl
      } =
        supabase
          .storage
          .from('laminas')
          .getPublicUrl(caminho)


      imagemUrl =
        publicUrl.publicUrl

    }


    let categoriaNome = ''


if (categoriaId) {

  const {
    data: categoriaData,
    error: categoriaError
  } =
    await supabase
      .from('categorias')
      .select('id, nome')
      .eq('id', categoriaId)
      .eq('ativo', true)
      .maybeSingle()


  if (categoriaError) {

    console.error(
      'Erro ao verificar categoria:',
      categoriaError
    )

    throw new Error(
      'Não foi possível verificar a categoria selecionada.'
    )

  }


  if (!categoriaData) {

    throw new Error(
      'A categoria selecionada não existe ou está inativa.'
    )

  }


  categoriaNome =
    categoriaData.nome

}

const estruturasNormalizadas =
      Array.isArray(estruturas)
        ? estruturas
            .map(normalizarEstrutura)
            .filter(estrutura => estrutura.nome)
        : []

    const dados = {

      nome,

      descricao,

      categoria:
        categoriaNome,

      categoria_id:
        categoriaId || null,

      tecnica,

      coloracao,

      publicado,

      estruturas:
        estruturasNormalizadas,

      updated_at:
        new Date().toISOString()

    }


    if (imagemUrl) {

      dados.imagem_url =
        imagemUrl

    }


    let resultado


    if (editarId) {

      resultado =
        await supabase
          .from('laminas')
          .update(dados)
          .eq('id', editarId)

    } else {

      resultado =
        await supabase
          .from('laminas')
          .insert(dados)

    }


    if (resultado.error) {

      console.error(
        'Erro ao salvar:',
        resultado.error
      )

      throw new Error(
        `Erro ao salvar a lâmina: ${resultado.error.message}`
      )

    }


    mostrarStatus(
      editarId
        ? 'Lâmina atualizada com sucesso!'
        : 'Lâmina cadastrada com sucesso!',
      'success'
    )


    setTimeout(
      () => {

        window.location.hash =
          '#admin'

      },
      1000
    )

  } catch (error) {

    console.error(error)


    mostrarStatus(
      error.message ||
      'Ocorreu um erro ao salvar.',
      'error'
    )


    button.disabled = false


    button.textContent =
      editarId
        ? 'Salvar alterações'
        : 'Cadastrar lâmina'

  }

}



async function carregarCategoriasLamina(
  categoriaIdAtual = '',
  categoriaNomeAtual = ''
) {

  const select =
    document.querySelector(
      '#categoria'
    )

  if (!select) return


  const {
    data,
    error
  } =
    await supabase
      .from('categorias')
      .select(
        'id, nome, ativo, ordem'
      )
      .eq(
        'ativo',
        true
      )
      .order(
        'ordem',
        {
          ascending: true
        }
      )
      .order(
        'nome',
        {
          ascending: true
        }
      )


  if (error) {

    console.error(
      'Erro ao carregar categorias:',
      error
    )

    mostrarStatus(
      'Não foi possível carregar as categorias.',
      'error'
    )

    return

  }


  select.innerHTML = `
    <option value="">
      Selecione uma categoria
    </option>
  `


  ;(data || [])
    .forEach(
      categoria => {

        const option =
          document.createElement(
            'option'
          )


        option.value =
          categoria.id


        option.textContent =
          categoria.nome


        if (
          categoria.id ===
          categoriaIdAtual
        ) {

          option.selected = true

        } else if (
          !categoriaIdAtual &&
          categoria.nome ===
          categoriaNomeAtual
        ) {

          option.selected = true

        }


        select.appendChild(
          option
        )

      }
    )

}


function mostrarStatus(
  mensagem,
  tipo
) {

  const elemento =
    document.querySelector(
      '#lamina-form-status'
    )


  if (!elemento) return


  elemento.textContent =
    mensagem


  elemento.className =
    `form-status ${tipo || ''}`

}


function escapeHtml(value = '') {

  return String(value)

    .replaceAll('&', '&amp;')

    .replaceAll('<', '&lt;')

    .replaceAll('>', '&gt;')

    .replaceAll('"', '&quot;')

    .replaceAll("'", '&#039;')
}











===== backup-editor-20260819-210237\style.css =====
:root {
  --cor-principal: #1f6feb;
  --cor-fundo: #f7f9fc;
  --cor-texto: #172033;
  --cor-destaque: #22c55e;
}
:root {
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  color: #17201d;
  background: #f5f7f5;
  font-synthesis: none;
  text-rendering: optimizeLegibility;
  --green: #174c3c;
  --green-light: #2d755d;
  --mint: #dcefe7;
  --cream: #f5f7f5;
  --white: #ffffff;
  --text: #17201d;
  --muted: #66736d;
  --border: #dfe6e2;
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  scroll-behavior: smooth;
}

body {
  min-width: 320px;
  background: var(--cream);
  color: var(--text);
}

button,
a {
  font: inherit;
}

button {
  border: 0;
  cursor: pointer;
}

a {
  color: inherit;
  text-decoration: none;
}

.container {
  width: min(1160px, calc(100% - 48px));
  margin: 0 auto;
}

.header {
  position: sticky;
  top: 0;
  z-index: 20;
  background: rgba(245, 247, 245, 0.9);
  backdrop-filter: blur(16px);
  border-bottom: 1px solid rgba(223, 230, 226, 0.8);
}

.header-content {
  height: 76px;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.brand {
  display: flex;
  align-items: center;
  gap: 11px;
}

.brand-icon {
  width: 42px;
  height: 42px;
  display: grid;
  place-items: center;
  border-radius: 12px;
  background: var(--green);
  color: white;
  font-size: 20px;
}

.brand strong,
.brand small {
  display: block;
}

.brand strong {
  font-size: 17px;
  letter-spacing: -0.4px;
}

.brand small {
  color: var(--muted);
  font-size: 11px;
  letter-spacing: 1.5px;
  text-transform: uppercase;
}

.nav {
  display: flex;
  gap: 34px;
  color: #53605a;
  font-size: 14px;
}

.nav a {
  transition: color 0.2s ease;
}

.nav a:hover {
  color: var(--green);
}

.menu-button {
  display: none;
  background: transparent;
  font-size: 22px;
}

.hero {
  padding: 100px 0 90px;
  overflow: hidden;
}

.hero-grid {
  display: grid;
  grid-template-columns: 1fr 0.9fr;
  align-items: center;
  gap: 70px;
}

.eyebrow {
  display: inline-block;
  margin-bottom: 18px;
  color: var(--green-light);
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 2px;
}

.hero h1 {
  max-width: 650px;
  font-size: clamp(48px, 6vw, 76px);
  line-height: 0.98;
  letter-spacing: -4px;
}

.hero h1 span {
  display: block;
  color: var(--green);
}

.hero-text > p {
  max-width: 570px;
  margin-top: 28px;
  color: var(--muted);
  font-size: 17px;
  line-height: 1.7;
}

.hero-actions {
  display: flex;
  gap: 12px;
  margin-top: 34px;
}

.button {
  display: inline-flex;
  align-items: center;
  gap: 20px;
  padding: 14px 20px;
  border-radius: 10px;
  font-size: 14px;
  font-weight: 700;
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.button:hover {
  transform: translateY(-2px);
}

.button.primary {
  background: var(--green);
  color: white;
  box-shadow: 0 12px 30px rgba(23, 76, 60, 0.18);
}

.button.secondary {
  border: 1px solid var(--border);
  background: white;
}

.hero-visual {
  min-height: 480px;
  display: grid;
  place-items: center;
}

.microscope-card {
  width: min(430px, 100%);
  height: 430px;
  position: relative;
  overflow: hidden;
  border-radius: 28px;
  background: #dbece5;
  border: 1px solid #cbded5;
}

.microscope-glow {
  position: absolute;
  width: 270px;
  height: 270px;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.65);
  filter: blur(8px);
}

.microscope {
  position: absolute;
  width: 190px;
  height: 280px;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -45%);
}

.microscope-top {
  position: absolute;
  width: 80px;
  height: 115px;
  right: 28px;
  top: 10px;
  border-radius: 30px 30px 12px 12px;
  background: #263e36;
  transform: rotate(23deg);
}

.microscope-body {
  position: absolute;
  width: 75px;
  height: 190px;
  left: 53px;
  top: 55px;
  border-radius: 45px 20px 30px 25px;
  background: #345b4e;
  transform: rotate(22deg);
}

.microscope-base {
  position: absolute;
  width: 190px;
  height: 38px;
  bottom: 0;
  border-radius: 30px 30px 14px 14px;
  background: #263e36;
}

.microscope-lens {
  position: absolute;
  width: 100px;
  height: 22px;
  left: 45px;
  bottom: 66px;
  border-radius: 15px;
  background: #1d302a;
}

.floating-tag {
  position: absolute;
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 9px 13px;
  border-radius: 10px;
  background: rgba(255,255,255,0.88);
  backdrop-filter: blur(8px);
  box-shadow: 0 10px 30px rgba(40, 70, 60, 0.12);
  font-size: 12px;
  font-weight: 700;
}

.floating-tag span {
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: var(--green-light);
}

.tag-one {
  left: 24px;
  top: 75px;
}

.tag-two {
  right: 22px;
  top: 155px;
}

.tag-three {
  left: 30px;
  bottom: 70px;
}

.scan-line {
  position: absolute;
  width: 100%;
  height: 1px;
  top: 50%;
  background: rgba(23, 76, 60, 0.12);
}

.section {
  padding: 95px 0;
  background: white;
}

.section-heading {
  display: flex;
  align-items: end;
  justify-content: space-between;
  gap: 40px;
  margin-bottom: 40px;
}

.section-heading h2,
.about h2 {
  font-size: clamp(32px, 4vw, 48px);
  line-height: 1.05;
  letter-spacing: -2px;
}

.section-heading p {
  max-width: 390px;
  color: var(--muted);
  line-height: 1.6;
}

.tissue-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 16px;
}

.tissue-card {
  position: relative;
  min-height: 290px;
  padding: 26px;
  overflow: hidden;
  border: 1px solid var(--border);
  border-radius: 18px;
  background: var(--cream);
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}

.tissue-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 18px 40px rgba(23, 76, 60, 0.09);
}

.card-number {
  color: #a4b0ab;
  font-size: 12px;
  font-weight: 800;
}

.card-icon {
  margin-top: 48px;
  color: var(--green);
  font-size: 34px;
}

.tissue-card h3 {
  margin-top: 20px;
  font-size: 19px;
}

.tissue-card p {
  margin-top: 8px;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.5;
}

.tissue-card button {
  position: absolute;
  left: 26px;
  bottom: 24px;
  background: transparent;
  color: var(--green);
  font-size: 13px;
  font-weight: 800;
}

.tissue-card button span {
  margin-left: 8px;
}

.about {
  padding: 105px 0;
  background: #eaf2ee;
}

.about-grid {
  display: grid;
  grid-template-columns: 0.8fr 1fr;
  gap: 100px;
}

.about-text > p {
  color: #56645e;
  font-size: 17px;
  line-height: 1.7;
}

.features {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 25px;
  margin-top: 35px;
}

.features div {
  padding-top: 20px;
  border-top: 1px solid #cbdad3;
}

.features strong,
.features span {
  display: block;
}

.features strong {
  font-size: 14px;
}

.features span {
  margin-top: 7px;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.5;
}

.footer {
  padding: 25px 0;
  background: var(--green);
  color: white;
}

.footer .container {
  display: flex;
  justify-content: space-between;
  color: rgba(255,255,255,0.7);
  font-size: 12px;
}

.footer span:first-child {
  color: white;
  font-weight: 700;
}

@media (max-width: 850px) {
  .hero-grid,
  .about-grid {
    grid-template-columns: 1fr;
    gap: 55px;
  }

  .hero {
    padding-top: 65px;
  }

  .hero-visual {
    min-height: 400px;
  }

  .tissue-grid {
    grid-template-columns: 1fr 1fr;
  }
}

@media (max-width: 600px) {
  .container {
    width: min(100% - 32px, 1160px);
  }

  .nav {
    display: none;
  }

  .menu-button {
    display: block;
  }

  .hero h1 {
    font-size: 50px;
    letter-spacing: -3px;
  }

  .hero-actions {
    flex-direction: column;
  }

  .button {
    justify-content: center;
  }

  .section-heading {
    display: block;
  }

  .section-heading p {
    margin-top: 15px;
  }

  .tissue-grid {
    grid-template-columns: 1fr;
  }

  .about-grid {
    gap: 35px;
  }

  .features {
    grid-template-columns: 1fr;
  }

  .footer .container {
    flex-direction: column;
    gap: 8px;
  }
}

.slide-header {
  padding: 55px 0 35px;
  background: white;
  border-bottom: 1px solid var(--border);
}

.back-link {
  display: inline-block;
  margin-bottom: 30px;
  color: var(--muted);
  font-size: 13px;
}

.slide-title {
  display: flex;
  justify-content: space-between;
  align-items: end;
  gap: 40px;
}

.slide-title h1 {
  font-size: clamp(36px, 5vw, 58px);
  letter-spacing: -3px;
  line-height: 1;
}

.slide-title p {
  margin-top: 15px;
  color: var(--muted);
}

.slide-meta {
  display: flex;
  flex-direction: column;
  gap: 8px;
  color: var(--muted);
  font-size: 12px;
}

.viewer-section {
  padding: 40px 0 80px;
  background: #f1f4f2;
}

.viewer-layout {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 340px;
  gap: 24px;
  align-items: start;
}

.viewer-container {
  overflow: hidden;
  border: 1px solid var(--border);
  border-radius: 18px;
  background: #101513;
  box-shadow: 0 20px 50px rgba(0,0,0,0.08);
}

.viewer {
  width: 100%;
  height: 650px;
  background: #101513;
}

.viewer-controls {
  min-height: 58px;
  padding: 10px 14px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 15px;
  background: #18201d;
  color: #aab7b1;
  font-size: 11px;
}

.zoom-buttons {
  display: flex;
  gap: 5px;
}

.zoom-buttons button {
  width: 38px;
  height: 34px;
  border-radius: 7px;
  background: #29332f;
  color: white;
  font-size: 18px;
}

.zoom-buttons button:hover {
  background: var(--green-light);
}

.info-panel {
  padding: 28px;
  border: 1px solid var(--border);
  border-radius: 18px;
  background: white;
}

.panel-label {
  margin-bottom: 15px;
  color: var(--green-light);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 1.7px;
}

.info-panel h2 {
  font-size: 25px;
  line-height: 1.1;
}

.description {
  margin-top: 15px;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.7;
}

.info-section {
  margin-top: 30px;
}

.info-section h3 {
  font-size: 14px;
}

.structure-list {
  margin-top: 15px;
  display: grid;
  gap: 8px;
}

.structure {
  width: 100%;
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px;
  text-align: left;
  border-radius: 10px;
  background: #f4f7f5;
}

.structure:hover {
  background: #e8f1ec;
}

.structure-dot {
  width: 9px;
  height: 9px;
  flex: 0 0 9px;
  border-radius: 50%;
  background: var(--green-light);
}

.structure strong,
.structure small {
  display: block;
}

.structure strong {
  font-size: 12px;
}

.structure small {
  margin-top: 3px;
  color: var(--muted);
  font-size: 10px;
}

.content-button {
  width: 100%;
  margin-top: 25px;
  padding: 14px;
  display: flex;
  justify-content: space-between;
  border-radius: 10px;
  background: var(--green);
  color: white;
  font-size: 12px;
  font-weight: 700;
}

.learning-section {
  padding: 90px 0;
  background: white;
}

.learning-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
}

.learning-card {
  padding: 35px;
  border: 1px solid var(--border);
  border-radius: 18px;
}

.learning-card h2 {
  font-size: 28px;
  letter-spacing: -1px;
}

.learning-card > p {
  max-width: 500px;
  margin-top: 12px;
  color: var(--muted);
  line-height: 1.6;
  font-size: 14px;
}

.video-placeholder {
  height: 190px;
  margin-top: 25px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 10px;
  border-radius: 12px;
  background: #edf2ef;
  color: var(--green);
}

.video-placeholder span {
  font-size: 35px;
}

.video-placeholder small {
  color: var(--muted);
}

.learning-card .button {
  margin-top: 25px;
}

@media (max-width: 900px) {
  .viewer-layout {
    grid-template-columns: 1fr;
  }

  .slide-title {
    display: block;
  }

  .slide-meta {
    margin-top: 25px;
  }

  .learning-grid {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 600px) {
  .viewer {
    height: 450px;
  }

  .viewer-controls {
    flex-direction: column;
    align-items: stretch;
  }

  .zoom-buttons {
    justify-content: center;
  }
}

.card-link {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  margin-top: 15px;
  color: var(--green-light);
  font-size: 12px;
  font-weight: 800;
  text-decoration: none;
}

.card-link span {
  transition: transform 0.2s ease;
}

.card-link:hover span {
  transform: translateX(4px);
}

.tissue-card .eyebrow {
  display: block;
  margin-top: 10px;
}

.auth-page {
  min-height: 100vh;
  display: grid;
  place-items: center;
  padding: 30px;
  background: #f1f4f2;
}

.auth-card {
  width: min(430px, 100%);
  padding: 45px;
  border: 1px solid var(--border);
  border-radius: 20px;
  background: white;
  box-shadow: 0 25px 70px rgba(0,0,0,.08);
}

.auth-logo {
  margin-bottom: 20px;
  font-size: 42px;
}

.auth-card h1 {
  margin-top: 8px;
  font-size: 34px;
}

.auth-card > p {
  margin: 12px 0 30px;
  color: var(--muted);
  line-height: 1.6;
  font-size: 13px;
}

.auth-card form {
  display: grid;
  gap: 18px;
}

.auth-card label {
  display: grid;
  gap: 7px;
  font-size: 12px;
  font-weight: 700;
}

.auth-card input {
  width: 100%;
  padding: 13px 14px;
  border: 1px solid var(--border);
  border-radius: 9px;
  outline: none;
  font: inherit;
}

.auth-card input:focus {
  border-color: var(--green-light);
}

#login-error {
  color: #b43b3b;
  font-size: 12px;
}

.auth-card .back-link {
  margin-top: 25px;
}

.admin-page {
  min-height: calc(100vh - 75px);
  padding: 60px 0 100px;
  background: #f1f4f2;
}

.admin-header {
  display: flex;
  align-items: center;
  gap: 15px;
}

.admin-user {
  color: var(--muted);
  font-size: 11px;
}

.logout-button {
  padding: 8px 13px;
  border: 1px solid var(--border);
  border-radius: 7px;
  background: white;
  font-size: 11px;
}

.admin-title {
  display: flex;
  align-items: end;
  justify-content: space-between;
  gap: 30px;
}

.admin-title h1 {
  margin-top: 8px;
  font-size: 42px;
  letter-spacing: -2px;
}

.admin-title p {
  margin-top: 10px;
  color: var(--muted);
}

.admin-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 15px;
  margin-top: 40px;
}

.admin-stat {
  padding: 25px;
  border: 1px solid var(--border);
  border-radius: 14px;
  background: white;
}

.admin-stat span,
.admin-stat strong,
.admin-stat small {
  display: block;
}

.admin-stat span {
  font-size: 20px;
}

.admin-stat strong {
  margin-top: 12px;
  font-size: 30px;
}

.admin-stat small {
  margin-top: 3px;
  color: var(--muted);
}

.admin-section {
  margin-top: 50px;
}

.admin-section-header h2 {
  margin-top: 6px;
}

.admin-list {
  margin-top: 18px;
  display: grid;
  gap: 8px;
}

.admin-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 18px 20px;
  border: 1px solid var(--border);
  border-radius: 12px;
  background: white;
}

.admin-item strong,
.admin-item small {
  display: block;
}

.admin-item small {
  margin-top: 4px;
  color: var(--muted);
  font-size: 11px;
}

.status {
  padding: 6px 10px;
  border-radius: 20px;
  background: #f0f0f0;
  color: var(--muted);
  font-size: 10px;
}

.status.published {
  background: #e5f3ea;
  color: #267244;
}

.empty-state {
  padding: 60px 20px;
  text-align: center;
  border: 1px dashed var(--border);
  border-radius: 14px;
  background: white;
}

.empty-state span {
  font-size: 35px;
}

.empty-state h3 {
  margin-top: 12px;
}

.empty-state p {
  margin-top: 7px;
  color: var(--muted);
  font-size: 13px;
}

@media (max-width: 700px) {

  .admin-title {
    display: block;
  }

  .admin-title .button {
    margin-top: 20px;
  }

  .admin-grid {
    grid-template-columns: 1fr;
  }

  .admin-user {
    display: none;
  }

  .auth-card {
    padding: 30px;
  }

}

.lamina-form {
  max-width: 900px;
  margin-top: 40px;
}

.form-card {
  margin-bottom: 20px;
  padding: 30px;
  border: 1px solid var(--border);
  border-radius: 16px;
  background: white;
}

.form-card h2 {
  margin-top: 7px;
  margin-bottom: 25px;
}

.form-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
}

.form-grid .full {
  grid-column: 1 / -1;
}

.form-card label {
  display: grid;
  gap: 8px;
  font-size: 12px;
  font-weight: 700;
}

.form-card input,
.form-card textarea {
  width: 100%;
  padding: 13px 14px;
  border: 1px solid var(--border);
  border-radius: 9px;
  outline: none;
  font: inherit;
  resize: vertical;
}

.form-card input:focus,
.form-card textarea:focus {
  border-color: var(--green-light);
}

.form-help {
  color: var(--muted);
  font-size: 13px;
  line-height: 1.6;
  margin-bottom: 20px;
}

.upload-area {
  min-height: 190px;
  display: flex !important;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 7px;
  padding: 30px;
  border: 2px dashed var(--border);
  border-radius: 12px;
  cursor: pointer;
  text-align: center;
}

.upload-area:hover {
  border-color: var(--green-light);
  background: #fafcfb;
}

.upload-area input {
  display: none;
}

.upload-icon {
  font-size: 36px;
}

.upload-area strong {
  font-size: 13px;
}

.upload-area small {
  color: var(--muted);
  font-size: 11px;
}

.image-preview {
  display: block;
  width: 100%;
  max-height: 400px;
  object-fit: contain;
  margin-top: 20px;
  border-radius: 10px;
  background: #111;
}

.publish-option {
  display: flex !important;
  grid-template-columns: auto 1fr;
  grid-template-rows: auto;
  gap: 12px !important;
  align-items: flex-start;
  cursor: pointer;
}

.publish-option input {
  width: auto;
  margin-top: 3px;
}

.publish-option span {
  display: grid;
  gap: 4px;
}

.publish-option small {
  color: var(--muted);
  font-weight: 400;
  line-height: 1.5;
}

.form-actions {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  margin-top: 25px;
}

.success-message {
  padding: 14px 16px;
  margin-top: 20px;
  border-radius: 9px;
  background: #e5f3ea;
  color: #267244;
  font-size: 13px;
}

#form-message:not(.success-message) {
  margin-top: 15px;
  color: #b43b3b;
  font-size: 12px;
}

@media (max-width: 700px) {

  .form-grid {
    grid-template-columns: 1fr;
  }

  .form-grid .full {
    grid-column: auto;
  }

  .form-card {
    padding: 22px;
  }

}

.catalog-page {
  min-height: 100vh;
  background: #f7f8f6;
}

.catalog-hero {
  padding: 80px 0 65px;
  background: white;
  border-bottom: 1px solid var(--border);
}

.catalog-hero h1 {
  margin: 12px 0;
  font-size: clamp(36px, 5vw, 64px);
  letter-spacing: -2px;
}

.catalog-hero p {
  max-width: 650px;
  color: var(--muted);
  font-size: 17px;
  line-height: 1.7;
}

.catalog-content {
  padding: 50px 0 80px;
}

.laminas-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
}

.lamina-card {
  overflow: hidden;
  background: white;
  border: 1px solid var(--border);
  border-radius: 18px;
  transition: transform .2s ease, box-shadow .2s ease;
}

.lamina-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 15px 35px rgba(0,0,0,.08);
}

.lamina-image {
  position: relative;
  height: 230px;
  overflow: hidden;
  background: #111;
}

.lamina-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform .4s ease;
}

.lamina-card:hover .lamina-image img {
  transform: scale(1.06);
}

.zoom-badge {
  position: absolute;
  right: 12px;
  bottom: 12px;
  padding: 8px 11px;
  border-radius: 8px;
  background: rgba(0,0,0,.72);
  color: white;
  font-size: 11px;
  font-weight: 700;
}

.lamina-card-content {
  padding: 22px;
}

.lamina-category {
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 1px;
  text-transform: uppercase;
  color: var(--green-light);
}

.lamina-card h2 {
  margin: 8px 0;
  font-size: 21px;
}

.lamina-card p {
  min-height: 55px;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.6;
}

.explore-lamina {
  width: 100%;
  margin-top: 10px;
}

.loading-state,
.error-state,
.empty-state {
  padding: 60px 20px;
  text-align: center;
  color: var(--muted);
}

.empty-state span {
  display: block;
  margin-bottom: 15px;
  font-size: 40px;
}

.empty-state h3 {
  margin-bottom: 8px;
  color: var(--text);
}

@media (max-width: 900px) {

  .laminas-grid {
    grid-template-columns: repeat(2, 1fr);
  }

}

@media (max-width: 600px) {

  .laminas-grid {
    grid-template-columns: 1fr;
  }

}



/* =========================================================
   LOGO DA PRÉVIA DO EDITOR DE APARÊNCIA
   ========================================================= */

.preview-logo {
  width: 48px;
  height: 48px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  overflow: visible;
}

.preview-logo img {
  width: 42px;
  height: 42px;
  max-width: none;
  max-height: none;
  object-fit: contain;
  display: block;
  transform-origin: center center;
  transition: transform 0.15s ease;
}
 
.logo-preview {
  width: 120px;
  height: 120px;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: visible;
}

.logo-preview img {
  width: 90px;
  height: 90px;
  max-width: none;
  max-height: none;
  object-fit: contain;
  display: block;
  transform-origin: center center;
  transition: transform 0.15s ease;
}


/* =========================================================
   APARÊNCIA — EDITOR MODERNO
   ========================================================= */

#admin-section-aparencia {
  max-width: 1180px;
}

#aparencia-form {
  display: flex;
  flex-direction: column;
  gap: 22px;
}

#aparencia-form .form-card {
  background: #fff;
  border: 1px solid #e4e9e7;
  border-radius: 18px;
  padding: 26px;
  box-shadow: 0 4px 18px rgba(16,47,41,.04);
}

#aparencia-form .form-card-title {
  display: flex;
  align-items: flex-start;
  gap: 14px;
  margin-bottom: 24px;
}

#aparencia-form .form-card-title > span {
  width: 42px;
  height: 42px;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #edf6f2;
  border-radius: 12px;
  font-size: 20px;
}

#aparencia-form .form-card-title h2 {
  margin: 2px 0 5px;
  font-size: 18px;
  color: #17211f;
}

#aparencia-form .form-card-title p {
  margin: 0;
  color: #7a8581;
  font-size: 13px;
}

#aparencia-form .form-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0,1fr));
  gap: 20px;
}

#aparencia-form .form-field.full {
  grid-column: 1 / -1;
}

#aparencia-form .form-field label {
  display: block;
  margin-bottom: 8px;
  color: #34413d;
  font-size: 13px;
  font-weight: 700;
}

#aparencia-form input[type="text"],
#aparencia-form input[type="url"],
#aparencia-form textarea {
  width: 100%;
  box-sizing: border-box;
  border: 1px solid #dce4e0;
  background: #fbfcfc;
  border-radius: 10px;
  padding: 12px 14px;
  color: #17211f;
  font: inherit;
  outline: none;
  transition: .18s ease;
}

#aparencia-form textarea {
  resize: vertical;
  min-height: 95px;
}

#aparencia-form input[type="text"]:focus,
#aparencia-form input[type="url"]:focus,
#aparencia-form textarea:focus {
  background: #fff;
  border-color: #1e6f5c;
  box-shadow: 0 0 0 3px rgba(30,111,92,.10);
}

#aparencia-form input[type="range"] {
  width: 100%;
  accent-color: #1e6f5c;
  cursor: pointer;
}

#aparencia-form #logo-tamanho-valor {
  display: inline-block;
  min-width: 55px;
  padding: 6px 9px;
  background: #edf6f2;
  color: #1e6f5c;
  border-radius: 8px;
  text-align: center;
  font-size: 12px;
  font-weight: 800;
}

#aparencia-form input[type="file"] {
  width: 100%;
  box-sizing: border-box;
  padding: 10px;
  border: 1px dashed #cbd8d3;
  border-radius: 12px;
  background: #f8faf9;
  color: #66736e;
  cursor: pointer;
}

#aparencia-form input[type="color"] {
  width: 52px;
  height: 42px;
  padding: 3px;
  border: 1px solid #dce4e0;
  border-radius: 10px;
  background: #fff;
  cursor: pointer;
}

#logo-preview {
  min-height: 120px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 12px;
  padding: 18px;
  border: 1px solid #e1e7e4;
  border-radius: 14px;
  background: #f7f9f8;
  overflow: hidden;
}

#logo-preview img {
  max-width: 100%;
  max-height: 120px;
  object-fit: contain;
}

#logo-nome {
  display: block;
  margin-top: 8px;
  color: #78837f;
  font-size: 12px;
}

#remover-logo {
  margin-top: 10px;
  border: 0;
  border-radius: 9px;
  padding: 9px 13px;
  background: #fff1f1;
  color: #a33b3b;
  font-size: 12px;
  font-weight: 700;
  cursor: pointer;
}

#remover-logo:hover {
  background: #ffe5e5;
}

.aparencia-preview-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 14px;
}

.preview-live {
  padding: 6px 10px;
  border-radius: 20px;
  background: #e9f7ef;
  color: #14734d;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: .5px;
}

.aparencia-preview {
  overflow: hidden;
  border: 1px solid #dfe6e3;
  border-radius: 18px;
  box-shadow: 0 10px 30px rgba(16,47,41,.08);
}

#aparencia-form .form-actions {
  position: sticky;
  bottom: 18px;
  z-index: 5;
  display: flex;
  justify-content: flex-end;
  padding: 14px 16px;
  background: rgba(255,255,255,.94);
  backdrop-filter: blur(12px);
  border: 1px solid #e1e7e4;
  border-radius: 14px;
  box-shadow: 0 8px 30px rgba(16,47,41,.10);
}

@media (max-width: 750px) {
  #aparencia-form .form-grid {
    grid-template-columns: 1fr;
  }

  #aparencia-form .form-field.full {
    grid-column: auto;
  }

  #aparencia-form .form-card {
    padding: 20px;
  }
}


/* =========================================================
   APARÊNCIA — EDITOR MODERNO
   ========================================================= */

#admin-section-aparencia {
  max-width: 1180px;
}

#aparencia-form {
  display: flex;
  flex-direction: column;
  gap: 22px;
}

#aparencia-form .form-card {
  background: #fff;
  border: 1px solid #e4e9e7;
  border-radius: 18px;
  padding: 26px;
  box-shadow: 0 4px 18px rgba(16,47,41,.04);
}

#aparencia-form .form-card-title {
  display: flex;
  align-items: flex-start;
  gap: 14px;
  margin-bottom: 24px;
}

#aparencia-form .form-card-title > span {
  width: 42px;
  height: 42px;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #edf6f2;
  border-radius: 12px;
  font-size: 20px;
}

#aparencia-form .form-card-title h2 {
  margin: 2px 0 5px;
  font-size: 18px;
  color: #17211f;
}

#aparencia-form .form-card-title p {
  margin: 0;
  color: #7a8581;
  font-size: 13px;
}

#aparencia-form .form-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0,1fr));
  gap: 20px;
}

#aparencia-form .form-field.full {
  grid-column: 1 / -1;
}

#aparencia-form .form-field label {
  display: block;
  margin-bottom: 8px;
  color: #34413d;
  font-size: 13px;
  font-weight: 700;
}

#aparencia-form input[type="text"],
#aparencia-form input[type="url"],
#aparencia-form textarea {
  width: 100%;
  box-sizing: border-box;
  border: 1px solid #dce4e0;
  background: #fbfcfc;
  border-radius: 10px;
  padding: 12px 14px;
  color: #17211f;
  font: inherit;
  outline: none;
  transition: .18s ease;
}

#aparencia-form textarea {
  resize: vertical;
  min-height: 95px;
}

#aparencia-form input[type="text"]:focus,
#aparencia-form input[type="url"]:focus,
#aparencia-form textarea:focus {
  background: #fff;
  border-color: #1e6f5c;
  box-shadow: 0 0 0 3px rgba(30,111,92,.10);
}

#aparencia-form input[type="range"] {
  width: 100%;
  accent-color: #1e6f5c;
  cursor: pointer;
}

#aparencia-form #logo-tamanho-valor {
  display: inline-block;
  min-width: 55px;
  padding: 6px 9px;
  background: #edf6f2;
  color: #1e6f5c;
  border-radius: 8px;
  text-align: center;
  font-size: 12px;
  font-weight: 800;
}

#aparencia-form input[type="file"] {
  width: 100%;
  box-sizing: border-box;
  padding: 10px;
  border: 1px dashed #cbd8d3;
  border-radius: 12px;
  background: #f8faf9;
  color: #66736e;
  cursor: pointer;
}

#aparencia-form input[type="color"] {
  width: 52px;
  height: 42px;
  padding: 3px;
  border: 1px solid #dce4e0;
  border-radius: 10px;
  background: #fff;
  cursor: pointer;
}

#logo-preview {
  min-height: 120px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 12px;
  padding: 18px;
  border: 1px solid #e1e7e4;
  border-radius: 14px;
  background: #f7f9f8;
  overflow: hidden;
}

#logo-preview img {
  max-width: 100%;
  max-height: 120px;
  object-fit: contain;
}

#logo-nome {
  display: block;
  margin-top: 8px;
  color: #78837f;
  font-size: 12px;
}

#remover-logo {
  margin-top: 10px;
  border: 0;
  border-radius: 9px;
  padding: 9px 13px;
  background: #fff1f1;
  color: #a33b3b;
  font-size: 12px;
  font-weight: 700;
  cursor: pointer;
}

#remover-logo:hover {
  background: #ffe5e5;
}

.aparencia-preview-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 14px;
}

.preview-live {
  padding: 6px 10px;
  border-radius: 20px;
  background: #e9f7ef;
  color: #14734d;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: .5px;
}

.aparencia-preview {
  overflow: hidden;
  border: 1px solid #dfe6e3;
  border-radius: 18px;
  box-shadow: 0 10px 30px rgba(16,47,41,.08);
}

#aparencia-form .form-actions {
  position: sticky;
  bottom: 18px;
  z-index: 5;
  display: flex;
  justify-content: flex-end;
  padding: 14px 16px;
  background: rgba(255,255,255,.94);
  backdrop-filter: blur(12px);
  border: 1px solid #e1e7e4;
  border-radius: 14px;
  box-shadow: 0 8px 30px rgba(16,47,41,.10);
}

@media (max-width: 750px) {
  #aparencia-form .form-grid {
    grid-template-columns: 1fr;
  }

  #aparencia-form .form-field.full {
    grid-column: auto;
  }

  #aparencia-form .form-card {
    padding: 20px;
  }
}


/* =========================================================
   APARÊNCIA — EDITOR DE CORES
   ========================================================= */

#aparencia-form input[type="color"] {
  appearance: none;
  -webkit-appearance: none;

  width: 58px;
  height: 46px;

  padding: 4px;

  border: 1px solid #d7e1dd;
  border-radius: 12px;

  background: #fff;

  cursor: pointer;

  transition:
    transform .15s ease,
    border-color .15s ease,
    box-shadow .15s ease;
}

#aparencia-form input[type="color"]:hover {
  transform: translateY(-1px);

  border-color: #1e6f5c;

  box-shadow:
    0 4px 12px rgba(30,111,92,.12);
}

#aparencia-form input[type="color"]:focus {
  outline: none;

  border-color: #1e6f5c;

  box-shadow:
    0 0 0 3px rgba(30,111,92,.12);
}

#aparencia-form input[type="color"]::-webkit-color-swatch-wrapper {
  padding: 0;
}

#aparencia-form input[type="color"]::-webkit-color-swatch {
  border: none;
  border-radius: 8px;
}

#aparencia-form .color-editor {
  display: flex;
  align-items: center;
  gap: 12px;
}

#aparencia-form .color-editor input[type="text"] {
  flex: 1;
  min-width: 0;

  font-family:
    ui-monospace,
    SFMono-Regular,
    Menlo,
    Monaco,
    Consolas,
    monospace;

  font-size: 13px;
  font-weight: 700;

  letter-spacing: .4px;

  text-transform: uppercase;
}

#aparencia-form .color-editor::before {
  content: "HEX";

  display: flex;
  align-items: center;
  justify-content: center;

  height: 36px;

  padding: 0 9px;

  border-radius: 8px;

  background: #f0f4f2;

  color: #687671;

  font-size: 10px;
  font-weight: 800;
  letter-spacing: .6px;
}

#aparencia-form .form-field:has(input[type="color"]) {
  padding: 16px;

  border: 1px solid #e5ebe8;
  border-radius: 14px;

  background: #fbfcfc;

  transition:
    border-color .18s ease,
    background .18s ease,
    box-shadow .18s ease;
}

#aparencia-form .form-field:has(input[type="color"]):hover {
  background: #fff;

  border-color: #d5e1dc;

  box-shadow:
    0 4px 14px rgba(16,47,41,.04);
}

#aparencia-form .form-field:has(input[type="color"]) label {
  margin-bottom: 12px;
}


/* =========================================================
   APARÊNCIA — PREVIEW AO VIVO
   ========================================================= */

#aparencia-preview {
  position: relative;
  overflow: hidden;

  border: 1px solid #dfe7e3;
  border-radius: 20px;

  background: var(--preview-fundo, #ffffff);

  box-shadow:
    0 18px 50px rgba(20, 45, 38, .10);

  transition:
    background .2s ease,
    border-color .2s ease,
    box-shadow .2s ease;
}

/* Barra superior */

#aparencia-preview .preview-header {
  min-height: 58px;

  display: flex;
  align-items: center;
  justify-content: space-between;

  padding: 0 22px;

  background:
    color-mix(
      in srgb,
      var(--preview-fundo, #ffffff) 94%,
      var(--preview-principal, #1e6f5c)
    );

  border-bottom: 1px solid
    color-mix(
      in srgb,
      var(--preview-principal, #1e6f5c) 12%,
      transparent
    );
}

/* Logo */

#aparencia-preview .preview-logo {
  display: flex;
  align-items: center;
  justify-content: center;

  width: 42px;
  height: 42px;

  flex-shrink: 0;

  overflow: hidden;
}

#aparencia-preview .preview-logo img {
  display: block;

  width: 42px;
  height: 42px;

  max-width: none;
  max-height: none;

  object-fit: contain;

  transition:
    width .15s ease,
    height .15s ease;
}

/* Nome */

#aparencia-preview #preview-nome {
  color: var(--preview-texto, #17211f);

  font-size: 14px;
  font-weight: 800;
}

#aparencia-preview #preview-subtitulo {
  color:
    color-mix(
      in srgb,
      var(--preview-texto, #17211f) 60%,
      transparent
    );

  font-size: 11px;
}

/* Conteúdo principal */

#aparencia-preview .preview-hero {
  min-height: 280px;

  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;

  padding: 50px 30px;

  text-align: center;

  background:
    radial-gradient(
      circle at 50% 0%,
      color-mix(
        in srgb,
        var(--preview-principal, #1e6f5c) 13%,
        transparent
      ),
      transparent 58%
    ),
    var(--preview-fundo, #ffffff);
}

/* Título */

#aparencia-preview #preview-titulo {
  max-width: 680px;

  margin: 0;

  color: var(--preview-texto, #17211f);

  font-size: clamp(26px, 4vw, 42px);
  line-height: 1.08;
  font-weight: 850;
  letter-spacing: -.8px;
}

/* Descrição */

#aparencia-preview #preview-descricao {
  max-width: 590px;

  margin: 16px auto 0;

  color:
    color-mix(
      in srgb,
      var(--preview-texto, #17211f) 68%,
      transparent
    );

  font-size: 14px;
  line-height: 1.7;
}

/* Botões */

#aparencia-preview .preview-actions {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-wrap: wrap;

  gap: 10px;

  margin-top: 26px;
}

#aparencia-preview .preview-button {
  min-height: 42px;

  padding: 0 18px;

  border-radius: 10px;

  font-size: 12px;
  font-weight: 800;

  cursor: default;

  transition:
    transform .15s ease,
    box-shadow .15s ease;
}

#aparencia-preview .preview-primary {
  border: 1px solid var(--preview-principal, #1e6f5c);

  background: var(--preview-principal, #1e6f5c);

  color: #fff;

  box-shadow:
    0 7px 18px
    color-mix(
      in srgb,
      var(--preview-principal, #1e6f5c) 25%,
      transparent
    );
}

#aparencia-preview .preview-secondary {
  border: 1px solid
    color-mix(
      in srgb,
      var(--preview-principal, #1e6f5c) 30%,
      transparent
    );

  background: transparent;

  color: var(--preview-principal, #1e6f5c);
}

/* Rodapé */

#aparencia-preview .preview-footer {
  padding: 14px 22px;

  border-top: 1px solid
    color-mix(
      in srgb,
      var(--preview-texto, #17211f) 8%,
      transparent
    );

  color:
    color-mix(
      in srgb,
      var(--preview-texto, #17211f) 55%,
      transparent
    );

  font-size: 10px;

  text-align: center;
}

/* Indicador */

#aparencia-preview::before {
  content: "PRÉ-VISUALIZAÇÃO AO VIVO";

  position: absolute;
  top: 12px;
  right: 14px;

  z-index: 10;

  padding: 5px 8px;

  border-radius: 999px;

  background:
    color-mix(
      in srgb,
      var(--preview-principal, #1e6f5c) 12%,
      #ffffff
    );

  color: var(--preview-principal, #1e6f5c);

  font-size: 8px;
  font-weight: 900;
  letter-spacing: .5px;

  pointer-events: none;
}

/* Responsivo */

@media (max-width: 650px) {

  #aparencia-preview .preview-header {
    padding: 0 14px;
  }

  #aparencia-preview .preview-hero {
    padding: 38px 18px;
  }

  #aparencia-preview .preview-actions {
    flex-direction: column;
    width: 100%;
  }

  #aparencia-preview .preview-button {
    width: 100%;
  }

}


/* COMBO 3 - LAMINA */

/* Visual profissional da página individual */

.slide-header {
  margin-bottom: 28px;
}

.slide-title {
  gap: 24px;
}

.slide-title h1 {
  letter-spacing: -1.5px;
}

.viewer-section {
  margin-top: 10px;
}

.viewer-layout {
  gap: 24px;
  align-items: start;
}

.viewer-container {
  overflow: hidden;
  border-radius: 18px;
  background: #101614;
  box-shadow: 0 18px 45px rgba(0,0,0,.12);
}

.viewer {
  min-height: 520px;
}

.viewer-controls {
  border-radius: 12px;
  padding: 6px;
  backdrop-filter: blur(10px);
}

.viewer-controls button {
  transition:
    transform .18s ease,
    background .18s ease;
}

.viewer-controls button:hover {
  transform: translateY(-2px);
}

.info-panel {
  position: sticky;
  top: 24px;

  border-radius: 18px;

  box-shadow:
    0 10px 30px rgba(0,0,0,.06);

  transition:
    transform .2s ease,
    box-shadow .2s ease;
}

.info-panel:hover {
  transform: translateY(-2px);

  box-shadow:
    0 15px 35px rgba(0,0,0,.09);
}

/* Mobile */

@media (max-width: 900px) {

  .viewer-layout {
    grid-template-columns: 1fr;
  }

  .info-panel {
    position: static;
  }

  .slide-title {
    flex-direction: column;
    align-items: flex-start;
  }

}

@media (max-width: 600px) {

  .slide-header {
    margin-bottom: 20px;
  }

  .slide-title h1 {
    font-size: 2rem;
    letter-spacing: -1px;
  }

  .viewer-container {
    border-radius: 14px;
  }

  .viewer {
    min-height: 330px;
  }

  .info-panel {
    padding: 18px;
    border-radius: 14px;
  }

}


/* ============================================================
   MARCAÇÕES DAS ESTRUTURAS DA LÂMINA
   ============================================================ */

.estrutura-overlay {

  pointer-events: auto;

  overflow: visible;

}


.estrutura-marcacao {

  position: relative;

  width: 100%;

  height: 100%;

  overflow: visible;

}


.estrutura-caixa {

  position: absolute;

  inset: 0;

  border: 3px solid #ff3b30;

  background: rgba(255, 59, 48, 0.10);

  border-radius: 8px;

  box-shadow:
    0 0 0 1px rgba(255,255,255,.8),
    0 0 18px rgba(255,59,48,.45);

}


.estrutura-label {

  position: absolute;

  left: 50%;

  top: -10px;

  transform:
    translate(-50%, -100%);

  padding: 6px 10px;

  border-radius: 7px;

  background: #ff3b30;

  color: white;

  font-size: 13px;

  font-weight: 700;

  line-height: 1.2;

  white-space: nowrap;

  box-shadow:
    0 4px 14px rgba(0,0,0,.28);

  pointer-events: none;

}


.estrutura-marcacao:hover
.estrutura-caixa {

  border-color: #fff;

  background:
    rgba(255, 59, 48, 0.18);

}


.estrutura-marcacao:hover
.estrutura-label {

  background: #c62828;

}


/* Em telas pequenas */

@media (max-width: 700px) {

  .estrutura-label {

    font-size: 11px;

    padding: 4px 7px;

  }

}




/* =====================================================
   MARCADORES DAS ESTRUTURAS HISTOLÓGICAS
   ===================================================== */

.estrutura-overlay {
  pointer-events: none;
  overflow: visible;
  z-index: 50;
}

.estrutura-marcacao {
  position: relative;
  width: 100%;
  height: 100%;
  min-width: 18px;
  min-height: 18px;
  pointer-events: none;
}

.estrutura-ponto {
  position: absolute;
  inset: 0;
  border: 3px solid #ff3b30;
  border-radius: 50%;
  box-sizing: border-box;
  background: rgba(255, 59, 48, 0.08);
  box-shadow:
    0 0 0 2px rgba(255,255,255,0.85),
    0 0 14px rgba(255,59,48,0.65);
}

.estrutura-label {
  position: absolute;
  left: 50%;
  top: -34px;
  transform: translateX(-50%);

  padding: 5px 9px;

  background: rgba(20, 20, 20, 0.92);
  color: white;

  border-radius: 7px;

  font-size: 13px;
  font-weight: 700;

  white-space: nowrap;

  box-shadow:
    0 4px 14px rgba(0,0,0,0.35);

  border: 1px solid rgba(255,255,255,0.15);
}


/* ============================================================
   COMBO 5 - ESTRUTURAS INTERATIVAS
   ============================================================ */

.structure {
  cursor: pointer;
  transition:
    transform 0.2s ease,
    box-shadow 0.2s ease;
}

.structure:hover {
  transform: translateX(3px);
}

.structure.estrutura-selecionada {
  transform: translateX(5px);
  box-shadow:
    0 0 0 2px rgba(255,255,255,0.12),
    0 8px 24px rgba(0,0,0,0.12);
}

.estrutura-overlay {
  cursor: pointer;
}

.estrutura-overlay .estrutura-ponto {
  transition:
    transform 0.2s ease,
    box-shadow 0.2s ease;
}

.estrutura-overlay.estrutura-selecionada .estrutura-ponto {
  transform: scale(1.5);
  box-shadow:
    0 0 0 6px rgba(255,255,255,0.22),
    0 0 22px rgba(255,255,255,0.75);
}

.estrutura-overlay.estrutura-selecionada .estrutura-label {
  transform: scale(1.06);
  transform-origin: left center;
}


/* ============================================================
   COMBO 6 - NAVEGACAO ENTRE ESTRUTURAS
   ============================================================ */

.estrutura-navegacao {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  margin-top: 10px;
  margin-bottom: 10px;
}

.estrutura-nav-button {
  width: 38px;
  height: 34px;
  border: 0;
  border-radius: 8px;
  cursor: pointer;
  font-size: 18px;
  line-height: 1;
  transition:
    transform 0.18s ease,
    opacity 0.18s ease,
    box-shadow 0.18s ease;
}

.estrutura-nav-button:hover:not(:disabled) {
  transform: scale(1.06);
}

.estrutura-nav-button:disabled {
  opacity: 0.35;
  cursor: not-allowed;
}

.estrutura-contador {
  min-width: 125px;
  text-align: center;
  font-size: 13px;
  font-weight: 600;
  white-space: nowrap;
}


/* ============================================================
   COMBO 7 - MODO ESTUDO
   ============================================================ */

.estrutura-estudo-panel {
  position: absolute;
  left: 20px;
  bottom: 20px;
  width: min(360px, calc(100% - 40px));
  z-index: 30;
  padding: 18px;
  border-radius: 14px;
  background: rgba(15, 15, 15, 0.94);
  color: white;
  box-shadow:
    0 15px 45px rgba(0, 0, 0, 0.35);
  backdrop-filter: blur(12px);
}

.estrutura-estudo-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}

.estrutura-estudo-eyebrow {
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 0.12em;
  opacity: 0.65;
}

#estrutura-estudo-fechar {
  width: 30px;
  height: 30px;
  border: 0;
  border-radius: 50%;
  cursor: pointer;
  font-size: 20px;
  line-height: 1;
}

.estrutura-estudo-panel h3 {
  margin: 10px 0 6px;
  font-size: 20px;
}

.estrutura-estudo-panel p {
  margin: 0;
  line-height: 1.55;
  font-size: 14px;
  opacity: 0.8;
}

@media (max-width: 700px) {

  .estrutura-estudo-panel {
    left: 10px;
    right: 10px;
    bottom: 10px;
    width: auto;
  }

}


/* ============================================================
   PAINEL DE ESTUDO - ESTILO FINAL
   ============================================================ */

#estrutura-estudo-panel.estrutura-estudo-panel-refinado {
  box-sizing: border-box !important;
  width: 100% !important;
  margin-top: 16px !important;
  padding: 20px !important;

  background: #ffffff !important;
  border: 1px solid #e2e6eb !important;
  border-radius: 16px !important;

  box-shadow:
    0 8px 24px rgba(0, 0, 0, .08) !important;

  color: #1f2937 !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
.estrutura-estudo-header {
  display: flex !important;
  align-items: center !important;
  justify-content: space-between !important;
  gap: 12px !important;
  margin-bottom: 12px !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
.estrutura-estudo-eyebrow {
  display: block !important;

  font-size: 10px !important;
  font-weight: 800 !important;
  letter-spacing: .10em !important;
  text-transform: uppercase !important;

  color: #6b7280 !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-fechar {
  flex: 0 0 auto !important;

  width: 32px !important;
  height: 32px !important;

  border: 0 !important;
  border-radius: 50% !important;

  font-size: 22px !important;
  line-height: 1 !important;

  cursor: pointer !important;

  background: #f1f3f5 !important;
  color: #555 !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-fechar:hover {
  background: #e5e7eb !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-nome {
  margin: 0 0 8px 0 !important;

  font-size: 23px !important;
  font-weight: 750 !important;
  line-height: 1.2 !important;

  color: #111827 !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-descricao {
  margin: 0 !important;

  font-size: 15px !important;
  line-height: 1.65 !important;

  color: #4b5563 !important;
}


/* PAINEL DE ESTUDO - COMPACTO */

#estrutura-estudo-panel.estrutura-estudo-panel-refinado {
  padding: 14px !important;
  border-radius: 12px !important;
  margin-top: 10px !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
.estrutura-estudo-header {
  margin-bottom: 7px !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
.estrutura-estudo-eyebrow {
  font-size: 9px !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-nome {
  margin-bottom: 5px !important;
  font-size: 18px !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-descricao {
  font-size: 13px !important;
  line-height: 1.45 !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-fechar {
  width: 27px !important;
  height: 27px !important;
  font-size: 18px !important;
}


/* ============================================================
   PAINEL DE ESTUDO - COMPACTO DE VERDADE
   ============================================================ */

#estrutura-estudo-panel.estrutura-estudo-panel-refinado {
  width: auto !important;
  max-width: 360px !important;
  padding: 11px 14px !important;
  margin: 8px auto 0 0 !important;
  border-radius: 10px !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
.estrutura-estudo-header {
  margin-bottom: 4px !important;
  gap: 8px !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
.estrutura-estudo-eyebrow {
  font-size: 8px !important;
  letter-spacing: .07em !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-nome {
  font-size: 16px !important;
  margin: 0 0 3px 0 !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-descricao {
  font-size: 12px !important;
  line-height: 1.35 !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-fechar {
  width: 24px !important;
  height: 24px !important;
  font-size: 16px !important;
}


/* ============================================================
   DESTAQUE DA ESTRUTURA SELECIONADA
   ============================================================ */

.structure {
  transition:
    background .18s ease,
    border-color .18s ease,
    box-shadow .18s ease,
    transform .18s ease !important;
}

.structure.estrutura-selecionada {
  background: #f1f5f9 !important;
  border-color: #94a3b8 !important;
  box-shadow: 0 3px 10px rgba(0, 0, 0, .08) !important;
  transform: translateX(2px) !important;
}

.structure.estrutura-selecionada .structure-dot {
  transform: scale(1.15) !important;
}



===== backup-reparador-completo-20260819-212337\src\admin.css =====
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap');

.admin-page {
  min-height: 100vh;
  background: #f5f7f6;
  color: #17211f;
  font-family: Inter, Arial, sans-serif;
}

.admin-header {
  height: 76px;
  background: white;
  border-bottom: 1px solid #e5e9e7;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 32px;
}

.admin-brand {
  display: flex;
  align-items: center;
  gap: 12px;
}

.admin-logo {
  width: 42px;
  height: 42px;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #1e6f5c;
  color: white;
  text-decoration: none;
  font-size: 21px;
}

.admin-brand strong {
  display: block;
  font-size: 16px;
}

.admin-brand small {
  display: block;
  margin-top: 3px;
  color: #78827f;
  font-size: 12px;
}

.admin-user {
  display: flex;
  align-items: center;
  gap: 18px;
  color: #68736f;
  font-size: 13px;
}

.admin-logout {
  border: 0;
  background: #f0f3f2;
  color: #25312d;
  padding: 9px 15px;
  border-radius: 9px;
  cursor: pointer;
  font-weight: 600;
}

.admin-layout {
  display: flex;
  min-height: calc(100vh - 76px);
}

.admin-sidebar {
  width: 230px;
  flex-shrink: 0;
  background: #102f29;
  padding: 24px 14px;
  display: flex;
  flex-direction: column;
}

.admin-menu {
  width: 100%;
  border: 0;
  background: transparent;
  color: #b9c8c3;
  padding: 13px 15px;
  border-radius: 10px;
  margin-bottom: 5px;
  display: flex;
  align-items: center;
  gap: 12px;
  cursor: pointer;
  text-align: left;
  font-size: 14px;
}

.admin-menu:hover {
  background: rgba(255,255,255,.08);
  color: white;
}

.admin-menu.active {
  background: #1e6f5c;
  color: white;
}

.admin-sidebar-bottom {
  margin-top: auto;
  padding: 15px 5px 5px;
}

.admin-sidebar-bottom a {
  color: #aebdb8;
  text-decoration: none;
  font-size: 13px;
}

.admin-content {
  flex: 1;
  padding: 42px;
  max-width: 1300px;
}

.admin-section {
  display: none;
}

.admin-section.active {
  display: block;
}

.admin-title-row {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 30px;
  margin-bottom: 30px;
}

.admin-title-row h1 {
  margin: 6px 0 8px;
  font-size: 34px;
  letter-spacing: -1px;
}

.admin-title-row p {
  margin: 0;
  color: #707b77;
}

.eyebrow {
  color: #1e6f5c;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 1.5px;
}

.admin-stats {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
  margin-bottom: 25px;
}

.admin-stat {
  background: white;
  border: 1px solid #e5e9e7;
  border-radius: 14px;
  padding: 20px;
  display: flex;
  align-items: center;
  gap: 14px;
}

.admin-stat > span {
  width: 42px;
  height: 42px;
  border-radius: 11px;
  background: #edf5f2;
  display: flex;
  align-items: center;
  justify-content: center;
}

.admin-stat strong {
  display: block;
  font-size: 24px;
}

.admin-stat small {
  color: #7a8581;
}

.admin-toolbar {
  display: flex;
  gap: 12px;
  margin-bottom: 20px;
}

.admin-search {
  flex: 1;
  background: white;
  border: 1px solid #dfe5e2;
  border-radius: 10px;
  padding: 0 14px;
  display: flex;
  align-items: center;
  gap: 9px;
}

.admin-search input {
  width: 100%;
  border: 0;
  outline: 0;
  padding: 13px 0;
  font: inherit;
}

.admin-toolbar select {
  min-width: 170px;
  border: 1px solid #dfe5e2;
  border-radius: 10px;
  padding: 0 13px;
  background: white;
  color: #26322e;
}

.admin-laminas-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.admin-lamina-card {
  background: white;
  border: 1px solid #e2e7e5;
  border-radius: 14px;
  padding: 14px;
  display: grid;
  grid-template-columns: 150px minmax(0,1fr) auto;
  align-items: center;
  gap: 20px;
}

.admin-lamina-card:hover {
  border-color: #b8cec7;
  box-shadow: 0 8px 25px rgba(20,45,39,.06);
}

.admin-lamina-thumb {
  height: 105px;
  border-radius: 10px;
  overflow: hidden;
  background: #edf1f0;
}

.admin-lamina-thumb img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.admin-no-image {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 30px;
}

.admin-lamina-top {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 7px;
}

.admin-category {
  color: #1e6f5c;
  font-size: 11px;
  font-weight: 800;
  text-transform: uppercase;
}

.admin-status {
  font-size: 11px;
  font-weight: 700;
  padding: 4px 8px;
  border-radius: 20px;
}

.admin-status.published {
  color: #14734d;
  background: #e9f7ef;
}

.admin-status.hidden {
  color: #6f7774;
  background: #eef0ef;
}

.admin-lamina-info h2 {
  margin: 0 0 6px;
  font-size: 18px;
}

.admin-lamina-info p {
  margin: 0;
  color: #747e7b;
  font-size: 13px;
  line-height: 1.5;
  max-width: 650px;
}

.admin-tags {
  display: flex;
  gap: 7px;
  flex-wrap: wrap;
  margin-top: 9px;
}

.admin-tags span {
  background: #f1f4f3;
  color: #66716d;
  padding: 5px 8px;
  border-radius: 6px;
  font-size: 11px;
}

.admin-lamina-actions {
  display: flex;
  align-items: center;
  gap: 7px;
}

.admin-action {
  width: 38px;
  height: 38px;
  border: 1px solid #e0e5e3;
  border-radius: 9px;
  background: white;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  text-decoration: none;
  font-size: 15px;
}

.admin-action:hover {
  background: #f1f5f3;
  transform: translateY(-1px);
}

.admin-action.delete:hover {
  background: #fff0f0;
  border-color: #f0caca;
}

.admin-placeholder,
.admin-empty {
  background: white;
  border: 1px solid #e2e7e5;
  border-radius: 16px;
  padding: 60px 30px;
  text-align: center;
}

.admin-placeholder > div,
.admin-empty > div {
  font-size: 42px;
  margin-bottom: 12px;
}

.admin-placeholder h2,
.admin-empty h2 {
  margin: 0 0 8px;
}

.admin-placeholder p,
.admin-empty p {
  color: #737d79;
}

.admin-error {
  margin: 40px;
  padding: 20px;
  background: #fff0f0;
  color: #9b3131;
  border-radius: 10px;
}

@media (max-width: 850px) {

  .admin-sidebar {
    width: 70px;
  }

  .admin-menu {
    justify-content: center;
  }

  .admin-menu span {
    display: none;
  }

  .admin-content {
    padding: 25px;
  }

  .admin-lamina-card {
    grid-template-columns: 100px 1fr;
  }

  .admin-lamina-actions {
    grid-column: 1 / -1;
    justify-content: flex-end;
  }

}

@media (max-width: 600px) {

  .admin-header {
    padding: 0 15px;
  }

  .admin-user span {
    display: none;
  }

  .admin-content {
    padding: 18px;
  }

  .admin-title-row {
    flex-direction: column;
  }

  .admin-stats {
    grid-template-columns: 1fr;
  }

  .admin-toolbar {
    flex-direction: column;
  }

  .admin-lamina-card {
    grid-template-columns: 1fr;
  }

  .admin-lamina-thumb {
    height: 160px;
  }

}


===== backup-reparador-completo-20260819-212337\src\main.backup-admin-20260819-165208.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      error ||
      !session?.user
    ) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      error ||
      !session?.user
    ) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()






































































===== backup-reparador-completo-20260819-212337\src\main.backup-admin-route-final.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      error ||
      !session?.user
    ) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      error ||
      !session?.user
    ) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()







































































===== backup-reparador-completo-20260819-212337\src\main.backup-auth-session-final.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()

    if (user) {

      window.location.hash =
        '#admin'

      return
    }

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }

if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()




































































===== backup-reparador-completo-20260819-212337\src\main.backup-clique-20260819-172639.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'ESTRUTURAS PARA DESENHAR:',
    estruturas
  )

  if (!estruturas.length) {
    console.warn(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )
    return
  }

  const tiledImage =
    viewer.world.getItemAt(0)

  if (!tiledImage) {
    console.warn(
      'Imagem ainda não disponível.'
    )
    return
  }

  const imageSize =
    tiledImage.getContentSize()

  const imageWidth =
    Number(imageSize.x)

  const imageHeight =
    Number(imageSize.y)

  console.log(
    'TAMANHO DA IMAGEM:',
    imageWidth,
    imageHeight
  )

  if (!imageWidth || !imageHeight) {
    console.warn(
      'Tamanho da imagem inválido.'
    )
    return
  }

  viewer.clearOverlays()

  estruturas.forEach(
    (estrutura, index) => {

      console.log(
        'CRIANDO MARCADOR:',
        index,
        estrutura
      )

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )

      const descricao =
        String(
          estrutura.descricao || ''
        )

      let x =
        Number(estrutura.x)

      let y =
        Number(estrutura.y)

      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )

      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )

      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {
        console.warn(
          'Estrutura inválida:',
          estrutura
        )
        return
      }

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth

      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight

      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth

      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )

      console.log(
        'RECT:',
        index,
        rect
      )

      const overlay =
        document.createElement('div')

      overlay.className =
        'estrutura-overlay'

      overlay.dataset.estruturaIndex =
        String(index)

      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `

      overlay.title =
        descricao
          ? `${nome}: ${descricao}`
          : nome

      overlay.style.display =
        'block'

      overlay.style.visibility =
        'visible'

      overlay.style.opacity =
        '1'

      overlay.style.zIndex =
        '999999'

      overlay.style.pointerEvents =
        'auto'

      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

      })

      console.log(
        'MARCADOR ADICIONADO:',
        index,
        overlay
      )

    }
  )

}

function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


 viewer.addOnceHandler(
  'open',
  () => {

    console.log(
      '========================================'
    )

    console.log(
      'DIAGNOSTICO DA LAMINA NO OPEN'
    )

    console.log(
      'LAMINA NO OPEN:',
      lamina
    )

    console.log(
      'ESTRUTURAS NO OPEN:',
      lamina?.estruturas
    )

    console.log(
      'TIPO DE ESTRUTURAS:',
      typeof lamina?.estruturas
    )

    console.log(
      'E ARRAY:',
      Array.isArray(lamina?.estruturas)
    )

    console.log(
      'QUANTIDADE:',
      Array.isArray(lamina?.estruturas)
        ? lamina.estruturas.length
        : 'NAO E ARRAY'
    )

    console.log(
      '========================================'
    )

    desenharEstruturas(
      viewer,
      lamina
    )

  }
)

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()










































































===== backup-reparador-completo-20260819-212337\src\main.backup-clique-20260819-173237.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'ESTRUTURAS PARA DESENHAR:',
    estruturas
  )

  if (!estruturas.length) {
    console.warn(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )
    return
  }

  const tiledImage =
    viewer.world.getItemAt(0)

  if (!tiledImage) {
    console.warn(
      'Imagem ainda não disponível.'
    )
    return
  }

  const imageSize =
    tiledImage.getContentSize()

  const imageWidth =
    Number(imageSize.x)

  const imageHeight =
    Number(imageSize.y)

  console.log(
    'TAMANHO DA IMAGEM:',
    imageWidth,
    imageHeight
  )

  if (!imageWidth || !imageHeight) {
    console.warn(
      'Tamanho da imagem inválido.'
    )
    return
  }

  viewer.clearOverlays()

  estruturas.forEach(
    (estrutura, index) => {

      console.log(
        'CRIANDO MARCADOR:',
        index,
        estrutura
      )

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )

      const descricao =
        String(
          estrutura.descricao || ''
        )

      let x =
        Number(estrutura.x)

      let y =
        Number(estrutura.y)

      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )

      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )

      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {
        console.warn(
          'Estrutura inválida:',
          estrutura
        )
        return
      }

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth

      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight

      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth

      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )

      console.log(
        'RECT:',
        index,
        rect
      )

      const overlay =
        document.createElement('div')

      overlay.className =
        'estrutura-overlay'

      overlay.dataset.estruturaIndex =
        String(index)

      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `

      overlay.title =
        descricao
          ? `${nome}: ${descricao}`
          : nome

      overlay.style.display =
        'block'

      overlay.style.visibility =
        'visible'

      overlay.style.opacity =
        '1'

      overlay.style.zIndex =
        '999999'

      overlay.style.pointerEvents =
        'auto'

      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

      })

      console.log(
        'MARCADOR ADICIONADO:',
        index,
        overlay
      )

    }
  )

}

function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}


/* ============================================================
   CLIQUE DIRETO NOS MARCADORES DAS ESTRUTURAS
   ============================================================ */

function instalarCliqueDiretoEstruturas() {

  const marcadores =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    marcadores.length,
    'marcadores'
  )

  marcadores.forEach(
    (marcador, index) => {

      marcador.style.pointerEvents = 'auto'

      marcador.style.cursor = 'pointer'

      marcador.onclick = null

      marcador.addEventListener(
        'click',
        (event) => {

          event.preventDefault()
          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          try {

            if (
              typeof mostrarEstrutura ===
              'function'
            ) {

              console.log(
                'Executando mostrarEstrutura:',
                index
              )

              mostrarEstrutura(
                index,
                true
              )

              return

            }

            if (
              typeof mostrarInfoEstruturaSimples ===
              'function'
            ) {

              console.log(
                'Executando mostrarInfoEstruturaSimples:',
                index
              )

              if (
                typeof window.laminaAtual !==
                'undefined'
              ) {

                mostrarInfoEstruturaSimples(
                  index,
                  window.laminaAtual
                )

              }

              return

            }

            console.warn(
              'Nenhuma funcao de estrutura encontrada.'
            )

          } catch (erro) {

            console.error(
              'ERRO AO CLICAR NA ESTRUTURA:',
              erro
            )

          }

        },
        true
      )

    }
  )

  console.log(
    'CLIQUE DIRETO INSTALADO.'
  )

}
function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {

    console.warn(
      'CLIQUE: lamina nao encontrada.'
    )

    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    overlays.length,
    'marcadores'
  )

  overlays.forEach(
    (overlay, index) => {

      overlay.style.pointerEvents =
        'auto'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        (event) => {

          event.preventDefault()

          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          const estrutura =
            estruturas[index]

          if (!estrutura) {

            console.warn(
              'Estrutura nao encontrada:',
              index
            )

            return
          }

          console.log(
            'ESTRUTURA CLICADA:',
            estrutura
          )

          /*
           * Tenta usar a funcao principal
           * de exibicao/zoom existente.
           */

          if (
            typeof mostrarEstrutura ===
            'function'
          ) {

            console.log(
              'Chamando mostrarEstrutura...'
            )

            mostrarEstrutura(
              index,
              true
            )

            return
          }

          /*
           * Compatibilidade caso a funcao
           * principal nao esteja disponivel.
           */

          if (
            typeof mostrarInfoEstruturaSimples ===
            'function'
          ) {

            console.log(
              'Chamando mostrarInfoEstruturaSimples...'
            )

            mostrarInfoEstruturaSimples(
              index,
              lamina
            )

            return
          }

          if (
            typeof mostrarTextoEstrutura ===
            'function'
          ) {

            console.log(
              'Chamando mostrarTextoEstrutura...'
            )

            mostrarTextoEstrutura(
              index,
              lamina
            )

            return
          }

          console.warn(
            'Nenhuma funcao de painel encontrada.'
          )

        }

    }
  )

}

function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


 viewer.addOnceHandler(
  'open',
  () => {

    console.log(
      '========================================'
    )

    console.log(
      'DIAGNOSTICO DA LAMINA NO OPEN'
    )

    console.log(
      'LAMINA NO OPEN:',
      lamina
    )

    console.log(
      'ESTRUTURAS NO OPEN:',
      lamina?.estruturas
    )

    console.log(
      'TIPO DE ESTRUTURAS:',
      typeof lamina?.estruturas
    )

    console.log(
      'E ARRAY:',
      Array.isArray(lamina?.estruturas)
    )

    console.log(
      'QUANTIDADE:',
      Array.isArray(lamina?.estruturas)
        ? lamina.estruturas.length
        : 'NAO E ARRAY'
    )

    console.log(
      '========================================'
    )

    desenharEstruturas(
      viewer,
      lamina
    )

  }
)

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)



/*
 * Instala clique depois que os overlays
 * forem criados.
 */

setTimeout(
  () => {

    instalarCliqueDiretoEstruturas()

  },
  100
)

}


window.addEventListener(
  'hashchange',
  render
)


render()












































































===== backup-reparador-completo-20260819-212337\src\main.backup-clique-20260819-173303.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'ESTRUTURAS PARA DESENHAR:',
    estruturas
  )

  if (!estruturas.length) {
    console.warn(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )
    return
  }

  const tiledImage =
    viewer.world.getItemAt(0)

  if (!tiledImage) {
    console.warn(
      'Imagem ainda não disponível.'
    )
    return
  }

  const imageSize =
    tiledImage.getContentSize()

  const imageWidth =
    Number(imageSize.x)

  const imageHeight =
    Number(imageSize.y)

  console.log(
    'TAMANHO DA IMAGEM:',
    imageWidth,
    imageHeight
  )

  if (!imageWidth || !imageHeight) {
    console.warn(
      'Tamanho da imagem inválido.'
    )
    return
  }

  viewer.clearOverlays()

  estruturas.forEach(
    (estrutura, index) => {

      console.log(
        'CRIANDO MARCADOR:',
        index,
        estrutura
      )

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )

      const descricao =
        String(
          estrutura.descricao || ''
        )

      let x =
        Number(estrutura.x)

      let y =
        Number(estrutura.y)

      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )

      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )

      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {
        console.warn(
          'Estrutura inválida:',
          estrutura
        )
        return
      }

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth

      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight

      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth

      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )

      console.log(
        'RECT:',
        index,
        rect
      )

      const overlay =
        document.createElement('div')

      overlay.className =
        'estrutura-overlay'

      overlay.dataset.estruturaIndex =
        String(index)

      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `

      overlay.title =
        descricao
          ? `${nome}: ${descricao}`
          : nome

      overlay.style.display =
        'block'

      overlay.style.visibility =
        'visible'

      overlay.style.opacity =
        '1'

      overlay.style.zIndex =
        '999999'

      overlay.style.pointerEvents =
        'auto'

      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

      })

      console.log(
        'MARCADOR ADICIONADO:',
        index,
        overlay
      )

    }
  )

}

function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}


/* ============================================================
   CLIQUE DIRETO NOS MARCADORES DAS ESTRUTURAS
   ============================================================ */

function instalarCliqueDiretoEstruturas() {

  const marcadores =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    marcadores.length,
    'marcadores'
  )

  marcadores.forEach(
    (marcador, index) => {

      marcador.style.pointerEvents = 'auto'

      marcador.style.cursor = 'pointer'

      marcador.onclick = null

      marcador.addEventListener(
        'click',
        (event) => {

          event.preventDefault()
          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          try {

            if (
              typeof mostrarEstrutura ===
              'function'
            ) {

              console.log(
                'Executando mostrarEstrutura:',
                index
              )

              mostrarEstrutura(
                index,
                true
              )

              return

            }

            if (
              typeof mostrarInfoEstruturaSimples ===
              'function'
            ) {

              console.log(
                'Executando mostrarInfoEstruturaSimples:',
                index
              )

              if (
                typeof window.laminaAtual !==
                'undefined'
              ) {

                mostrarInfoEstruturaSimples(
                  index,
                  window.laminaAtual
                )

              }

              return

            }

            console.warn(
              'Nenhuma funcao de estrutura encontrada.'
            )

          } catch (erro) {

            console.error(
              'ERRO AO CLICAR NA ESTRUTURA:',
              erro
            )

          }

        },
        true
      )

    }
  )

  console.log(
    'CLIQUE DIRETO INSTALADO.'
  )

}
function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {

    console.warn(
      'CLIQUE: lamina nao encontrada.'
    )

    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    overlays.length,
    'marcadores'
  )

  overlays.forEach(
    (overlay, index) => {

      overlay.style.pointerEvents =
        'auto'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        (event) => {

          event.preventDefault()

          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          const estrutura =
            estruturas[index]

          if (!estrutura) {

            console.warn(
              'Estrutura nao encontrada:',
              index
            )

            return
          }

          console.log(
            'ESTRUTURA CLICADA:',
            estrutura
          )

          /*
           * Tenta usar a funcao principal
           * de exibicao/zoom existente.
           */

          if (
            typeof mostrarEstrutura ===
            'function'
          ) {

            console.log(
              'Chamando mostrarEstrutura...'
            )

            mostrarEstrutura(
              index,
              true
            )

            return
          }

          /*
           * Compatibilidade caso a funcao
           * principal nao esteja disponivel.
           */

          if (
            typeof mostrarInfoEstruturaSimples ===
            'function'
          ) {

            console.log(
              'Chamando mostrarInfoEstruturaSimples...'
            )

            mostrarInfoEstruturaSimples(
              index,
              lamina
            )

            return
          }

          if (
            typeof mostrarTextoEstrutura ===
            'function'
          ) {

            console.log(
              'Chamando mostrarTextoEstrutura...'
            )

            mostrarTextoEstrutura(
              index,
              lamina
            )

            return
          }

          console.warn(
            'Nenhuma funcao de painel encontrada.'
          )

        }

    }
  )

}

function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


 viewer.addOnceHandler(
  'open',
  () => {

    console.log(
      '========================================'
    )

    console.log(
      'DIAGNOSTICO DA LAMINA NO OPEN'
    )

    console.log(
      'LAMINA NO OPEN:',
      lamina
    )

    console.log(
      'ESTRUTURAS NO OPEN:',
      lamina?.estruturas
    )

    console.log(
      'TIPO DE ESTRUTURAS:',
      typeof lamina?.estruturas
    )

    console.log(
      'E ARRAY:',
      Array.isArray(lamina?.estruturas)
    )

    console.log(
      'QUANTIDADE:',
      Array.isArray(lamina?.estruturas)
        ? lamina.estruturas.length
        : 'NAO E ARRAY'
    )

    console.log(
      '========================================'
    )

    desenharEstruturas(
      viewer,
      lamina
    )

  }
)

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)



/*
 * Instala clique depois que os overlays
 * forem criados.
 */

setTimeout(
  () => {

    instalarCliqueDiretoEstruturas()

  },
  100
)

}


window.addEventListener(
  'hashchange',
  render
)


render()












































































===== backup-reparador-completo-20260819-212337\src\main.backup-clique-direto-20260819-173141.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'ESTRUTURAS PARA DESENHAR:',
    estruturas
  )

  if (!estruturas.length) {
    console.warn(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )
    return
  }

  const tiledImage =
    viewer.world.getItemAt(0)

  if (!tiledImage) {
    console.warn(
      'Imagem ainda não disponível.'
    )
    return
  }

  const imageSize =
    tiledImage.getContentSize()

  const imageWidth =
    Number(imageSize.x)

  const imageHeight =
    Number(imageSize.y)

  console.log(
    'TAMANHO DA IMAGEM:',
    imageWidth,
    imageHeight
  )

  if (!imageWidth || !imageHeight) {
    console.warn(
      'Tamanho da imagem inválido.'
    )
    return
  }

  viewer.clearOverlays()

  estruturas.forEach(
    (estrutura, index) => {

      console.log(
        'CRIANDO MARCADOR:',
        index,
        estrutura
      )

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )

      const descricao =
        String(
          estrutura.descricao || ''
        )

      let x =
        Number(estrutura.x)

      let y =
        Number(estrutura.y)

      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )

      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )

      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {
        console.warn(
          'Estrutura inválida:',
          estrutura
        )
        return
      }

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth

      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight

      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth

      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )

      console.log(
        'RECT:',
        index,
        rect
      )

      const overlay =
        document.createElement('div')

      overlay.className =
        'estrutura-overlay'

      overlay.dataset.estruturaIndex =
        String(index)

      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `

      overlay.title =
        descricao
          ? `${nome}: ${descricao}`
          : nome

      overlay.style.display =
        'block'

      overlay.style.visibility =
        'visible'

      overlay.style.opacity =
        '1'

      overlay.style.zIndex =
        '999999'

      overlay.style.pointerEvents =
        'auto'

      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

      })

      console.log(
        'MARCADOR ADICIONADO:',
        index,
        overlay
      )

    }
  )

}

function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}


/* ============================================================
   CLIQUE DIRETO NOS MARCADORES DAS ESTRUTURAS
   ============================================================ */

function instalarCliqueDiretoEstruturas() {

  const marcadores =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    marcadores.length,
    'marcadores'
  )

  marcadores.forEach(
    (marcador, index) => {

      marcador.style.pointerEvents = 'auto'

      marcador.style.cursor = 'pointer'

      marcador.onclick = null

      marcador.addEventListener(
        'click',
        (event) => {

          event.preventDefault()
          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          try {

            if (
              typeof mostrarEstrutura ===
              'function'
            ) {

              console.log(
                'Executando mostrarEstrutura:',
                index
              )

              mostrarEstrutura(
                index,
                true
              )

              return

            }

            if (
              typeof mostrarInfoEstruturaSimples ===
              'function'
            ) {

              console.log(
                'Executando mostrarInfoEstruturaSimples:',
                index
              )

              if (
                typeof window.laminaAtual !==
                'undefined'
              ) {

                mostrarInfoEstruturaSimples(
                  index,
                  window.laminaAtual
                )

              }

              return

            }

            console.warn(
              'Nenhuma funcao de estrutura encontrada.'
            )

          } catch (erro) {

            console.error(
              'ERRO AO CLICAR NA ESTRUTURA:',
              erro
            )

          }

        },
        true
      )

    }
  )

  console.log(
    'CLIQUE DIRETO INSTALADO.'
  )

}
function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {

    console.warn(
      'CLIQUE: lamina nao encontrada.'
    )

    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    overlays.length,
    'marcadores'
  )

  overlays.forEach(
    (overlay, index) => {

      overlay.style.pointerEvents =
        'auto'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        (event) => {

          event.preventDefault()

          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          const estrutura =
            estruturas[index]

          if (!estrutura) {

            console.warn(
              'Estrutura nao encontrada:',
              index
            )

            return
          }

          console.log(
            'ESTRUTURA CLICADA:',
            estrutura
          )

          /*
           * Tenta usar a funcao principal
           * de exibicao/zoom existente.
           */

          if (
            typeof mostrarEstrutura ===
            'function'
          ) {

            console.log(
              'Chamando mostrarEstrutura...'
            )

            mostrarEstrutura(
              index,
              true
            )

            return
          }

          /*
           * Compatibilidade caso a funcao
           * principal nao esteja disponivel.
           */

          if (
            typeof mostrarInfoEstruturaSimples ===
            'function'
          ) {

            console.log(
              'Chamando mostrarInfoEstruturaSimples...'
            )

            mostrarInfoEstruturaSimples(
              index,
              lamina
            )

            return
          }

          if (
            typeof mostrarTextoEstrutura ===
            'function'
          ) {

            console.log(
              'Chamando mostrarTextoEstrutura...'
            )

            mostrarTextoEstrutura(
              index,
              lamina
            )

            return
          }

          console.warn(
            'Nenhuma funcao de painel encontrada.'
          )

        }

    }
  )

}

function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


 viewer.addOnceHandler(
  'open',
  () => {

    console.log(
      '========================================'
    )

    console.log(
      'DIAGNOSTICO DA LAMINA NO OPEN'
    )

    console.log(
      'LAMINA NO OPEN:',
      lamina
    )

    console.log(
      'ESTRUTURAS NO OPEN:',
      lamina?.estruturas
    )

    console.log(
      'TIPO DE ESTRUTURAS:',
      typeof lamina?.estruturas
    )

    console.log(
      'E ARRAY:',
      Array.isArray(lamina?.estruturas)
    )

    console.log(
      'QUANTIDADE:',
      Array.isArray(lamina?.estruturas)
        ? lamina.estruturas.length
        : 'NAO E ARRAY'
    )

    console.log(
      '========================================'
    )

    desenharEstruturas(
      viewer,
      lamina
    )

  }
)

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)



/*
 * Instala clique depois que os overlays
 * forem criados.
 */

setTimeout(
  () => {

    instalarCliqueDiretoEstruturas()

  },
  100
)

}


window.addEventListener(
  'hashchange',
  render
)


render()












































































===== backup-reparador-completo-20260819-212337\src\main.backup-clique-direto-20260819-173410.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'ESTRUTURAS PARA DESENHAR:',
    estruturas
  )

  if (!estruturas.length) {
    console.warn(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )
    return
  }

  const tiledImage =
    viewer.world.getItemAt(0)

  if (!tiledImage) {
    console.warn(
      'Imagem ainda não disponível.'
    )
    return
  }

  const imageSize =
    tiledImage.getContentSize()

  const imageWidth =
    Number(imageSize.x)

  const imageHeight =
    Number(imageSize.y)

  console.log(
    'TAMANHO DA IMAGEM:',
    imageWidth,
    imageHeight
  )

  if (!imageWidth || !imageHeight) {
    console.warn(
      'Tamanho da imagem inválido.'
    )
    return
  }

  viewer.clearOverlays()

  estruturas.forEach(
    (estrutura, index) => {

      console.log(
        'CRIANDO MARCADOR:',
        index,
        estrutura
      )

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )

      const descricao =
        String(
          estrutura.descricao || ''
        )

      let x =
        Number(estrutura.x)

      let y =
        Number(estrutura.y)

      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )

      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )

      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {
        console.warn(
          'Estrutura inválida:',
          estrutura
        )
        return
      }

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth

      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight

      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth

      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )

      console.log(
        'RECT:',
        index,
        rect
      )

      const overlay =
        document.createElement('div')

      overlay.className =
        'estrutura-overlay'

      overlay.dataset.estruturaIndex =
        String(index)

      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `

      overlay.title =
        descricao
          ? `${nome}: ${descricao}`
          : nome

      overlay.style.display =
        'block'

      overlay.style.visibility =
        'visible'

      overlay.style.opacity =
        '1'

      overlay.style.zIndex =
        '999999'

      overlay.style.pointerEvents =
        'auto'

      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

      })

      console.log(
        'MARCADOR ADICIONADO:',
        index,
        overlay
      )

    }
  )

}

function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}


/* ============================================================
   CLIQUE DIRETO NOS MARCADORES DAS ESTRUTURAS
   ============================================================ */

function instalarCliqueDiretoEstruturas() {

  const marcadores =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    marcadores.length,
    'marcadores'
  )

  marcadores.forEach(
    (marcador, index) => {

      marcador.style.pointerEvents = 'auto'

      marcador.style.cursor = 'pointer'

      marcador.onclick = null

      marcador.addEventListener(
        'click',
        (event) => {

          event.preventDefault()
          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          try {

            if (
              typeof mostrarEstrutura ===
              'function'
            ) {

              console.log(
                'Executando mostrarEstrutura:',
                index
              )

              mostrarEstrutura(
                index,
                true
              )

              return

            }

            if (
              typeof mostrarInfoEstruturaSimples ===
              'function'
            ) {

              console.log(
                'Executando mostrarInfoEstruturaSimples:',
                index
              )

              if (
                typeof window.laminaAtual !==
                'undefined'
              ) {

                mostrarInfoEstruturaSimples(
                  index,
                  window.laminaAtual
                )

              }

              return

            }

            console.warn(
              'Nenhuma funcao de estrutura encontrada.'
            )

          } catch (erro) {

            console.error(
              'ERRO AO CLICAR NA ESTRUTURA:',
              erro
            )

          }

        },
        true
      )

    }
  )

  console.log(
    'CLIQUE DIRETO INSTALADO.'
  )

}
function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {

    console.warn(
      'CLIQUE: lamina nao encontrada.'
    )

    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    overlays.length,
    'marcadores'
  )

  overlays.forEach(
    (overlay, index) => {

      overlay.style.pointerEvents =
        'auto'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        (event) => {

          event.preventDefault()

          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          const estrutura =
            estruturas[index]

          if (!estrutura) {

            console.warn(
              'Estrutura nao encontrada:',
              index
            )

            return
          }

          console.log(
            'ESTRUTURA CLICADA:',
            estrutura
          )

          /*
           * Tenta usar a funcao principal
           * de exibicao/zoom existente.
           */

          if (
            typeof mostrarEstrutura ===
            'function'
          ) {

            console.log(
              'Chamando mostrarEstrutura...'
            )

            mostrarEstrutura(
              index,
              true
            )

            return
          }

          /*
           * Compatibilidade caso a funcao
           * principal nao esteja disponivel.
           */

          if (
            typeof mostrarInfoEstruturaSimples ===
            'function'
          ) {

            console.log(
              'Chamando mostrarInfoEstruturaSimples...'
            )

            mostrarInfoEstruturaSimples(
              index,
              lamina
            )

            return
          }

          if (
            typeof mostrarTextoEstrutura ===
            'function'
          ) {

            console.log(
              'Chamando mostrarTextoEstrutura...'
            )

            mostrarTextoEstrutura(
              index,
              lamina
            )

            return
          }

          console.warn(
            'Nenhuma funcao de painel encontrada.'
          )

        }

    }
  )

}

function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )
    setTimeout(() => {

      const marcadores =
        document.querySelectorAll(
          '.estrutura-overlay'
        )

      console.log(
        'CLIQUE DIRETO: marcadores encontrados:',
        marcadores.length
      )

      marcadores.forEach(
        (marcador, index) => {

          marcador.style.pointerEvents =
            'auto'

          marcador.onclick = (event) => {

            event.preventDefault()
            event.stopPropagation()

            console.log(
              'CLIQUE DIRETO NA ESTRUTURA:',
              index
            )

            const estruturas =
              Array.isArray(lamina?.estruturas)
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {

              console.warn(
                'ESTRUTURA CLICADA NAO ENCONTRADA:',
                index
              )

              return
            }

            console.log(
              'ESTRUTURA CLICADA:',
              estrutura
            )

            if (
              typeof mostrarEstrutura ===
              'function'
            ) {

              mostrarEstrutura(
                index,
                true
              )

            } else {

              console.warn(
                'mostrarEstrutura nao encontrada.'
              )

            }

          }

        }

      )

    }, 500)

    }
  )


 viewer.addOnceHandler(
  'open',
  () => {

    console.log(
      '========================================'
    )

    console.log(
      'DIAGNOSTICO DA LAMINA NO OPEN'
    )

    console.log(
      'LAMINA NO OPEN:',
      lamina
    )

    console.log(
      'ESTRUTURAS NO OPEN:',
      lamina?.estruturas
    )

    console.log(
      'TIPO DE ESTRUTURAS:',
      typeof lamina?.estruturas
    )

    console.log(
      'E ARRAY:',
      Array.isArray(lamina?.estruturas)
    )

    console.log(
      'QUANTIDADE:',
      Array.isArray(lamina?.estruturas)
        ? lamina.estruturas.length
        : 'NAO E ARRAY'
    )

    console.log(
      '========================================'
    )

    desenharEstruturas(
      viewer,
      lamina
    )

  }
)

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)



/*
 * Instala clique depois que os overlays
 * forem criados.
 */

setTimeout(
  () => {

    instalarCliqueDiretoEstruturas()

  },
  100
)

}


window.addEventListener(
  'hashchange',
  render
)


render()













































































===== backup-reparador-completo-20260819-212337\src\main.backup-clique-final-20260819-172753.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'ESTRUTURAS PARA DESENHAR:',
    estruturas
  )

  if (!estruturas.length) {
    console.warn(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )
    return
  }

  const tiledImage =
    viewer.world.getItemAt(0)

  if (!tiledImage) {
    console.warn(
      'Imagem ainda não disponível.'
    )
    return
  }

  const imageSize =
    tiledImage.getContentSize()

  const imageWidth =
    Number(imageSize.x)

  const imageHeight =
    Number(imageSize.y)

  console.log(
    'TAMANHO DA IMAGEM:',
    imageWidth,
    imageHeight
  )

  if (!imageWidth || !imageHeight) {
    console.warn(
      'Tamanho da imagem inválido.'
    )
    return
  }

  viewer.clearOverlays()

  estruturas.forEach(
    (estrutura, index) => {

      console.log(
        'CRIANDO MARCADOR:',
        index,
        estrutura
      )

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )

      const descricao =
        String(
          estrutura.descricao || ''
        )

      let x =
        Number(estrutura.x)

      let y =
        Number(estrutura.y)

      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )

      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )

      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {
        console.warn(
          'Estrutura inválida:',
          estrutura
        )
        return
      }

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth

      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight

      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth

      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )

      console.log(
        'RECT:',
        index,
        rect
      )

      const overlay =
        document.createElement('div')

      overlay.className =
        'estrutura-overlay'

      overlay.dataset.estruturaIndex =
        String(index)

      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `

      overlay.title =
        descricao
          ? `${nome}: ${descricao}`
          : nome

      overlay.style.display =
        'block'

      overlay.style.visibility =
        'visible'

      overlay.style.opacity =
        '1'

      overlay.style.zIndex =
        '999999'

      overlay.style.pointerEvents =
        'auto'

      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

      })

      console.log(
        'MARCADOR ADICIONADO:',
        index,
        overlay
      )

    }
  )

}

function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}


/* ============================================================
   CLIQUE DIRETO NOS MARCADORES DAS ESTRUTURAS
   ============================================================ */

function instalarCliqueDiretoEstruturas() {

  const marcadores =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    marcadores.length,
    'marcadores'
  )

  marcadores.forEach(
    (marcador, index) => {

      marcador.style.pointerEvents = 'auto'

      marcador.style.cursor = 'pointer'

      marcador.onclick = null

      marcador.addEventListener(
        'click',
        (event) => {

          event.preventDefault()
          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          try {

            if (
              typeof mostrarEstrutura ===
              'function'
            ) {

              console.log(
                'Executando mostrarEstrutura:',
                index
              )

              mostrarEstrutura(
                index,
                true
              )

              return

            }

            if (
              typeof mostrarInfoEstruturaSimples ===
              'function'
            ) {

              console.log(
                'Executando mostrarInfoEstruturaSimples:',
                index
              )

              if (
                typeof window.laminaAtual !==
                'undefined'
              ) {

                mostrarInfoEstruturaSimples(
                  index,
                  window.laminaAtual
                )

              }

              return

            }

            console.warn(
              'Nenhuma funcao de estrutura encontrada.'
            )

          } catch (erro) {

            console.error(
              'ERRO AO CLICAR NA ESTRUTURA:',
              erro
            )

          }

        },
        true
      )

    }
  )

  console.log(
    'CLIQUE DIRETO INSTALADO.'
  )

}
function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


 viewer.addOnceHandler(
  'open',
  () => {

    console.log(
      '========================================'
    )

    console.log(
      'DIAGNOSTICO DA LAMINA NO OPEN'
    )

    console.log(
      'LAMINA NO OPEN:',
      lamina
    )

    console.log(
      'ESTRUTURAS NO OPEN:',
      lamina?.estruturas
    )

    console.log(
      'TIPO DE ESTRUTURAS:',
      typeof lamina?.estruturas
    )

    console.log(
      'E ARRAY:',
      Array.isArray(lamina?.estruturas)
    )

    console.log(
      'QUANTIDADE:',
      Array.isArray(lamina?.estruturas)
        ? lamina.estruturas.length
        : 'NAO E ARRAY'
    )

    console.log(
      '========================================'
    )

    desenharEstruturas(
      viewer,
      lamina
    )

  }
)

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)



/*
 * Instala clique depois que os overlays
 * forem criados.
 */

setTimeout(
  () => {

    instalarCliqueDiretoEstruturas()

  },
  100
)

}


window.addEventListener(
  'hashchange',
  render
)


render()











































































===== backup-reparador-completo-20260819-212337\src\main.backup-clique-final-20260819-175706.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)
    console.log(
  'TILED IMAGE:',
  tiledImage
)

console.log(
  'IMAGE SIZE:',
  tiledImage?.getContentSize?.()
)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {
      console.log(
        'CRIANDO MARCADOR:',
        index,
        estrutura
      )
      console.log(
  'COORDENADAS ORIGINAIS:',
  estrutura.x,
  estrutura.y,
  estrutura.largura,
  estrutura.altura
)

const nome =
  String(
    estrutura.nome ||
    `Estrutura ${index + 1}`
  )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


 viewer.addOnceHandler(
  'open',
  () => {

    console.log(
      '========================================'
    )

    console.log(
      'DIAGNOSTICO DA LAMINA NO OPEN'
    )

    console.log(
      'LAMINA NO OPEN:',
      lamina
    )

    console.log(
      'ESTRUTURAS NO OPEN:',
      lamina?.estruturas
    )

    console.log(
      'TIPO DE ESTRUTURAS:',
      typeof lamina?.estruturas
    )

    console.log(
      'E ARRAY:',
      Array.isArray(lamina?.estruturas)
    )

    console.log(
      'QUANTIDADE:',
      Array.isArray(lamina?.estruturas)
        ? lamina.estruturas.length
        : 'NAO E ARRAY'
    )

    console.log(
      '========================================'
    )

    desenharEstruturas(
      viewer,
      lamina
    )

  }
)

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()









































































===== backup-reparador-completo-20260819-212337\src\main.backup-clique-lateral-20260819-175013.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'ESTRUTURAS PARA DESENHAR:',
    estruturas
  )

  if (!estruturas.length) {
    console.warn(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )
    return
  }

  const tiledImage =
    viewer.world.getItemAt(0)

  if (!tiledImage) {
    console.warn(
      'Imagem ainda não disponível.'
    )
    return
  }

  const imageSize =
    tiledImage.getContentSize()

  const imageWidth =
    Number(imageSize.x)

  const imageHeight =
    Number(imageSize.y)

  console.log(
    'TAMANHO DA IMAGEM:',
    imageWidth,
    imageHeight
  )

  if (!imageWidth || !imageHeight) {
    console.warn(
      'Tamanho da imagem inválido.'
    )
    return
  }

  viewer.clearOverlays()

  estruturas.forEach(
    (estrutura, index) => {

      console.log(
        'CRIANDO MARCADOR:',
        index,
        estrutura
      )

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )

      const descricao =
        String(
          estrutura.descricao || ''
        )

      let x =
        Number(estrutura.x)

      let y =
        Number(estrutura.y)

      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )

      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )

      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {
        console.warn(
          'Estrutura inválida:',
          estrutura
        )
        return
      }

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth

      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight

      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth

      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )

      console.log(
        'RECT:',
        index,
        rect
      )

      const overlay =
        document.createElement('div')

      overlay.className =
        'estrutura-overlay'

      overlay.dataset.estruturaIndex =
        String(index)

      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `

      overlay.title =
        descricao
          ? `${nome}: ${descricao}`
          : nome

      overlay.style.display =
        'block'

      overlay.style.visibility =
        'visible'

      overlay.style.opacity =
        '1'

      overlay.style.zIndex =
        '999999'

      overlay.style.pointerEvents =
        'auto'

      
      /*
       * CLIQUE DIRETO NO MARCADOR
       */

      overlay.style.pointerEvents =
        'auto'

      overlay.addEventListener(
        'click',
        (event) => {

          event.preventDefault()
          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          const estruturas =
            Array.isArray(lamina?.estruturas)
              ? lamina.estruturas
              : []

          const estrutura =
            estruturas[index]

          if (!estrutura) {

            console.warn(
              'ESTRUTURA CLICADA NAO ENCONTRADA:',
              index
            )

            return
          }

          console.log(
            'ESTRUTURA CLICADA:',
            estrutura
          )

          if (
            typeof mostrarTextoEstrutura ===
            'function'
          ) {

            mostrarTextoEstrutura(index, lamina)

          } else {

            console.error(
              'FUNCAO mostrarTextoEstrutura NAO ENCONTRADA'
            )

          }

        }
      )
viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

      })

      console.log(
        'MARCADOR ADICIONADO:',
        index,
        overlay
      )

    }
  )

}

function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarTextoEstrutura(index, lamina)

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarTextoEstrutura(index, lamina)

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}


/* ============================================================
   CLIQUE DIRETO NOS MARCADORES DAS ESTRUTURAS
   ============================================================ */

function instalarCliqueDiretoEstruturas() {

  const marcadores =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    marcadores.length,
    'marcadores'
  )

  marcadores.forEach(
    (marcador, index) => {

      marcador.style.pointerEvents = 'auto'

      marcador.style.cursor = 'pointer'

      marcador.onclick = null

      marcador.addEventListener(
        'click',
        (event) => {

          event.preventDefault()
          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          try {

            if (
              typeof mostrarTextoEstrutura ===
              'function'
            ) {

              console.log(
                'Executando mostrarEstrutura:',
                index
              )

              mostrarTextoEstrutura(index, lamina)

              return

            }

            if (
              typeof mostrarInfoEstruturaSimples ===
              'function'
            ) {

              console.log(
                'Executando mostrarInfoEstruturaSimples:',
                index
              )

              if (
                typeof window.laminaAtual !==
                'undefined'
              ) {

                mostrarInfoEstruturaSimples(
                  index,
                  window.laminaAtual
                )

              }

              return

            }

            console.warn(
              'Nenhuma funcao de estrutura encontrada.'
            )

          } catch (erro) {

            console.error(
              'ERRO AO CLICAR NA ESTRUTURA:',
              erro
            )

          }

        },
        true
      )

    }
  )

  console.log(
    'CLIQUE DIRETO INSTALADO.'
  )

}
function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {

    console.warn(
      'CLIQUE: lamina nao encontrada.'
    )

    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    overlays.length,
    'marcadores'
  )

  overlays.forEach(
    (overlay, index) => {

      overlay.style.pointerEvents =
        'auto'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        (event) => {

          event.preventDefault()

          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          const estrutura =
            estruturas[index]

          if (!estrutura) {

            console.warn(
              'Estrutura nao encontrada:',
              index
            )

            return
          }

          console.log(
            'ESTRUTURA CLICADA:',
            estrutura
          )

          /*
           * Tenta usar a funcao principal
           * de exibicao/zoom existente.
           */

          if (
            typeof mostrarTextoEstrutura ===
            'function'
          ) {

            console.log(
              'Chamando mostrarEstrutura...'
            )

            mostrarTextoEstrutura(index, lamina)

            return
          }

          /*
           * Compatibilidade caso a funcao
           * principal nao esteja disponivel.
           */

          if (
            typeof mostrarInfoEstruturaSimples ===
            'function'
          ) {

            console.log(
              'Chamando mostrarInfoEstruturaSimples...'
            )

            mostrarInfoEstruturaSimples(
              index,
              lamina
            )

            return
          }

          if (
            typeof mostrarTextoEstrutura ===
            'function'
          ) {

            console.log(
              'Chamando mostrarTextoEstrutura...'
            )

            mostrarTextoEstrutura(
              index,
              lamina
            )

            return
          }

          console.warn(
            'Nenhuma funcao de painel encontrada.'
          )

        }

    }
  )

}

function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )
    setTimeout(() => {

      const marcadores =
        document.querySelectorAll(
          '.estrutura-overlay'
        )

      console.log(
        'CLIQUE DIRETO: marcadores encontrados:',
        marcadores.length
      )

      marcadores.forEach(
        (marcador, index) => {

          marcador.style.pointerEvents =
            'auto'

          marcador.onclick = (event) => {

            event.preventDefault()
            event.stopPropagation()

            console.log(
              'CLIQUE DIRETO NA ESTRUTURA:',
              index
            )

            const estruturas =
              Array.isArray(lamina?.estruturas)
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {

              console.warn(
                'ESTRUTURA CLICADA NAO ENCONTRADA:',
                index
              )

              return
            }

            console.log(
              'ESTRUTURA CLICADA:',
              estrutura
            )

            if (
              typeof mostrarTextoEstrutura ===
              'function'
            ) {

              mostrarTextoEstrutura(index, lamina)

            } else {

              console.warn(
                'mostrarEstrutura nao encontrada.'
              )

            }

          }

        }

      )

    }, 500)

    }
  )


 viewer.addOnceHandler(
  'open',
  () => {

    console.log(
      '========================================'
    )

    console.log(
      'DIAGNOSTICO DA LAMINA NO OPEN'
    )

    console.log(
      'LAMINA NO OPEN:',
      lamina
    )

    console.log(
      'ESTRUTURAS NO OPEN:',
      lamina?.estruturas
    )

    console.log(
      'TIPO DE ESTRUTURAS:',
      typeof lamina?.estruturas
    )

    console.log(
      'E ARRAY:',
      Array.isArray(lamina?.estruturas)
    )

    console.log(
      'QUANTIDADE:',
      Array.isArray(lamina?.estruturas)
        ? lamina.estruturas.length
        : 'NAO E ARRAY'
    )

    console.log(
      '========================================'
    )

    desenharEstruturas(
      viewer,
      lamina
    )

  }
)

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)



/*
 * Instala clique depois que os overlays
 * forem criados.
 */

setTimeout(
  () => {

    instalarCliqueDiretoEstruturas()

  },
  100
)

}


window.addEventListener(
  'hashchange',
  render
)


render()















































































===== backup-reparador-completo-20260819-212337\src\main.backup-clique-OSD-20260819-180616.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)
    console.log(
  'TILED IMAGE:',
  tiledImage
)

console.log(
  'IMAGE SIZE:',
  tiledImage?.getContentSize?.()
)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {
      console.log(
        'CRIANDO MARCADOR:',
        index,
        estrutura
      )
      console.log(
  'COORDENADAS ORIGINAIS:',
  estrutura.x,
  estrutura.y,
  estrutura.largura,
  estrutura.altura
)

const nome =
  String(
    estrutura.nome ||
    `Estrutura ${index + 1}`
  )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


 viewer.addOnceHandler(
  'open',
  () => {

    console.log(
      '========================================'
    )

    console.log(
      'DIAGNOSTICO DA LAMINA NO OPEN'
    )

    console.log(
      'LAMINA NO OPEN:',
      lamina
    )

    console.log(
      'ESTRUTURAS NO OPEN:',
      lamina?.estruturas
    )

    console.log(
      'TIPO DE ESTRUTURAS:',
      typeof lamina?.estruturas
    )

    console.log(
      'E ARRAY:',
      Array.isArray(lamina?.estruturas)
    )

    console.log(
      'QUANTIDADE:',
      Array.isArray(lamina?.estruturas)
        ? lamina.estruturas.length
        : 'NAO E ARRAY'
    )

    console.log(
      '========================================'
    )

    desenharEstruturas(
      viewer,
      lamina
    )

  }
)

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()









































































===== backup-reparador-completo-20260819-212337\src\main.backup-clique-robusto-20260819-175100.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'ESTRUTURAS PARA DESENHAR:',
    estruturas
  )

  if (!estruturas.length) {
    console.warn(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )
    return
  }

  const tiledImage =
    viewer.world.getItemAt(0)

  if (!tiledImage) {
    console.warn(
      'Imagem ainda não disponível.'
    )
    return
  }

  const imageSize =
    tiledImage.getContentSize()

  const imageWidth =
    Number(imageSize.x)

  const imageHeight =
    Number(imageSize.y)

  console.log(
    'TAMANHO DA IMAGEM:',
    imageWidth,
    imageHeight
  )

  if (!imageWidth || !imageHeight) {
    console.warn(
      'Tamanho da imagem inválido.'
    )
    return
  }

  viewer.clearOverlays()

  estruturas.forEach(
    (estrutura, index) => {

      console.log(
        'CRIANDO MARCADOR:',
        index,
        estrutura
      )

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )

      const descricao =
        String(
          estrutura.descricao || ''
        )

      let x =
        Number(estrutura.x)

      let y =
        Number(estrutura.y)

      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )

      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )

      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {
        console.warn(
          'Estrutura inválida:',
          estrutura
        )
        return
      }

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth

      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight

      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth

      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )

      console.log(
        'RECT:',
        index,
        rect
      )

      const overlay =
        document.createElement('div')

      overlay.className =
        'estrutura-overlay'

      overlay.dataset.estruturaIndex =
        String(index)

      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `

      overlay.title =
        descricao
          ? `${nome}: ${descricao}`
          : nome

      overlay.style.display =
        'block'

      overlay.style.visibility =
        'visible'

      overlay.style.opacity =
        '1'

      overlay.style.zIndex =
        '999999'

      overlay.style.pointerEvents =
        'auto'

      
      /*
       * CLIQUE DIRETO NO MARCADOR
       */

      overlay.style.pointerEvents =
        'auto'

      overlay.addEventListener(
        'click',
        (event) => {

          event.preventDefault()
          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          const estruturas =
            Array.isArray(lamina?.estruturas)
              ? lamina.estruturas
              : []

          const estrutura =
            estruturas[index]

          if (!estrutura) {

            console.warn(
              'ESTRUTURA CLICADA NAO ENCONTRADA:',
              index
            )

            return
          }

          console.log(
            'ESTRUTURA CLICADA:',
            estrutura
          )

          if (
            typeof mostrarTextoEstrutura ===
            'function'
          ) {

            mostrarTextoEstrutura(index, lamina)

          } else {

            console.error(
              'FUNCAO mostrarTextoEstrutura NAO ENCONTRADA'
            )

          }

        }
      )
viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

      })

      console.log(
        'MARCADOR ADICIONADO:',
        index,
        overlay
      )

    }
  )

}

function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarTextoEstrutura(index, lamina)

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarTextoEstrutura(index, lamina)

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}


/* ============================================================
   CLIQUE DIRETO NOS MARCADORES DAS ESTRUTURAS
   ============================================================ */

function instalarCliqueDiretoEstruturas() {

  const marcadores =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    marcadores.length,
    'marcadores'
  )

  marcadores.forEach(
    (marcador, index) => {

      marcador.style.pointerEvents = 'auto'

      marcador.style.cursor = 'pointer'

      marcador.onclick = null

      marcador.addEventListener(
        'click',
        (event) => {

          event.preventDefault()
          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          try {

            if (
              typeof mostrarTextoEstrutura ===
              'function'
            ) {

              console.log(
                'Executando mostrarEstrutura:',
                index
              )

              mostrarTextoEstrutura(index, lamina)

              return

            }

            if (
              typeof mostrarInfoEstruturaSimples ===
              'function'
            ) {

              console.log(
                'Executando mostrarInfoEstruturaSimples:',
                index
              )

              if (
                typeof window.laminaAtual !==
                'undefined'
              ) {

                mostrarInfoEstruturaSimples(
                  index,
                  window.laminaAtual
                )

              }

              return

            }

            console.warn(
              'Nenhuma funcao de estrutura encontrada.'
            )

          } catch (erro) {

            console.error(
              'ERRO AO CLICAR NA ESTRUTURA:',
              erro
            )

          }

        },
        true
      )

    }
  )

  console.log(
    'CLIQUE DIRETO INSTALADO.'
  )

}
function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {

    console.warn(
      'CLIQUE: lamina nao encontrada.'
    )

    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    overlays.length,
    'marcadores'
  )

  overlays.forEach(
    (overlay, index) => {

      overlay.style.pointerEvents =
        'auto'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        (event) => {

          event.preventDefault()

          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          const estrutura =
            estruturas[index]

          if (!estrutura) {

            console.warn(
              'Estrutura nao encontrada:',
              index
            )

            return
          }

          console.log(
            'ESTRUTURA CLICADA:',
            estrutura
          )

          /*
           * Tenta usar a funcao principal
           * de exibicao/zoom existente.
           */

          if (
            typeof mostrarTextoEstrutura ===
            'function'
          ) {

            console.log(
              'Chamando mostrarEstrutura...'
            )

            mostrarTextoEstrutura(index, lamina)

            return
          }

          /*
           * Compatibilidade caso a funcao
           * principal nao esteja disponivel.
           */

          if (
            typeof mostrarInfoEstruturaSimples ===
            'function'
          ) {

            console.log(
              'Chamando mostrarInfoEstruturaSimples...'
            )

            mostrarInfoEstruturaSimples(
              index,
              lamina
            )

            return
          }

          if (
            typeof mostrarTextoEstrutura ===
            'function'
          ) {

            console.log(
              'Chamando mostrarTextoEstrutura...'
            )

            mostrarTextoEstrutura(
              index,
              lamina
            )

            return
          }

          console.warn(
            'Nenhuma funcao de painel encontrada.'
          )

        }

    }
  )

}

function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )
    setTimeout(() => {

      const marcadores =
        document.querySelectorAll(
          '.estrutura-overlay'
        )

      console.log(
        'CLIQUE DIRETO: marcadores encontrados:',
        marcadores.length
      )

      marcadores.forEach(
        (marcador, index) => {

          marcador.style.pointerEvents =
            'auto'

          marcador.onclick = (event) => {

            event.preventDefault()
            event.stopPropagation()

            console.log(
              'CLIQUE DIRETO NA ESTRUTURA:',
              index
            )

            const estruturas =
              Array.isArray(lamina?.estruturas)
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {

              console.warn(
                'ESTRUTURA CLICADA NAO ENCONTRADA:',
                index
              )

              return
            }

            console.log(
              'ESTRUTURA CLICADA:',
              estrutura
            )

            if (
              typeof mostrarTextoEstrutura ===
              'function'
            ) {

              mostrarTextoEstrutura(index, lamina)

            } else {

              console.warn(
                'mostrarEstrutura nao encontrada.'
              )

            }

          }

        }

      )

    }, 500)

    }
  )


 viewer.addOnceHandler(
  'open',
  () => {

    console.log(
      '========================================'
    )

    console.log(
      'DIAGNOSTICO DA LAMINA NO OPEN'
    )

    console.log(
      'LAMINA NO OPEN:',
      lamina
    )

    console.log(
      'ESTRUTURAS NO OPEN:',
      lamina?.estruturas
    )

    console.log(
      'TIPO DE ESTRUTURAS:',
      typeof lamina?.estruturas
    )

    console.log(
      'E ARRAY:',
      Array.isArray(lamina?.estruturas)
    )

    console.log(
      'QUANTIDADE:',
      Array.isArray(lamina?.estruturas)
        ? lamina.estruturas.length
        : 'NAO E ARRAY'
    )

    console.log(
      '========================================'
    )

    desenharEstruturas(
      viewer,
      lamina
    )

  }
)

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)



/*
 * Instala clique depois que os overlays
 * forem criados.
 */

setTimeout(
  () => {

    instalarCliqueDiretoEstruturas()

  },
  100
)

}


window.addEventListener(
  'hashchange',
  render
)


render()















































































===== backup-reparador-completo-20260819-212337\src\main.backup-conexao-final-20260819-172939.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'ESTRUTURAS PARA DESENHAR:',
    estruturas
  )

  if (!estruturas.length) {
    console.warn(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )
    return
  }

  const tiledImage =
    viewer.world.getItemAt(0)

  if (!tiledImage) {
    console.warn(
      'Imagem ainda não disponível.'
    )
    return
  }

  const imageSize =
    tiledImage.getContentSize()

  const imageWidth =
    Number(imageSize.x)

  const imageHeight =
    Number(imageSize.y)

  console.log(
    'TAMANHO DA IMAGEM:',
    imageWidth,
    imageHeight
  )

  if (!imageWidth || !imageHeight) {
    console.warn(
      'Tamanho da imagem inválido.'
    )
    return
  }

  viewer.clearOverlays()

  estruturas.forEach(
    (estrutura, index) => {

      console.log(
        'CRIANDO MARCADOR:',
        index,
        estrutura
      )

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )

      const descricao =
        String(
          estrutura.descricao || ''
        )

      let x =
        Number(estrutura.x)

      let y =
        Number(estrutura.y)

      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )

      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )

      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {
        console.warn(
          'Estrutura inválida:',
          estrutura
        )
        return
      }

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth

      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight

      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth

      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )

      console.log(
        'RECT:',
        index,
        rect
      )

      const overlay =
        document.createElement('div')

      overlay.className =
        'estrutura-overlay'

      overlay.dataset.estruturaIndex =
        String(index)

      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `

      overlay.title =
        descricao
          ? `${nome}: ${descricao}`
          : nome

      overlay.style.display =
        'block'

      overlay.style.visibility =
        'visible'

      overlay.style.opacity =
        '1'

      overlay.style.zIndex =
        '999999'

      overlay.style.pointerEvents =
        'auto'

      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

      })

      console.log(
        'MARCADOR ADICIONADO:',
        index,
        overlay
      )

    }
  )

}

function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}


/* ============================================================
   CLIQUE DIRETO NOS MARCADORES DAS ESTRUTURAS
   ============================================================ */

function instalarCliqueDiretoEstruturas() {

  const marcadores =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    marcadores.length,
    'marcadores'
  )

  marcadores.forEach(
    (marcador, index) => {

      marcador.style.pointerEvents = 'auto'

      marcador.style.cursor = 'pointer'

      marcador.onclick = null

      marcador.addEventListener(
        'click',
        (event) => {

          event.preventDefault()
          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          try {

            if (
              typeof mostrarEstrutura ===
              'function'
            ) {

              console.log(
                'Executando mostrarEstrutura:',
                index
              )

              mostrarEstrutura(
                index,
                true
              )

              return

            }

            if (
              typeof mostrarInfoEstruturaSimples ===
              'function'
            ) {

              console.log(
                'Executando mostrarInfoEstruturaSimples:',
                index
              )

              if (
                typeof window.laminaAtual !==
                'undefined'
              ) {

                mostrarInfoEstruturaSimples(
                  index,
                  window.laminaAtual
                )

              }

              return

            }

            console.warn(
              'Nenhuma funcao de estrutura encontrada.'
            )

          } catch (erro) {

            console.error(
              'ERRO AO CLICAR NA ESTRUTURA:',
              erro
            )

          }

        },
        true
      )

    }
  )

  console.log(
    'CLIQUE DIRETO INSTALADO.'
  )

}
function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {

    console.warn(
      'CLIQUE: lamina nao encontrada.'
    )

    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    overlays.length,
    'marcadores'
  )

  overlays.forEach(
    (overlay, index) => {

      overlay.style.pointerEvents =
        'auto'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        (event) => {

          event.preventDefault()

          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          const estrutura =
            estruturas[index]

          if (!estrutura) {

            console.warn(
              'Estrutura nao encontrada:',
              index
            )

            return
          }

          console.log(
            'ESTRUTURA CLICADA:',
            estrutura
          )

          /*
           * Tenta usar a funcao principal
           * de exibicao/zoom existente.
           */

          if (
            typeof mostrarEstrutura ===
            'function'
          ) {

            console.log(
              'Chamando mostrarEstrutura...'
            )

            mostrarEstrutura(
              index,
              true
            )

            return
          }

          /*
           * Compatibilidade caso a funcao
           * principal nao esteja disponivel.
           */

          if (
            typeof mostrarInfoEstruturaSimples ===
            'function'
          ) {

            console.log(
              'Chamando mostrarInfoEstruturaSimples...'
            )

            mostrarInfoEstruturaSimples(
              index,
              lamina
            )

            return
          }

          if (
            typeof mostrarTextoEstrutura ===
            'function'
          ) {

            console.log(
              'Chamando mostrarTextoEstrutura...'
            )

            mostrarTextoEstrutura(
              index,
              lamina
            )

            return
          }

          console.warn(
            'Nenhuma funcao de painel encontrada.'
          )

        }

    }
  )

}

function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


 viewer.addOnceHandler(
  'open',
  () => {

    console.log(
      '========================================'
    )

    console.log(
      'DIAGNOSTICO DA LAMINA NO OPEN'
    )

    console.log(
      'LAMINA NO OPEN:',
      lamina
    )

    console.log(
      'ESTRUTURAS NO OPEN:',
      lamina?.estruturas
    )

    console.log(
      'TIPO DE ESTRUTURAS:',
      typeof lamina?.estruturas
    )

    console.log(
      'E ARRAY:',
      Array.isArray(lamina?.estruturas)
    )

    console.log(
      'QUANTIDADE:',
      Array.isArray(lamina?.estruturas)
        ? lamina.estruturas.length
        : 'NAO E ARRAY'
    )

    console.log(
      '========================================'
    )

    desenharEstruturas(
      viewer,
      lamina
    )

  }
)

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)



/*
 * Instala clique depois que os overlays
 * forem criados.
 */

setTimeout(
  () => {

    instalarCliqueDiretoEstruturas()

  },
  100
)

}


window.addEventListener(
  'hashchange',
  render
)


render()












































































===== backup-reparador-completo-20260819-212337\src\main.backup-corrige-clique-20260819-174410.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'ESTRUTURAS PARA DESENHAR:',
    estruturas
  )

  if (!estruturas.length) {
    console.warn(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )
    return
  }

  const tiledImage =
    viewer.world.getItemAt(0)

  if (!tiledImage) {
    console.warn(
      'Imagem ainda não disponível.'
    )
    return
  }

  const imageSize =
    tiledImage.getContentSize()

  const imageWidth =
    Number(imageSize.x)

  const imageHeight =
    Number(imageSize.y)

  console.log(
    'TAMANHO DA IMAGEM:',
    imageWidth,
    imageHeight
  )

  if (!imageWidth || !imageHeight) {
    console.warn(
      'Tamanho da imagem inválido.'
    )
    return
  }

  viewer.clearOverlays()

  estruturas.forEach(
    (estrutura, index) => {

      console.log(
        'CRIANDO MARCADOR:',
        index,
        estrutura
      )

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )

      const descricao =
        String(
          estrutura.descricao || ''
        )

      let x =
        Number(estrutura.x)

      let y =
        Number(estrutura.y)

      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )

      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )

      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {
        console.warn(
          'Estrutura inválida:',
          estrutura
        )
        return
      }

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth

      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight

      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth

      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )

      console.log(
        'RECT:',
        index,
        rect
      )

      const overlay =
        document.createElement('div')

      overlay.className =
        'estrutura-overlay'

      overlay.dataset.estruturaIndex =
        String(index)

      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `

      overlay.title =
        descricao
          ? `${nome}: ${descricao}`
          : nome

      overlay.style.display =
        'block'

      overlay.style.visibility =
        'visible'

      overlay.style.opacity =
        '1'

      overlay.style.zIndex =
        '999999'

      overlay.style.pointerEvents =
        'auto'

      
      /*
       * CLIQUE DIRETO NO MARCADOR
       */

      overlay.style.pointerEvents =
        'auto'

      overlay.addEventListener(
        'click',
        (event) => {

          event.preventDefault()
          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          const estruturas =
            Array.isArray(lamina?.estruturas)
              ? lamina.estruturas
              : []

          const estrutura =
            estruturas[index]

          if (!estrutura) {

            console.warn(
              'ESTRUTURA CLICADA NAO ENCONTRADA:',
              index
            )

            return
          }

          console.log(
            'ESTRUTURA CLICADA:',
            estrutura
          )

          if (
            typeof mostrarTextoEstrutura ===
            'function'
          ) {

            mostrarTextoEstrutura(index, lamina)

          } else {

            console.error(
              'FUNCAO mostrarTextoEstrutura NAO ENCONTRADA'
            )

          }

        }
      )
viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

      })

      console.log(
        'MARCADOR ADICIONADO:',
        index,
        overlay
      )

    }
  )

}

function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarTextoEstrutura(index, lamina)

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarTextoEstrutura(index, lamina)

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}


/* ============================================================
   CLIQUE DIRETO NOS MARCADORES DAS ESTRUTURAS
   ============================================================ */

function instalarCliqueDiretoEstruturas() {

  const marcadores =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    marcadores.length,
    'marcadores'
  )

  marcadores.forEach(
    (marcador, index) => {

      marcador.style.pointerEvents = 'auto'

      marcador.style.cursor = 'pointer'

      marcador.onclick = null

      marcador.addEventListener(
        'click',
        (event) => {

          event.preventDefault()
          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          try {

            if (
              typeof mostrarTextoEstrutura ===
              'function'
            ) {

              console.log(
                'Executando mostrarEstrutura:',
                index
              )

              mostrarTextoEstrutura(index, lamina)

              return

            }

            if (
              typeof mostrarInfoEstruturaSimples ===
              'function'
            ) {

              console.log(
                'Executando mostrarInfoEstruturaSimples:',
                index
              )

              if (
                typeof window.laminaAtual !==
                'undefined'
              ) {

                mostrarInfoEstruturaSimples(
                  index,
                  window.laminaAtual
                )

              }

              return

            }

            console.warn(
              'Nenhuma funcao de estrutura encontrada.'
            )

          } catch (erro) {

            console.error(
              'ERRO AO CLICAR NA ESTRUTURA:',
              erro
            )

          }

        },
        true
      )

    }
  )

  console.log(
    'CLIQUE DIRETO INSTALADO.'
  )

}
function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {

    console.warn(
      'CLIQUE: lamina nao encontrada.'
    )

    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    overlays.length,
    'marcadores'
  )

  overlays.forEach(
    (overlay, index) => {

      overlay.style.pointerEvents =
        'auto'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        (event) => {

          event.preventDefault()

          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          const estrutura =
            estruturas[index]

          if (!estrutura) {

            console.warn(
              'Estrutura nao encontrada:',
              index
            )

            return
          }

          console.log(
            'ESTRUTURA CLICADA:',
            estrutura
          )

          /*
           * Tenta usar a funcao principal
           * de exibicao/zoom existente.
           */

          if (
            typeof mostrarTextoEstrutura ===
            'function'
          ) {

            console.log(
              'Chamando mostrarEstrutura...'
            )

            mostrarTextoEstrutura(index, lamina)

            return
          }

          /*
           * Compatibilidade caso a funcao
           * principal nao esteja disponivel.
           */

          if (
            typeof mostrarInfoEstruturaSimples ===
            'function'
          ) {

            console.log(
              'Chamando mostrarInfoEstruturaSimples...'
            )

            mostrarInfoEstruturaSimples(
              index,
              lamina
            )

            return
          }

          if (
            typeof mostrarTextoEstrutura ===
            'function'
          ) {

            console.log(
              'Chamando mostrarTextoEstrutura...'
            )

            mostrarTextoEstrutura(
              index,
              lamina
            )

            return
          }

          console.warn(
            'Nenhuma funcao de painel encontrada.'
          )

        }

    }
  )

}

function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )
    setTimeout(() => {

      const marcadores =
        document.querySelectorAll(
          '.estrutura-overlay'
        )

      console.log(
        'CLIQUE DIRETO: marcadores encontrados:',
        marcadores.length
      )

      marcadores.forEach(
        (marcador, index) => {

          marcador.style.pointerEvents =
            'auto'

          marcador.onclick = (event) => {

            event.preventDefault()
            event.stopPropagation()

            console.log(
              'CLIQUE DIRETO NA ESTRUTURA:',
              index
            )

            const estruturas =
              Array.isArray(lamina?.estruturas)
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {

              console.warn(
                'ESTRUTURA CLICADA NAO ENCONTRADA:',
                index
              )

              return
            }

            console.log(
              'ESTRUTURA CLICADA:',
              estrutura
            )

            if (
              typeof mostrarTextoEstrutura ===
              'function'
            ) {

              mostrarTextoEstrutura(index, lamina)

            } else {

              console.warn(
                'mostrarEstrutura nao encontrada.'
              )

            }

          }

        }

      )

    }, 500)

    }
  )


 viewer.addOnceHandler(
  'open',
  () => {

    console.log(
      '========================================'
    )

    console.log(
      'DIAGNOSTICO DA LAMINA NO OPEN'
    )

    console.log(
      'LAMINA NO OPEN:',
      lamina
    )

    console.log(
      'ESTRUTURAS NO OPEN:',
      lamina?.estruturas
    )

    console.log(
      'TIPO DE ESTRUTURAS:',
      typeof lamina?.estruturas
    )

    console.log(
      'E ARRAY:',
      Array.isArray(lamina?.estruturas)
    )

    console.log(
      'QUANTIDADE:',
      Array.isArray(lamina?.estruturas)
        ? lamina.estruturas.length
        : 'NAO E ARRAY'
    )

    console.log(
      '========================================'
    )

    desenharEstruturas(
      viewer,
      lamina
    )

  }
)

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)



/*
 * Instala clique depois que os overlays
 * forem criados.
 */

setTimeout(
  () => {

    instalarCliqueDiretoEstruturas()

  },
  100
)

}


window.addEventListener(
  'hashchange',
  render
)


render()















































































===== backup-reparador-completo-20260819-212337\src\main.backup-diagnostico-20260819-170054.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()








































































===== backup-reparador-completo-20260819-212337\src\main.backup-final-20260819-172150.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)
    console.log(
  'TILED IMAGE:',
  tiledImage
)

console.log(
  'IMAGE SIZE:',
  tiledImage?.getContentSize?.()
)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {
      console.log(
        'CRIANDO MARCADOR:',
        index,
        estrutura
      )
      console.log(
  'COORDENADAS ORIGINAIS:',
  estrutura.x,
  estrutura.y,
  estrutura.largura,
  estrutura.altura
)

const nome =
  String(
    estrutura.nome ||
    `Estrutura ${index + 1}`
  )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


 viewer.addOnceHandler(
  'open',
  () => {

    console.log(
      '========================================'
    )

    console.log(
      'DIAGNOSTICO DA LAMINA NO OPEN'
    )

    console.log(
      'LAMINA NO OPEN:',
      lamina
    )

    console.log(
      'ESTRUTURAS NO OPEN:',
      lamina?.estruturas
    )

    console.log(
      'TIPO DE ESTRUTURAS:',
      typeof lamina?.estruturas
    )

    console.log(
      'E ARRAY:',
      Array.isArray(lamina?.estruturas)
    )

    console.log(
      'QUANTIDADE:',
      Array.isArray(lamina?.estruturas)
        ? lamina.estruturas.length
        : 'NAO E ARRAY'
    )

    console.log(
      '========================================'
    )

    desenharEstruturas(
      viewer,
      lamina
    )

  }
)

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()









































































===== backup-reparador-completo-20260819-212337\src\main.backup-funcao-clique-20260819-173807.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'ESTRUTURAS PARA DESENHAR:',
    estruturas
  )

  if (!estruturas.length) {
    console.warn(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )
    return
  }

  const tiledImage =
    viewer.world.getItemAt(0)

  if (!tiledImage) {
    console.warn(
      'Imagem ainda não disponível.'
    )
    return
  }

  const imageSize =
    tiledImage.getContentSize()

  const imageWidth =
    Number(imageSize.x)

  const imageHeight =
    Number(imageSize.y)

  console.log(
    'TAMANHO DA IMAGEM:',
    imageWidth,
    imageHeight
  )

  if (!imageWidth || !imageHeight) {
    console.warn(
      'Tamanho da imagem inválido.'
    )
    return
  }

  viewer.clearOverlays()

  estruturas.forEach(
    (estrutura, index) => {

      console.log(
        'CRIANDO MARCADOR:',
        index,
        estrutura
      )

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )

      const descricao =
        String(
          estrutura.descricao || ''
        )

      let x =
        Number(estrutura.x)

      let y =
        Number(estrutura.y)

      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )

      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )

      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {
        console.warn(
          'Estrutura inválida:',
          estrutura
        )
        return
      }

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth

      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight

      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth

      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )

      console.log(
        'RECT:',
        index,
        rect
      )

      const overlay =
        document.createElement('div')

      overlay.className =
        'estrutura-overlay'

      overlay.dataset.estruturaIndex =
        String(index)

      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `

      overlay.title =
        descricao
          ? `${nome}: ${descricao}`
          : nome

      overlay.style.display =
        'block'

      overlay.style.visibility =
        'visible'

      overlay.style.opacity =
        '1'

      overlay.style.zIndex =
        '999999'

      overlay.style.pointerEvents =
        'auto'

      
      /*
       * CLIQUE DIRETO NO MARCADOR
       */

      overlay.style.pointerEvents =
        'auto'

      overlay.addEventListener(
        'click',
        (event) => {

          event.preventDefault()
          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          const estruturas =
            Array.isArray(lamina?.estruturas)
              ? lamina.estruturas
              : []

          const estrutura =
            estruturas[index]

          if (!estrutura) {

            console.warn(
              'ESTRUTURA CLICADA NAO ENCONTRADA:',
              index
            )

            return
          }

          console.log(
            'ESTRUTURA CLICADA:',
            estrutura
          )

          if (
            typeof mostrarEstrutura ===
            'function'
          ) {

            mostrarEstrutura(
              index,
              true
            )

          } else {

            console.error(
              'FUNCAO mostrarEstrutura NAO ENCONTRADA'
            )

          }

        }
      )
viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

      })

      console.log(
        'MARCADOR ADICIONADO:',
        index,
        overlay
      )

    }
  )

}

function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}


/* ============================================================
   CLIQUE DIRETO NOS MARCADORES DAS ESTRUTURAS
   ============================================================ */

function instalarCliqueDiretoEstruturas() {

  const marcadores =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    marcadores.length,
    'marcadores'
  )

  marcadores.forEach(
    (marcador, index) => {

      marcador.style.pointerEvents = 'auto'

      marcador.style.cursor = 'pointer'

      marcador.onclick = null

      marcador.addEventListener(
        'click',
        (event) => {

          event.preventDefault()
          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          try {

            if (
              typeof mostrarEstrutura ===
              'function'
            ) {

              console.log(
                'Executando mostrarEstrutura:',
                index
              )

              mostrarEstrutura(
                index,
                true
              )

              return

            }

            if (
              typeof mostrarInfoEstruturaSimples ===
              'function'
            ) {

              console.log(
                'Executando mostrarInfoEstruturaSimples:',
                index
              )

              if (
                typeof window.laminaAtual !==
                'undefined'
              ) {

                mostrarInfoEstruturaSimples(
                  index,
                  window.laminaAtual
                )

              }

              return

            }

            console.warn(
              'Nenhuma funcao de estrutura encontrada.'
            )

          } catch (erro) {

            console.error(
              'ERRO AO CLICAR NA ESTRUTURA:',
              erro
            )

          }

        },
        true
      )

    }
  )

  console.log(
    'CLIQUE DIRETO INSTALADO.'
  )

}
function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {

    console.warn(
      'CLIQUE: lamina nao encontrada.'
    )

    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  console.log(
    'INSTALANDO CLIQUE DIRETO:',
    overlays.length,
    'marcadores'
  )

  overlays.forEach(
    (overlay, index) => {

      overlay.style.pointerEvents =
        'auto'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        (event) => {

          event.preventDefault()

          event.stopPropagation()

          console.log(
            'CLIQUE DIRETO NA ESTRUTURA:',
            index
          )

          const estrutura =
            estruturas[index]

          if (!estrutura) {

            console.warn(
              'Estrutura nao encontrada:',
              index
            )

            return
          }

          console.log(
            'ESTRUTURA CLICADA:',
            estrutura
          )

          /*
           * Tenta usar a funcao principal
           * de exibicao/zoom existente.
           */

          if (
            typeof mostrarEstrutura ===
            'function'
          ) {

            console.log(
              'Chamando mostrarEstrutura...'
            )

            mostrarEstrutura(
              index,
              true
            )

            return
          }

          /*
           * Compatibilidade caso a funcao
           * principal nao esteja disponivel.
           */

          if (
            typeof mostrarInfoEstruturaSimples ===
            'function'
          ) {

            console.log(
              'Chamando mostrarInfoEstruturaSimples...'
            )

            mostrarInfoEstruturaSimples(
              index,
              lamina
            )

            return
          }

          if (
            typeof mostrarTextoEstrutura ===
            'function'
          ) {

            console.log(
              'Chamando mostrarTextoEstrutura...'
            )

            mostrarTextoEstrutura(
              index,
              lamina
            )

            return
          }

          console.warn(
            'Nenhuma funcao de painel encontrada.'
          )

        }

    }
  )

}

function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )
    setTimeout(() => {

      const marcadores =
        document.querySelectorAll(
          '.estrutura-overlay'
        )

      console.log(
        'CLIQUE DIRETO: marcadores encontrados:',
        marcadores.length
      )

      marcadores.forEach(
        (marcador, index) => {

          marcador.style.pointerEvents =
            'auto'

          marcador.onclick = (event) => {

            event.preventDefault()
            event.stopPropagation()

            console.log(
              'CLIQUE DIRETO NA ESTRUTURA:',
              index
            )

            const estruturas =
              Array.isArray(lamina?.estruturas)
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {

              console.warn(
                'ESTRUTURA CLICADA NAO ENCONTRADA:',
                index
              )

              return
            }

            console.log(
              'ESTRUTURA CLICADA:',
              estrutura
            )

            if (
              typeof mostrarEstrutura ===
              'function'
            ) {

              mostrarEstrutura(
                index,
                true
              )

            } else {

              console.warn(
                'mostrarEstrutura nao encontrada.'
              )

            }

          }

        }

      )

    }, 500)

    }
  )


 viewer.addOnceHandler(
  'open',
  () => {

    console.log(
      '========================================'
    )

    console.log(
      'DIAGNOSTICO DA LAMINA NO OPEN'
    )

    console.log(
      'LAMINA NO OPEN:',
      lamina
    )

    console.log(
      'ESTRUTURAS NO OPEN:',
      lamina?.estruturas
    )

    console.log(
      'TIPO DE ESTRUTURAS:',
      typeof lamina?.estruturas
    )

    console.log(
      'E ARRAY:',
      Array.isArray(lamina?.estruturas)
    )

    console.log(
      'QUANTIDADE:',
      Array.isArray(lamina?.estruturas)
        ? lamina.estruturas.length
        : 'NAO E ARRAY'
    )

    console.log(
      '========================================'
    )

    desenharEstruturas(
      viewer,
      lamina
    )

  }
)

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)



/*
 * Instala clique depois que os overlays
 * forem criados.
 */

setTimeout(
  () => {

    instalarCliqueDiretoEstruturas()

  },
  100
)

}


window.addEventListener(
  'hashchange',
  render
)


render()














































































===== backup-reparador-completo-20260819-212337\src\main.backup-login-session.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()



































































===== backup-reparador-completo-20260819-212337\src\main.backup-marcadores-20260819-170509.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


 viewer.addOnceHandler(
  'open',
  () => {

    console.log(
      '========================================'
    )

    console.log(
      'DIAGNOSTICO DA LAMINA NO OPEN'
    )

    console.log(
      'LAMINA NO OPEN:',
      lamina
    )

    console.log(
      'ESTRUTURAS NO OPEN:',
      lamina?.estruturas
    )

    console.log(
      'TIPO DE ESTRUTURAS:',
      typeof lamina?.estruturas
    )

    console.log(
      'E ARRAY:',
      Array.isArray(lamina?.estruturas)
    )

    console.log(
      'QUANTIDADE:',
      Array.isArray(lamina?.estruturas)
        ? lamina.estruturas.length
        : 'NAO E ARRAY'
    )

    console.log(
      '========================================'
    )

    desenharEstruturas(
      viewer,
      lamina
    )

  }
)

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()








































































===== backup-reparador-completo-20260819-212337\src\main.backup-marcadores-css-20260819-172103.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)
    console.log(
  'TILED IMAGE:',
  tiledImage
)

console.log(
  'IMAGE SIZE:',
  tiledImage?.getContentSize?.()
)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {
      console.log(
        'CRIANDO MARCADOR:',
        index,
        estrutura
      )
      console.log(
  'COORDENADAS ORIGINAIS:',
  estrutura.x,
  estrutura.y,
  estrutura.largura,
  estrutura.altura
)

const nome =
  String(
    estrutura.nome ||
    `Estrutura ${index + 1}`
  )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


 viewer.addOnceHandler(
  'open',
  () => {

    console.log(
      '========================================'
    )

    console.log(
      'DIAGNOSTICO DA LAMINA NO OPEN'
    )

    console.log(
      'LAMINA NO OPEN:',
      lamina
    )

    console.log(
      'ESTRUTURAS NO OPEN:',
      lamina?.estruturas
    )

    console.log(
      'TIPO DE ESTRUTURAS:',
      typeof lamina?.estruturas
    )

    console.log(
      'E ARRAY:',
      Array.isArray(lamina?.estruturas)
    )

    console.log(
      'QUANTIDADE:',
      Array.isArray(lamina?.estruturas)
        ? lamina.estruturas.length
        : 'NAO E ARRAY'
    )

    console.log(
      '========================================'
    )

    desenharEstruturas(
      viewer,
      lamina
    )

  }
)

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const autorizado =
      await isAdmin()

    if (!autorizado) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()









































































===== backup-reparador-completo-20260819-212337\src\main.backup-session-final-v2.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config =
    await carregarConfiguracoesSite()

  const route =
    getRoute()


  /*
   * ============================================================
   * LOGIN
   * ============================================================
   */

  if (route === 'login') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      !error &&
      session?.user
    ) {

      window.location.hash =
        '#admin'

      return
    }


    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  /*
   * ============================================================
   * ADMIN
   * ============================================================
   */

  if (route === 'admin') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      error ||
      !session?.user
    ) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  /*
   * ============================================================
   * NOVA LÂMINA
   * ============================================================
   */

  if (route === 'nova-lamina') {

    const {
      data: {
        session
      },
      error
    } =
      await supabase.auth.getSession()


    if (
      error ||
      !session?.user
    ) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }

  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()





































































===== backup-reparador-completo-20260819-212337\src\main.backup-session-safe.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()



































































===== backup-reparador-completo-20260819-212337\src\main.backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()



































































===== backup-reparador-completo-20260819-212337\src\main.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase, isAdmin } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'


const app = document.querySelector('#app')


/* ============================================================
   CONFIGURAÃ‡Ã•ES VISUAIS
   ============================================================ */

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}


async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {

    console.error(
      'Erro ao carregar configuraÃ§Ãµes do site:',
      error
    )

    return {}
  }

  return data || {}

}


/* ============================================================
   ROTAS
   ============================================================ */

function getRoute() {

  const hash =
    window.location.hash

  if (hash === '#login') {
    return 'login'
  }

  if (hash === '#admin') {
    return 'admin'
  }

  if (hash.startsWith('#nova-lamina')) {
    return 'nova-lamina'
  }

  if (hash === '#laminas') {
    return 'catalogo'
  }

  if (hash.startsWith('#lamina/')) {
    return 'lamina'
  }

  return 'home'

}


/* ============================================================
   HEADER
   ============================================================ */

function renderHeader(config = {}) {

  return `
    <header class="header">

      <div class="container header-content">

        <a href="#inicio" class="brand">

          <span
            class="brand-icon"
            style="
              width: ${Number(config.logo_tamanho || 100) * 0.42}px;
              height: ${Number(config.logo_tamanho || 100) * 0.42}px;
            "
          >

            ${
              config.logo_url
                ? `
                  <img
                    src="${escapeHtml(config.logo_url)}"
                    alt="Logo"
                    style="
                      width:100%;
                      height:100%;
                      object-fit:contain;
                    "
                  >
                `
                : 'ðŸ”¬'
            }

          </span>

          <span>

            <strong>
              ${escapeHtml(config.nome_site || 'Atlas')}
            </strong>

            <small>
              ${escapeHtml(config.subtitulo || 'HistolÃ³gico')}
            </small>

          </span>

        </a>


        <nav class="nav">

          <a href="#inicio">
            InÃ­cio
          </a>

          <a href="#laminas">
            LÃ¢minas
          </a>

          <a href="#sobre">
            Sobre
          </a>

          <a href="#login">
            AdministraÃ§Ã£o
          </a>

        </nav>

      </div>

    </header>
  `

}


/* ============================================================
   HOME
   ============================================================ */

function renderHome(config = {}) {

  return `
    ${renderHeader(config)}

    <main>

      <section
        class="hero"
        id="inicio"
      >

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              ðŸ”¬ MICROSCOPIA â€¢ ESTUDO â€¢ EXPLORAÃ‡ÃƒO
            </span>

            <h1>
              ${escapeHtml(
                config.titulo_inicio ||
                'Explore o mundo microscÃ³pico.'
              )}
            </h1>

            <p>
              ${escapeHtml(
                config.descricao_inicio ||
                'Um atlas de histologia interativo para explorar tecidos, estruturas e lÃ¢minas histolÃ³gicas de uma forma visual e dinÃ¢mica.'
              )}
            </p>

            <div class="hero-actions">

              <a
                href="#laminas"
                class="button primary"
              >
                ${escapeHtml(
                  config.texto_botao_principal ||
                  'Explorar ' +
                  (config.nome_site || 'Atlas')
                )}

                <span>
                  â†’
                </span>

              </a>

              <a
                href="#sobre"
                class="button secondary"
              >
                ${escapeHtml(
                  config.texto_botao_secundario ||
                  'ConheÃ§a o projeto'
                )}
              </a>

            </div>

          </div>


          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section
        class="section"
        id="sobre"
      >

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>


          <div class="about-text">

            <p>
              ${escapeHtml(
                config.texto_sobre ||
                (
                  'O ' +
                  (config.nome_site || 'Atlas') +
                  ' ' +
                  (config.subtitulo || 'HistolÃ³gico') +
                  ' foi pensado para transformar a observaÃ§Ã£o das lÃ¢minas em uma experiÃªncia de aprendizagem visual e interativa.'
                )
              )}
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Explorar lÃ¢minas â†’
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>
          ðŸ”¬
          ${escapeHtml(config.nome_site || 'Atlas')}
          ${escapeHtml(config.subtitulo || 'HistolÃ³gico')}
        </span>

        <span>
          ${escapeHtml(
            config.texto_rodape ||
            'Projeto acadÃªmico'
          )}
        </span>

      </div>

    </footer>
  `

}


/* ============================================================
   ESCAPE HTML
   ============================================================ */

function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


/* ============================================================
   RENDER DA LÃ‚MINA
   ============================================================ */

function renderLamina(lamina, config = {}) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a
            href="#laminas"
            class="back-link"
          >
            â† Voltar para as lÃ¢minas
          </a>


          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(
                  lamina.categoria ||
                  'Histologia'
                )}
              </span>

              <h1>
                ${escapeHtml(
                  lamina.nome ||
                  'LÃ¢mina'
                )}
              </h1>

              <p>
                Explore a lÃ¢mina utilizando zoom e movimentaÃ§Ã£o.
              </p>

            </div>


            <div class="slide-meta">

              ${
                lamina.tecnica
                  ? `
                    <span>
                      ðŸ”¬
                      ${escapeHtml(lamina.tecnica)}
                    </span>
                  `
                  : ''
              }

              ${
                lamina.coloracao
                  ? `
                    <span>
                      ðŸ§«
                      ${escapeHtml(lamina.coloracao)}
                    </span>
                  `
                  : ''
              }

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">


          <div class="viewer-container">


            <!-- PAINEL DE ESTUDO -->

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  Ã—
                </button>

              </div>


              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>


              <p id="estrutura-estudo-descricao"></p>

            </div>


            <!-- VIEWER -->

            <div
              id="openseadragon"
              class="viewer"
            ></div>


            <div class="estrutura-navegacao"></div>


            <!-- CONTROLES -->

            <div class="viewer-controls">

              <span>
                Arraste para movimentar â€¢
                Use a roda do mouse para ampliar
              </span>


              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >
                  âˆ’
                </button>


                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posiÃ§Ã£o"
                  aria-label="Restaurar posiÃ§Ã£o original"
                >
                  âŒ‚
                </button>


                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >
                  +
                </button>


                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >
                  â›¶
                </button>


                <span
                  id="zoom-value"
                  class="zoom-value"
                >
                  100%
                </span>

              </div>

            </div>

          </div>


          <!-- PAINEL LATERAL -->

          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÃ‚MINA
            </div>


            <h2>
              ${escapeHtml(
                lamina.nome ||
                'LÃ¢mina'
              )}
            </h2>


            <p class="description">
              ${escapeHtml(
                lamina.descricao ||
                ''
              )}
            </p>


            ${
              estruturas.length
                ? `
                  <div class="info-section">

                    <h3>
                      ðŸ”Ž O que observar
                    </h3>


                    <div class="structure-list">

                      ${
                        estruturas
                          .map(
                            (estrutura, index) => `
                              <button
                                class="structure"
                                data-estrutura-index="${index}"
                                type="button"
                              >

                                <span class="structure-dot"></span>

                                <span>

                                  <strong>
                                    ${escapeHtml(
                                      estrutura.nome ||
                                      ''
                                    )}
                                  </strong>

                                  <small>
                                    ${escapeHtml(
                                      estrutura.descricao ||
                                      ''
                                    )}
                                  </small>

                                </span>

                              </button>
                            `
                          )
                          .join('')
                      }

                    </div>

                  </div>
                `
                : ''
            }

          </aside>

        </div>

      </section>

    </main>
  `

}


/* ============================================================
   BUSCAR LÃ‚MINA
   ============================================================ */

async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lÃ¢mina:',
      error
    )

    return null
  }


  return data

}


/* ============================================================
   PAINEL DE ESTUDO
   ============================================================ */

function mostrarTextoEstrutura(
  index,
  lamina
) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []


  const indice =
    Number(index)


  const estrutura =
    estruturas[indice]


  if (!estrutura) {

    console.warn(
      'Estrutura nÃ£o encontrada:',
      index
    )

    return

  }


  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )


  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )


  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )


  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nÃ£o encontrado.'
    )

    return

  }


  nome.textContent =
    estrutura.nome ||
    `Estrutura ${indice + 1}`


  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descriÃ§Ã£o cadastrada.'


  panel.hidden = false


  panel.dataset.estruturaIndex =
    String(indice)


  document
    .querySelectorAll('.estrutura-overlay')
    .forEach(overlay => {

      overlay.classList.toggle(
        'estrutura-selecionada',
        overlay.dataset.estruturaIndex ===
        String(indice)
      )

    })


  document
    .querySelectorAll('.structure')
    .forEach((botao, botaoIndex) => {

      botao.classList.toggle(
        'estrutura-selecionada',
        botaoIndex === indice
      )

    })


  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}


/* ============================================================
   DESENHAR ESTRUTURAS
   ============================================================ */

function desenharEstruturas(
  viewer,
  lamina
) {

  if (!viewer || !lamina) {
    return
  }


  const estruturas =
    Array.isArray(
      lamina.estruturas
    )
      ? lamina.estruturas
      : []


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {
    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !Number.isFinite(imageWidth) ||
    !Number.isFinite(imageHeight) ||
    imageWidth <= 0 ||
    imageHeight <= 0
  ) {
    return
  }


  viewer
    .currentOverlays
    .slice()
    .forEach(
      overlay => {

        const elemento =
          overlay?.element


        if (
          elemento?.classList?.contains(
            'estrutura-overlay'
          )
        ) {

          try {
            viewer.removeOverlay(
              elemento
            )
          } catch {
            // Overlay jÃƒÂ¡ removido.
          }

        }

      }
    )


  estruturas.forEach(
    (estrutura, index) => {

      const tipo =
        estrutura.tipo ||
        'ponto'


      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao ||
          ''
        )


      const x =
        Math.max(
          0,
          Math.min(
            1,
            Number(
              estrutura.x ??
              0.5
            )
          )
        )


      const y =
        Math.max(
          0,
          Math.min(
            1,
            Number(
              estrutura.y ??
              0.5
            )
          )
        )


      const largura =
        Math.max(
          0.001,
          Math.min(
            1,
            Number(
              estrutura.largura ??
              0.08
            )
          )
        )


      const altura =
        Math.max(
          0.001,
          Math.min(
            1,
            Number(
              estrutura.altura ??
              0.08
            )
          )
        )


      const elemento =
        document.createElement(
          'div'
        )


      elemento.className =
        `estrutura-overlay estrutura-overlay-${tipo}`


      elemento.dataset.estruturaIndex =
        String(index)


      elemento.title =
        descricao
          ? `${nome}: ${descricao}`
          : nome


      elemento.style.cursor =
        'pointer'


      let rect


      if (
        tipo === 'seta'
      ) {

        const x2 =
          Math.max(
            0,
            Math.min(
              1,
              Number(
                estrutura.x2 ??
                x + largura
              )
            )
          )


        const y2 =
          Math.max(
            0,
            Math.min(
              1,
              Number(
                estrutura.y2 ??
                y + altura
              )
            )
          )


        const inicioX =
          x * imageWidth


        const inicioY =
          y * imageHeight


        const fimX =
          x2 * imageWidth


        const fimY =
          y2 * imageHeight


        const dx =
          fimX -
          inicioX


        const dy =
          fimY -
          inicioY


        const comprimento =
          Math.sqrt(
            dx * dx +
            dy * dy
          )


        const angulo =
          Math.atan2(
            dy,
            dx
          ) *
          180 /
          Math.PI


        const ponto =
          viewer.viewport
            .imageToViewportCoordinates(
              inicioX,
              inicioY
            )


        const tamanho =
          viewer.viewport
            .imageToViewportCoordinates(
              comprimento,
              0
            )


        elemento.style.width =
          `${Math.max(
            20,
            Math.abs(
              tamanho.x
            )
          )}px`


        elemento.style.height =
          '4px'


        elemento.style.transform =
          `rotate(${angulo}deg)`


        elemento.style.transformOrigin =
          '0 50%'


        viewer.addOverlay({

          element:
            elemento,

          location:
            ponto,

          placement:
            OpenSeadragon.Placement.TOP_LEFT

        })


        elemento.addEventListener(
          'click',
          event => {

            event.preventDefault()

            event.stopPropagation()


            mostrarTextoEstrutura(
              index,
              lamina
            )

          }
        )


        return

      }


      if (
        tipo === 'texto'
      ) {

        const imageX =
          x *
          imageWidth


        const imageY =
          y *
          imageHeight


        const ponto =
          viewer.viewport
            .imageToViewportCoordinates(
              imageX,
              imageY
            )


        elemento.innerHTML = `
          <span class="estrutura-texto-label">
            ${escapeHtml(
              estrutura.texto ||
              nome
            )}
          </span>
        `


        viewer.addOverlay({

          element:
            elemento,

          location:
            ponto,

          placement:
            OpenSeadragon.Placement.TOP_LEFT

        })


        elemento.addEventListener(
          'click',
          event => {

            event.preventDefault()

            event.stopPropagation()


            mostrarTextoEstrutura(
              index,
              lamina
            )

          }
        )


        return

      }


      const imageX =
        x *
        imageWidth


      const imageY =
        y *
        imageHeight


      const imageWidthEstrutura =
        largura *
        imageWidth


      const imageHeightEstrutura =
        altura *
        imageHeight


      rect =
        viewer.viewport
          .imageToViewportRectangle(
            imageX,
            imageY,
            imageWidthEstrutura,
            imageHeightEstrutura
          )


      if (!rect) {
        return
      }


      if (
        tipo === 'ponto'
      ) {

        elemento.innerHTML = `
          <div class="estrutura-box estrutura-box-ponto">
            <span class="estrutura-ponto"></span>
            <span class="estrutura-label">
              ${escapeHtml(nome)}
            </span>
          </div>
        `

      } else {

        elemento.innerHTML = `
          <div class="estrutura-box estrutura-box-retangulo">
            <span class="estrutura-label">
              ${escapeHtml(nome)}
            </span>
          </div>
        `

      }


      viewer.addOverlay({

        element:
          elemento,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

      })


      elemento.addEventListener(
        'click',
        event => {

          event.preventDefault()

          event.stopPropagation()


          mostrarTextoEstrutura(
            index,
            lamina
          )


          if (
            typeof centralizarEstrutura ===
            'function'
          ) {

            centralizarEstrutura(
              viewer,
              elemento,
              true
            )

          }

        }
      )

    }
  )

}


function centralizarEstrutura(
  viewer,
  overlay,
  aplicarZoom = true
) {

  if (!viewer || !overlay) {
    return
  }


  const x =
    Number(
      overlay.dataset.imageX
    )


  const y =
    Number(
      overlay.dataset.imageY
    )


  const largura =
    Number(
      overlay.dataset.imageWidth
    )


  const altura =
    Number(
      overlay.dataset.imageHeight
    )


  if (
    !Number.isFinite(x) ||
    !Number.isFinite(y)
  ) {

    return

  }


  const centroX =
    x +
    (
      Number.isFinite(largura)
        ? largura / 2
        : 0
    )


  const centroY =
    y +
    (
      Number.isFinite(altura)
        ? altura / 2
        : 0
    )


  const ponto =
    viewer.viewport.imageToViewportCoordinates(
      centroX,
      centroY
    )


  viewer.viewport.panTo(
    ponto,
    true
  )


  if (!aplicarZoom) {
    return
  }


  const zoomAtual =
    viewer.viewport.getZoom()


  const zoomMaximo =
    viewer.viewport.getMaxZoom()


  const zoomDesejado =
    Math.min(
      zoomAtual * 1.8,
      zoomMaximo
    )


  viewer.viewport.zoomTo(
    zoomDesejado,
    ponto,
    true
  )

}


/* ============================================================
   MODO ESTUDO
   ============================================================ */

function configurarModoEstudo(
  viewer,
  lamina
) {

  if (!viewer || !lamina) {
    return
  }


  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )


  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )


  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )


  const fechar =
    document.querySelector(
      '#estrutura-estudo-fechar'
    )


  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nÃ£o encontrado.'
    )

    return

  }


  let indiceAtual =
    -1


  function limparSelecao() {

    document
      .querySelectorAll(
        '.estrutura-overlay'
      )
      .forEach(
        overlay => {

          overlay.classList.remove(
            'estrutura-selecionada'
          )

        }
      )


    document
      .querySelectorAll(
        '.structure'
      )
      .forEach(
        botao => {

          botao.classList.remove(
            'estrutura-selecionada'
          )

        }
      )

  }


  function selecionarVisual(index) {

    const indice =
      String(index)


    document
      .querySelectorAll(
        '.estrutura-overlay'
      )
      .forEach(
        overlay => {

          overlay.classList.toggle(
            'estrutura-selecionada',
            overlay.dataset.estruturaIndex ===
            indice
          )

        }
      )


    document
      .querySelectorAll(
        '.structure'
      )
      .forEach(
        (botao, botaoIndex) => {

          botao.classList.toggle(
            'estrutura-selecionada',
            String(botaoIndex) === indice
          )

        }
      )

  }


  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      Number(index)


    const estrutura =
      estruturas[indice]


    if (!estrutura) {
      return
    }


    indiceAtual =
      indice


    nome.textContent =
      estrutura.nome ||
      `Estrutura ${indice + 1}`


    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descriÃ§Ã£o cadastrada.'


    panel.hidden =
      false


    panel.dataset.estruturaIndex =
      String(indice)


    selecionarVisual(
      indice
    )


    if (centralizar) {

      const overlay =
        document.querySelector(
          `.estrutura-overlay[data-estrutura-index="${indice}"]`
        )


      if (overlay) {

        centralizarEstrutura(
          viewer,
          overlay,
          true
        )

      }

    }

  }


  function fecharPainel() {

    panel.hidden =
      true


    indiceAtual =
      -1


    limparSelecao()

  }


  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )


  /*
   * Clique na lista.
   *
   * IMPORTANTE:
   * Este Ã© o Ãºnico controlador de clique
   * necessÃ¡rio para os botÃµes .structure.
   */

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, index) => {

        botao.dataset.estruturaIndex =
          String(index)


        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()


            mostrarEstrutura(
              index,
              true
            )

          }
        )

      }
    )


  /*
   * Teclado.
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }


      if (
        panel.hidden ||
        !estruturas.length
      ) {

        return

      }


      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {

        return

      }


      event.preventDefault()


      if (indiceAtual < 0) {

        mostrarEstrutura(
          0,
          true
        )

        return

      }


      let novoIndice =
        indiceAtual


      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1


        if (
          novoIndice >=
          estruturas.length
        ) {

          novoIndice =
            0

        }

      } else {

        novoIndice =
          indiceAtual - 1


        if (novoIndice < 0) {

          novoIndice =
            estruturas.length - 1

        }

      }


      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )


  console.log(
    'Modo estudo conectado:',
    estruturas.length,
    'estruturas.'
  )

}


/* ============================================================
   VIEWER
   ============================================================ */

function iniciarViewer(lamina) {

  const elemento =
    document.querySelector(
      '#openseadragon'
    )


  if (!elemento) {

    console.error(
      'Elemento #openseadragon nÃ£o encontrado.'
    )

    return

  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `

      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">

        Imagem da lÃ¢mina nÃ£o encontrada.

      </div>

    `

    return

  }


  /*
   * CriaÃ§Ã£o do OpenSeadragon.
   */

  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url:
          lamina.imagem_url

      },

      showNavigationControl:
        false,

      animationTime:
        0.8,

      zoomPerScroll:
        1.5,

      maxZoomPixelRatio:
        8,

      visibilityRatio:
        1,

      constrainDuringPan:
        true

    })


  /*
   * Zoom.
   */

  const zoomValue =
    document.querySelector(
      '#zoom-value'
    )


  function atualizarZoom() {

    if (!zoomValue) {
      return
    }


    const zoom =
      viewer.viewport.getZoom()


    zoomValue.textContent =
      Math.round(
        zoom * 100
      ) + '%'

  }


  document
    .querySelector(
      '#zoom-in'
    )
    ?.addEventListener(
      'click',
      event => {

        event.preventDefault()

        viewer.viewport.zoomBy(
          1.5
        )

        viewer.viewport.applyConstraints()

        atualizarZoom()

      }
    )


  document
    .querySelector(
      '#zoom-out'
    )
    ?.addEventListener(
      'click',
      event => {

        event.preventDefault()

        viewer.viewport.zoomBy(
          0.67
        )

        viewer.viewport.applyConstraints()

        atualizarZoom()

      }
    )


  document
    .querySelector(
      '#zoom-home'
    )
    ?.addEventListener(
      'click',
      event => {

        event.preventDefault()

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document
    .querySelector(
      '#zoom-fullscreen'
    )
    ?.addEventListener(
      'click',
      event => {

        event.preventDefault()


        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            true
          )

        } else {

          viewer.setFullPage(
            true
          )

        }

      }
    )


  /*
   * AtualizaÃ§Ã£o do zoom.
   */

  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * ABERTURA DA IMAGEM
   *
   * Aqui existe APENAS UM handler.
   */

  viewer.addOnceHandler(
    'open',
    () => {

      console.log(
        '========================================'
      )

      console.log(
        'DIAGNÃ“STICO DA LÃ‚MINA NO OPEN'
      )

      console.log(
        'LÃ‚MINA:',
        lamina
      )

      console.log(
        'ESTRUTURAS:',
        lamina?.estruturas
      )

      console.log(
        'TIPO:',
        typeof lamina?.estruturas
      )

      console.log(
        'Ã‰ ARRAY:',
        Array.isArray(
          lamina?.estruturas
        )
      )

      console.log(
        'QUANTIDADE:',
        Array.isArray(
          lamina?.estruturas
        )
          ? lamina.estruturas.length
          : 'NÃƒO Ã‰ ARRAY'
      )

      console.log(
        '========================================'
      )


      /*
       * Primeiro desenha os marcadores.
       */

      desenharEstruturas(
        viewer,
        lamina
      )


      /*
       * Depois conecta o modo estudo.
       */

      configurarModoEstudo(
        viewer,
        lamina
      )


      atualizarZoom()

    }
  )


  /*
   * Atalhos de teclado do viewer.
   */

  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {

        return

      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(
          1.25
        )

        viewer.viewport.applyConstraints()

        atualizarZoom()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(
          0.8
        )

        viewer.viewport.applyConstraints()

        atualizarZoom()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }

    }
  )


  atualizarZoom()


  /*
   * Guarda referÃªncia para diagnÃ³stico.
   */

  window.__atlasViewer =
    viewer


  return viewer

}


/* ============================================================
   LIMPAR ESTRUTURAS
   ============================================================ */

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const overlays =
    viewer.currentOverlays || []


  overlays
    .slice()
    .forEach(
      overlay => {

        const elemento =
          overlay?.element


        if (
          elemento?.classList?.contains(
            'estrutura-overlay'
          )
        ) {

          try {

            viewer.removeOverlay(
              elemento
            )

          } catch (erro) {

            console.warn(
              'Erro ao remover estrutura:',
              erro
            )

          }

        }

      }
    )

}


/* ============================================================
   INFORMAÃ‡ÃƒO SIMPLES
   ============================================================ */

function mostrarInfoEstruturaSimples(
  index,
  lamina
) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []


  const indice =
    Number(index)


  const estrutura =
    estruturas[indice]


  if (!estrutura) {

    console.warn(
      'Estrutura nÃ£o encontrada:',
      index
    )

    return

  }


  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )


  if (!area) {

    area =
      document.createElement(
        'div'
      )


    area.id =
      'estrutura-info-simples'


    area.style.cssText = `

      margin-top:16px;

      padding:20px;

      background:#ffffff;

      border:1px solid #dcdcdc;

      border-radius:12px;

      box-shadow:
        0 4px 16px
        rgba(0,0,0,0.08);

      position:relative;

      z-index:20;

    `


    const titulo =
      document.createElement(
        'h3'
      )


    titulo.id =
      'estrutura-info-simples-nome'


    titulo.style.cssText = `

      margin:
        0 35px 8px 0;

      font-size:20px;

    `


    const descricao =
      document.createElement(
        'p'
      )


    descricao.id =
      'estrutura-info-simples-descricao'


    descricao.style.cssText = `

      margin:0;

      line-height:1.6;

      color:#555;

    `


    const fechar =
      document.createElement(
        'button'
      )


    fechar.type =
      'button'


    fechar.textContent =
      'Ã—'


    fechar.title =
      'Fechar'


    fechar.style.cssText = `

      position:absolute;

      top:10px;

      right:12px;

      border:none;

      background:transparent;

      font-size:26px;

      cursor:pointer;

      color:#777;

      line-height:1;

    `


    fechar.addEventListener(
      'click',
      () => {

        area.hidden =
          true

      }
    )


    area.appendChild(
      fechar
    )


    area.appendChild(
      titulo
    )


    area.appendChild(
      descricao
    )


    const viewer =
      document.querySelector(
        '#openseadragon'
      )


    if (
      viewer?.parentElement
    ) {

      viewer.parentElement.appendChild(
        area
      )

    } else {

      document.body.appendChild(
        area
      )

    }

  }


  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )


  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )


  if (
    !titulo ||
    !descricao
  ) {

    return

  }


  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${indice + 1}`


  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descriÃ§Ã£o cadastrada.'


  area.hidden =
    false


  area.dataset.estruturaIndex =
    String(indice)


  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   INFORMAÃ‡ÃƒO DA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(
  index,
  lamina
) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []


  const indice =
    Number(index)


  const estrutura =
    estruturas[indice]


  if (!estrutura) {

    console.warn(
      'Estrutura nÃ£o encontrada:',
      index
    )

    return

  }


  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )


  if (!area) {

    const layout =
      document.querySelector(
        '.viewer-layout'
      )


    if (!layout) {

      console.warn(
        'viewer-layout nÃ£o encontrado.'
      )

      return

    }


    area =
      document.createElement(
        'div'
      )


    area.id =
      'estrutura-info-lista'


    area.style.cssText = `

      grid-column:1 / -1;

      margin-top:18px;

      padding:22px;

      background:white;

      border:1px solid #ddd;

      border-radius:14px;

      box-shadow:
        0 4px 15px
        rgba(0,0,0,.08);

    `


    layout.appendChild(
      area
    )

  }


  const nome =
    estrutura.nome ||
    `Estrutura ${indice + 1}`


  const descricao =
    estrutura.descricao ||
    'Nenhuma descriÃ§Ã£o cadastrada.'


  area.innerHTML = `

    <div style="
      font-size:11px;
      font-weight:700;
      letter-spacing:.08em;
      text-transform:uppercase;
      color:#777;
      margin-bottom:6px;
    ">
      Estrutura selecionada
    </div>


    <h3 style="
      margin:0 0 8px 0;
      font-size:21px;
    ">
      ${escapeHtml(nome)}
    </h3>


    <p style="
      margin:0;
      font-size:15px;
      line-height:1.6;
      color:#555;
    ">
      ${escapeHtml(descricao)}
    </p>

  `


  area.hidden =
    false


  console.log(
    'INFORMAÃ‡ÃƒO DA LISTA EXIBIDA:',
    nome
  )

}


/* ============================================================
   NAVEGAÃ‡ÃƒO DAS ESTRUTURAS
   ============================================================ */

function configurarNavegacaoEstruturas(
  viewer,
  lamina
) {

  if (!viewer || !lamina) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []


  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botÃµes /',
    estruturas.length,
    'estruturas'
  )

}


/* ============================================================
   CONECTAR INFO DOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }


  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples ===
        '1'
      ) {

        return

      }


      overlay.dataset.infoSimples =
        '1'


      overlay.style.cursor =
        'pointer'


      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }
      )

    }
  )


  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}


/* ============================================================
   INSTALAR CLIQUE DA LISTA
   ============================================================ */

function instalarCliqueListaEstruturas(
  lamina
) {

  if (!lamina) {

    console.warn(
      'Clique lista: lÃ¢mina nÃ£o encontrada.'
    )

    return

  }


  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, index) => {

        botao.dataset.estruturaIndex =
          String(index)

      }
    )


  console.log(
    'Ãndices da lista instalados:',
    document.querySelectorAll(
      '.structure'
    ).length
  )

}


/* ============================================================
   RENDER PRINCIPAL
   ============================================================ */

async function render() {

  try {

    /*
     * ConfiguraÃ§Ãµes.
     */

    const config =
      await carregarConfiguracoesSite()


    /*
     * Aplica imediatamente.
     */

    aplicarConfiguracoesVisuais(
      config
    )


    /*
     * Rota atual.
     */

    const route =
      getRoute()


    console.log(
      'ROTA:',
      route
    )


    /* ========================================================
       LOGIN
       ======================================================== */

    if (
      route === 'login'
    ) {

      const {
        data: {
          session
        },
        error
      } =
        await supabase.auth.getSession()


      if (
        !error &&
        session?.user
      ) {

        window.location.hash =
          '#admin'

        return

      }


      app.innerHTML =
        renderLogin()


      setupLogin()


      return

    }


    /* ========================================================
       ADMIN
       ======================================================== */

    if (
      route === 'admin'
    ) {

      const autorizado =
        await isAdmin()


      if (!autorizado) {

        window.location.hash =
          '#login'

        return

      }


      app.innerHTML =
        await renderAdmin()


      setupAdmin()


      return

    }


    /* ========================================================
       NOVA LÃ‚MINA
       ======================================================== */

    if (
      route === 'nova-lamina'
    ) {

      const autorizado =
        await isAdmin()


      if (!autorizado) {

        window.location.hash =
          '#login'

        return

      }


      app.innerHTML =
        renderNovaLamina()


      setupNovaLamina()


      return

    }


    /* ========================================================
       CATÃLOGO
       ======================================================== */

    if (
      route === 'catalogo'
    ) {

      await renderCatalogo()

      return

    }


    /* ========================================================
       LÃ‚MINA
       ======================================================== */

    if (
      route === 'lamina'
    ) {

      const id =
        window.location.hash
          .replace(
            '#lamina/',
            ''
          )


      if (!id) {

        window.location.hash =
          '#laminas'

        return

      }


      app.innerHTML = `

        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
        ">

          <div>

            <h1>
              Carregando lÃ¢mina...
            </h1>

          </div>

        </main>

      `


      const lamina =
        await buscarLamina(id)


      if (!lamina) {

        app.innerHTML = `

          ${renderHeader(config)}

          <main style="
            min-height:60vh;
            display:flex;
            align-items:center;
            justify-content:center;
            text-align:center;
          ">

            <div>

              <h1>
                LÃ¢mina nÃ£o encontrada
              </h1>


              <p>
                NÃ£o foi possÃ­vel carregar esta lÃ¢mina.
              </p>


              <a
                href="#laminas"
                class="button primary"
              >
                Voltar para lÃ¢minas
              </a>

            </div>

          </main>

        `

        return

      }


      /*
       * Renderiza a pÃ¡gina.
       */

      app.innerHTML =
        renderLamina(
          lamina,
          config
        )


      /*
       * Inicia o viewer.
       */

      iniciarViewer(
        lamina
      )


      return

    }


    /* ========================================================
       HOME
       ======================================================== */

    app.innerHTML =
      renderHome(
        config
      )

  } catch (erro) {

    console.error(
      'ERRO FATAL NO RENDER:',
      erro
    )


    /*
     * NÃ£o deixa o site simplesmente
     * ficar branco se alguma funÃ§Ã£o falhar.
     */

    if (app) {

      app.innerHTML = `

        <main style="
          min-height:70vh;
          display:flex;
          align-items:center;
          justify-content:center;
          padding:30px;
          font-family:system-ui,sans-serif;
        ">

          <div style="
            max-width:700px;
            width:100%;
            padding:30px;
            border:1px solid #ddd;
            border-radius:16px;
            background:white;
          ">

            <h1>
              Ocorreu um erro ao carregar o Atlas.
            </h1>


            <p>
              Abra o console do navegador
              para ver os detalhes.
            </p>


            <button
              type="button"
              onclick="location.reload()"
              style="
                padding:10px 18px;
                border:none;
                border-radius:8px;
                cursor:pointer;
              "
            >
              Recarregar
            </button>

          </div>

        </main>

      `

    }

  }

}


/* ============================================================
   HASHCHANGE
   ============================================================ */

window.addEventListener(
  'hashchange',
  render
)


/* ============================================================
   INICIALIZAÃ‡ÃƒO
   ============================================================ */

render()


===== backup-reparador-completo-20260819-212337\src\main.js.backup-antes-botoes-final.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

            if (lamina) {

              mostrarTextoEstrutura(index, lamina)

            }

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const index =
            Number(
              overlay.dataset.estruturaIndex
            )

          if (
            !Number.isFinite(index)
          ) {
            return
          }

          if (
            typeof lamina !== 'undefined'
          ) {

            mostrarTextoEstrutura(index, lamina)

          }

        }
      )

    }
  )

  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()




























































===== backup-reparador-completo-20260819-212337\src\main.js.backup-antes-conexao-final.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function abrirPainelEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

            if (lamina) {

              abrirPainelEstrutura(
                index,
                lamina
              )

            }

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const index =
            Number(
              overlay.dataset.estruturaIndex
            )

          if (
            !Number.isFinite(index)
          ) {
            return
          }

          if (
            typeof lamina !== 'undefined'
          ) {

            abrirPainelEstrutura(
              index,
              lamina
            )

          }

        }
      )

    }
  )

  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()




















































===== backup-reparador-completo-20260819-212337\src\main.js.backup-clique-final-20260819-133709.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function abrirPainelEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

            if (lamina) {

              abrirPainelEstrutura(
                index,
                lamina
              )

            }

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const index =
            Number(
              overlay.dataset.estruturaIndex
            )

          if (
            !Number.isFinite(index)
          ) {
            return
          }

          if (
            typeof lamina !== 'undefined'
          ) {

            abrirPainelEstrutura(
              index,
              lamina
            )

          }

        }
      )

    }
  )

  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()




















































===== backup-reparador-completo-20260819-212337\src\main.js.backup-clique-lista-20260819-144217.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

            if (lamina) {

              mostrarTextoEstrutura(index, lamina)

            }

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const index =
            Number(
              overlay.dataset.estruturaIndex
            )

          if (
            !Number.isFinite(index)
          ) {
            return
          }

          if (
            typeof lamina !== 'undefined'
          ) {

            mostrarTextoEstrutura(index, lamina)

          }

        }
      )

    }
  )

  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()


























































===== backup-reparador-completo-20260819-212337\src\main.js.backup-duplicata-mostrarTextoEstrutura.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

            if (lamina) {

              mostrarTextoEstrutura(index, lamina)

            }

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const index =
            Number(
              overlay.dataset.estruturaIndex
            )

          if (
            !Number.isFinite(index)
          ) {
            return
          }

          if (
            typeof lamina !== 'undefined'
          ) {

            mostrarTextoEstrutura(index, lamina)

          }

        }
      )

    }
  )

  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}



function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {
    return
  }

  let area =
    document.querySelector('#estrutura-info-simples')

  if (!area) {

    const viewer =
      document.querySelector('#openseadragon')

    if (!viewer) {
      return
    }

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin: 20px auto;
      padding: 20px;
      max-width: 1000px;
      background: #111827;
      color: white;
      border-radius: 12px;
      border: 1px solid rgba(255,255,255,.12);
      box-sizing: border-box;
      font-family: inherit;
    `

    viewer.parentElement?.appendChild(area)

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size:12px;
      font-weight:700;
      letter-spacing:.08em;
      text-transform:uppercase;
      opacity:.65;
      margin-bottom:6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size:22px;
      font-weight:700;
      margin-bottom:8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size:15px;
      line-height:1.6;
      opacity:.9;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Estrutura exibida abaixo:',
    nome
  )
}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()
























































===== backup-reparador-completo-20260819-212337\src\main.js.backup-fix-zoom-home-20260819-152415.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado modo-estudo-card"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar posição"
                  aria-label="Restaurar posição original"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()



































































===== backup-reparador-completo-20260819-212337\src\main.js.backup-indice-botoes-20260819-144503.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

            if (lamina) {

              mostrarTextoEstrutura(index, lamina)

            }

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const index =
            Number(
              overlay.dataset.estruturaIndex
            )

          if (
            !Number.isFinite(index)
          ) {
            return
          }

          if (
            typeof lamina !== 'undefined'
          ) {

            mostrarTextoEstrutura(index, lamina)

          }

        }
      )

    }
  )

  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()



























































===== backup-reparador-completo-20260819-212337\src\main.js.backup-indice-correto-20260819-144535.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

            if (lamina) {

              mostrarTextoEstrutura(index, lamina)

            }

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const index =
            Number(
              overlay.dataset.estruturaIndex
            )

          if (
            !Number.isFinite(index)
          ) {
            return
          }

          if (
            typeof lamina !== 'undefined'
          ) {

            mostrarTextoEstrutura(index, lamina)

          }

        }
      )

    }
  )

  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()



























































===== backup-reparador-completo-20260819-212337\src\main.js.backup-info-simples-20260819-134257.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function abrirPainelEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

            if (lamina) {

              abrirPainelEstrutura(
                index,
                lamina
              )

            }

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const index =
            Number(
              overlay.dataset.estruturaIndex
            )

          if (
            !Number.isFinite(index)
          ) {
            return
          }

          if (
            typeof lamina !== 'undefined'
          ) {

            abrirPainelEstrutura(
              index,
              lamina
            )

          }

        }
      )

    }
  )

  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()





















































===== backup-reparador-completo-20260819-212337\src\main.js.backup-limpeza-lista-20260819-145306.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()




























































===== backup-reparador-completo-20260819-212337\src\main.js.backup-lista-info-20260819-140846.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configuraÃ§Ãµes do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : 'ðŸ”¬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "HistolÃ³gico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">InÃ­cio</a>
          <a href="#laminas">LÃ¢minas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">AdministraÃ§Ã£o</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              ðŸ”¬ MICROSCOPIA â€¢ ESTUDO â€¢ EXPLORAÃ‡ÃƒO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscÃ³pico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lÃ¢minas histolÃ³gicas de uma forma visual e dinÃ¢mica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>â†’</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "ConheÃ§a o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "HistolÃ³gico") + " foi pensado para transformar a observaÃ§Ã£o das lÃ¢minas em uma experiÃªncia de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lÃ¢minas â†’
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>ðŸ”¬ ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "HistolÃ³gico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadÃªmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            â† Voltar para as lÃ¢minas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'LÃ¢mina')}
              </h1>

              <p>
                Explore a lÃ¢mina utilizando zoom e movimentaÃ§Ã£o.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>ðŸ”¬ ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>ðŸ§« ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  Ã—
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                â†
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="PrÃ³xima estrutura"
                aria-label="PrÃ³xima estrutura"
              >
                â†’
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar â€¢
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >âˆ’</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >âŒ‚</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualizaÃ§Ã£o"
                  aria-label="Restaurar visualizaÃ§Ã£o"
                >â†º</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >â›¶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÃ‚MINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'LÃ¢mina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    ðŸ”Ž O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lÃ¢mina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descriÃ§Ã£o cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lÃ¢mina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda nÃ£o disponÃ­vel para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'NÃ£o foi possÃ­vel determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores invÃ¡lidos:',
          estrutura
        )

        return
      }


      /*
       * As posiÃ§Ãµes cadastradas no editor sÃ£o
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

            if (lamina) {

              mostrarTextoEstrutura(index, lamina)

            }

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const index =
            Number(
              overlay.dataset.estruturaIndex
            )

          if (
            !Number.isFinite(index)
          ) {
            return
          }

          if (
            typeof lamina !== 'undefined'
          ) {

            mostrarTextoEstrutura(index, lamina)

          }

        }
      )

    }
  )

  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descriÃ§Ã£o cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}





function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon nÃ£o encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lÃ¢mina nÃ£o encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lÃ¢mina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      'Ã—'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descriÃ§Ã£o cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lÃ¢mina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              LÃ¢mina nÃ£o encontrada
            </h1>

            <p>
              NÃ£o foi possÃ­vel carregar esta lÃ¢mina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lÃ¢minas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()
























































===== backup-reparador-completo-20260819-212337\src\main.js.backup-lista-info-20260819-140848.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

            if (lamina) {

              mostrarTextoEstrutura(index, lamina)

            }

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const index =
            Number(
              overlay.dataset.estruturaIndex
            )

          if (
            !Number.isFinite(index)
          ) {
            return
          }

          if (
            typeof lamina !== 'undefined'
          ) {

            mostrarTextoEstrutura(index, lamina)

          }

        }
      )

    }
  )

  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()

























































===== backup-reparador-completo-20260819-212337\src\main.js.backup-refino-painel-20260819-145526.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()





























































===== backup-reparador-completo-20260819-212337\src\main.js.backup-seguranca-20260819-133801.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function abrirPainelEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

            if (lamina) {

              abrirPainelEstrutura(
                index,
                lamina
              )

            }

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const index =
            Number(
              overlay.dataset.estruturaIndex
            )

          if (
            !Number.isFinite(index)
          ) {
            return
          }

          if (
            typeof lamina !== 'undefined'
          ) {

            abrirPainelEstrutura(
              index,
              lamina
            )

          }

        }
      )

    }
  )

  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()




















































===== backup-reparador-completo-20260819-212337\src\main.js.backup-texto-20260819-134401.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function abrirPainelEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

            if (lamina) {

              abrirPainelEstrutura(
                index,
                lamina
              )

            }

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const index =
            Number(
              overlay.dataset.estruturaIndex
            )

          if (
            !Number.isFinite(index)
          ) {
            return
          }

          if (
            typeof lamina !== 'undefined'
          ) {

            abrirPainelEstrutura(
              index,
              lamina
            )

          }

        }
      )

    }
  )

  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()






















































===== backup-reparador-completo-20260819-212337\src\main.js.backup-texto-20260819-134408.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

            if (lamina) {

              mostrarTextoEstrutura(index, lamina)

            }

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const index =
            Number(
              overlay.dataset.estruturaIndex
            )

          if (
            !Number.isFinite(index)
          ) {
            return
          }

          if (
            typeof lamina !== 'undefined'
          ) {

            mostrarTextoEstrutura(index, lamina)

          }

        }
      )

    }
  )

  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}



function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {
    return
  }

  let area =
    document.querySelector('#estrutura-info-simples')

  if (!area) {

    const viewer =
      document.querySelector('#openseadragon')

    if (!viewer) {
      return
    }

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin: 20px auto;
      padding: 20px;
      max-width: 1000px;
      background: #111827;
      color: white;
      border-radius: 12px;
      border: 1px solid rgba(255,255,255,.12);
      box-sizing: border-box;
      font-family: inherit;
    `

    viewer.parentElement?.appendChild(area)

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size:12px;
      font-weight:700;
      letter-spacing:.08em;
      text-transform:uppercase;
      opacity:.65;
      margin-bottom:6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size:22px;
      font-weight:700;
      margin-bottom:8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size:15px;
      line-height:1.6;
      opacity:.9;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Estrutura exibida abaixo:',
    nome
  )
}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()























































===== backup-reparador-completo-20260819-212337\src\main.js.backup-visual-estudo-20260819-145758.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel estrutura-estudo-panel-refinado"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map((estrutura, index) => `
                      <button class="structure" data-estrutura-index="${index}">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function mostrarTextoEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura_DESATIVADA(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura_DESATIVADA(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  const botoes =
    document.querySelectorAll('.structure')

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  console.log(
    'LISTA FINAL:',
    botoes.length,
    'botoes /',
    estruturas.length,
    'estruturas'
  )

  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)

      botao.onclick = () => {

        const estrutura =
          estruturas[index]

        if (!estrutura) {
          console.warn(
            'Estrutura nao encontrada:',
            index
          )
          return
        }

        let area =
          document.querySelector(
            '#estrutura-info-simples'
          )

        if (!area) {

          area =
            document.createElement('div')

          area.id =
            'estrutura-info-simples'

          area.style.cssText = `
            margin-top:18px;
            padding:18px;
            background:#f5f7fa;
            border:1px solid #e1e5ea;
            border-radius:12px;
          `

          const lista =
            document.querySelector(
              '.structure-list'
            )

          if (lista) {
            lista.after(area)
          } else {
            return
          }
        }

        area.innerHTML = `
          <div style="
            font-size:11px;
            font-weight:700;
            letter-spacing:.08em;
            text-transform:uppercase;
            margin-bottom:8px;
            opacity:.65;
          ">
            ESTRUTURA
          </div>

          <h3 style="
            margin:0 0 8px 0;
            font-size:20px;
          ">
            ${escapeHtml(
              estrutura.nome ||
              'Estrutura ' + (index + 1)
            )}
          </h3>

          <p style="
            margin:0;
            line-height:1.6;
          ">
            ${escapeHtml(
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'
            )}
          </p>
        `

        area.hidden = false

        console.log(
          'LISTA CLICADA:',
          index,
          estrutura.nome
        )
      }

    }
  )

}
function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura_DESATIVADA(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura_DESATIVADA(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}






/* ============================================================
   INFORMACAO DA ESTRUTURA - CLIQUE NA LISTA
   ============================================================ */

function mostrarInfoListaEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-lista'
    )

  if (!area) {

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (!viewer) {

      console.warn(
        'Viewer #openseadragon nao encontrado.'
      )

      return

    }

    area =
      document.createElement(
        'div'
      )

    area.id =
      'estrutura-info-lista'

    area.style.cssText = `
      margin: 18px 0;
      padding: 20px 22px;
      background: #ffffff;
      border: 1px solid #dddddd;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,.08);
      box-sizing: border-box;
      width: 100%;
    `

    viewer.parentElement?.appendChild(
      area
    )

  }

  const nome =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  const descricao =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.innerHTML = `
    <div style="
      font-size: 11px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 6px;
    ">
      Estrutura selecionada
    </div>

    <div style="
      font-size: 21px;
      font-weight: 700;
      margin-bottom: 8px;
    ">
      ${escapeHtml(nome)}
    </div>

    <div style="
      font-size: 15px;
      line-height: 1.6;
      color: #555;
    ">
      ${escapeHtml(descricao)}
    </div>
  `

  area.hidden = false

  area.scrollIntoView({
    behavior: 'smooth',
    block: 'nearest'
  })

  console.log(
    'Informacao exibida:',
    nome
  )

}



/* ============================================================
   CLIQUE NAS ESTRUTURAS DA LISTA
   Controlador unico
   ============================================================ */

function instalarCliqueListaEstruturas(lamina) {

  if (!lamina) {
    console.warn(
      'Clique lista: lamina nao encontrada.'
    )
    return
  }

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      botao => {

        if (
          botao.dataset.listaInfoAtiva ===
          'true'
        ) {
          return
        }

        botao.dataset.listaInfoAtiva =
          'true'

        botao.addEventListener(
          'click',
          event => {

            event.preventDefault()
            event.stopPropagation()

            const index =
              Number(
                botao.dataset.estruturaIndex
              )

            if (
              !Number.isFinite(index)
            ) {
              console.warn(
                'Indice da estrutura invalido:',
                botao
              )
              return
            }

            const estruturas =
              Array.isArray(
                lamina.estruturas
              )
                ? lamina.estruturas
                : []

            const estrutura =
              estruturas[index]

            if (!estrutura) {
              console.warn(
                'Estrutura nao encontrada:',
                index
              )
              return
            }

            let info =
              document.querySelector(
                '#estrutura-info-lista'
              )

            if (!info) {

              const layout =
                document.querySelector(
                  '.viewer-layout'
                )

              if (!layout) {
                console.warn(
                  'viewer-layout nao encontrado.'
                )
                return
              }

              info =
                document.createElement(
                  'div'
                )

              info.id =
                'estrutura-info-lista'

              info.style.cssText = `
                grid-column: 1 / -1;
                margin-top: 18px;
                padding: 22px;
                background: white;
                border: 1px solid #ddd;
                border-radius: 14px;
                box-shadow: 0 4px 15px rgba(0,0,0,.08);
              `

              layout.appendChild(
                info
              )

            }

            const nome =
              estrutura.nome ||
              `Estrutura ${index + 1}`

            const descricao =
              estrutura.descricao ||
              'Nenhuma descrição cadastrada.'

            info.innerHTML = `
              <div style="
                font-size:11px;
                font-weight:700;
                letter-spacing:.08em;
                text-transform:uppercase;
                color:#777;
                margin-bottom:6px;
              ">
                Estrutura selecionada
              </div>

              <h3 style="
                margin:0 0 8px 0;
                font-size:21px;
              ">
                ${escapeHtml(nome)}
              </h3>

              <p style="
                margin:0;
                font-size:15px;
                line-height:1.6;
                color:#555;
              ">
                ${escapeHtml(descricao)}
              </p>
            `

            info.hidden = false

            document
              .querySelectorAll(
                '.structure'
              )
              .forEach(
                item => {

                  item.classList.toggle(
                    'estrutura-selecionada',
                    item === botao
                  )

                }
              )

            console.log(
              'INFO DA ESTRUTURA EXIBIDA:',
              nome
            )

          }
        )

      }
    )

  console.log(
    'Clique da lista instalado:',
    document.querySelectorAll('.structure').length,
    'botoes.'
  )

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}



/* ============================================================
   INFORMACAO SIMPLES DA ESTRUTURA
   ============================================================ */

function mostrarInfoEstruturaSimples(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[Number(index)]

  if (!estrutura) {

    console.warn(
      'Estrutura nao encontrada:',
      index
    )

    return

  }

  let area =
    document.querySelector(
      '#estrutura-info-simples'
    )

  if (!area) {

    area =
      document.createElement('div')

    area.id =
      'estrutura-info-simples'

    area.style.cssText = `
      margin-top: 16px;
      padding: 20px;
      background: #ffffff;
      border: 1px solid #dcdcdc;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      position: relative;
      z-index: 20;
    `

    const titulo =
      document.createElement('h3')

    titulo.id =
      'estrutura-info-simples-nome'

    titulo.style.cssText = `
      margin: 0 35px 8px 0;
      font-size: 20px;
    `

    const descricao =
      document.createElement('p')

    descricao.id =
      'estrutura-info-simples-descricao'

    descricao.style.cssText = `
      margin: 0;
      line-height: 1.6;
      color: #555;
    `

    const fechar =
      document.createElement('button')

    fechar.type =
      'button'

    fechar.textContent =
      '×'

    fechar.title =
      'Fechar'

    fechar.style.cssText = `
      position: absolute;
      top: 10px;
      right: 12px;
      border: none;
      background: transparent;
      font-size: 26px;
      cursor: pointer;
      color: #777;
      line-height: 1;
    `

    fechar.addEventListener(
      'click',
      () => {

        area.hidden = true

      }
    )

    area.appendChild(
      fechar
    )

    area.appendChild(
      titulo
    )

    area.appendChild(
      descricao
    )

    const viewer =
      document.querySelector(
        '#openseadragon'
      )

    if (viewer?.parentElement) {

      viewer.parentElement.appendChild(
        area
      )

    }
    else {

      document.body.appendChild(
        area
      )

    }

  }

  const titulo =
    document.querySelector(
      '#estrutura-info-simples-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-info-simples-descricao'
    )

  if (!titulo || !descricao) {
    return
  }

  titulo.textContent =
    estrutura.nome ||
    `Estrutura ${Number(index) + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  area.hidden = false

  area.dataset.estruturaIndex =
    String(index)

  console.log(
    'INFO DA ESTRUTURA ABERTA:',
    estrutura.nome
  )

}


/* ============================================================
   CONECTAR CLIQUE AOS MARCADORES
   ============================================================ */

function conectarInfoEstruturasSimples(
  lamina
) {

  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )

  if (!overlays.length) {

    console.warn(
      'Nenhum marcador encontrado.'
    )

    return

  }

  overlays.forEach(
    overlay => {

      if (
        overlay.dataset.infoSimples === '1'
      ) {
        return
      }

      overlay.dataset.infoSimples =
        '1'

      overlay.style.cursor =
        'pointer'

      overlay.onclick =
        event => {

          event.preventDefault()
          event.stopPropagation()

          mostrarInfoEstruturaSimples(
            overlay.dataset.estruturaIndex,
            lamina
          )

        }

    }
  )

  console.log(
    'INFO SIMPLES CONECTADA:',
    overlays.length,
    'marcadores.'
  )

}

async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()






























































===== backup-reparador-completo-20260819-212337\src\main.js.combo10-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function abrirPainelEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const indice =
            Number(
              overlay.dataset.estruturaIndex
            )

          const estrutura =
            estruturas[indice]

          if (!estrutura) {
            return
          }

          const panel =
            document.querySelector(
              '#estrutura-estudo-panel'
            )

          const nomeElemento =
            document.querySelector(
              '#estrutura-estudo-nome'
            )

          const descricaoElemento =
            document.querySelector(
              '#estrutura-estudo-descricao'
            )

          if (
            !panel ||
            !nomeElemento ||
            !descricaoElemento
          ) {

            console.warn(
              'Painel de estudo nao encontrado.'
            )

            return
          }

          nomeElemento.textContent =
            estrutura.nome || `Estrutura ${indice + 1}`
          descricaoElemento.textContent =
            estrutura.descricao ||
            'Nenhuma descrição cadastrada.'
          panel.hidden = false

          panel.dataset.estruturaIndex =
            String(indice)

          document
            .querySelectorAll(
              '.estrutura-overlay'
            )
            .forEach(
              item => {

                item.classList.toggle(
                  'estrutura-selecionada',
                  item === overlay
                )

              }
            )

          document
            .querySelectorAll(
              '.structure'
            )
            .forEach(
              (botao, botaoIndex) => {

                botao.classList.toggle(
                  'estrutura-selecionada',
                  botaoIndex === indice
                )

              }
            )

        }
      )
    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'


      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          selecionarEstrutura(
            overlay.dataset.estruturaIndex,
            false
          )

        }
      )

    }
  )

}


function configurarNavegacaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          const index =
            Number(
              overlay.dataset.estruturaIndex
            )


          if (
            Number.isFinite(index)
          ) {

            selecionar(index)

          }

        }
      )

    }
  )


  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()


















































===== backup-reparador-completo-20260819-212337\src\main.js.combo12-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function abrirPainelEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          const index =
            Number(
              overlay.dataset.estruturaIndex
            )


          if (
            Number.isFinite(index)
          ) {

            selecionar(index)

          }

        }
      )

    }
  )


  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()


















































===== backup-reparador-completo-20260819-212337\src\main.js.combo13-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function abrirPainelEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

            if (typeof lamina !== 'undefined') {

              abrirPainelEstrutura(
                index,
                lamina
              )

            }

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          const index =
            Number(
              overlay.dataset.estruturaIndex
            )


          if (
            Number.isFinite(index)
          ) {

            selecionar(index)

          }

        }
      )

    }
  )


  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()


















































===== backup-reparador-completo-20260819-212337\src\main.js.combo14-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function abrirPainelEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

            if (typeof lamina !== 'undefined') {

              abrirPainelEstrutura(
                index,
                lamina
              )

            }

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const index =
            Number(
              overlay.dataset.estruturaIndex
            )

          if (
            !Number.isFinite(index)
          ) {
            return
          }

          if (
            typeof lamina !== 'undefined'
          ) {

            abrirPainelEstrutura(
              index,
              lamina
            )

          }

        }
      )

    }
  )

  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()



















































===== backup-reparador-completo-20260819-212337\src\main.js.combo15-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function abrirPainelEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'



    }
  )

}


function configurarNavegacaoEstruturas(viewer, lamina) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

            if (lamina) {

              abrirPainelEstrutura(
                index,
                lamina
              )

            }

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const index =
            Number(
              overlay.dataset.estruturaIndex
            )

          if (
            !Number.isFinite(index)
          ) {
            return
          }

          if (
            typeof lamina !== 'undefined'
          ) {

            abrirPainelEstrutura(
              index,
              lamina
            )

          }

        }
      )

    }
  )

  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()




















































===== backup-reparador-completo-20260819-212337\src\main.js.combo7-3cliques-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const indice =
            Number(
              overlay.dataset.estruturaIndex
            )

          const estrutura =
            estruturas[indice]

          if (!estrutura) {
            return
          }

          const panel =
            document.querySelector(
              '#estrutura-estudo-panel'
            )

          const nomeElemento =
            document.querySelector(
              '#estrutura-estudo-nome'
            )

          const descricaoElemento =
            document.querySelector(
              '#estrutura-estudo-descricao'
            )

          if (
            !panel ||
            !nomeElemento ||
            !descricaoElemento
          ) {

            console.warn(
              'Painel de estudo nao encontrado.'
            )

            return
          }

          nomeElemento.textContent =
            estrutura.nome ||
            Estrutura 

          descricaoElemento.textContent =
            estrutura.descricao ||
            'Nenhuma descrição cadastrada.'

          panel.hidden = false

          panel.dataset.estruturaIndex =
            String(indice)

          document
            .querySelectorAll(
              '.estrutura-overlay'
            )
            .forEach(
              item => {

                item.classList.toggle(
                  'estrutura-selecionada',
                  item === overlay
                )

              }
            )

          document
            .querySelectorAll(
              '.structure'
            )
            .forEach(
              (botao, botaoIndex) => {

                botao.classList.toggle(
                  'estrutura-selecionada',
                  botaoIndex === indice
                )

              }
            )

        }
      )
    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'


      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          selecionarEstrutura(
            overlay.dataset.estruturaIndex,
            false
          )

        }
      )

    }
  )

}


function configurarNavegacaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          const index =
            Number(
              overlay.dataset.estruturaIndex
            )


          if (
            Number.isFinite(index)
          ) {

            selecionar(index)

          }

        }
      )

    }
  )


  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()














































===== backup-reparador-completo-20260819-212337\src\main.js.combo7-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

      })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'


      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          selecionarEstrutura(
            overlay.dataset.estruturaIndex,
            false
          )

        }
      )

    }
  )

}


function configurarNavegacaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          const index =
            Number(
              overlay.dataset.estruturaIndex
            )


          if (
            Number.isFinite(index)
          ) {

            selecionar(index)

          }

        }
      )

    }
  )


  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }


  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )


  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )


  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )


  const fechar =
    document.querySelector(
      '#estrutura-estudo-fechar'
    )


  const viewerElement =
    document.querySelector(
      '#openseadragon'
    )


  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel do modo estudo nao encontrado.'
    )

    return

  }


  if (
    viewerElement &&
    viewerElement.dataset.modoEstudoConfigurado === 'true'
  ) {

    return

  }


  if (viewerElement) {

    viewerElement.dataset.modoEstudoConfigurado =
      'true'

  }


  let indiceAtual = -1


  function limparSelecao() {

    document
      .querySelectorAll(
        '.estrutura-overlay'
      )
      .forEach(
        overlay => {

          overlay.classList.remove(
            'estrutura-selecionada'
          )

        }
      )


    document
      .querySelectorAll(
        '.structure'
      )
      .forEach(
        botao => {

          botao.classList.remove(
            'estrutura-selecionada'
          )

        }
      )

  }


  function selecionarVisual(index) {

    const indice =
      String(index)


    document
      .querySelectorAll(
        '.estrutura-overlay'
      )
      .forEach(
        overlay => {

          overlay.classList.toggle(
            'estrutura-selecionada',
            overlay.dataset.estruturaIndex === indice
          )

        }
      )


    document
      .querySelectorAll(
        '.structure'
      )
      .forEach(
        (botao, botaoIndex) => {

          botao.classList.toggle(
            'estrutura-selecionada',
            String(botaoIndex) === indice
          )

        }
      )

  }


  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex ===
          String(index)
      )


    if (!overlay) {
      return
    }


    const x =
      Number(
        overlay.dataset.imageX
      )


    const y =
      Number(
        overlay.dataset.imageY
      )


    const largura =
      Number(
        overlay.dataset.imageWidth
      )


    const altura =
      Number(
        overlay.dataset.imageHeight
      )


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {

      return

    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]


    if (!estrutura) {
      return
    }


    indiceAtual =
      Number(index)


    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`


    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'


    panel.hidden = false


    panel.dataset.estruturaIndex =
      String(index)


    selecionarVisual(index)


    if (centralizar) {

      centralizarEstrutura(index)

    }

  }


  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }


  fechar?.addEventListener(
    'click',
    fecharPainel
  )


  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        fecharPainel()

      }

    }
  )


  /*
   * ==========================================================
   * CLIQUE NAS ESTRUTURAS DA LISTA LATERAL
   * ==========================================================
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest(
          '.structure'
        )


      if (!botao) {
        return
      }


      const index =
        Number(
          botao.dataset.estruturaIndex
        )


      if (
        !Number.isFinite(index)
      ) {

        return

      }


      mostrarEstrutura(
        index,
        true
      )

    }
  )


  /*
   * ==========================================================
   * CLIQUE DIRETO NOS MARCADORES DA IMAGEM
   * ==========================================================
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )


        if (!overlay) {
          return
        }


        event.preventDefault()
        event.stopPropagation()


        const index =
          Number(
            overlay.dataset.estruturaIndex
          )


        if (
          !Number.isFinite(index)
        ) {

          return

        }


        mostrarEstrutura(
          index,
          true
        )

      }
    )

  }


  /*
   * ==========================================================
   * TECLAS PARA NAVEGAR ENTRE ESTRUTURAS
   * ==========================================================
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        panel.hidden
      ) {

        return

      }


      if (
        !estruturas.length
      ) {

        return

      }


      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {

        return

      }


      event.preventDefault()


      if (
        indiceAtual < 0
      ) {

        mostrarEstrutura(
          0,
          true
        )

        return

      }


      let proximo =
        indiceAtual


      if (
        event.key === 'ArrowRight'
      ) {

        proximo =
          (
            indiceAtual + 1
          ) %
          estruturas.length

      }
      else {

        proximo =
          (
            indiceAtual - 1 +
            estruturas.length
          ) %
          estruturas.length

      }


      mostrarEstrutura(
        proximo,
        true
      )

    }
  )


  /*
   * ==========================================================
   * BOTAO ANTERIOR
   * ==========================================================
   */

  document
    .querySelector(
      '#estrutura-anterior'
    )
    ?.addEventListener(
      'click',
      () => {

        if (
          !estruturas.length
        ) {

          return

        }


        const proximo =
          indiceAtual <= 0
            ? estruturas.length - 1
            : indiceAtual - 1


        mostrarEstrutura(
          proximo,
          true
        )

      }
    )


  /*
   * ==========================================================
   * BOTAO PROXIMA
   * ==========================================================
   */

  document
    .querySelector(
      '#estrutura-proxima'
    )
    ?.addEventListener(
      'click',
      () => {

        if (
          !estruturas.length
        ) {

          return

        }


        const proximo =
          indiceAtual < 0 ||
          indiceAtual >= estruturas.length - 1
            ? 0
            : indiceAtual + 1


        mostrarEstrutura(
          proximo,
          true
        )

      }
    )


  console.log(
    'Modo estudo configurado:',
    estruturas.length,
    'estruturas'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()












































===== backup-reparador-completo-20260819-212337\src\main.js.combo7-clique-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

      })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'


      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          selecionarEstrutura(
            overlay.dataset.estruturaIndex,
            false
          )

        }
      )

    }
  )

}


function configurarNavegacaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          const index =
            Number(
              overlay.dataset.estruturaIndex
            )


          if (
            Number.isFinite(index)
          ) {

            selecionar(index)

          }

        }
      )

    }
  )


  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()













































===== backup-reparador-completo-20260819-212337\src\main.js.combo7-clique-final-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function abrirPainelEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const indice =
            Number(
              overlay.dataset.estruturaIndex
            )

          const estrutura =
            estruturas[indice]

          if (!estrutura) {
            return
          }

          const panel =
            document.querySelector(
              '#estrutura-estudo-panel'
            )

          const nomeElemento =
            document.querySelector(
              '#estrutura-estudo-nome'
            )

          const descricaoElemento =
            document.querySelector(
              '#estrutura-estudo-descricao'
            )

          if (
            !panel ||
            !nomeElemento ||
            !descricaoElemento
          ) {

            console.warn(
              'Painel de estudo nao encontrado.'
            )

            return
          }

          nomeElemento.textContent =
            estrutura.nome ||
            Estrutura 

          descricaoElemento.textContent =
            estrutura.descricao ||
            'Nenhuma descrição cadastrada.'

          panel.hidden = false

          panel.dataset.estruturaIndex =
            String(indice)

          document
            .querySelectorAll(
              '.estrutura-overlay'
            )
            .forEach(
              item => {

                item.classList.toggle(
                  'estrutura-selecionada',
                  item === overlay
                )

              }
            )

          document
            .querySelectorAll(
              '.structure'
            )
            .forEach(
              (botao, botaoIndex) => {

                botao.classList.toggle(
                  'estrutura-selecionada',
                  botaoIndex === indice
                )

              }
            )

        }
      )
    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'


      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          selecionarEstrutura(
            overlay.dataset.estruturaIndex,
            false
          )

        }
      )

    }
  )

}


function configurarNavegacaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          const index =
            Number(
              overlay.dataset.estruturaIndex
            )


          if (
            Number.isFinite(index)
          ) {

            selecionar(index)

          }

        }
      )

    }
  )


  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()















































===== backup-reparador-completo-20260819-212337\src\main.js.combo7-clique2-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

      })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'


      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          selecionarEstrutura(
            overlay.dataset.estruturaIndex,
            false
          )

        }
      )

    }
  )

}


function configurarNavegacaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          const index =
            Number(
              overlay.dataset.estruturaIndex
            )


          if (
            Number.isFinite(index)
          ) {

            selecionar(index)

          }

        }
      )

    }
  )


  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()













































===== backup-reparador-completo-20260819-212337\src\main.js.combo7-correcao-nome-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function abrirPainelEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const indice =
            Number(
              overlay.dataset.estruturaIndex
            )

          const estrutura =
            estruturas[indice]

          if (!estrutura) {
            return
          }

          const panel =
            document.querySelector(
              '#estrutura-estudo-panel'
            )

          const nomeElemento =
            document.querySelector(
              '#estrutura-estudo-nome'
            )

          const descricaoElemento =
            document.querySelector(
              '#estrutura-estudo-descricao'
            )

          if (
            !panel ||
            !nomeElemento ||
            !descricaoElemento
          ) {

            console.warn(
              'Painel de estudo nao encontrado.'
            )

            return
          }

          nomeElemento.textContent =
            estrutura.nome ||
            Estrutura 

          descricaoElemento.textContent =
            estrutura.descricao ||
            'Nenhuma descrição cadastrada.'

          panel.hidden = false

          panel.dataset.estruturaIndex =
            String(indice)

          document
            .querySelectorAll(
              '.estrutura-overlay'
            )
            .forEach(
              item => {

                item.classList.toggle(
                  'estrutura-selecionada',
                  item === overlay
                )

              }
            )

          document
            .querySelectorAll(
              '.structure'
            )
            .forEach(
              (botao, botaoIndex) => {

                botao.classList.toggle(
                  'estrutura-selecionada',
                  botaoIndex === indice
                )

              }
            )

        }
      )
    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'


      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          selecionarEstrutura(
            overlay.dataset.estruturaIndex,
            false
          )

        }
      )

    }
  )

}


function configurarNavegacaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          const index =
            Number(
              overlay.dataset.estruturaIndex
            )


          if (
            Number.isFinite(index)
          ) {

            selecionar(index)

          }

        }
      )

    }
  )


  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()















































===== backup-reparador-completo-20260819-212337\src\main.js.combo7-final-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const indice =
            Number(
              overlay.dataset.estruturaIndex
            )

          const estrutura =
            estruturas[indice]

          if (!estrutura) {
            return
          }

          const panel =
            document.querySelector(
              '#estrutura-estudo-panel'
            )

          const nomeElemento =
            document.querySelector(
              '#estrutura-estudo-nome'
            )

          const descricaoElemento =
            document.querySelector(
              '#estrutura-estudo-descricao'
            )

          if (
            !panel ||
            !nomeElemento ||
            !descricaoElemento
          ) {

            console.warn(
              'Painel de estudo nao encontrado.'
            )

            return
          }

          nomeElemento.textContent =
            estrutura.nome ||
            Estrutura 

          descricaoElemento.textContent =
            estrutura.descricao ||
            'Nenhuma descrição cadastrada.'

          panel.hidden = false

          panel.dataset.estruturaIndex =
            String(indice)

          document
            .querySelectorAll(
              '.estrutura-overlay'
            )
            .forEach(
              item => {

                item.classList.toggle(
                  'estrutura-selecionada',
                  item === overlay
                )

              }
            )

          document
            .querySelectorAll(
              '.structure'
            )
            .forEach(
              (botao, botaoIndex) => {

                botao.classList.toggle(
                  'estrutura-selecionada',
                  botaoIndex === indice
                )

              }
            )

        }
      )
    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'


      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          selecionarEstrutura(
            overlay.dataset.estruturaIndex,
            false
          )

        }
      )

    }
  )

}


function configurarNavegacaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          const index =
            Number(
              overlay.dataset.estruturaIndex
            )


          if (
            Number.isFinite(index)
          ) {

            selecionar(index)

          }

        }
      )

    }
  )


  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()














































===== backup-reparador-completo-20260819-212337\src\main.js.combo7-limpeza-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const indice =
            Number(
              overlay.dataset.estruturaIndex
            )

          const estrutura =
            estruturas[indice]

          if (!estrutura) {
            return
          }

          const panel =
            document.querySelector(
              '#estrutura-estudo-panel'
            )

          const nomeElemento =
            document.querySelector(
              '#estrutura-estudo-nome'
            )

          const descricaoElemento =
            document.querySelector(
              '#estrutura-estudo-descricao'
            )

          if (
            !panel ||
            !nomeElemento ||
            !descricaoElemento
          ) {

            console.warn(
              'Painel de estudo nao encontrado.'
            )

            return
          }

          nomeElemento.textContent =
            estrutura.nome ||
            Estrutura 

          descricaoElemento.textContent =
            estrutura.descricao ||
            'Nenhuma descrição cadastrada.'

          panel.hidden = false

          panel.dataset.estruturaIndex =
            String(indice)

          document
            .querySelectorAll(
              '.estrutura-overlay'
            )
            .forEach(
              item => {

                item.classList.toggle(
                  'estrutura-selecionada',
                  item === overlay
                )

              }
            )

          document
            .querySelectorAll(
              '.structure'
            )
            .forEach(
              (botao, botaoIndex) => {

                botao.classList.toggle(
                  'estrutura-selecionada',
                  botaoIndex === indice
                )

              }
            )

        }
      )
    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'


      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          selecionarEstrutura(
            overlay.dataset.estruturaIndex,
            false
          )

        }
      )

    }
  )

}


function configurarNavegacaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          const index =
            Number(
              overlay.dataset.estruturaIndex
            )


          if (
            Number.isFinite(index)
          ) {

            selecionar(index)

          }

        }
      )

    }
  )


  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()














































===== backup-reparador-completo-20260819-212337\src\main.js.combo7-listener-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function abrirPainelEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const indice =
            Number(
              overlay.dataset.estruturaIndex
            )

          const estrutura =
            estruturas[indice]

          if (!estrutura) {
            return
          }

          const panel =
            document.querySelector(
              '#estrutura-estudo-panel'
            )

          const nomeElemento =
            document.querySelector(
              '#estrutura-estudo-nome'
            )

          const descricaoElemento =
            document.querySelector(
              '#estrutura-estudo-descricao'
            )

          if (
            !panel ||
            !nomeElemento ||
            !descricaoElemento
          ) {

            console.warn(
              'Painel de estudo nao encontrado.'
            )

            return
          }

          nomeElemento.textContent =
            estrutura.nome ||
            Estrutura 

          descricaoElemento.textContent =
            estrutura.descricao ||
            'Nenhuma descrição cadastrada.'

          panel.hidden = false

          panel.dataset.estruturaIndex =
            String(indice)

          document
            .querySelectorAll(
              '.estrutura-overlay'
            )
            .forEach(
              item => {

                item.classList.toggle(
                  'estrutura-selecionada',
                  item === overlay
                )

              }
            )

          document
            .querySelectorAll(
              '.structure'
            )
            .forEach(
              (botao, botaoIndex) => {

                botao.classList.toggle(
                  'estrutura-selecionada',
                  botaoIndex === indice
                )

              }
            )

        }
      )
    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'


      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          selecionarEstrutura(
            overlay.dataset.estruturaIndex,
            false
          )

        }
      )

    }
  )

}


function configurarNavegacaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          const index =
            Number(
              overlay.dataset.estruturaIndex
            )


          if (
            Number.isFinite(index)
          ) {

            selecionar(index)

          }

        }
      )

    }
  )


  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()















































===== backup-reparador-completo-20260819-212337\src\main.js.combo7-seguranca-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function abrirPainelEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const indice =
            Number(
              overlay.dataset.estruturaIndex
            )

          const estrutura =
            estruturas[indice]

          if (!estrutura) {
            return
          }

          const panel =
            document.querySelector(
              '#estrutura-estudo-panel'
            )

          const nomeElemento =
            document.querySelector(
              '#estrutura-estudo-nome'
            )

          const descricaoElemento =
            document.querySelector(
              '#estrutura-estudo-descricao'
            )

          if (
            !panel ||
            !nomeElemento ||
            !descricaoElemento
          ) {

            console.warn(
              'Painel de estudo nao encontrado.'
            )

            return
          }

          nomeElemento.textContent =
            estrutura.nome ||
            Estrutura 

          descricaoElemento.textContent =
            estrutura.descricao ||
            'Nenhuma descrição cadastrada.'

          panel.hidden = false

          panel.dataset.estruturaIndex =
            String(indice)

          document
            .querySelectorAll(
              '.estrutura-overlay'
            )
            .forEach(
              item => {

                item.classList.toggle(
                  'estrutura-selecionada',
                  item === overlay
                )

              }
            )

          document
            .querySelectorAll(
              '.structure'
            )
            .forEach(
              (botao, botaoIndex) => {

                botao.classList.toggle(
                  'estrutura-selecionada',
                  botaoIndex === indice
                )

              }
            )

        }
      )
    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'


      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          selecionarEstrutura(
            overlay.dataset.estruturaIndex,
            false
          )

        }
      )

    }
  )

}


function configurarNavegacaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          const index =
            Number(
              overlay.dataset.estruturaIndex
            )


          if (
            Number.isFinite(index)
          ) {

            selecionar(index)

          }

        }
      )

    }
  )


  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()















































===== backup-reparador-completo-20260819-212337\src\main.js.combo8-1-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function abrirPainelEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const indice =
            Number(
              overlay.dataset.estruturaIndex
            )

          const estrutura =
            estruturas[indice]

          if (!estrutura) {
            return
          }

          const panel =
            document.querySelector(
              '#estrutura-estudo-panel'
            )

          const nomeElemento =
            document.querySelector(
              '#estrutura-estudo-nome'
            )

          const descricaoElemento =
            document.querySelector(
              '#estrutura-estudo-descricao'
            )

          if (
            !panel ||
            !nomeElemento ||
            !descricaoElemento
          ) {

            console.warn(
              'Painel de estudo nao encontrado.'
            )

            return
          }

          nomeElemento.textContent =
            estrutura.nome || `Estrutura ${indice + 1}`descricaoElemento.textContent =
            estrutura.descricao ||
            'Nenhuma descrição cadastrada.'

          panel.hidden = false

          panel.dataset.estruturaIndex =
            String(indice)

          document
            .querySelectorAll(
              '.estrutura-overlay'
            )
            .forEach(
              item => {

                item.classList.toggle(
                  'estrutura-selecionada',
                  item === overlay
                )

              }
            )

          document
            .querySelectorAll(
              '.structure'
            )
            .forEach(
              (botao, botaoIndex) => {

                botao.classList.toggle(
                  'estrutura-selecionada',
                  botaoIndex === indice
                )

              }
            )

        }
      )
    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'


      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          selecionarEstrutura(
            overlay.dataset.estruturaIndex,
            false
          )

        }
      )

    }
  )

}


function configurarNavegacaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          const index =
            Number(
              overlay.dataset.estruturaIndex
            )


          if (
            Number.isFinite(index)
          ) {

            selecionar(index)

          }

        }
      )

    }
  )


  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()

















































===== backup-reparador-completo-20260819-212337\src\main.js.combo8-2-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function abrirPainelEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const indice =
            Number(
              overlay.dataset.estruturaIndex
            )

          const estrutura =
            estruturas[indice]

          if (!estrutura) {
            return
          }

          const panel =
            document.querySelector(
              '#estrutura-estudo-panel'
            )

          const nomeElemento =
            document.querySelector(
              '#estrutura-estudo-nome'
            )

          const descricaoElemento =
            document.querySelector(
              '#estrutura-estudo-descricao'
            )

          if (
            !panel ||
            !nomeElemento ||
            !descricaoElemento
          ) {

            console.warn(
              'Painel de estudo nao encontrado.'
            )

            return
          }

          nomeElemento.textContent =
            estrutura.nome || `Estrutura ${indice + 1}`
          descricaoElemento.textContent =
            'Nenhuma descrição cadastrada.'

          panel.hidden = false

          panel.dataset.estruturaIndex =
            String(indice)

          document
            .querySelectorAll(
              '.estrutura-overlay'
            )
            .forEach(
              item => {

                item.classList.toggle(
                  'estrutura-selecionada',
                  item === overlay
                )

              }
            )

          document
            .querySelectorAll(
              '.structure'
            )
            .forEach(
              (botao, botaoIndex) => {

                botao.classList.toggle(
                  'estrutura-selecionada',
                  botaoIndex === indice
                )

              }
            )

        }
      )
    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'


      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          selecionarEstrutura(
            overlay.dataset.estruturaIndex,
            false
          )

        }
      )

    }
  )

}


function configurarNavegacaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          const index =
            Number(
              overlay.dataset.estruturaIndex
            )


          if (
            Number.isFinite(index)
          ) {

            selecionar(index)

          }

        }
      )

    }
  )


  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()

















































===== backup-reparador-completo-20260819-212337\src\main.js.combo8-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function abrirPainelEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const indice =
            Number(
              overlay.dataset.estruturaIndex
            )

          const estrutura =
            estruturas[indice]

          if (!estrutura) {
            return
          }

          const panel =
            document.querySelector(
              '#estrutura-estudo-panel'
            )

          const nomeElemento =
            document.querySelector(
              '#estrutura-estudo-nome'
            )

          const descricaoElemento =
            document.querySelector(
              '#estrutura-estudo-descricao'
            )

          if (
            !panel ||
            !nomeElemento ||
            !descricaoElemento
          ) {

            console.warn(
              'Painel de estudo nao encontrado.'
            )

            return
          }

          nomeElemento.textContent =
            estrutura.nome || `Estrutura ${indice + 1}`descricaoElemento.textContent =
            estrutura.descricao ||
            'Nenhuma descrição cadastrada.'

          panel.hidden = false

          panel.dataset.estruturaIndex =
            String(indice)

          document
            .querySelectorAll(
              '.estrutura-overlay'
            )
            .forEach(
              item => {

                item.classList.toggle(
                  'estrutura-selecionada',
                  item === overlay
                )

              }
            )

          document
            .querySelectorAll(
              '.structure'
            )
            .forEach(
              (botao, botaoIndex) => {

                botao.classList.toggle(
                  'estrutura-selecionada',
                  botaoIndex === indice
                )

              }
            )

        }
      )
    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'


      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          selecionarEstrutura(
            overlay.dataset.estruturaIndex,
            false
          )

        }
      )

    }
  )

}


function configurarNavegacaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          const index =
            Number(
              overlay.dataset.estruturaIndex
            )


          if (
            Number.isFinite(index)
          ) {

            selecionar(index)

          }

        }
      )

    }
  )


  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()
















































===== backup-reparador-completo-20260819-212337\src\main.js.combo9-backup.js =====
import './style.css'
import OpenSeadragon from 'openseadragon'
import { supabase } from './lib/supabase.js'

import { renderLogin, setupLogin } from './pages/login.js'
import { renderAdmin, setupAdmin } from './pages/admin.js'
import { renderCatalogo } from './pages/catalogo.js'
import { renderNovaLamina, setupNovaLamina } from './pages/nova-lamina.js'

const app = document.querySelector('#app')

function aplicarConfiguracoesVisuais(config = {}) {

  const root = document.documentElement

  if (config.cor_principal) {
    root.style.setProperty('--green', config.cor_principal)
    root.style.setProperty('--green-light', config.cor_principal)
    root.style.setProperty('--cor-principal', config.cor_principal)
  }

  if (config.cor_fundo) {
    root.style.setProperty('--cream', config.cor_fundo)
    root.style.setProperty('--cor-fundo', config.cor_fundo)
  }

  if (config.cor_texto) {
    root.style.setProperty('--text', config.cor_texto)
    root.style.setProperty('--cor-texto', config.cor_texto)
  }

  if (config.cor_destaque) {
    root.style.setProperty('--green-light', config.cor_destaque)
    root.style.setProperty('--cor-destaque', config.cor_destaque)
  }

}

async function carregarConfiguracoesSite() {

  const { data, error } =
    await supabase
      .from('configuracoes_site')
      .select('*')
      .limit(1)
      .maybeSingle()

  if (error) {
    console.error('Erro ao carregar configurações do site:', error)
    return {}
  }

  return data || {}
}

function getRoute() {
  const hash = window.location.hash

  if (hash === '#login') return 'login'
  if (hash === '#admin') return 'admin'
  if (hash.startsWith('#nova-lamina')) return 'nova-lamina'
  if (hash === '#laminas') return 'catalogo'
  if (hash.startsWith('#lamina/')) return 'lamina'

  return 'home'
}

function renderHeader(config = {}) {
  return `
    <header class="header">
      <div class="container header-content">

        <a href="#inicio" class="brand">
          <span
  class="brand-icon"
  style="
    width: ${Number(config.logo_tamanho || 100) * 0.42}px;
    height: ${Number(config.logo_tamanho || 100) * 0.42}px;
  "
>
  ${
    config.logo_url
      ? `<img src="${escapeHtml(config.logo_url)}" alt="Logo" style="width:100%;height:100%;object-fit:contain;">`
      : '🔬'
  }
</span>

          <span>
            <strong>${escapeHtml(config.nome_site || "Atlas")}</strong>
            <small>${escapeHtml(config.subtitulo || "Histológico")}</small>
          </span>
        </a>

        <nav class="nav">
          <a href="#inicio">Início</a>
          <a href="#laminas">Lâminas</a>
          <a href="#sobre">Sobre</a>
          <a href="#login">Administração</a>
        </nav>

      </div>
    </header>
  `
}

function renderHome(config = {}) {
  return `
    ${renderHeader(config)}

    <main>

      <section class="hero" id="inicio">

        <div class="container hero-grid">

          <div class="hero-text">

            <span class="eyebrow">
              🔬 MICROSCOPIA • ESTUDO • EXPLORAÇÃO
            </span>

            <h1>
              ${escapeHtml(config.titulo_inicio || "Explore o mundo microscópico.")}
            </h1>

            <p>
              ${escapeHtml(config.descricao_inicio || "Um atlas de histologia interativo para explorar tecidos, estruturas e lâminas histológicas de uma forma visual e dinâmica.")}
            </p>

            <div class="hero-actions">

              <a href="#laminas" class="button primary">
                ${escapeHtml(config.texto_botao_principal || "Explorar " + (config.nome_site || "Atlas"))}
                <span>→</span>
              </a>

              <a href="#sobre" class="button secondary">
                ${escapeHtml(config.texto_botao_secundario || "Conheça o projeto")}
              </a>

            </div>

          </div>

          <div class="hero-visual">

            <div class="microscope-card">

              <div class="microscope-glow"></div>

              <div class="microscope">

                <div class="microscope-top"></div>
                <div class="microscope-body"></div>
                <div class="microscope-base"></div>
                <div class="microscope-lens"></div>

              </div>

              <div class="scan-line"></div>

            </div>

          </div>

        </div>

      </section>


      <section class="section" id="sobre">

        <div class="container about-grid">

          <div>

            <span class="eyebrow">
              SOBRE O ATLAS
            </span>

            <h2>
              Aprender histologia
              de uma forma diferente.
            </h2>

          </div>

          <div class="about-text">

            <p>
              ${escapeHtml(config.texto_sobre || ("O " + (config.nome_site || "Atlas") + " " + (config.subtitulo || "Histológico") + " foi pensado para transformar a observação das lâminas em uma experiência de aprendizagem visual e interativa."))}
            </p>

            <a href="#laminas" class="button primary">
              Explorar lâminas →
            </a>

          </div>

        </div>

      </section>

    </main>


    <footer class="footer">

      <div class="container">

        <span>🔬 ${escapeHtml(config.nome_site || "Atlas")} ${escapeHtml(config.subtitulo || "Histológico")}</span>
        <span>${escapeHtml(config.texto_rodape || "Projeto acadêmico")}</span>

      </div>

    </footer>
  `
}


function escapeHtml(value = '') {

  return String(value)
    .replaceAll('&', '&amp;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;')
    .replaceAll('"', '&quot;')
    .replaceAll("'", '&#039;')

}


function renderLamina(lamina, config) {

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []


  return `
    ${renderHeader(config)}

    <main>

      <section class="slide-header">

        <div class="container">

          <a href="#laminas" class="back-link">
            ← Voltar para as lâminas
          </a>

          <div class="slide-title">

            <div>

              <span class="eyebrow">
                ${escapeHtml(lamina.categoria || 'Histologia')}
              </span>

              <h1>
                ${escapeHtml(lamina.nome || 'Lâmina')}
              </h1>

              <p>
                Explore a lâmina utilizando zoom e movimentação.
              </p>

            </div>

            <div class="slide-meta">

              ${lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''}

              ${lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''}

            </div>

          </div>

        </div>

      </section>


      <section class="viewer-section">

        <div class="container viewer-layout">

          <div class="viewer-container">

            <div
              id="estrutura-estudo-panel"
              class="estrutura-estudo-panel"
              hidden
            >

              <div class="estrutura-estudo-header">

                <span class="estrutura-estudo-eyebrow">
                  ESTRUTURA SELECIONADA
                </span>

                <button
                  id="estrutura-estudo-fechar"
                  type="button"
                  title="Fechar"
                  aria-label="Fechar modo estudo"
                >
                  ×
                </button>

              </div>

              <h3 id="estrutura-estudo-nome">
                Estrutura
              </h3>

              <p id="estrutura-estudo-descricao"></p>

            </div>

            <div
              id="openseadragon"
              class="viewer"
            ></div>

            <div class="estrutura-navegacao">

              <button
                id="estrutura-anterior"
                type="button"
                class="estrutura-nav-button"
                title="Estrutura anterior"
                aria-label="Estrutura anterior"
              >
                ←
              </button>

              <span
                id="estrutura-contador"
                class="estrutura-contador"
              >
                Estrutura 0 de 0
              </span>

              <button
                id="estrutura-proxima"
                type="button"
                class="estrutura-nav-button"
                title="Próxima estrutura"
                aria-label="Próxima estrutura"
              >
                →
              </button>

            </div>


            <div class="viewer-controls">

              <span>
                Arraste para movimentar •
                Use a roda do mouse para ampliar
              </span>

              <div class="zoom-buttons">

                <button
                  id="zoom-out"
                  type="button"
                  title="Diminuir zoom"
                  aria-label="Diminuir zoom"
                >−</button>

                <button
                  id="zoom-home"
                  type="button"
                  title="Restaurar"
                  aria-label="Restaurar zoom"
                >⌂</button>

                <button
                  id="zoom-in"
                  type="button"
                  title="Aumentar zoom"
                  aria-label="Aumentar zoom"
                >+</button>

                <button
                  id="zoom-reset"
                  type="button"
                  title="Restaurar visualização"
                  aria-label="Restaurar visualização"
                >↺</button>

                <button
                  id="zoom-fullscreen"
                  type="button"
                  title="Tela cheia"
                  aria-label="Abrir em tela cheia"
                >⛶</button>

                <span
                  id="zoom-value"
                  class="zoom-value"
                >100%</span>

              </div>

            </div>

          </div>


          <aside class="info-panel">

            <div class="panel-label">
              SOBRE A LÂMINA
            </div>

            <h2>
              ${escapeHtml(lamina.nome || 'Lâmina')}
            </h2>

            <p class="description">
              ${escapeHtml(lamina.descricao || '')}
            </p>


            ${estruturas.length
              ? `
                <div class="info-section">

                  <h3>
                    🔎 O que observar
                  </h3>

                  <div class="structure-list">

                    ${estruturas.map(estrutura => `
                      <button class="structure">

                        <span class="structure-dot"></span>

                        <span>

                          <strong>
                            ${escapeHtml(estrutura.nome || '')}
                          </strong>

                          <small>
                            ${escapeHtml(estrutura.descricao || '')}
                          </small>

                        </span>

                      </button>
                    `).join('')}

                  </div>

                </div>
              `
              : ''}

          </aside>

        </div>

      </section>

    </main>
  `
}


async function buscarLamina(id) {

  const { data, error } =
    await supabase
      .from('laminas')
      .select('*')
      .eq('id', id)
      .eq('publicado', true)
      .single()


  if (error) {

    console.error(
      'Erro ao buscar lâmina:',
      error
    )

    return null
  }


  return data
}





function abrirPainelEstrutura(index, lamina) {

  const estruturas =
    Array.isArray(lamina?.estruturas)
      ? lamina.estruturas
      : []

  const estrutura =
    estruturas[index]

  if (!estrutura) {
    console.warn(
      'Estrutura nao encontrada:',
      index
    )
    return
  }

  const panel =
    document.querySelector(
      '#estrutura-estudo-panel'
    )

  const nome =
    document.querySelector(
      '#estrutura-estudo-nome'
    )

  const descricao =
    document.querySelector(
      '#estrutura-estudo-descricao'
    )

  if (
    !panel ||
    !nome ||
    !descricao
  ) {

    console.warn(
      'Painel de estudo nao encontrado.'
    )

    return
  }

  nome.textContent =
    estrutura.nome ||
    `Estrutura ${index + 1}`

  descricao.textContent =
    estrutura.descricao ||
    'Nenhuma descrição cadastrada.'

  panel.hidden = false

  panel.dataset.estruturaIndex =
    String(index)

  document
    .querySelectorAll(
      '.estrutura-overlay'
    )
    .forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex ===
          String(index)
        )

      }
    )

  document
    .querySelectorAll(
      '.structure'
    )
    .forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          botaoIndex === index
        )

      }
    )

  console.log(
    'Painel aberto:',
    estrutura.nome
  )

}
function desenharEstruturas(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  if (!estruturas.length) {

    console.log(
      'Nenhuma estrutura cadastrada nesta lâmina.'
    )

    return
  }


  console.log(
    'Estruturas carregadas:',
    estruturas
  )


  viewer.clearOverlays()


  const tiledImage =
    viewer.world.getItemAt(0)


  if (!tiledImage) {

    console.warn(
      'Imagem ainda não disponível para desenhar estruturas.'
    )

    return
  }


  const imageSize =
    tiledImage.getContentSize()


  const imageWidth =
    Number(imageSize.x)


  const imageHeight =
    Number(imageSize.y)


  if (
    !imageWidth ||
    !imageHeight
  ) {

    console.warn(
      'Não foi possível determinar o tamanho da imagem.'
    )

    return
  }


  estruturas.forEach(
    (estrutura, index) => {

      const nome =
        String(
          estrutura.nome ||
          `Estrutura ${index + 1}`
        )


      const descricao =
        String(
          estrutura.descricao || ''
        )


      let x =
        Number(estrutura.x)


      let y =
        Number(estrutura.y)


      let largura =
        Number(
          estrutura.largura ??
          estrutura.width ??
          0.08
        )


      let altura =
        Number(
          estrutura.altura ??
          estrutura.height ??
          0.08
        )


      if (
        !Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(largura) ||
        !Number.isFinite(altura)
      ) {

        console.warn(
          'Estrutura ignorada por valores inválidos:',
          estrutura
        )

        return
      }


      /*
       * As posições cadastradas no editor são
       * normalizadas de 0 a 1.
       *
       * Convertemos para pixels da imagem.
       */

      x =
        Math.max(
          0,
          Math.min(1, x)
        ) * imageWidth


      y =
        Math.max(
          0,
          Math.min(1, y)
        ) * imageHeight


      largura =
        Math.max(
          0.001,
          Math.min(1, largura)
        ) * imageWidth


      altura =
        Math.max(
          0.001,
          Math.min(1, altura)
        ) * imageHeight


      /*
       * Converte coordenadas da imagem
       * para coordenadas do viewport.
       */

      const rect =
        viewer.viewport.imageToViewportRectangle(
          x,
          y,
          largura,
          altura
        )


      const overlay =
        document.createElement('div')


      overlay.className =
        'estrutura-overlay'


      overlay.dataset.estruturaIndex =
        String(index)




      overlay.dataset.imageX =
        String(x)

      overlay.dataset.imageY =
        String(y)

      overlay.dataset.imageWidth =
        String(largura)

      overlay.dataset.imageHeight =
        String(altura)
      overlay.innerHTML = `

        <div class="estrutura-box">

          <span class="estrutura-ponto"></span>

          <span class="estrutura-label">
            ${escapeHtml(nome)}
          </span>

        </div>

      `


      if (descricao) {

        overlay.title =
          `${nome}: ${descricao}`

      } else {

        overlay.title =
          nome

      }


      viewer.addOverlay({

        element:
          overlay,

        location:
          rect,

        placement:
          OpenSeadragon.Placement.TOP_LEFT

  
      /*
       * COMBO 7 - CLIQUE DIRETO
       */

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          const indice =
            Number(
              overlay.dataset.estruturaIndex
            )

          const estrutura =
            estruturas[indice]

          if (!estrutura) {
            return
          }

          const panel =
            document.querySelector(
              '#estrutura-estudo-panel'
            )

          const nomeElemento =
            document.querySelector(
              '#estrutura-estudo-nome'
            )

          const descricaoElemento =
            document.querySelector(
              '#estrutura-estudo-descricao'
            )

          if (
            !panel ||
            !nomeElemento ||
            !descricaoElemento
          ) {

            console.warn(
              'Painel de estudo nao encontrado.'
            )

            return
          }

          nomeElemento.textContent =
            estrutura.nome || `Estrutura ${indice + 1}`
          descricaoElemento.textContent =
            estrutura.descricao ||
            'Nenhuma descrição cadastrada.'
          panel.hidden = false

          panel.dataset.estruturaIndex =
            String(indice)

          document
            .querySelectorAll(
              '.estrutura-overlay'
            )
            .forEach(
              item => {

                item.classList.toggle(
                  'estrutura-selecionada',
                  item === overlay
                )

              }
            )

          document
            .querySelectorAll(
              '.structure'
            )
            .forEach(
              (botao, botaoIndex) => {

                botao.classList.toggle(
                  'estrutura-selecionada',
                  botaoIndex === indice
                )

              }
            )

        }
      )
    })

    }
  )

}


function configurarInteracaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  function selecionarEstrutura(
    index,
    centralizar = false
  ) {

    const indice =
      String(index)


    botoes.forEach(
      (botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      }
    )


    if (!centralizar) {
      return
    }


    const overlay =
      Array.from(overlays).find(
        item =>
          item.dataset.estruturaIndex === indice
      )


    if (!overlay) {
      return
    }


    const x =
      Number(overlay.dataset.imageX)


    const y =
      Number(overlay.dataset.imageY)


    const largura =
      Number(overlay.dataset.imageWidth)


    const altura =
      Number(overlay.dataset.imageHeight)


    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }


    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )


    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )


    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )


    viewer.viewport.panTo(
      ponto,
      true
    )


    const zoomAtual =
      viewer.viewport.getZoom()


    const zoomMaximo =
      viewer.viewport.getMaxZoom()


    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )


    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionarEstrutura(
            index,
            true
          )

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.style.cursor =
        'pointer'


      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()

          selecionarEstrutura(
            overlay.dataset.estruturaIndex,
            false
          )

        }
      )

    }
  )

}


function configurarNavegacaoEstruturas(viewer) {

  if (!viewer) {
    return
  }


  const botoes =
    document.querySelectorAll(
      '.structure'
    )


  const overlays =
    document.querySelectorAll(
      '.estrutura-overlay'
    )


  const contador =
    document.querySelector(
      '#estrutura-contador'
    )


  const anterior =
    document.querySelector(
      '#estrutura-anterior'
    )


  const proxima =
    document.querySelector(
      '#estrutura-proxima'
    )


  if (!botoes.length) {

    if (contador) {
      contador.textContent =
        'Nenhuma estrutura'
    }

    return
  }


  let indiceAtual = 0


  function atualizarInterface() {

    const total =
      botoes.length


    if (contador) {

      contador.textContent =
        `Estrutura ${indiceAtual + 1} de ${total}`

    }


    if (anterior) {

      anterior.disabled =
        indiceAtual <= 0

    }


    if (proxima) {

      proxima.disabled =
        indiceAtual >= total - 1

    }

  }


  function selecionar(index) {

    if (
      index < 0 ||
      index >= botoes.length
    ) {
      return
    }


    indiceAtual = index


    botoes.forEach(
      (botao, i) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          i === indiceAtual
        )

      }
    )


    overlays.forEach(
      overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          Number(
            overlay.dataset.estruturaIndex
          ) === indiceAtual
        )

      }
    )


    const overlay =
      Array.from(overlays).find(
        item =>
          Number(
            item.dataset.estruturaIndex
          ) === indiceAtual
      )


    if (overlay) {

      const x =
        Number(
          overlay.dataset.imageX
        )


      const y =
        Number(
          overlay.dataset.imageY
        )


      const largura =
        Number(
          overlay.dataset.imageWidth
        )


      const altura =
        Number(
          overlay.dataset.imageHeight
        )


      if (
        Number.isFinite(x) &&
        Number.isFinite(y)
      ) {

        const centroX =
          x +
          (
            Number.isFinite(largura)
              ? largura / 2
              : 0
          )


        const centroY =
          y +
          (
            Number.isFinite(altura)
              ? altura / 2
              : 0
          )


        const ponto =
          viewer.viewport.imageToViewportCoordinates(
            centroX,
            centroY
          )


        viewer.viewport.panTo(
          ponto,
          true
        )


        const zoomAtual =
          viewer.viewport.getZoom()


        const zoomMaximo =
          viewer.viewport.getMaxZoom()


        const zoomDesejado =
          Math.min(
            Math.max(
              zoomAtual * 1.5,
              viewer.viewport.getHomeZoom() * 1.8
            ),
            zoomMaximo
          )


        viewer.viewport.zoomTo(
          zoomDesejado,
          ponto,
          true
        )

      }

    }


    atualizarInterface()

  }


  botoes.forEach(
    (botao, index) => {

      botao.dataset.estruturaIndex =
        String(index)


      botao.addEventListener(
        'click',
        () => {

          selecionar(index)

        }
      )

    }
  )


  overlays.forEach(
    overlay => {

      overlay.addEventListener(
        'click',
        event => {

          event.preventDefault()
          event.stopPropagation()


          const index =
            Number(
              overlay.dataset.estruturaIndex
            )


          if (
            Number.isFinite(index)
          ) {

            selecionar(index)

          }

        }
      )

    }
  )


  anterior?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual - 1
      )

    }
  )


  proxima?.addEventListener(
    'click',
    () => {

      selecionar(
        indiceAtual + 1
      )

    }
  )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName


      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === 'ArrowLeft'
      ) {

        selecionar(
          indiceAtual - 1
        )

      }


      if (
        event.key === 'ArrowRight'
      ) {

        selecionar(
          indiceAtual + 1
        )

      }

    }
  )


  atualizarInterface()

}


function configurarModoEstudo(viewer, lamina) {

  if (!viewer || !lamina) {
    return
  }

  const estruturas =
    Array.isArray(lamina.estruturas)
      ? lamina.estruturas
      : []

  const panel =
    document.querySelector('#estrutura-estudo-panel')

  const nome =
    document.querySelector('#estrutura-estudo-nome')

  const descricao =
    document.querySelector('#estrutura-estudo-descricao')

  const fechar =
    document.querySelector('#estrutura-estudo-fechar')

  const viewerElement =
    document.querySelector('#openseadragon')

  if (!panel || !nome || !descricao) {
    console.warn(
      'COMBO 7: painel de estudo nao encontrado.'
    )
    return
  }

  let indiceAtual = -1

  function limparSelecao() {

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.remove(
          'estrutura-selecionada'
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach(botao => {

        botao.classList.remove(
          'estrutura-selecionada'
        )

      })

  }

  function selecionarVisual(index) {

    const indice = String(index)

    document
      .querySelectorAll('.estrutura-overlay')
      .forEach(overlay => {

        overlay.classList.toggle(
          'estrutura-selecionada',
          overlay.dataset.estruturaIndex === indice
        )

      })

    document
      .querySelectorAll('.structure')
      .forEach((botao, botaoIndex) => {

        botao.classList.toggle(
          'estrutura-selecionada',
          String(botaoIndex) === indice
        )

      })

  }

  function centralizarEstrutura(index) {

    const overlay =
      Array.from(
        document.querySelectorAll(
          '.estrutura-overlay'
        )
      ).find(
        item =>
          item.dataset.estruturaIndex === String(index)
      )

    if (!overlay) {
      return
    }

    const x =
      Number(overlay.dataset.imageX)

    const y =
      Number(overlay.dataset.imageY)

    const largura =
      Number(overlay.dataset.imageWidth)

    const altura =
      Number(overlay.dataset.imageHeight)

    if (
      !Number.isFinite(x) ||
      !Number.isFinite(y)
    ) {
      return
    }

    const centroX =
      x +
      (
        Number.isFinite(largura)
          ? largura / 2
          : 0
      )

    const centroY =
      y +
      (
        Number.isFinite(altura)
          ? altura / 2
          : 0
      )

    const ponto =
      viewer.viewport.imageToViewportCoordinates(
        centroX,
        centroY
      )

    viewer.viewport.panTo(
      ponto,
      true
    )

    const zoomAtual =
      viewer.viewport.getZoom()

    const zoomMaximo =
      viewer.viewport.getMaxZoom()

    const zoomDesejado =
      Math.min(
        zoomAtual * 1.8,
        zoomMaximo
      )

    viewer.viewport.zoomTo(
      zoomDesejado,
      ponto,
      true
    )

  }

  function mostrarEstrutura(
    index,
    centralizar = false
  ) {

    const estrutura =
      estruturas[index]

    if (!estrutura) {
      return
    }

    indiceAtual =
      Number(index)

    nome.textContent =
      estrutura.nome ||
      `Estrutura ${Number(index) + 1}`

    descricao.textContent =
      estrutura.descricao ||
      'Nenhuma descrição cadastrada.'

    panel.hidden = false

    panel.dataset.estruturaIndex =
      String(index)

    selecionarVisual(index)

    if (centralizar) {
      centralizarEstrutura(index)
    }

  }

  function fecharPainel() {

    panel.hidden = true

    indiceAtual = -1

    limparSelecao()

  }

  fechar?.addEventListener(
    'click',
    event => {

      event.preventDefault()
      event.stopPropagation()

      fecharPainel()

    }
  )

  /*
   * LISTA LATERAL
   */

  document.addEventListener(
    'click',
    event => {

      const botao =
        event.target.closest('.structure')

      if (!botao) {
        return
      }

      const index =
        Number(
          botao.dataset.estruturaIndex
        )

      if (!Number.isFinite(index)) {
        return
      }

      mostrarEstrutura(
        index,
        true
      )

    }
  )

  /*
   * MARCADOR SOBRE A IMAGEM
   */

  if (viewerElement) {

    viewerElement.addEventListener(
      'click',
      event => {

        const overlay =
          event.target.closest(
            '.estrutura-overlay'
          )

        if (!overlay) {
          return
        }

        event.preventDefault()
        event.stopPropagation()

        const index =
          Number(
            overlay.dataset.estruturaIndex
          )

        if (!Number.isFinite(index)) {
          return
        }

        mostrarEstrutura(
          index,
          true
        )

      },
      true
    )

  }

  /*
   * NAVEGACAO POR TECLADO
   */

  document.addEventListener(
    'keydown',
    event => {

      if (
        event.key === 'Escape'
      ) {

        if (!panel.hidden) {
          fecharPainel()
        }

        return

      }

      if (
        panel.hidden ||
        !estruturas.length
      ) {
        return
      }

      if (
        event.key !== 'ArrowRight' &&
        event.key !== 'ArrowLeft'
      ) {
        return
      }

      event.preventDefault()

      if (indiceAtual < 0) {
        mostrarEstrutura(0, true)
        return
      }

      let novoIndice =
        indiceAtual

      if (
        event.key === 'ArrowRight'
      ) {

        novoIndice =
          indiceAtual + 1

        if (
          novoIndice >= estruturas.length
        ) {
          novoIndice = 0
        }

      } else {

        novoIndice =
          indiceAtual - 1

        if (novoIndice < 0) {
          novoIndice =
            estruturas.length - 1
        }

      }

      mostrarEstrutura(
        novoIndice,
        true
      )

    }
  )

  console.log(
    'COMBO 7: modo estudo conectado.',
    estruturas.length,
    'estruturas.'
  )

}

function limparEstruturas(viewer) {

  if (!viewer) {
    return
  }

  const overlays =
    viewer.currentOverlays || []

  overlays
    .filter(overlay => {

      const elemento =
        overlay.element

      return (
        elemento &&
        elemento.classList &&
        elemento.classList.contains(
          'estrutura-overlay'
        )
      )

    })
    .forEach(overlay => {

      try {

        viewer.removeOverlay(
          overlay.element
        )

      } catch (erro) {

        console.warn(
          'Erro ao remover estrutura:',
          erro
        )

      }

    })

}


function iniciarViewer(lamina) {

  const elemento =
    document.querySelector('#openseadragon')


  if (!elemento) {

    console.error(
      'Elemento #openseadragon não encontrado.'
    )

    return
  }


  if (!lamina.imagem_url) {

    elemento.innerHTML = `
      <div style="
        display:flex;
        align-items:center;
        justify-content:center;
        height:100%;
        color:white;
        padding:30px;
        text-align:center;
      ">
        Imagem da lâmina não encontrada.
      </div>
    `

    return
  }


  const viewer =
    OpenSeadragon({

      element: elemento,

      drawer: 'canvas',

      prefixUrl:
        'https://openseadragon.github.io/openseadragon/images/',

      tileSources: {

        type: 'image',

        url: lamina.imagem_url

      },

      showNavigationControl: false,

      animationTime: 0.8,

      zoomPerScroll: 1.5,

      maxZoomPixelRatio: 8,

      visibilityRatio: 1,

      constrainDuringPan: true

    })




  viewer.addOnceHandler(
    'open',
    () => {

      desenharEstruturas(
        viewer,
        lamina
      )

    }
  )


  viewer.addOnceHandler(
    'open',
    () => {

      configurarModoEstudo(
        viewer,
        lamina
      )

    }
  )

document
    .querySelector('#zoom-in')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(1.5)
    )


  document
    .querySelector('#zoom-out')
    ?.addEventListener(
      'click',
      () => viewer.viewport.zoomBy(0.67)
    )


  document
    .querySelector('#zoom-home')
    ?.addEventListener(
      'click',
      () => viewer.viewport.goHome()
    )


  /* =====================================================
     COMBO 4 - VIEWER AVANCADO
     ===================================================== */


  const zoomValue =
    document.querySelector('#zoom-value')


  const atualizarZoom =
    () => {

      if (!zoomValue) {
        return
      }

      const zoom =
        viewer.viewport.getZoom()

      zoomValue.textContent =
        Math.round(zoom * 100) + '%'

    }


  viewer.addHandler(
    'zoom',
    atualizarZoom
  )


  viewer.addHandler(
    'open',
    atualizarZoom
  )


  /*
   * Desenha todas as estruturas da lâmina
   * depois que a imagem estiver aberta.
   */



  document
    .querySelector('#zoom-fullscreen')
    ?.addEventListener(
      'click',
      () => {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(true)

        } else {

          viewer.setFullPage(true)

        }

      }
    )


  document
    .querySelector('#zoom-reset')
    ?.addEventListener(
      'click',
      () => {

        viewer.viewport.goHome()

        atualizarZoom()

      }
    )


  document.addEventListener(
    'keydown',
    event => {

      const tag =
        event.target?.tagName

      if (
        tag === 'INPUT' ||
        tag === 'TEXTAREA' ||
        tag === 'SELECT'
      ) {
        return
      }


      if (
        event.key === '+' ||
        event.key === '='
      ) {

        viewer.viewport.zoomBy(1.25)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '-' ||
        event.key === '_'
      ) {

        viewer.viewport.zoomBy(0.8)

        viewer.viewport.applyConstraints()

      }


      if (
        event.key === '0'
      ) {

        viewer.viewport.goHome()

        atualizarZoom()

      }


      if (
        event.key.toLowerCase() === 'f'
      ) {

        if (
          typeof viewer.setFullScreen ===
          'function'
        ) {

          viewer.setFullScreen(
            !viewer.isFullScreen()
          )

        }

      }


      atualizarZoom()

    }
  )


  atualizarZoom()

}


async function render() {

  const config = await carregarConfiguracoesSite()

  const route =
    getRoute()


  if (route === 'login') {

    app.innerHTML =
      renderLogin()

    setupLogin()

    return
  }


  if (route === 'admin') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      await renderAdmin()

    setupAdmin()

    return
  }


  if (route === 'nova-lamina') {

    const {
      data: {
        user
      }
    } =
      await supabase.auth.getUser()


    if (!user) {

      window.location.hash =
        '#login'

      return
    }


    app.innerHTML =
      renderNovaLamina()

    setupNovaLamina()

    return
  }


  if (route === 'catalogo') {

    await renderCatalogo()

    return
  }


  if (route === 'lamina') {

    const id =
      window.location.hash
        .replace('#lamina/', '')


    if (!id) {

      window.location.hash =
        '#laminas'

      return
    }


    app.innerHTML = `
      ${renderHeader(config)}

      <main style="
        min-height:60vh;
        display:flex;
        align-items:center;
        justify-content:center;
      ">

        <div>
          <h1>Carregando lâmina...</h1>
        </div>

      </main>
    `


    const lamina =
      await buscarLamina(id)


    if (!lamina) {

      app.innerHTML = `
        ${renderHeader(config)}

        <main style="
          min-height:60vh;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
        ">

          <div>

            <h1>
              Lâmina não encontrada
            </h1>

            <p>
              Não foi possível carregar esta lâmina.
            </p>

            <a
              href="#laminas"
              class="button primary"
            >
              Voltar para lâminas
            </a>

          </div>

        </main>
      `

      return
    }


    app.innerHTML =
      renderLamina(lamina, config)


    iniciarViewer(lamina)

    return
  }
    aplicarConfiguracoesVisuais(config)

    app.innerHTML = renderHome(config)

}


window.addEventListener(
  'hashchange',
  render
)


render()

















































===== backup-reparador-completo-20260819-212337\src\style.css =====
:root {
  --cor-principal: #1f6feb;
  --cor-fundo: #f7f9fc;
  --cor-texto: #172033;
  --cor-destaque: #22c55e;
}
:root {
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  color: #17201d;
  background: #f5f7f5;
  font-synthesis: none;
  text-rendering: optimizeLegibility;
  --green: #174c3c;
  --green-light: #2d755d;
  --mint: #dcefe7;
  --cream: #f5f7f5;
  --white: #ffffff;
  --text: #17201d;
  --muted: #66736d;
  --border: #dfe6e2;
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  scroll-behavior: smooth;
}

body {
  min-width: 320px;
  background: var(--cream);
  color: var(--text);
}

button,
a {
  font: inherit;
}

button {
  border: 0;
  cursor: pointer;
}

a {
  color: inherit;
  text-decoration: none;
}

.container {
  width: min(1160px, calc(100% - 48px));
  margin: 0 auto;
}

.header {
  position: sticky;
  top: 0;
  z-index: 20;
  background: rgba(245, 247, 245, 0.9);
  backdrop-filter: blur(16px);
  border-bottom: 1px solid rgba(223, 230, 226, 0.8);
}

.header-content {
  height: 76px;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.brand {
  display: flex;
  align-items: center;
  gap: 11px;
}

.brand-icon {
  width: 42px;
  height: 42px;
  display: grid;
  place-items: center;
  border-radius: 12px;
  background: var(--green);
  color: white;
  font-size: 20px;
}

.brand strong,
.brand small {
  display: block;
}

.brand strong {
  font-size: 17px;
  letter-spacing: -0.4px;
}

.brand small {
  color: var(--muted);
  font-size: 11px;
  letter-spacing: 1.5px;
  text-transform: uppercase;
}

.nav {
  display: flex;
  gap: 34px;
  color: #53605a;
  font-size: 14px;
}

.nav a {
  transition: color 0.2s ease;
}

.nav a:hover {
  color: var(--green);
}

.menu-button {
  display: none;
  background: transparent;
  font-size: 22px;
}

.hero {
  padding: 100px 0 90px;
  overflow: hidden;
}

.hero-grid {
  display: grid;
  grid-template-columns: 1fr 0.9fr;
  align-items: center;
  gap: 70px;
}

.eyebrow {
  display: inline-block;
  margin-bottom: 18px;
  color: var(--green-light);
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 2px;
}

.hero h1 {
  max-width: 650px;
  font-size: clamp(48px, 6vw, 76px);
  line-height: 0.98;
  letter-spacing: -4px;
}

.hero h1 span {
  display: block;
  color: var(--green);
}

.hero-text > p {
  max-width: 570px;
  margin-top: 28px;
  color: var(--muted);
  font-size: 17px;
  line-height: 1.7;
}

.hero-actions {
  display: flex;
  gap: 12px;
  margin-top: 34px;
}

.button {
  display: inline-flex;
  align-items: center;
  gap: 20px;
  padding: 14px 20px;
  border-radius: 10px;
  font-size: 14px;
  font-weight: 700;
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.button:hover {
  transform: translateY(-2px);
}

.button.primary {
  background: var(--green);
  color: white;
  box-shadow: 0 12px 30px rgba(23, 76, 60, 0.18);
}

.button.secondary {
  border: 1px solid var(--border);
  background: white;
}

.hero-visual {
  min-height: 480px;
  display: grid;
  place-items: center;
}

.microscope-card {
  width: min(430px, 100%);
  height: 430px;
  position: relative;
  overflow: hidden;
  border-radius: 28px;
  background: #dbece5;
  border: 1px solid #cbded5;
}

.microscope-glow {
  position: absolute;
  width: 270px;
  height: 270px;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.65);
  filter: blur(8px);
}

.microscope {
  position: absolute;
  width: 190px;
  height: 280px;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -45%);
}

.microscope-top {
  position: absolute;
  width: 80px;
  height: 115px;
  right: 28px;
  top: 10px;
  border-radius: 30px 30px 12px 12px;
  background: #263e36;
  transform: rotate(23deg);
}

.microscope-body {
  position: absolute;
  width: 75px;
  height: 190px;
  left: 53px;
  top: 55px;
  border-radius: 45px 20px 30px 25px;
  background: #345b4e;
  transform: rotate(22deg);
}

.microscope-base {
  position: absolute;
  width: 190px;
  height: 38px;
  bottom: 0;
  border-radius: 30px 30px 14px 14px;
  background: #263e36;
}

.microscope-lens {
  position: absolute;
  width: 100px;
  height: 22px;
  left: 45px;
  bottom: 66px;
  border-radius: 15px;
  background: #1d302a;
}

.floating-tag {
  position: absolute;
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 9px 13px;
  border-radius: 10px;
  background: rgba(255,255,255,0.88);
  backdrop-filter: blur(8px);
  box-shadow: 0 10px 30px rgba(40, 70, 60, 0.12);
  font-size: 12px;
  font-weight: 700;
}

.floating-tag span {
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: var(--green-light);
}

.tag-one {
  left: 24px;
  top: 75px;
}

.tag-two {
  right: 22px;
  top: 155px;
}

.tag-three {
  left: 30px;
  bottom: 70px;
}

.scan-line {
  position: absolute;
  width: 100%;
  height: 1px;
  top: 50%;
  background: rgba(23, 76, 60, 0.12);
}

.section {
  padding: 95px 0;
  background: white;
}

.section-heading {
  display: flex;
  align-items: end;
  justify-content: space-between;
  gap: 40px;
  margin-bottom: 40px;
}

.section-heading h2,
.about h2 {
  font-size: clamp(32px, 4vw, 48px);
  line-height: 1.05;
  letter-spacing: -2px;
}

.section-heading p {
  max-width: 390px;
  color: var(--muted);
  line-height: 1.6;
}

.tissue-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 16px;
}

.tissue-card {
  position: relative;
  min-height: 290px;
  padding: 26px;
  overflow: hidden;
  border: 1px solid var(--border);
  border-radius: 18px;
  background: var(--cream);
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}

.tissue-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 18px 40px rgba(23, 76, 60, 0.09);
}

.card-number {
  color: #a4b0ab;
  font-size: 12px;
  font-weight: 800;
}

.card-icon {
  margin-top: 48px;
  color: var(--green);
  font-size: 34px;
}

.tissue-card h3 {
  margin-top: 20px;
  font-size: 19px;
}

.tissue-card p {
  margin-top: 8px;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.5;
}

.tissue-card button {
  position: absolute;
  left: 26px;
  bottom: 24px;
  background: transparent;
  color: var(--green);
  font-size: 13px;
  font-weight: 800;
}

.tissue-card button span {
  margin-left: 8px;
}

.about {
  padding: 105px 0;
  background: #eaf2ee;
}

.about-grid {
  display: grid;
  grid-template-columns: 0.8fr 1fr;
  gap: 100px;
}

.about-text > p {
  color: #56645e;
  font-size: 17px;
  line-height: 1.7;
}

.features {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 25px;
  margin-top: 35px;
}

.features div {
  padding-top: 20px;
  border-top: 1px solid #cbdad3;
}

.features strong,
.features span {
  display: block;
}

.features strong {
  font-size: 14px;
}

.features span {
  margin-top: 7px;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.5;
}

.footer {
  padding: 25px 0;
  background: var(--green);
  color: white;
}

.footer .container {
  display: flex;
  justify-content: space-between;
  color: rgba(255,255,255,0.7);
  font-size: 12px;
}

.footer span:first-child {
  color: white;
  font-weight: 700;
}

@media (max-width: 850px) {
  .hero-grid,
  .about-grid {
    grid-template-columns: 1fr;
    gap: 55px;
  }

  .hero {
    padding-top: 65px;
  }

  .hero-visual {
    min-height: 400px;
  }

  .tissue-grid {
    grid-template-columns: 1fr 1fr;
  }
}

@media (max-width: 600px) {
  .container {
    width: min(100% - 32px, 1160px);
  }

  .nav {
    display: none;
  }

  .menu-button {
    display: block;
  }

  .hero h1 {
    font-size: 50px;
    letter-spacing: -3px;
  }

  .hero-actions {
    flex-direction: column;
  }

  .button {
    justify-content: center;
  }

  .section-heading {
    display: block;
  }

  .section-heading p {
    margin-top: 15px;
  }

  .tissue-grid {
    grid-template-columns: 1fr;
  }

  .about-grid {
    gap: 35px;
  }

  .features {
    grid-template-columns: 1fr;
  }

  .footer .container {
    flex-direction: column;
    gap: 8px;
  }
}

.slide-header {
  padding: 55px 0 35px;
  background: white;
  border-bottom: 1px solid var(--border);
}

.back-link {
  display: inline-block;
  margin-bottom: 30px;
  color: var(--muted);
  font-size: 13px;
}

.slide-title {
  display: flex;
  justify-content: space-between;
  align-items: end;
  gap: 40px;
}

.slide-title h1 {
  font-size: clamp(36px, 5vw, 58px);
  letter-spacing: -3px;
  line-height: 1;
}

.slide-title p {
  margin-top: 15px;
  color: var(--muted);
}

.slide-meta {
  display: flex;
  flex-direction: column;
  gap: 8px;
  color: var(--muted);
  font-size: 12px;
}

.viewer-section {
  padding: 40px 0 80px;
  background: #f1f4f2;
}

.viewer-layout {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 340px;
  gap: 24px;
  align-items: start;
}

.viewer-container {
  overflow: hidden;
  border: 1px solid var(--border);
  border-radius: 18px;
  background: #101513;
  box-shadow: 0 20px 50px rgba(0,0,0,0.08);
}

.viewer {
  width: 100%;
  height: 650px;
  background: #101513;
}

.viewer-controls {
  min-height: 58px;
  padding: 10px 14px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 15px;
  background: #18201d;
  color: #aab7b1;
  font-size: 11px;
}

.zoom-buttons {
  display: flex;
  gap: 5px;
}

.zoom-buttons button {
  width: 38px;
  height: 34px;
  border-radius: 7px;
  background: #29332f;
  color: white;
  font-size: 18px;
}

.zoom-buttons button:hover {
  background: var(--green-light);
}

.info-panel {
  padding: 28px;
  border: 1px solid var(--border);
  border-radius: 18px;
  background: white;
}

.panel-label {
  margin-bottom: 15px;
  color: var(--green-light);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 1.7px;
}

.info-panel h2 {
  font-size: 25px;
  line-height: 1.1;
}

.description {
  margin-top: 15px;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.7;
}

.info-section {
  margin-top: 30px;
}

.info-section h3 {
  font-size: 14px;
}

.structure-list {
  margin-top: 15px;
  display: grid;
  gap: 8px;
}

.structure {
  width: 100%;
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px;
  text-align: left;
  border-radius: 10px;
  background: #f4f7f5;
}

.structure:hover {
  background: #e8f1ec;
}

.structure-dot {
  width: 9px;
  height: 9px;
  flex: 0 0 9px;
  border-radius: 50%;
  background: var(--green-light);
}

.structure strong,
.structure small {
  display: block;
}

.structure strong {
  font-size: 12px;
}

.structure small {
  margin-top: 3px;
  color: var(--muted);
  font-size: 10px;
}

.content-button {
  width: 100%;
  margin-top: 25px;
  padding: 14px;
  display: flex;
  justify-content: space-between;
  border-radius: 10px;
  background: var(--green);
  color: white;
  font-size: 12px;
  font-weight: 700;
}

.learning-section {
  padding: 90px 0;
  background: white;
}

.learning-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
}

.learning-card {
  padding: 35px;
  border: 1px solid var(--border);
  border-radius: 18px;
}

.learning-card h2 {
  font-size: 28px;
  letter-spacing: -1px;
}

.learning-card > p {
  max-width: 500px;
  margin-top: 12px;
  color: var(--muted);
  line-height: 1.6;
  font-size: 14px;
}

.video-placeholder {
  height: 190px;
  margin-top: 25px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 10px;
  border-radius: 12px;
  background: #edf2ef;
  color: var(--green);
}

.video-placeholder span {
  font-size: 35px;
}

.video-placeholder small {
  color: var(--muted);
}

.learning-card .button {
  margin-top: 25px;
}

@media (max-width: 900px) {
  .viewer-layout {
    grid-template-columns: 1fr;
  }

  .slide-title {
    display: block;
  }

  .slide-meta {
    margin-top: 25px;
  }

  .learning-grid {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 600px) {
  .viewer {
    height: 450px;
  }

  .viewer-controls {
    flex-direction: column;
    align-items: stretch;
  }

  .zoom-buttons {
    justify-content: center;
  }
}

.card-link {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  margin-top: 15px;
  color: var(--green-light);
  font-size: 12px;
  font-weight: 800;
  text-decoration: none;
}

.card-link span {
  transition: transform 0.2s ease;
}

.card-link:hover span {
  transform: translateX(4px);
}

.tissue-card .eyebrow {
  display: block;
  margin-top: 10px;
}

.auth-page {
  min-height: 100vh;
  display: grid;
  place-items: center;
  padding: 30px;
  background: #f1f4f2;
}

.auth-card {
  width: min(430px, 100%);
  padding: 45px;
  border: 1px solid var(--border);
  border-radius: 20px;
  background: white;
  box-shadow: 0 25px 70px rgba(0,0,0,.08);
}

.auth-logo {
  margin-bottom: 20px;
  font-size: 42px;
}

.auth-card h1 {
  margin-top: 8px;
  font-size: 34px;
}

.auth-card > p {
  margin: 12px 0 30px;
  color: var(--muted);
  line-height: 1.6;
  font-size: 13px;
}

.auth-card form {
  display: grid;
  gap: 18px;
}

.auth-card label {
  display: grid;
  gap: 7px;
  font-size: 12px;
  font-weight: 700;
}

.auth-card input {
  width: 100%;
  padding: 13px 14px;
  border: 1px solid var(--border);
  border-radius: 9px;
  outline: none;
  font: inherit;
}

.auth-card input:focus {
  border-color: var(--green-light);
}

#login-error {
  color: #b43b3b;
  font-size: 12px;
}

.auth-card .back-link {
  margin-top: 25px;
}

.admin-page {
  min-height: calc(100vh - 75px);
  padding: 60px 0 100px;
  background: #f1f4f2;
}

.admin-header {
  display: flex;
  align-items: center;
  gap: 15px;
}

.admin-user {
  color: var(--muted);
  font-size: 11px;
}

.logout-button {
  padding: 8px 13px;
  border: 1px solid var(--border);
  border-radius: 7px;
  background: white;
  font-size: 11px;
}

.admin-title {
  display: flex;
  align-items: end;
  justify-content: space-between;
  gap: 30px;
}

.admin-title h1 {
  margin-top: 8px;
  font-size: 42px;
  letter-spacing: -2px;
}

.admin-title p {
  margin-top: 10px;
  color: var(--muted);
}

.admin-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 15px;
  margin-top: 40px;
}

.admin-stat {
  padding: 25px;
  border: 1px solid var(--border);
  border-radius: 14px;
  background: white;
}

.admin-stat span,
.admin-stat strong,
.admin-stat small {
  display: block;
}

.admin-stat span {
  font-size: 20px;
}

.admin-stat strong {
  margin-top: 12px;
  font-size: 30px;
}

.admin-stat small {
  margin-top: 3px;
  color: var(--muted);
}

.admin-section {
  margin-top: 50px;
}

.admin-section-header h2 {
  margin-top: 6px;
}

.admin-list {
  margin-top: 18px;
  display: grid;
  gap: 8px;
}

.admin-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 18px 20px;
  border: 1px solid var(--border);
  border-radius: 12px;
  background: white;
}

.admin-item strong,
.admin-item small {
  display: block;
}

.admin-item small {
  margin-top: 4px;
  color: var(--muted);
  font-size: 11px;
}

.status {
  padding: 6px 10px;
  border-radius: 20px;
  background: #f0f0f0;
  color: var(--muted);
  font-size: 10px;
}

.status.published {
  background: #e5f3ea;
  color: #267244;
}

.empty-state {
  padding: 60px 20px;
  text-align: center;
  border: 1px dashed var(--border);
  border-radius: 14px;
  background: white;
}

.empty-state span {
  font-size: 35px;
}

.empty-state h3 {
  margin-top: 12px;
}

.empty-state p {
  margin-top: 7px;
  color: var(--muted);
  font-size: 13px;
}

@media (max-width: 700px) {

  .admin-title {
    display: block;
  }

  .admin-title .button {
    margin-top: 20px;
  }

  .admin-grid {
    grid-template-columns: 1fr;
  }

  .admin-user {
    display: none;
  }

  .auth-card {
    padding: 30px;
  }

}

.lamina-form {
  max-width: 900px;
  margin-top: 40px;
}

.form-card {
  margin-bottom: 20px;
  padding: 30px;
  border: 1px solid var(--border);
  border-radius: 16px;
  background: white;
}

.form-card h2 {
  margin-top: 7px;
  margin-bottom: 25px;
}

.form-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
}

.form-grid .full {
  grid-column: 1 / -1;
}

.form-card label {
  display: grid;
  gap: 8px;
  font-size: 12px;
  font-weight: 700;
}

.form-card input,
.form-card textarea {
  width: 100%;
  padding: 13px 14px;
  border: 1px solid var(--border);
  border-radius: 9px;
  outline: none;
  font: inherit;
  resize: vertical;
}

.form-card input:focus,
.form-card textarea:focus {
  border-color: var(--green-light);
}

.form-help {
  color: var(--muted);
  font-size: 13px;
  line-height: 1.6;
  margin-bottom: 20px;
}

.upload-area {
  min-height: 190px;
  display: flex !important;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 7px;
  padding: 30px;
  border: 2px dashed var(--border);
  border-radius: 12px;
  cursor: pointer;
  text-align: center;
}

.upload-area:hover {
  border-color: var(--green-light);
  background: #fafcfb;
}

.upload-area input {
  display: none;
}

.upload-icon {
  font-size: 36px;
}

.upload-area strong {
  font-size: 13px;
}

.upload-area small {
  color: var(--muted);
  font-size: 11px;
}

.image-preview {
  display: block;
  width: 100%;
  max-height: 400px;
  object-fit: contain;
  margin-top: 20px;
  border-radius: 10px;
  background: #111;
}

.publish-option {
  display: flex !important;
  grid-template-columns: auto 1fr;
  grid-template-rows: auto;
  gap: 12px !important;
  align-items: flex-start;
  cursor: pointer;
}

.publish-option input {
  width: auto;
  margin-top: 3px;
}

.publish-option span {
  display: grid;
  gap: 4px;
}

.publish-option small {
  color: var(--muted);
  font-weight: 400;
  line-height: 1.5;
}

.form-actions {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  margin-top: 25px;
}

.success-message {
  padding: 14px 16px;
  margin-top: 20px;
  border-radius: 9px;
  background: #e5f3ea;
  color: #267244;
  font-size: 13px;
}

#form-message:not(.success-message) {
  margin-top: 15px;
  color: #b43b3b;
  font-size: 12px;
}

@media (max-width: 700px) {

  .form-grid {
    grid-template-columns: 1fr;
  }

  .form-grid .full {
    grid-column: auto;
  }

  .form-card {
    padding: 22px;
  }

}

.catalog-page {
  min-height: 100vh;
  background: #f7f8f6;
}

.catalog-hero {
  padding: 80px 0 65px;
  background: white;
  border-bottom: 1px solid var(--border);
}

.catalog-hero h1 {
  margin: 12px 0;
  font-size: clamp(36px, 5vw, 64px);
  letter-spacing: -2px;
}

.catalog-hero p {
  max-width: 650px;
  color: var(--muted);
  font-size: 17px;
  line-height: 1.7;
}

.catalog-content {
  padding: 50px 0 80px;
}

.laminas-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
}

.lamina-card {
  overflow: hidden;
  background: white;
  border: 1px solid var(--border);
  border-radius: 18px;
  transition: transform .2s ease, box-shadow .2s ease;
}

.lamina-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 15px 35px rgba(0,0,0,.08);
}

.lamina-image {
  position: relative;
  height: 230px;
  overflow: hidden;
  background: #111;
}

.lamina-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform .4s ease;
}

.lamina-card:hover .lamina-image img {
  transform: scale(1.06);
}

.zoom-badge {
  position: absolute;
  right: 12px;
  bottom: 12px;
  padding: 8px 11px;
  border-radius: 8px;
  background: rgba(0,0,0,.72);
  color: white;
  font-size: 11px;
  font-weight: 700;
}

.lamina-card-content {
  padding: 22px;
}

.lamina-category {
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 1px;
  text-transform: uppercase;
  color: var(--green-light);
}

.lamina-card h2 {
  margin: 8px 0;
  font-size: 21px;
}

.lamina-card p {
  min-height: 55px;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.6;
}

.explore-lamina {
  width: 100%;
  margin-top: 10px;
}

.loading-state,
.error-state,
.empty-state {
  padding: 60px 20px;
  text-align: center;
  color: var(--muted);
}

.empty-state span {
  display: block;
  margin-bottom: 15px;
  font-size: 40px;
}

.empty-state h3 {
  margin-bottom: 8px;
  color: var(--text);
}

@media (max-width: 900px) {

  .laminas-grid {
    grid-template-columns: repeat(2, 1fr);
  }

}

@media (max-width: 600px) {

  .laminas-grid {
    grid-template-columns: 1fr;
  }

}



/* =========================================================
   LOGO DA PRÃ‰VIA DO EDITOR DE APARÃŠNCIA
   ========================================================= */

.preview-logo {
  width: 48px;
  height: 48px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  overflow: visible;
}

.preview-logo img {
  width: 42px;
  height: 42px;
  max-width: none;
  max-height: none;
  object-fit: contain;
  display: block;
  transform-origin: center center;
  transition: transform 0.15s ease;
}
 
.logo-preview {
  width: 120px;
  height: 120px;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: visible;
}

.logo-preview img {
  width: 90px;
  height: 90px;
  max-width: none;
  max-height: none;
  object-fit: contain;
  display: block;
  transform-origin: center center;
  transition: transform 0.15s ease;
}


/* =========================================================
   APARÃŠNCIA â€” EDITOR MODERNO
   ========================================================= */

#admin-section-aparencia {
  max-width: 1180px;
}

#aparencia-form {
  display: flex;
  flex-direction: column;
  gap: 22px;
}

#aparencia-form .form-card {
  background: #fff;
  border: 1px solid #e4e9e7;
  border-radius: 18px;
  padding: 26px;
  box-shadow: 0 4px 18px rgba(16,47,41,.04);
}

#aparencia-form .form-card-title {
  display: flex;
  align-items: flex-start;
  gap: 14px;
  margin-bottom: 24px;
}

#aparencia-form .form-card-title > span {
  width: 42px;
  height: 42px;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #edf6f2;
  border-radius: 12px;
  font-size: 20px;
}

#aparencia-form .form-card-title h2 {
  margin: 2px 0 5px;
  font-size: 18px;
  color: #17211f;
}

#aparencia-form .form-card-title p {
  margin: 0;
  color: #7a8581;
  font-size: 13px;
}

#aparencia-form .form-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0,1fr));
  gap: 20px;
}

#aparencia-form .form-field.full {
  grid-column: 1 / -1;
}

#aparencia-form .form-field label {
  display: block;
  margin-bottom: 8px;
  color: #34413d;
  font-size: 13px;
  font-weight: 700;
}

#aparencia-form input[type="text"],
#aparencia-form input[type="url"],
#aparencia-form textarea {
  width: 100%;
  box-sizing: border-box;
  border: 1px solid #dce4e0;
  background: #fbfcfc;
  border-radius: 10px;
  padding: 12px 14px;
  color: #17211f;
  font: inherit;
  outline: none;
  transition: .18s ease;
}

#aparencia-form textarea {
  resize: vertical;
  min-height: 95px;
}

#aparencia-form input[type="text"]:focus,
#aparencia-form input[type="url"]:focus,
#aparencia-form textarea:focus {
  background: #fff;
  border-color: #1e6f5c;
  box-shadow: 0 0 0 3px rgba(30,111,92,.10);
}

#aparencia-form input[type="range"] {
  width: 100%;
  accent-color: #1e6f5c;
  cursor: pointer;
}

#aparencia-form #logo-tamanho-valor {
  display: inline-block;
  min-width: 55px;
  padding: 6px 9px;
  background: #edf6f2;
  color: #1e6f5c;
  border-radius: 8px;
  text-align: center;
  font-size: 12px;
  font-weight: 800;
}

#aparencia-form input[type="file"] {
  width: 100%;
  box-sizing: border-box;
  padding: 10px;
  border: 1px dashed #cbd8d3;
  border-radius: 12px;
  background: #f8faf9;
  color: #66736e;
  cursor: pointer;
}

#aparencia-form input[type="color"] {
  width: 52px;
  height: 42px;
  padding: 3px;
  border: 1px solid #dce4e0;
  border-radius: 10px;
  background: #fff;
  cursor: pointer;
}

#logo-preview {
  min-height: 120px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 12px;
  padding: 18px;
  border: 1px solid #e1e7e4;
  border-radius: 14px;
  background: #f7f9f8;
  overflow: hidden;
}

#logo-preview img {
  max-width: 100%;
  max-height: 120px;
  object-fit: contain;
}

#logo-nome {
  display: block;
  margin-top: 8px;
  color: #78837f;
  font-size: 12px;
}

#remover-logo {
  margin-top: 10px;
  border: 0;
  border-radius: 9px;
  padding: 9px 13px;
  background: #fff1f1;
  color: #a33b3b;
  font-size: 12px;
  font-weight: 700;
  cursor: pointer;
}

#remover-logo:hover {
  background: #ffe5e5;
}

.aparencia-preview-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 14px;
}

.preview-live {
  padding: 6px 10px;
  border-radius: 20px;
  background: #e9f7ef;
  color: #14734d;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: .5px;
}

.aparencia-preview {
  overflow: hidden;
  border: 1px solid #dfe6e3;
  border-radius: 18px;
  box-shadow: 0 10px 30px rgba(16,47,41,.08);
}

#aparencia-form .form-actions {
  position: sticky;
  bottom: 18px;
  z-index: 5;
  display: flex;
  justify-content: flex-end;
  padding: 14px 16px;
  background: rgba(255,255,255,.94);
  backdrop-filter: blur(12px);
  border: 1px solid #e1e7e4;
  border-radius: 14px;
  box-shadow: 0 8px 30px rgba(16,47,41,.10);
}

@media (max-width: 750px) {
  #aparencia-form .form-grid {
    grid-template-columns: 1fr;
  }

  #aparencia-form .form-field.full {
    grid-column: auto;
  }

  #aparencia-form .form-card {
    padding: 20px;
  }
}


/* =========================================================
   APARÃŠNCIA â€” EDITOR MODERNO
   ========================================================= */

#admin-section-aparencia {
  max-width: 1180px;
}

#aparencia-form {
  display: flex;
  flex-direction: column;
  gap: 22px;
}

#aparencia-form .form-card {
  background: #fff;
  border: 1px solid #e4e9e7;
  border-radius: 18px;
  padding: 26px;
  box-shadow: 0 4px 18px rgba(16,47,41,.04);
}

#aparencia-form .form-card-title {
  display: flex;
  align-items: flex-start;
  gap: 14px;
  margin-bottom: 24px;
}

#aparencia-form .form-card-title > span {
  width: 42px;
  height: 42px;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #edf6f2;
  border-radius: 12px;
  font-size: 20px;
}

#aparencia-form .form-card-title h2 {
  margin: 2px 0 5px;
  font-size: 18px;
  color: #17211f;
}

#aparencia-form .form-card-title p {
  margin: 0;
  color: #7a8581;
  font-size: 13px;
}

#aparencia-form .form-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0,1fr));
  gap: 20px;
}

#aparencia-form .form-field.full {
  grid-column: 1 / -1;
}

#aparencia-form .form-field label {
  display: block;
  margin-bottom: 8px;
  color: #34413d;
  font-size: 13px;
  font-weight: 700;
}

#aparencia-form input[type="text"],
#aparencia-form input[type="url"],
#aparencia-form textarea {
  width: 100%;
  box-sizing: border-box;
  border: 1px solid #dce4e0;
  background: #fbfcfc;
  border-radius: 10px;
  padding: 12px 14px;
  color: #17211f;
  font: inherit;
  outline: none;
  transition: .18s ease;
}

#aparencia-form textarea {
  resize: vertical;
  min-height: 95px;
}

#aparencia-form input[type="text"]:focus,
#aparencia-form input[type="url"]:focus,
#aparencia-form textarea:focus {
  background: #fff;
  border-color: #1e6f5c;
  box-shadow: 0 0 0 3px rgba(30,111,92,.10);
}

#aparencia-form input[type="range"] {
  width: 100%;
  accent-color: #1e6f5c;
  cursor: pointer;
}

#aparencia-form #logo-tamanho-valor {
  display: inline-block;
  min-width: 55px;
  padding: 6px 9px;
  background: #edf6f2;
  color: #1e6f5c;
  border-radius: 8px;
  text-align: center;
  font-size: 12px;
  font-weight: 800;
}

#aparencia-form input[type="file"] {
  width: 100%;
  box-sizing: border-box;
  padding: 10px;
  border: 1px dashed #cbd8d3;
  border-radius: 12px;
  background: #f8faf9;
  color: #66736e;
  cursor: pointer;
}

#aparencia-form input[type="color"] {
  width: 52px;
  height: 42px;
  padding: 3px;
  border: 1px solid #dce4e0;
  border-radius: 10px;
  background: #fff;
  cursor: pointer;
}

#logo-preview {
  min-height: 120px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 12px;
  padding: 18px;
  border: 1px solid #e1e7e4;
  border-radius: 14px;
  background: #f7f9f8;
  overflow: hidden;
}

#logo-preview img {
  max-width: 100%;
  max-height: 120px;
  object-fit: contain;
}

#logo-nome {
  display: block;
  margin-top: 8px;
  color: #78837f;
  font-size: 12px;
}

#remover-logo {
  margin-top: 10px;
  border: 0;
  border-radius: 9px;
  padding: 9px 13px;
  background: #fff1f1;
  color: #a33b3b;
  font-size: 12px;
  font-weight: 700;
  cursor: pointer;
}

#remover-logo:hover {
  background: #ffe5e5;
}

.aparencia-preview-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 14px;
}

.preview-live {
  padding: 6px 10px;
  border-radius: 20px;
  background: #e9f7ef;
  color: #14734d;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: .5px;
}

.aparencia-preview {
  overflow: hidden;
  border: 1px solid #dfe6e3;
  border-radius: 18px;
  box-shadow: 0 10px 30px rgba(16,47,41,.08);
}

#aparencia-form .form-actions {
  position: sticky;
  bottom: 18px;
  z-index: 5;
  display: flex;
  justify-content: flex-end;
  padding: 14px 16px;
  background: rgba(255,255,255,.94);
  backdrop-filter: blur(12px);
  border: 1px solid #e1e7e4;
  border-radius: 14px;
  box-shadow: 0 8px 30px rgba(16,47,41,.10);
}

@media (max-width: 750px) {
  #aparencia-form .form-grid {
    grid-template-columns: 1fr;
  }

  #aparencia-form .form-field.full {
    grid-column: auto;
  }

  #aparencia-form .form-card {
    padding: 20px;
  }
}


/* =========================================================
   APARÃŠNCIA â€” EDITOR DE CORES
   ========================================================= */

#aparencia-form input[type="color"] {
  appearance: none;
  -webkit-appearance: none;

  width: 58px;
  height: 46px;

  padding: 4px;

  border: 1px solid #d7e1dd;
  border-radius: 12px;

  background: #fff;

  cursor: pointer;

  transition:
    transform .15s ease,
    border-color .15s ease,
    box-shadow .15s ease;
}

#aparencia-form input[type="color"]:hover {
  transform: translateY(-1px);

  border-color: #1e6f5c;

  box-shadow:
    0 4px 12px rgba(30,111,92,.12);
}

#aparencia-form input[type="color"]:focus {
  outline: none;

  border-color: #1e6f5c;

  box-shadow:
    0 0 0 3px rgba(30,111,92,.12);
}

#aparencia-form input[type="color"]::-webkit-color-swatch-wrapper {
  padding: 0;
}

#aparencia-form input[type="color"]::-webkit-color-swatch {
  border: none;
  border-radius: 8px;
}

#aparencia-form .color-editor {
  display: flex;
  align-items: center;
  gap: 12px;
}

#aparencia-form .color-editor input[type="text"] {
  flex: 1;
  min-width: 0;

  font-family:
    ui-monospace,
    SFMono-Regular,
    Menlo,
    Monaco,
    Consolas,
    monospace;

  font-size: 13px;
  font-weight: 700;

  letter-spacing: .4px;

  text-transform: uppercase;
}

#aparencia-form .color-editor::before {
  content: "HEX";

  display: flex;
  align-items: center;
  justify-content: center;

  height: 36px;

  padding: 0 9px;

  border-radius: 8px;

  background: #f0f4f2;

  color: #687671;

  font-size: 10px;
  font-weight: 800;
  letter-spacing: .6px;
}

#aparencia-form .form-field:has(input[type="color"]) {
  padding: 16px;

  border: 1px solid #e5ebe8;
  border-radius: 14px;

  background: #fbfcfc;

  transition:
    border-color .18s ease,
    background .18s ease,
    box-shadow .18s ease;
}

#aparencia-form .form-field:has(input[type="color"]):hover {
  background: #fff;

  border-color: #d5e1dc;

  box-shadow:
    0 4px 14px rgba(16,47,41,.04);
}

#aparencia-form .form-field:has(input[type="color"]) label {
  margin-bottom: 12px;
}


/* =========================================================
   APARÃŠNCIA â€” PREVIEW AO VIVO
   ========================================================= */

#aparencia-preview {
  position: relative;
  overflow: hidden;

  border: 1px solid #dfe7e3;
  border-radius: 20px;

  background: var(--preview-fundo, #ffffff);

  box-shadow:
    0 18px 50px rgba(20, 45, 38, .10);

  transition:
    background .2s ease,
    border-color .2s ease,
    box-shadow .2s ease;
}

/* Barra superior */

#aparencia-preview .preview-header {
  min-height: 58px;

  display: flex;
  align-items: center;
  justify-content: space-between;

  padding: 0 22px;

  background:
    color-mix(
      in srgb,
      var(--preview-fundo, #ffffff) 94%,
      var(--preview-principal, #1e6f5c)
    );

  border-bottom: 1px solid
    color-mix(
      in srgb,
      var(--preview-principal, #1e6f5c) 12%,
      transparent
    );
}

/* Logo */

#aparencia-preview .preview-logo {
  display: flex;
  align-items: center;
  justify-content: center;

  width: 42px;
  height: 42px;

  flex-shrink: 0;

  overflow: hidden;
}

#aparencia-preview .preview-logo img {
  display: block;

  width: 42px;
  height: 42px;

  max-width: none;
  max-height: none;

  object-fit: contain;

  transition:
    width .15s ease,
    height .15s ease;
}

/* Nome */

#aparencia-preview #preview-nome {
  color: var(--preview-texto, #17211f);

  font-size: 14px;
  font-weight: 800;
}

#aparencia-preview #preview-subtitulo {
  color:
    color-mix(
      in srgb,
      var(--preview-texto, #17211f) 60%,
      transparent
    );

  font-size: 11px;
}

/* ConteÃºdo principal */

#aparencia-preview .preview-hero {
  min-height: 280px;

  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;

  padding: 50px 30px;

  text-align: center;

  background:
    radial-gradient(
      circle at 50% 0%,
      color-mix(
        in srgb,
        var(--preview-principal, #1e6f5c) 13%,
        transparent
      ),
      transparent 58%
    ),
    var(--preview-fundo, #ffffff);
}

/* TÃ­tulo */

#aparencia-preview #preview-titulo {
  max-width: 680px;

  margin: 0;

  color: var(--preview-texto, #17211f);

  font-size: clamp(26px, 4vw, 42px);
  line-height: 1.08;
  font-weight: 850;
  letter-spacing: -.8px;
}

/* DescriÃ§Ã£o */

#aparencia-preview #preview-descricao {
  max-width: 590px;

  margin: 16px auto 0;

  color:
    color-mix(
      in srgb,
      var(--preview-texto, #17211f) 68%,
      transparent
    );

  font-size: 14px;
  line-height: 1.7;
}

/* BotÃµes */

#aparencia-preview .preview-actions {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-wrap: wrap;

  gap: 10px;

  margin-top: 26px;
}

#aparencia-preview .preview-button {
  min-height: 42px;

  padding: 0 18px;

  border-radius: 10px;

  font-size: 12px;
  font-weight: 800;

  cursor: default;

  transition:
    transform .15s ease,
    box-shadow .15s ease;
}

#aparencia-preview .preview-primary {
  border: 1px solid var(--preview-principal, #1e6f5c);

  background: var(--preview-principal, #1e6f5c);

  color: #fff;

  box-shadow:
    0 7px 18px
    color-mix(
      in srgb,
      var(--preview-principal, #1e6f5c) 25%,
      transparent
    );
}

#aparencia-preview .preview-secondary {
  border: 1px solid
    color-mix(
      in srgb,
      var(--preview-principal, #1e6f5c) 30%,
      transparent
    );

  background: transparent;

  color: var(--preview-principal, #1e6f5c);
}

/* RodapÃ© */

#aparencia-preview .preview-footer {
  padding: 14px 22px;

  border-top: 1px solid
    color-mix(
      in srgb,
      var(--preview-texto, #17211f) 8%,
      transparent
    );

  color:
    color-mix(
      in srgb,
      var(--preview-texto, #17211f) 55%,
      transparent
    );

  font-size: 10px;

  text-align: center;
}

/* Indicador */

#aparencia-preview::before {
  content: "PRÃ‰-VISUALIZAÃ‡ÃƒO AO VIVO";

  position: absolute;
  top: 12px;
  right: 14px;

  z-index: 10;

  padding: 5px 8px;

  border-radius: 999px;

  background:
    color-mix(
      in srgb,
      var(--preview-principal, #1e6f5c) 12%,
      #ffffff
    );

  color: var(--preview-principal, #1e6f5c);

  font-size: 8px;
  font-weight: 900;
  letter-spacing: .5px;

  pointer-events: none;
}

/* Responsivo */

@media (max-width: 650px) {

  #aparencia-preview .preview-header {
    padding: 0 14px;
  }

  #aparencia-preview .preview-hero {
    padding: 38px 18px;
  }

  #aparencia-preview .preview-actions {
    flex-direction: column;
    width: 100%;
  }

  #aparencia-preview .preview-button {
    width: 100%;
  }

}


/* COMBO 3 - LAMINA */

/* Visual profissional da pÃ¡gina individual */

.slide-header {
  margin-bottom: 28px;
}

.slide-title {
  gap: 24px;
}

.slide-title h1 {
  letter-spacing: -1.5px;
}

.viewer-section {
  margin-top: 10px;
}

.viewer-layout {
  gap: 24px;
  align-items: start;
}

.viewer-container {
  overflow: hidden;
  border-radius: 18px;
  background: #101614;
  box-shadow: 0 18px 45px rgba(0,0,0,.12);
}

.viewer {
  min-height: 520px;
}

.viewer-controls {
  border-radius: 12px;
  padding: 6px;
  backdrop-filter: blur(10px);
}

.viewer-controls button {
  transition:
    transform .18s ease,
    background .18s ease;
}

.viewer-controls button:hover {
  transform: translateY(-2px);
}

.info-panel {
  position: sticky;
  top: 24px;

  border-radius: 18px;

  box-shadow:
    0 10px 30px rgba(0,0,0,.06);

  transition:
    transform .2s ease,
    box-shadow .2s ease;
}

.info-panel:hover {
  transform: translateY(-2px);

  box-shadow:
    0 15px 35px rgba(0,0,0,.09);
}

/* Mobile */

@media (max-width: 900px) {

  .viewer-layout {
    grid-template-columns: 1fr;
  }

  .info-panel {
    position: static;
  }

  .slide-title {
    flex-direction: column;
    align-items: flex-start;
  }

}

@media (max-width: 600px) {

  .slide-header {
    margin-bottom: 20px;
  }

  .slide-title h1 {
    font-size: 2rem;
    letter-spacing: -1px;
  }

  .viewer-container {
    border-radius: 14px;
  }

  .viewer {
    min-height: 330px;
  }

  .info-panel {
    padding: 18px;
    border-radius: 14px;
  }

}


/* ============================================================
   MARCAÃ‡Ã•ES DAS ESTRUTURAS DA LÃ‚MINA
   ============================================================ */

.estrutura-overlay {

  pointer-events: auto;

  overflow: visible;

}


.estrutura-marcacao {

  position: relative;

  width: 100%;

  height: 100%;

  overflow: visible;

}


.estrutura-caixa {

  position: absolute;

  inset: 0;

  border: 3px solid #ff3b30;

  background: rgba(255, 59, 48, 0.10);

  border-radius: 8px;

  box-shadow:
    0 0 0 1px rgba(255,255,255,.8),
    0 0 18px rgba(255,59,48,.45);

}


.estrutura-label {

  position: absolute;

  left: 50%;

  top: -10px;

  transform:
    translate(-50%, -100%);

  padding: 6px 10px;

  border-radius: 7px;

  background: #ff3b30;

  color: white;

  font-size: 13px;

  font-weight: 700;

  line-height: 1.2;

  white-space: nowrap;

  box-shadow:
    0 4px 14px rgba(0,0,0,.28);

  pointer-events: none;

}


.estrutura-marcacao:hover
.estrutura-caixa {

  border-color: #fff;

  background:
    rgba(255, 59, 48, 0.18);

}


.estrutura-marcacao:hover
.estrutura-label {

  background: #c62828;

}


/* Em telas pequenas */

@media (max-width: 700px) {

  .estrutura-label {

    font-size: 11px;

    padding: 4px 7px;

  }

}




/* =====================================================
   MARCADORES DAS ESTRUTURAS HISTOLÃ“GICAS
   ===================================================== */

.estrutura-overlay {
  pointer-events: none;
  overflow: visible;
  z-index: 50;
}

.estrutura-marcacao {
  position: relative;
  width: 100%;
  height: 100%;
  min-width: 18px;
  min-height: 18px;
  pointer-events: none;
}

.estrutura-ponto {
  position: absolute;
  inset: 0;
  border: 3px solid #ff3b30;
  border-radius: 50%;
  box-sizing: border-box;
  background: rgba(255, 59, 48, 0.08);
  box-shadow:
    0 0 0 2px rgba(255,255,255,0.85),
    0 0 14px rgba(255,59,48,0.65);
}

.estrutura-label {
  position: absolute;
  left: 50%;
  top: -34px;
  transform: translateX(-50%);

  padding: 5px 9px;

  background: rgba(20, 20, 20, 0.92);
  color: white;

  border-radius: 7px;

  font-size: 13px;
  font-weight: 700;

  white-space: nowrap;

  box-shadow:
    0 4px 14px rgba(0,0,0,0.35);

  border: 1px solid rgba(255,255,255,0.15);
}


/* ============================================================
   COMBO 5 - ESTRUTURAS INTERATIVAS
   ============================================================ */

.structure {
  cursor: pointer;
  transition:
    transform 0.2s ease,
    box-shadow 0.2s ease;
}

.structure:hover {
  transform: translateX(3px);
}

.structure.estrutura-selecionada {
  transform: translateX(5px);
  box-shadow:
    0 0 0 2px rgba(255,255,255,0.12),
    0 8px 24px rgba(0,0,0,0.12);
}

.estrutura-overlay {
  cursor: pointer;
}

.estrutura-overlay .estrutura-ponto {
  transition:
    transform 0.2s ease,
    box-shadow 0.2s ease;
}

.estrutura-overlay.estrutura-selecionada .estrutura-ponto {
  transform: scale(1.5);
  box-shadow:
    0 0 0 6px rgba(255,255,255,0.22),
    0 0 22px rgba(255,255,255,0.75);
}

.estrutura-overlay.estrutura-selecionada .estrutura-label {
  transform: scale(1.06);
  transform-origin: left center;
}


/* ============================================================
   COMBO 6 - NAVEGACAO ENTRE ESTRUTURAS
   ============================================================ */

.estrutura-navegacao {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  margin-top: 10px;
  margin-bottom: 10px;
}

.estrutura-nav-button {
  width: 38px;
  height: 34px;
  border: 0;
  border-radius: 8px;
  cursor: pointer;
  font-size: 18px;
  line-height: 1;
  transition:
    transform 0.18s ease,
    opacity 0.18s ease,
    box-shadow 0.18s ease;
}

.estrutura-nav-button:hover:not(:disabled) {
  transform: scale(1.06);
}

.estrutura-nav-button:disabled {
  opacity: 0.35;
  cursor: not-allowed;
}

.estrutura-contador {
  min-width: 125px;
  text-align: center;
  font-size: 13px;
  font-weight: 600;
  white-space: nowrap;
}


/* ============================================================
   COMBO 7 - MODO ESTUDO
   ============================================================ */

.estrutura-estudo-panel {
  position: absolute;
  left: 20px;
  bottom: 20px;
  width: min(360px, calc(100% - 40px));
  z-index: 30;
  padding: 18px;
  border-radius: 14px;
  background: rgba(15, 15, 15, 0.94);
  color: white;
  box-shadow:
    0 15px 45px rgba(0, 0, 0, 0.35);
  backdrop-filter: blur(12px);
}

.estrutura-estudo-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}

.estrutura-estudo-eyebrow {
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 0.12em;
  opacity: 0.65;
}

#estrutura-estudo-fechar {
  width: 30px;
  height: 30px;
  border: 0;
  border-radius: 50%;
  cursor: pointer;
  font-size: 20px;
  line-height: 1;
}

.estrutura-estudo-panel h3 {
  margin: 10px 0 6px;
  font-size: 20px;
}

.estrutura-estudo-panel p {
  margin: 0;
  line-height: 1.55;
  font-size: 14px;
  opacity: 0.8;
}

@media (max-width: 700px) {

  .estrutura-estudo-panel {
    left: 10px;
    right: 10px;
    bottom: 10px;
    width: auto;
  }

}


/* ============================================================
   PAINEL DE ESTUDO - ESTILO FINAL
   ============================================================ */

#estrutura-estudo-panel.estrutura-estudo-panel-refinado {
  box-sizing: border-box !important;
  width: 100% !important;
  margin-top: 16px !important;
  padding: 20px !important;

  background: #ffffff !important;
  border: 1px solid #e2e6eb !important;
  border-radius: 16px !important;

  box-shadow:
    0 8px 24px rgba(0, 0, 0, .08) !important;

  color: #1f2937 !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
.estrutura-estudo-header {
  display: flex !important;
  align-items: center !important;
  justify-content: space-between !important;
  gap: 12px !important;
  margin-bottom: 12px !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
.estrutura-estudo-eyebrow {
  display: block !important;

  font-size: 10px !important;
  font-weight: 800 !important;
  letter-spacing: .10em !important;
  text-transform: uppercase !important;

  color: #6b7280 !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-fechar {
  flex: 0 0 auto !important;

  width: 32px !important;
  height: 32px !important;

  border: 0 !important;
  border-radius: 50% !important;

  font-size: 22px !important;
  line-height: 1 !important;

  cursor: pointer !important;

  background: #f1f3f5 !important;
  color: #555 !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-fechar:hover {
  background: #e5e7eb !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-nome {
  margin: 0 0 8px 0 !important;

  font-size: 23px !important;
  font-weight: 750 !important;
  line-height: 1.2 !important;

  color: #111827 !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-descricao {
  margin: 0 !important;

  font-size: 15px !important;
  line-height: 1.65 !important;

  color: #4b5563 !important;
}


/* PAINEL DE ESTUDO - COMPACTO */

#estrutura-estudo-panel.estrutura-estudo-panel-refinado {
  padding: 14px !important;
  border-radius: 12px !important;
  margin-top: 10px !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
.estrutura-estudo-header {
  margin-bottom: 7px !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
.estrutura-estudo-eyebrow {
  font-size: 9px !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-nome {
  margin-bottom: 5px !important;
  font-size: 18px !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-descricao {
  font-size: 13px !important;
  line-height: 1.45 !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-fechar {
  width: 27px !important;
  height: 27px !important;
  font-size: 18px !important;
}


/* ============================================================
   PAINEL DE ESTUDO - COMPACTO DE VERDADE
   ============================================================ */

#estrutura-estudo-panel.estrutura-estudo-panel-refinado {
  width: auto !important;
  max-width: 360px !important;
  padding: 11px 14px !important;
  margin: 8px auto 0 0 !important;
  border-radius: 10px !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
.estrutura-estudo-header {
  margin-bottom: 4px !important;
  gap: 8px !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
.estrutura-estudo-eyebrow {
  font-size: 8px !important;
  letter-spacing: .07em !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-nome {
  font-size: 16px !important;
  margin: 0 0 3px 0 !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-descricao {
  font-size: 12px !important;
  line-height: 1.35 !important;
}

#estrutura-estudo-panel.estrutura-estudo-panel-refinado
#estrutura-estudo-fechar {
  width: 24px !important;
  height: 24px !important;
  font-size: 16px !important;
}


/* ============================================================
   DESTAQUE DA ESTRUTURA SELECIONADA
   ============================================================ */

.structure {
  transition:
    background .18s ease,
    border-color .18s ease,
    box-shadow .18s ease,
    transform .18s ease !important;
}

.structure.estrutura-selecionada {
  background: #f1f5f9 !important;
  border-color: #94a3b8 !important;
  box-shadow: 0 3px 10px rgba(0, 0, 0, .08) !important;
  transform: translateX(2px) !important;
}

.structure.estrutura-selecionada .structure-dot {
  transform: scale(1.15) !important;
}


/* ATLAS EDITOR DE ESTRUTURAS V2 */

.estrutura-editor-toolbar {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16px;
  margin-bottom: 18px;
}

.estrutura-ferramentas {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 8px;
}

.estrutura-tool {
  border: 1px solid rgba(0,0,0,.12);
  background: #fff;
  border-radius: 10px;
  padding: 9px 13px;
  cursor: pointer;
  transition: .15s ease;
}

.estrutura-tool:hover {
  transform: translateY(-1px);
}

.estrutura-tool.active {
  border-color: currentColor;
  box-shadow: 0 0 0 2px rgba(0,0,0,.06);
}

.estrutura-imagem-editor {
  position: relative;
  width: 100%;
  min-height: 320px;
  margin-top: 18px;
  overflow: hidden;
  border-radius: 14px;
  background: #111;
  border: 1px solid rgba(255,255,255,.08);
  user-select: none;
  touch-action: none;
}

.estrutura-imagem-wrapper {
  position: relative;
  display: inline-block;
  width: 100%;
  line-height: 0;
}

.estrutura-imagem {
  display: block;
  width: 100%;
  height: auto;
  max-height: 70vh;
  object-fit: contain;
  margin: 0 auto;
}

.estrutura-imagem-overlay {
  position: absolute;
  inset: 0;
  cursor: crosshair;
  touch-action: none;
}

.estrutura-imagem-placeholder {
  min-height: 320px;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 30px;
  text-align: center;
  color: rgba(255,255,255,.75);
}

.estrutura-editor-marcacao {
  position: absolute;
  box-sizing: border-box;
  pointer-events: auto;
}

.estrutura-editor-ponto {
  border: 3px solid #fff;
  border-radius: 50%;
  background: rgba(220,40,40,.85);
  box-shadow: 0 0 0 2px rgba(0,0,0,.5);
}

.estrutura-editor-retangulo {
  border: 3px solid #fff;
  background: rgba(220,40,40,.16);
  box-shadow: inset 0 0 0 1px rgba(0,0,0,.35);
}

.estrutura-editor-seta {
  height: 4px;
  background: #fff;
  border-radius: 999px;
  transform-origin: 0 50%;
  box-shadow: 0 0 0 1px rgba(0,0,0,.5);
}

.estrutura-editor-seta::after {
  content: "";
  position: absolute;
  right: -1px;
  top: 50%;
  width: 0;
  height: 0;
  border-top: 7px solid transparent;
  border-bottom: 7px solid transparent;
  border-left: 12px solid #fff;
  transform: translateY(-50%);
}

.estrutura-editor-texto {
  color: #fff;
  font-weight: 700;
  white-space: nowrap;
  text-shadow:
    0 1px 3px #000,
    0 -1px 3px #000;
  transform: translate(-50%, -50%);
}

.estrutura-preview {
  position: absolute;
  pointer-events: none;
  box-sizing: border-box;
}

.estrutura-preview-ponto {
  border: 3px solid #fff;
  border-radius: 50%;
  background: rgba(220,40,40,.85);
}

.estrutura-preview-retangulo {
  border: 3px dashed #fff;
  background: rgba(220,40,40,.15);
}

.estrutura-preview-seta {
  height: 4px;
  background: #fff;
  border-radius: 999px;
  transform-origin: 0 50%;
}

.estrutura-preview-seta::after {
  content: "";
  position: absolute;
  right: -1px;
  top: 50%;
  width: 0;
  height: 0;
  border-top: 7px solid transparent;
  border-bottom: 7px solid transparent;
  border-left: 12px solid #fff;
  transform: translateY(-50%);
}

.estrutura-preview-texto {
  color: #fff;
  font-weight: 700;
  text-shadow: 0 1px 3px #000;
}

.estrutura-editor-ajuda {
  margin-top: 10px;
  font-size: .9rem;
  opacity: .75;
}

.estrutura-texto-editor {
  margin-top: 12px;
}

.estrutura-overlay {
  box-sizing: border-box;
  pointer-events: auto;
}

.estrutura-overlay-ponto {
  overflow: visible;
}

.estrutura-overlay-retangulo {
  overflow: visible;
}

.estrutura-overlay-seta {
  height: 4px;
  background: #fff;
  border-radius: 999px;
  transform-origin: 0 50%;
  box-shadow: 0 0 0 1px rgba(0,0,0,.5);
}

.estrutura-overlay-seta::after {
  content: "";
  position: absolute;
  right: -1px;
  top: 50%;
  width: 0;
  height: 0;
  border-top: 7px solid transparent;
  border-bottom: 7px solid transparent;
  border-left: 12px solid #fff;
  transform: translateY(-50%);
}

.estrutura-overlay-texto {
  overflow: visible;
}

.estrutura-texto-label {
  display: block;
  color: #fff;
  font-weight: 700;
  white-space: nowrap;
  text-shadow:
    0 1px 3px #000,
    0 -1px 3px #000;
  transform: translateY(-50%);
}

.estrutura-box-retangulo {
  width: 100%;
  height: 100%;
  border: 3px solid #fff;
  background: rgba(220,40,40,.12);
  box-sizing: border-box;
}

.estrutura-box-ponto {
  width: 100%;
  height: 100%;
  position: relative;
}

.estrutura-box-ponto .estrutura-ponto {
  position: absolute;
  left: 50%;
  top: 50%;
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: rgba(220,40,40,.9);
  border: 3px solid #fff;
  transform: translate(-50%, -50%);
  box-shadow: 0 0 0 2px rgba(0,0,0,.5);
}

@media (max-width: 700px) {

  .estrutura-editor-toolbar {
    grid-template-columns: 1fr;
  }

  .estrutura-ferramentas {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
  }

}



===== backup-reparador-completo-20260819-212337\src\style.css.backup-modo-estudo-20260819-145850.css =====
:root {
  --cor-principal: #1f6feb;
  --cor-fundo: #f7f9fc;
  --cor-texto: #172033;
  --cor-destaque: #22c55e;
}
:root {
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  color: #17201d;
  background: #f5f7f5;
  font-synthesis: none;
  text-rendering: optimizeLegibility;
  --green: #174c3c;
  --green-light: #2d755d;
  --mint: #dcefe7;
  --cream: #f5f7f5;
  --white: #ffffff;
  --text: #17201d;
  --muted: #66736d;
  --border: #dfe6e2;
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  scroll-behavior: smooth;
}

body {
  min-width: 320px;
  background: var(--cream);
  color: var(--text);
}

button,
a {
  font: inherit;
}

button {
  border: 0;
  cursor: pointer;
}

a {
  color: inherit;
  text-decoration: none;
}

.container {
  width: min(1160px, calc(100% - 48px));
  margin: 0 auto;
}

.header {
  position: sticky;
  top: 0;
  z-index: 20;
  background: rgba(245, 247, 245, 0.9);
  backdrop-filter: blur(16px);
  border-bottom: 1px solid rgba(223, 230, 226, 0.8);
}

.header-content {
  height: 76px;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.brand {
  display: flex;
  align-items: center;
  gap: 11px;
}

.brand-icon {
  width: 42px;
  height: 42px;
  display: grid;
  place-items: center;
  border-radius: 12px;
  background: var(--green);
  color: white;
  font-size: 20px;
}

.brand strong,
.brand small {
  display: block;
}

.brand strong {
  font-size: 17px;
  letter-spacing: -0.4px;
}

.brand small {
  color: var(--muted);
  font-size: 11px;
  letter-spacing: 1.5px;
  text-transform: uppercase;
}

.nav {
  display: flex;
  gap: 34px;
  color: #53605a;
  font-size: 14px;
}

.nav a {
  transition: color 0.2s ease;
}

.nav a:hover {
  color: var(--green);
}

.menu-button {
  display: none;
  background: transparent;
  font-size: 22px;
}

.hero {
  padding: 100px 0 90px;
  overflow: hidden;
}

.hero-grid {
  display: grid;
  grid-template-columns: 1fr 0.9fr;
  align-items: center;
  gap: 70px;
}

.eyebrow {
  display: inline-block;
  margin-bottom: 18px;
  color: var(--green-light);
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 2px;
}

.hero h1 {
  max-width: 650px;
  font-size: clamp(48px, 6vw, 76px);
  line-height: 0.98;
  letter-spacing: -4px;
}

.hero h1 span {
  display: block;
  color: var(--green);
}

.hero-text > p {
  max-width: 570px;
  margin-top: 28px;
  color: var(--muted);
  font-size: 17px;
  line-height: 1.7;
}

.hero-actions {
  display: flex;
  gap: 12px;
  margin-top: 34px;
}

.button {
  display: inline-flex;
  align-items: center;
  gap: 20px;
  padding: 14px 20px;
  border-radius: 10px;
  font-size: 14px;
  font-weight: 700;
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.button:hover {
  transform: translateY(-2px);
}

.button.primary {
  background: var(--green);
  color: white;
  box-shadow: 0 12px 30px rgba(23, 76, 60, 0.18);
}

.button.secondary {
  border: 1px solid var(--border);
  background: white;
}

.hero-visual {
  min-height: 480px;
  display: grid;
  place-items: center;
}

.microscope-card {
  width: min(430px, 100%);
  height: 430px;
  position: relative;
  overflow: hidden;
  border-radius: 28px;
  background: #dbece5;
  border: 1px solid #cbded5;
}

.microscope-glow {
  position: absolute;
  width: 270px;
  height: 270px;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.65);
  filter: blur(8px);
}

.microscope {
  position: absolute;
  width: 190px;
  height: 280px;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -45%);
}

.microscope-top {
  position: absolute;
  width: 80px;
  height: 115px;
  right: 28px;
  top: 10px;
  border-radius: 30px 30px 12px 12px;
  background: #263e36;
  transform: rotate(23deg);
}

.microscope-body {
  position: absolute;
  width: 75px;
  height: 190px;
  left: 53px;
  top: 55px;
  border-radius: 45px 20px 30px 25px;
  background: #345b4e;
  transform: rotate(22deg);
}

.microscope-base {
  position: absolute;
  width: 190px;
  height: 38px;
  bottom: 0;
  border-radius: 30px 30px 14px 14px;
  background: #263e36;
}

.microscope-lens {
  position: absolute;
  width: 100px;
  height: 22px;
  left: 45px;
  bottom: 66px;
  border-radius: 15px;
  background: #1d302a;
}

.floating-tag {
  position: absolute;
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 9px 13px;
  border-radius: 10px;
  background: rgba(255,255,255,0.88);
  backdrop-filter: blur(8px);
  box-shadow: 0 10px 30px rgba(40, 70, 60, 0.12);
  font-size: 12px;
  font-weight: 700;
}

.floating-tag span {
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: var(--green-light);
}

.tag-one {
  left: 24px;
  top: 75px;
}

.tag-two {
  right: 22px;
  top: 155px;
}

.tag-three {
  left: 30px;
  bottom: 70px;
}

.scan-line {
  position: absolute;
  width: 100%;
  height: 1px;
  top: 50%;
  background: rgba(23, 76, 60, 0.12);
}

.section {
  padding: 95px 0;
  background: white;
}

.section-heading {
  display: flex;
  align-items: end;
  justify-content: space-between;
  gap: 40px;
  margin-bottom: 40px;
}

.section-heading h2,
.about h2 {
  font-size: clamp(32px, 4vw, 48px);
  line-height: 1.05;
  letter-spacing: -2px;
}

.section-heading p {
  max-width: 390px;
  color: var(--muted);
  line-height: 1.6;
}

.tissue-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 16px;
}

.tissue-card {
  position: relative;
  min-height: 290px;
  padding: 26px;
  overflow: hidden;
  border: 1px solid var(--border);
  border-radius: 18px;
  background: var(--cream);
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}

.tissue-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 18px 40px rgba(23, 76, 60, 0.09);
}

.card-number {
  color: #a4b0ab;
  font-size: 12px;
  font-weight: 800;
}

.card-icon {
  margin-top: 48px;
  color: var(--green);
  font-size: 34px;
}

.tissue-card h3 {
  margin-top: 20px;
  font-size: 19px;
}

.tissue-card p {
  margin-top: 8px;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.5;
}

.tissue-card button {
  position: absolute;
  left: 26px;
  bottom: 24px;
  background: transparent;
  color: var(--green);
  font-size: 13px;
  font-weight: 800;
}

.tissue-card button span {
  margin-left: 8px;
}

.about {
  padding: 105px 0;
  background: #eaf2ee;
}

.about-grid {
  display: grid;
  grid-template-columns: 0.8fr 1fr;
  gap: 100px;
}

.about-text > p {
  color: #56645e;
  font-size: 17px;
  line-height: 1.7;
}

.features {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 25px;
  margin-top: 35px;
}

.features div {
  padding-top: 20px;
  border-top: 1px solid #cbdad3;
}

.features strong,
.features span {
  display: block;
}

.features strong {
  font-size: 14px;
}

.features span {
  margin-top: 7px;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.5;
}

.footer {
  padding: 25px 0;
  background: var(--green);
  color: white;
}

.footer .container {
  display: flex;
  justify-content: space-between;
  color: rgba(255,255,255,0.7);
  font-size: 12px;
}

.footer span:first-child {
  color: white;
  font-weight: 700;
}

@media (max-width: 850px) {
  .hero-grid,
  .about-grid {
    grid-template-columns: 1fr;
    gap: 55px;
  }

  .hero {
    padding-top: 65px;
  }

  .hero-visual {
    min-height: 400px;
  }

  .tissue-grid {
    grid-template-columns: 1fr 1fr;
  }
}

@media (max-width: 600px) {
  .container {
    width: min(100% - 32px, 1160px);
  }

  .nav {
    display: none;
  }

  .menu-button {
    display: block;
  }

  .hero h1 {
    font-size: 50px;
    letter-spacing: -3px;
  }

  .hero-actions {
    flex-direction: column;
  }

  .button {
    justify-content: center;
  }

  .section-heading {
    display: block;
  }

  .section-heading p {
    margin-top: 15px;
  }

  .tissue-grid {
    grid-template-columns: 1fr;
  }

  .about-grid {
    gap: 35px;
  }

  .features {
    grid-template-columns: 1fr;
  }

  .footer .container {
    flex-direction: column;
    gap: 8px;
  }
}

.slide-header {
  padding: 55px 0 35px;
  background: white;
  border-bottom: 1px solid var(--border);
}

.back-link {
  display: inline-block;
  margin-bottom: 30px;
  color: var(--muted);
  font-size: 13px;
}

.slide-title {
  display: flex;
  justify-content: space-between;
  align-items: end;
  gap: 40px;
}

.slide-title h1 {
  font-size: clamp(36px, 5vw, 58px);
  letter-spacing: -3px;
  line-height: 1;
}

.slide-title p {
  margin-top: 15px;
  color: var(--muted);
}

.slide-meta {
  display: flex;
  flex-direction: column;
  gap: 8px;
  color: var(--muted);
  font-size: 12px;
}

.viewer-section {
  padding: 40px 0 80px;
  background: #f1f4f2;
}

.viewer-layout {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 340px;
  gap: 24px;
  align-items: start;
}

.viewer-container {
  overflow: hidden;
  border: 1px solid var(--border);
  border-radius: 18px;
  background: #101513;
  box-shadow: 0 20px 50px rgba(0,0,0,0.08);
}

.viewer {
  width: 100%;
  height: 650px;
  background: #101513;
}

.viewer-controls {
  min-height: 58px;
  padding: 10px 14px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 15px;
  background: #18201d;
  color: #aab7b1;
  font-size: 11px;
}

.zoom-buttons {
  display: flex;
  gap: 5px;
}

.zoom-buttons button {
  width: 38px;
  height: 34px;
  border-radius: 7px;
  background: #29332f;
  color: white;
  font-size: 18px;
}

.zoom-buttons button:hover {
  background: var(--green-light);
}

.info-panel {
  padding: 28px;
  border: 1px solid var(--border);
  border-radius: 18px;
  background: white;
}

.panel-label {
  margin-bottom: 15px;
  color: var(--green-light);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 1.7px;
}

.info-panel h2 {
  font-size: 25px;
  line-height: 1.1;
}

.description {
  margin-top: 15px;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.7;
}

.info-section {
  margin-top: 30px;
}

.info-section h3 {
  font-size: 14px;
}

.structure-list {
  margin-top: 15px;
  display: grid;
  gap: 8px;
}

.structure {
  width: 100%;
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px;
  text-align: left;
  border-radius: 10px;
  background: #f4f7f5;
}

.structure:hover {
  background: #e8f1ec;
}

.structure-dot {
  width: 9px;
  height: 9px;
  flex: 0 0 9px;
  border-radius: 50%;
  background: var(--green-light);
}

.structure strong,
.structure small {
  display: block;
}

.structure strong {
  font-size: 12px;
}

.structure small {
  margin-top: 3px;
  color: var(--muted);
  font-size: 10px;
}

.content-button {
  width: 100%;
  margin-top: 25px;
  padding: 14px;
  display: flex;
  justify-content: space-between;
  border-radius: 10px;
  background: var(--green);
  color: white;
  font-size: 12px;
  font-weight: 700;
}

.learning-section {
  padding: 90px 0;
  background: white;
}

.learning-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
}

.learning-card {
  padding: 35px;
  border: 1px solid var(--border);
  border-radius: 18px;
}

.learning-card h2 {
  font-size: 28px;
  letter-spacing: -1px;
}

.learning-card > p {
  max-width: 500px;
  margin-top: 12px;
  color: var(--muted);
  line-height: 1.6;
  font-size: 14px;
}

.video-placeholder {
  height: 190px;
  margin-top: 25px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 10px;
  border-radius: 12px;
  background: #edf2ef;
  color: var(--green);
}

.video-placeholder span {
  font-size: 35px;
}

.video-placeholder small {
  color: var(--muted);
}

.learning-card .button {
  margin-top: 25px;
}

@media (max-width: 900px) {
  .viewer-layout {
    grid-template-columns: 1fr;
  }

  .slide-title {
    display: block;
  }

  .slide-meta {
    margin-top: 25px;
  }

  .learning-grid {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 600px) {
  .viewer {
    height: 450px;
  }

  .viewer-controls {
    flex-direction: column;
    align-items: stretch;
  }

  .zoom-buttons {
    justify-content: center;
  }
}

.card-link {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  margin-top: 15px;
  color: var(--green-light);
  font-size: 12px;
  font-weight: 800;
  text-decoration: none;
}

.card-link span {
  transition: transform 0.2s ease;
}

.card-link:hover span {
  transform: translateX(4px);
}

.tissue-card .eyebrow {
  display: block;
  margin-top: 10px;
}

.auth-page {
  min-height: 100vh;
  display: grid;
  place-items: center;
  padding: 30px;
  background: #f1f4f2;
}

.auth-card {
  width: min(430px, 100%);
  padding: 45px;
  border: 1px solid var(--border);
  border-radius: 20px;
  background: white;
  box-shadow: 0 25px 70px rgba(0,0,0,.08);
}

.auth-logo {
  margin-bottom: 20px;
  font-size: 42px;
}

.auth-card h1 {
  margin-top: 8px;
  font-size: 34px;
}

.auth-card > p {
  margin: 12px 0 30px;
  color: var(--muted);
  line-height: 1.6;
  font-size: 13px;
}

.auth-card form {
  display: grid;
  gap: 18px;
}

.auth-card label {
  display: grid;
  gap: 7px;
  font-size: 12px;
  font-weight: 700;
}

.auth-card input {
  width: 100%;
  padding: 13px 14px;
  border: 1px solid var(--border);
  border-radius: 9px;
  outline: none;
  font: inherit;
}

.auth-card input:focus {
  border-color: var(--green-light);
}

#login-error {
  color: #b43b3b;
  font-size: 12px;
}

.auth-card .back-link {
  margin-top: 25px;
}

.admin-page {
  min-height: calc(100vh - 75px);
  padding: 60px 0 100px;
  background: #f1f4f2;
}

.admin-header {
  display: flex;
  align-items: center;
  gap: 15px;
}

.admin-user {
  color: var(--muted);
  font-size: 11px;
}

.logout-button {
  padding: 8px 13px;
  border: 1px solid var(--border);
  border-radius: 7px;
  background: white;
  font-size: 11px;
}

.admin-title {
  display: flex;
  align-items: end;
  justify-content: space-between;
  gap: 30px;
}

.admin-title h1 {
  margin-top: 8px;
  font-size: 42px;
  letter-spacing: -2px;
}

.admin-title p {
  margin-top: 10px;
  color: var(--muted);
}

.admin-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 15px;
  margin-top: 40px;
}

.admin-stat {
  padding: 25px;
  border: 1px solid var(--border);
  border-radius: 14px;
  background: white;
}

.admin-stat span,
.admin-stat strong,
.admin-stat small {
  display: block;
}

.admin-stat span {
  font-size: 20px;
}

.admin-stat strong {
  margin-top: 12px;
  font-size: 30px;
}

.admin-stat small {
  margin-top: 3px;
  color: var(--muted);
}

.admin-section {
  margin-top: 50px;
}

.admin-section-header h2 {
  margin-top: 6px;
}

.admin-list {
  margin-top: 18px;
  display: grid;
  gap: 8px;
}

.admin-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 18px 20px;
  border: 1px solid var(--border);
  border-radius: 12px;
  background: white;
}

.admin-item strong,
.admin-item small {
  display: block;
}

.admin-item small {
  margin-top: 4px;
  color: var(--muted);
  font-size: 11px;
}

.status {
  padding: 6px 10px;
  border-radius: 20px;
  background: #f0f0f0;
  color: var(--muted);
  font-size: 10px;
}

.status.published {
  background: #e5f3ea;
  color: #267244;
}

.empty-state {
  padding: 60px 20px;
  text-align: center;
  border: 1px dashed var(--border);
  border-radius: 14px;
  background: white;
}

.empty-state span {
  font-size: 35px;
}

.empty-state h3 {
  margin-top: 12px;
}

.empty-state p {
  margin-top: 7px;
  color: var(--muted);
  font-size: 13px;
}

@media (max-width: 700px) {

  .admin-title {
    display: block;
  }

  .admin-title .button {
    margin-top: 20px;
  }

  .admin-grid {
    grid-template-columns: 1fr;
  }

  .admin-user {
    display: none;
  }

  .auth-card {
    padding: 30px;
  }

}

.lamina-form {
  max-width: 900px;
  margin-top: 40px;
}

.form-card {
  margin-bottom: 20px;
  padding: 30px;
  border: 1px solid var(--border);
  border-radius: 16px;
  background: white;
}

.form-card h2 {
  margin-top: 7px;
  margin-bottom: 25px;
}

.form-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
}

.form-grid .full {
  grid-column: 1 / -1;
}

.form-card label {
  display: grid;
  gap: 8px;
  font-size: 12px;
  font-weight: 700;
}

.form-card input,
.form-card textarea {
  width: 100%;
  padding: 13px 14px;
  border: 1px solid var(--border);
  border-radius: 9px;
  outline: none;
  font: inherit;
  resize: vertical;
}

.form-card input:focus,
.form-card textarea:focus {
  border-color: var(--green-light);
}

.form-help {
  color: var(--muted);
  font-size: 13px;
  line-height: 1.6;
  margin-bottom: 20px;
}

.upload-area {
  min-height: 190px;
  display: flex !important;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 7px;
  padding: 30px;
  border: 2px dashed var(--border);
  border-radius: 12px;
  cursor: pointer;
  text-align: center;
}

.upload-area:hover {
  border-color: var(--green-light);
  background: #fafcfb;
}

.upload-area input {
  display: none;
}

.upload-icon {
  font-size: 36px;
}

.upload-area strong {
  font-size: 13px;
}

.upload-area small {
  color: var(--muted);
  font-size: 11px;
}

.image-preview {
  display: block;
  width: 100%;
  max-height: 400px;
  object-fit: contain;
  margin-top: 20px;
  border-radius: 10px;
  background: #111;
}

.publish-option {
  display: flex !important;
  grid-template-columns: auto 1fr;
  grid-template-rows: auto;
  gap: 12px !important;
  align-items: flex-start;
  cursor: pointer;
}

.publish-option input {
  width: auto;
  margin-top: 3px;
}

.publish-option span {
  display: grid;
  gap: 4px;
}

.publish-option small {
  color: var(--muted);
  font-weight: 400;
  line-height: 1.5;
}

.form-actions {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  margin-top: 25px;
}

.success-message {
  padding: 14px 16px;
  margin-top: 20px;
  border-radius: 9px;
  background: #e5f3ea;
  color: #267244;
  font-size: 13px;
}

#form-message:not(.success-message) {
  margin-top: 15px;
  color: #b43b3b;
  font-size: 12px;
}

@media (max-width: 700px) {

  .form-grid {
    grid-template-columns: 1fr;
  }

  .form-grid .full {
    grid-column: auto;
  }

  .form-card {
    padding: 22px;
  }

}

.catalog-page {
  min-height: 100vh;
  background: #f7f8f6;
}

.catalog-hero {
  padding: 80px 0 65px;
  background: white;
  border-bottom: 1px solid var(--border);
}

.catalog-hero h1 {
  margin: 12px 0;
  font-size: clamp(36px, 5vw, 64px);
  letter-spacing: -2px;
}

.catalog-hero p {
  max-width: 650px;
  color: var(--muted);
  font-size: 17px;
  line-height: 1.7;
}

.catalog-content {
  padding: 50px 0 80px;
}

.laminas-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
}

.lamina-card {
  overflow: hidden;
  background: white;
  border: 1px solid var(--border);
  border-radius: 18px;
  transition: transform .2s ease, box-shadow .2s ease;
}

.lamina-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 15px 35px rgba(0,0,0,.08);
}

.lamina-image {
  position: relative;
  height: 230px;
  overflow: hidden;
  background: #111;
}

.lamina-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform .4s ease;
}

.lamina-card:hover .lamina-image img {
  transform: scale(1.06);
}

.zoom-badge {
  position: absolute;
  right: 12px;
  bottom: 12px;
  padding: 8px 11px;
  border-radius: 8px;
  background: rgba(0,0,0,.72);
  color: white;
  font-size: 11px;
  font-weight: 700;
}

.lamina-card-content {
  padding: 22px;
}

.lamina-category {
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 1px;
  text-transform: uppercase;
  color: var(--green-light);
}

.lamina-card h2 {
  margin: 8px 0;
  font-size: 21px;
}

.lamina-card p {
  min-height: 55px;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.6;
}

.explore-lamina {
  width: 100%;
  margin-top: 10px;
}

.loading-state,
.error-state,
.empty-state {
  padding: 60px 20px;
  text-align: center;
  color: var(--muted);
}

.empty-state span {
  display: block;
  margin-bottom: 15px;
  font-size: 40px;
}

.empty-state h3 {
  margin-bottom: 8px;
  color: var(--text);
}

@media (max-width: 900px) {

  .laminas-grid {
    grid-template-columns: repeat(2, 1fr);
  }

}

@media (max-width: 600px) {

  .laminas-grid {
    grid-template-columns: 1fr;
  }

}



/* =========================================================
   LOGO DA PRÉVIA DO EDITOR DE APARÊNCIA
   ========================================================= */

.preview-logo {
  width: 48px;
  height: 48px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  overflow: visible;
}

.preview-logo img {
  width: 42px;
  height: 42px;
  max-width: none;
  max-height: none;
  object-fit: contain;
  display: block;
  transform-origin: center center;
  transition: transform 0.15s ease;
}
 
.logo-preview {
  width: 120px;
  height: 120px;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: visible;
}

.logo-preview img {
  width: 90px;
  height: 90px;
  max-width: none;
  max-height: none;
  object-fit: contain;
  display: block;
  transform-origin: center center;
  transition: transform 0.15s ease;
}


/* =========================================================
   APARÊNCIA — EDITOR MODERNO
   ========================================================= */

#admin-section-aparencia {
  max-width: 1180px;
}

#aparencia-form {
  display: flex;
  flex-direction: column;
  gap: 22px;
}

#aparencia-form .form-card {
  background: #fff;
  border: 1px solid #e4e9e7;
  border-radius: 18px;
  padding: 26px;
  box-shadow: 0 4px 18px rgba(16,47,41,.04);
}

#aparencia-form .form-card-title {
  display: flex;
  align-items: flex-start;
  gap: 14px;
  margin-bottom: 24px;
}

#aparencia-form .form-card-title > span {
  width: 42px;
  height: 42px;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #edf6f2;
  border-radius: 12px;
  font-size: 20px;
}

#aparencia-form .form-card-title h2 {
  margin: 2px 0 5px;
  font-size: 18px;
  color: #17211f;
}

#aparencia-form .form-card-title p {
  margin: 0;
  color: #7a8581;
  font-size: 13px;
}

#aparencia-form .form-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0,1fr));
  gap: 20px;
}

#aparencia-form .form-field.full {
  grid-column: 1 / -1;
}

#aparencia-form .form-field label {
  display: block;
  margin-bottom: 8px;
  color: #34413d;
  font-size: 13px;
  font-weight: 700;
}

#aparencia-form input[type="text"],
#aparencia-form input[type="url"],
#aparencia-form textarea {
  width: 100%;
  box-sizing: border-box;
  border: 1px solid #dce4e0;
  background: #fbfcfc;
  border-radius: 10px;
  padding: 12px 14px;
  color: #17211f;
  font: inherit;
  outline: none;
  transition: .18s ease;
}

#aparencia-form textarea {
  resize: vertical;
  min-height: 95px;
}

#aparencia-form input[type="text"]:focus,
#aparencia-form input[type="url"]:focus,
#aparencia-form textarea:focus {
  background: #fff;
  border-color: #1e6f5c;
  box-shadow: 0 0 0 3px rgba(30,111,92,.10);
}

#aparencia-form input[type="range"] {
  width: 100%;
  accent-color: #1e6f5c;
  cursor: pointer;
}

#aparencia-form #logo-tamanho-valor {
  display: inline-block;
  min-width: 55px;
  padding: 6px 9px;
  background: #edf6f2;
  color: #1e6f5c;
  border-radius: 8px;
  text-align: center;
  font-size: 12px;
  font-weight: 800;
}

#aparencia-form input[type="file"] {
  width: 100%;
  box-sizing: border-box;
  padding: 10px;
  border: 1px dashed #cbd8d3;
  border-radius: 12px;
  background: #f8faf9;
  color: #66736e;
  cursor: pointer;
}

#aparencia-form input[type="color"] {
  width: 52px;
  height: 42px;
  padding: 3px;
  border: 1px solid #dce4e0;
  border-radius: 10px;
  background: #fff;
  cursor: pointer;
}

#logo-preview {
  min-height: 120px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 12px;
  padding: 18px;
  border: 1px solid #e1e7e4;
  border-radius: 14px;
  background: #f7f9f8;
  overflow: hidden;
}

#logo-preview img {
  max-width: 100%;
  max-height: 120px;
  object-fit: contain;
}

#logo-nome {
  display: block;
  margin-top: 8px;
  color: #78837f;
  font-size: 12px;
}

#remover-logo {
  margin-top: 10px;
  border: 0;
  border-radius: 9px;
  padding: 9px 13px;
  background: #fff1f1;
  color: #a33b3b;
  font-size: 12px;
  font-weight: 700;
  cursor: pointer;
}

#remover-logo:hover {
  background: #ffe5e5;
}

.aparencia-preview-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 14px;
}

.preview-live {
  padding: 6px 10px;
  border-radius: 20px;
  background: #e9f7ef;
  color: #14734d;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: .5px;
}

.aparencia-preview {
  overflow: hidden;
  border: 1px solid #dfe6e3;
  border-radius: 18px;
  box-shadow: 0 10px 30px rgba(16,47,41,.08);
}

#aparencia-form .form-actions {
  position: sticky;
  bottom: 18px;
  z-index: 5;
  display: flex;
  justify-content: flex-end;
  padding: 14px 16px;
  background: rgba(255,255,255,.94);
  backdrop-filter: blur(12px);
  border: 1px solid #e1e7e4;
  border-radius: 14px;
  box-shadow: 0 8px 30px rgba(16,47,41,.10);
}

@media (max-width: 750px) {
  #aparencia-form .form-grid {
    grid-template-columns: 1fr;
  }

  #aparencia-form .form-field.full {
    grid-column: auto;
  }

  #aparencia-form .form-card {
    padding: 20px;
  }
}


/* =========================================================
   APARÊNCIA — EDITOR MODERNO
   ========================================================= */

#admin-section-aparencia {
  max-width: 1180px;
}

#aparencia-form {
  display: flex;
  flex-direction: column;
  gap: 22px;
}

#aparencia-form .form-card {
  background: #fff;
  border: 1px solid #e4e9e7;
  border-radius: 18px;
  padding: 26px;
  box-shadow: 0 4px 18px rgba(16,47,41,.04);
}

#aparencia-form .form-card-title {
  display: flex;
  align-items: flex-start;
  gap: 14px;
  margin-bottom: 24px;
}

#aparencia-form .form-card-title > span {
  width: 42px;
  height: 42px;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #edf6f2;
  border-radius: 12px;
  font-size: 20px;
}

#aparencia-form .form-card-title h2 {
  margin: 2px 0 5px;
  font-size: 18px;
  color: #17211f;
}

#aparencia-form .form-card-title p {
  margin: 0;
  color: #7a8581;
  font-size: 13px;
}

#aparencia-form .form-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0,1fr));
  gap: 20px;
}

#aparencia-form .form-field.full {
  grid-column: 1 / -1;
}

#aparencia-form .form-field label {
  display: block;
  margin-bottom: 8px;
  color: #34413d;
  font-size: 13px;
  font-weight: 700;
}

#aparencia-form input[type="text"],
#aparencia-form input[type="url"],
#aparencia-form textarea {
  width: 100%;
  box-sizing: border-box;
  border: 1px solid #dce4e0;
  background: #fbfcfc;
  border-radius: 10px;
  padding: 12px 14px;
  color: #17211f;
  font: inherit;
  outline: none;
  transition: .18s ease;
}

#aparencia-form textarea {
  resize: vertical;
  min-height: 95px;
}

#aparencia-form input[type="text"]:focus,
#aparencia-form input[type="url"]:focus,
#aparencia-form textarea:focus {
  background: #fff;
  border-color: #1e6f5c;
  box-shadow: 0 0 0 3px rgba(30,111,92,.10);
}

#aparencia-form input[type="range"] {
  width: 100%;
  accent-color: #1e6f5c;
  cursor: pointer;
}

#aparencia-form #logo-tamanho-valor {
  display: inline-block;
  min-width: 55px;
  padding: 6px 9px;
  background: #edf6f2;
  color: #1e6f5c;
  border-radius: 8px;
  text-align: center;
  font-size: 12px;
  font-weight: 800;
}

#aparencia-form input[type="file"] {
  width: 100%;
  box-sizing: border-box;
  padding: 10px;
  border: 1px dashed #cbd8d3;
  border-radius: 12px;
  background: #f8faf9;
  color: #66736e;
  cursor: pointer;
}

#aparencia-form input[type="color"] {
  width: 52px;
  height: 42px;
  padding: 3px;
  border: 1px solid #dce4e0;
  border-radius: 10px;
  background: #fff;
  cursor: pointer;
}

#logo-preview {
  min-height: 120px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 12px;
  padding: 18px;
  border: 1px solid #e1e7e4;
  border-radius: 14px;
  background: #f7f9f8;
  overflow: hidden;
}

#logo-preview img {
  max-width: 100%;
  max-height: 120px;
  object-fit: contain;
}

#logo-nome {
  display: block;
  margin-top: 8px;
  color: #78837f;
  font-size: 12px;
}

#remover-logo {
  margin-top: 10px;
  border: 0;
  border-radius: 9px;
  padding: 9px 13px;
  background: #fff1f1;
  color: #a33b3b;
  font-size: 12px;
  font-weight: 700;
  cursor: pointer;
}

#remover-logo:hover {
  background: #ffe5e5;
}

.aparencia-preview-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 14px;
}

.preview-live {
  padding: 6px 10px;
  border-radius: 20px;
  background: #e9f7ef;
  color: #14734d;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: .5px;
}

.aparencia-preview {
  overflow: hidden;
  border: 1px solid #dfe6e3;
  border-radius: 18px;
  box-shadow: 0 10px 30px rgba(16,47,41,.08);
}

#aparencia-form .form-actions {
  position: sticky;
  bottom: 18px;
  z-index: 5;
  display: flex;
  justify-content: flex-end;
  padding: 14px 16px;
  background: rgba(255,255,255,.94);
  backdrop-filter: blur(12px);
  border: 1px solid #e1e7e4;
  border-radius: 14px;
  box-shadow: 0 8px 30px rgba(16,47,41,.10);
}

@media (max-width: 750px) {
  #aparencia-form .form-grid {
    grid-template-columns: 1fr;
  }

  #aparencia-form .form-field.full {
    grid-column: auto;
  }

  #aparencia-form .form-card {
    padding: 20px;
  }
}


/* =========================================================
   APARÊNCIA — EDITOR DE CORES
   ========================================================= */

#aparencia-form input[type="color"] {
  appearance: none;
  -webkit-appearance: none;

  width: 58px;
  height: 46px;

  padding: 4px;

  border: 1px solid #d7e1dd;
  border-radius: 12px;

  background: #fff;

  cursor: pointer;

  transition:
    transform .15s ease,
    border-color .15s ease,
    box-shadow .15s ease;
}

#aparencia-form input[type="color"]:hover {
  transform: translateY(-1px);

  border-color: #1e6f5c;

  box-shadow:
    0 4px 12px rgba(30,111,92,.12);
}

#aparencia-form input[type="color"]:focus {
  outline: none;

  border-color: #1e6f5c;

  box-shadow:
    0 0 0 3px rgba(30,111,92,.12);
}

#aparencia-form input[type="color"]::-webkit-color-swatch-wrapper {
  padding: 0;
}

#aparencia-form input[type="color"]::-webkit-color-swatch {
  border: none;
  border-radius: 8px;
}

#aparencia-form .color-editor {
  display: flex;
  align-items: center;
  gap: 12px;
}

#aparencia-form .color-editor input[type="text"] {
  flex: 1;
  min-width: 0;

  font-family:
    ui-monospace,
    SFMono-Regular,
    Menlo,
    Monaco,
    Consolas,
    monospace;

  font-size: 13px;
  font-weight: 700;

  letter-spacing: .4px;

  text-transform: uppercase;
}

#aparencia-form .color-editor::before {
  content: "HEX";

  display: flex;
  align-items: center;
  justify-content: center;

  height: 36px;

  padding: 0 9px;

  border-radius: 8px;

  background: #f0f4f2;

  color: #687671;

  font-size: 10px;
  font-weight: 800;
  letter-spacing: .6px;
}

#aparencia-form .form-field:has(input[type="color"]) {
  padding: 16px;

  border: 1px solid #e5ebe8;
  border-radius: 14px;

  background: #fbfcfc;

  transition:
    border-color .18s ease,
    background .18s ease,
    box-shadow .18s ease;
}

#aparencia-form .form-field:has(input[type="color"]):hover {
  background: #fff;

  border-color: #d5e1dc;

  box-shadow:
    0 4px 14px rgba(16,47,41,.04);
}

#aparencia-form .form-field:has(input[type="color"]) label {
  margin-bottom: 12px;
}


/* =========================================================
   APARÊNCIA — PREVIEW AO VIVO
   ========================================================= */

#aparencia-preview {
  position: relative;
  overflow: hidden;

  border: 1px solid #dfe7e3;
  border-radius: 20px;

  background: var(--preview-fundo, #ffffff);

  box-shadow:
    0 18px 50px rgba(20, 45, 38, .10);

  transition:
    background .2s ease,
    border-color .2s ease,
    box-shadow .2s ease;
}

/* Barra superior */

#aparencia-preview .preview-header {
  min-height: 58px;

  display: flex;
  align-items: center;
  justify-content: space-between;

  padding: 0 22px;

  background:
    color-mix(
      in srgb,
      var(--preview-fundo, #ffffff) 94%,
      var(--preview-principal, #1e6f5c)
    );

  border-bottom: 1px solid
    color-mix(
      in srgb,
      var(--preview-principal, #1e6f5c) 12%,
      transparent
    );
}

/* Logo */

#aparencia-preview .preview-logo {
  display: flex;
  align-items: center;
  justify-content: center;

  width: 42px;
  height: 42px;

  flex-shrink: 0;

  overflow: hidden;
}

#aparencia-preview .preview-logo img {
  display: block;

  width: 42px;
  height: 42px;

  max-width: none;
  max-height: none;

  object-fit: contain;

  transition:
    width .15s ease,
    height .15s ease;
}

/* Nome */

#aparencia-preview #preview-nome {
  color: var(--preview-texto, #17211f);

  font-size: 14px;
  font-weight: 800;
}

#aparencia-preview #preview-subtitulo {
  color:
    color-mix(
      in srgb,
      var(--preview-texto, #17211f) 60%,
      transparent
    );

  font-size: 11px;
}

/* Conteúdo principal */

#aparencia-preview .preview-hero {
  min-height: 280px;

  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;

  padding: 50px 30px;

  text-align: center;

  background:
    radial-gradient(
      circle at 50% 0%,
      color-mix(
        in srgb,
        var(--preview-principal, #1e6f5c) 13%,
        transparent
      ),
      transparent 58%
    ),
    var(--preview-fundo, #ffffff);
}

/* Título */

#aparencia-preview #preview-titulo {
  max-width: 680px;

  margin: 0;

  color: var(--preview-texto, #17211f);

  font-size: clamp(26px, 4vw, 42px);
  line-height: 1.08;
  font-weight: 850;
  letter-spacing: -.8px;
}

/* Descrição */

#aparencia-preview #preview-descricao {
  max-width: 590px;

  margin: 16px auto 0;

  color:
    color-mix(
      in srgb,
      var(--preview-texto, #17211f) 68%,
      transparent
    );

  font-size: 14px;
  line-height: 1.7;
}

/* Botões */

#aparencia-preview .preview-actions {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-wrap: wrap;

  gap: 10px;

  margin-top: 26px;
}

#aparencia-preview .preview-button {
  min-height: 42px;

  padding: 0 18px;

  border-radius: 10px;

  font-size: 12px;
  font-weight: 800;

  cursor: default;

  transition:
    transform .15s ease,
    box-shadow .15s ease;
}

#aparencia-preview .preview-primary {
  border: 1px solid var(--preview-principal, #1e6f5c);

  background: var(--preview-principal, #1e6f5c);

  color: #fff;

  box-shadow:
    0 7px 18px
    color-mix(
      in srgb,
      var(--preview-principal, #1e6f5c) 25%,
      transparent
    );
}

#aparencia-preview .preview-secondary {
  border: 1px solid
    color-mix(
      in srgb,
      var(--preview-principal, #1e6f5c) 30%,
      transparent
    );

  background: transparent;

  color: var(--preview-principal, #1e6f5c);
}

/* Rodapé */

#aparencia-preview .preview-footer {
  padding: 14px 22px;

  border-top: 1px solid
    color-mix(
      in srgb,
      var(--preview-texto, #17211f) 8%,
      transparent
    );

  color:
    color-mix(
      in srgb,
      var(--preview-texto, #17211f) 55%,
      transparent
    );

  font-size: 10px;

  text-align: center;
}

/* Indicador */

#aparencia-preview::before {
  content: "PRÉ-VISUALIZAÇÃO AO VIVO";

  position: absolute;
  top: 12px;
  right: 14px;

  z-index: 10;

  padding: 5px 8px;

  border-radius: 999px;

  background:
    color-mix(
      in srgb,
      var(--preview-principal, #1e6f5c) 12%,
      #ffffff
    );

  color: var(--preview-principal, #1e6f5c);

  font-size: 8px;
  font-weight: 900;
  letter-spacing: .5px;

  pointer-events: none;
}

/* Responsivo */

@media (max-width: 650px) {

  #aparencia-preview .preview-header {
    padding: 0 14px;
  }

  #aparencia-preview .preview-hero {
    padding: 38px 18px;
  }

  #aparencia-preview .preview-actions {
    flex-direction: column;
    width: 100%;
  }

  #aparencia-preview .preview-button {
    width: 100%;
  }

}


/* COMBO 3 - LAMINA */

/* Visual profissional da página individual */

.slide-header {
  margin-bottom: 28px;
}

.slide-title {
  gap: 24px;
}

.slide-title h1 {
  letter-spacing: -1.5px;
}

.viewer-section {
  margin-top: 10px;
}

.viewer-layout {
  gap: 24px;
  align-items: start;
}

.viewer-container {
  overflow: hidden;
  border-radius: 18px;
  background: #101614;
  box-shadow: 0 18px 45px rgba(0,0,0,.12);
}

.viewer {
  min-height: 520px;
}

.viewer-controls {
  border-radius: 12px;
  padding: 6px;
  backdrop-filter: blur(10px);
}

.viewer-controls button {
  transition:
    transform .18s ease,
    background .18s ease;
}

.viewer-controls button:hover {
  transform: translateY(-2px);
}

.info-panel {
  position: sticky;
  top: 24px;

  border-radius: 18px;

  box-shadow:
    0 10px 30px rgba(0,0,0,.06);

  transition:
    transform .2s ease,
    box-shadow .2s ease;
}

.info-panel:hover {
  transform: translateY(-2px);

  box-shadow:
    0 15px 35px rgba(0,0,0,.09);
}

/* Mobile */

@media (max-width: 900px) {

  .viewer-layout {
    grid-template-columns: 1fr;
  }

  .info-panel {
    position: static;
  }

  .slide-title {
    flex-direction: column;
    align-items: flex-start;
  }

}

@media (max-width: 600px) {

  .slide-header {
    margin-bottom: 20px;
  }

  .slide-title h1 {
    font-size: 2rem;
    letter-spacing: -1px;
  }

  .viewer-container {
    border-radius: 14px;
  }

  .viewer {
    min-height: 330px;
  }

  .info-panel {
    padding: 18px;
    border-radius: 14px;
  }

}


/* ============================================================
   MARCAÇÕES DAS ESTRUTURAS DA LÂMINA
   ============================================================ */

.estrutura-overlay {

  pointer-events: auto;

  overflow: visible;

}


.estrutura-marcacao {

  position: relative;

  width: 100%;

  height: 100%;

  overflow: visible;

}


.estrutura-caixa {

  position: absolute;

  inset: 0;

  border: 3px solid #ff3b30;

  background: rgba(255, 59, 48, 0.10);

  border-radius: 8px;

  box-shadow:
    0 0 0 1px rgba(255,255,255,.8),
    0 0 18px rgba(255,59,48,.45);

}


.estrutura-label {

  position: absolute;

  left: 50%;

  top: -10px;

  transform:
    translate(-50%, -100%);

  padding: 6px 10px;

  border-radius: 7px;

  background: #ff3b30;

  color: white;

  font-size: 13px;

  font-weight: 700;

  line-height: 1.2;

  white-space: nowrap;

  box-shadow:
    0 4px 14px rgba(0,0,0,.28);

  pointer-events: none;

}


.estrutura-marcacao:hover
.estrutura-caixa {

  border-color: #fff;

  background:
    rgba(255, 59, 48, 0.18);

}


.estrutura-marcacao:hover
.estrutura-label {

  background: #c62828;

}


/* Em telas pequenas */

@media (max-width: 700px) {

  .estrutura-label {

    font-size: 11px;

    padding: 4px 7px;

  }

}




/* =====================================================
   MARCADORES DAS ESTRUTURAS HISTOLÓGICAS
   ===================================================== */

.estrutura-overlay {
  pointer-events: none;
  overflow: visible;
  z-index: 50;
}

.estrutura-marcacao {
  position: relative;
  width: 100%;
  height: 100%;
  min-width: 18px;
  min-height: 18px;
  pointer-events: none;
}

.estrutura-ponto {
  position: absolute;
  inset: 0;
  border: 3px solid #ff3b30;
  border-radius: 50%;
  box-sizing: border-box;
  background: rgba(255, 59, 48, 0.08);
  box-shadow:
    0 0 0 2px rgba(255,255,255,0.85),
    0 0 14px rgba(255,59,48,0.65);
}

.estrutura-label {
  position: absolute;
  left: 50%;
  top: -34px;
  transform: translateX(-50%);

  padding: 5px 9px;

  background: rgba(20, 20, 20, 0.92);
  color: white;

  border-radius: 7px;

  font-size: 13px;
  font-weight: 700;

  white-space: nowrap;

  box-shadow:
    0 4px 14px rgba(0,0,0,0.35);

  border: 1px solid rgba(255,255,255,0.15);
}


/* ============================================================
   COMBO 5 - ESTRUTURAS INTERATIVAS
   ============================================================ */

.structure {
  cursor: pointer;
  transition:
    transform 0.2s ease,
    box-shadow 0.2s ease;
}

.structure:hover {
  transform: translateX(3px);
}

.structure.estrutura-selecionada {
  transform: translateX(5px);
  box-shadow:
    0 0 0 2px rgba(255,255,255,0.12),
    0 8px 24px rgba(0,0,0,0.12);
}

.estrutura-overlay {
  cursor: pointer;
}

.estrutura-overlay .estrutura-ponto {
  transition:
    transform 0.2s ease,
    box-shadow 0.2s ease;
}

.estrutura-overlay.estrutura-selecionada .estrutura-ponto {
  transform: scale(1.5);
  box-shadow:
    0 0 0 6px rgba(255,255,255,0.22),
    0 0 22px rgba(255,255,255,0.75);
}

.estrutura-overlay.estrutura-selecionada .estrutura-label {
  transform: scale(1.06);
  transform-origin: left center;
}


/* ============================================================
   COMBO 6 - NAVEGACAO ENTRE ESTRUTURAS
   ============================================================ */

.estrutura-navegacao {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  margin-top: 10px;
  margin-bottom: 10px;
}

.estrutura-nav-button {
  width: 38px;
  height: 34px;
  border: 0;
  border-radius: 8px;
  cursor: pointer;
  font-size: 18px;
  line-height: 1;
  transition:
    transform 0.18s ease,
    opacity 0.18s ease,
    box-shadow 0.18s ease;
}

.estrutura-nav-button:hover:not(:disabled) {
  transform: scale(1.06);
}

.estrutura-nav-button:disabled {
  opacity: 0.35;
  cursor: not-allowed;
}

.estrutura-contador {
  min-width: 125px;
  text-align: center;
  font-size: 13px;
  font-weight: 600;
  white-space: nowrap;
}


/* ============================================================
   COMBO 7 - MODO ESTUDO
   ============================================================ */

.estrutura-estudo-panel {
  position: absolute;
  left: 20px;
  bottom: 20px;
  width: min(360px, calc(100% - 40px));
  z-index: 30;
  padding: 18px;
  border-radius: 14px;
  background: rgba(15, 15, 15, 0.94);
  color: white;
  box-shadow:
    0 15px 45px rgba(0, 0, 0, 0.35);
  backdrop-filter: blur(12px);
}

.estrutura-estudo-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}

.estrutura-estudo-eyebrow {
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 0.12em;
  opacity: 0.65;
}

#estrutura-estudo-fechar {
  width: 30px;
  height: 30px;
  border: 0;
  border-radius: 50%;
  cursor: pointer;
  font-size: 20px;
  line-height: 1;
}

.estrutura-estudo-panel h3 {
  margin: 10px 0 6px;
  font-size: 20px;
}

.estrutura-estudo-panel p {
  margin: 0;
  line-height: 1.55;
  font-size: 14px;
  opacity: 0.8;
}

@media (max-width: 700px) {

  .estrutura-estudo-panel {
    left: 10px;
    right: 10px;
    bottom: 10px;
    width: auto;
  }

}



===== backup-reparador-completo-20260819-212337\src\style.css.backup-painel-final-20260819-150052.css =====
:root {
  --cor-principal: #1f6feb;
  --cor-fundo: #f7f9fc;
  --cor-texto: #172033;
  --cor-destaque: #22c55e;
}
:root {
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  color: #17201d;
  background: #f5f7f5;
  font-synthesis: none;
  text-rendering: optimizeLegibility;
  --green: #174c3c;
  --green-light: #2d755d;
  --mint: #dcefe7;
  --cream: #f5f7f5;
  --white: #ffffff;
  --text: #17201d;
  --muted: #66736d;
  --border: #dfe6e2;
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  scroll-behavior: smooth;
}

body {
  min-width: 320px;
  background: var(--cream);
  color: var(--text);
}

button,
a {
  font: inherit;
}

button {
  border: 0;
  cursor: pointer;
}

a {
  color: inherit;
  text-decoration: none;
}

.container {
  width: min(1160px, calc(100% - 48px));
  margin: 0 auto;
}

.header {
  position: sticky;
  top: 0;
  z-index: 20;
  background: rgba(245, 247, 245, 0.9);
  backdrop-filter: blur(16px);
  border-bottom: 1px solid rgba(223, 230, 226, 0.8);
}

.header-content {
  height: 76px;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.brand {
  display: flex;
  align-items: center;
  gap: 11px;
}

.brand-icon {
  width: 42px;
  height: 42px;
  display: grid;
  place-items: center;
  border-radius: 12px;
  background: var(--green);
  color: white;
  font-size: 20px;
}

.brand strong,
.brand small {
  display: block;
}

.brand strong {
  font-size: 17px;
  letter-spacing: -0.4px;
}

.brand small {
  color: var(--muted);
  font-size: 11px;
  letter-spacing: 1.5px;
  text-transform: uppercase;
}

.nav {
  display: flex;
  gap: 34px;
  color: #53605a;
  font-size: 14px;
}

.nav a {
  transition: color 0.2s ease;
}

.nav a:hover {
  color: var(--green);
}

.menu-button {
  display: none;
  background: transparent;
  font-size: 22px;
}

.hero {
  padding: 100px 0 90px;
  overflow: hidden;
}

.hero-grid {
  display: grid;
  grid-template-columns: 1fr 0.9fr;
  align-items: center;
  gap: 70px;
}

.eyebrow {
  display: inline-block;
  margin-bottom: 18px;
  color: var(--green-light);
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 2px;
}

.hero h1 {
  max-width: 650px;
  font-size: clamp(48px, 6vw, 76px);
  line-height: 0.98;
  letter-spacing: -4px;
}

.hero h1 span {
  display: block;
  color: var(--green);
}

.hero-text > p {
  max-width: 570px;
  margin-top: 28px;
  color: var(--muted);
  font-size: 17px;
  line-height: 1.7;
}

.hero-actions {
  display: flex;
  gap: 12px;
  margin-top: 34px;
}

.button {
  display: inline-flex;
  align-items: center;
  gap: 20px;
  padding: 14px 20px;
  border-radius: 10px;
  font-size: 14px;
  font-weight: 700;
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.button:hover {
  transform: translateY(-2px);
}

.button.primary {
  background: var(--green);
  color: white;
  box-shadow: 0 12px 30px rgba(23, 76, 60, 0.18);
}

.button.secondary {
  border: 1px solid var(--border);
  background: white;
}

.hero-visual {
  min-height: 480px;
  display: grid;
  place-items: center;
}

.microscope-card {
  width: min(430px, 100%);
  height: 430px;
  position: relative;
  overflow: hidden;
  border-radius: 28px;
  background: #dbece5;
  border: 1px solid #cbded5;
}

.microscope-glow {
  position: absolute;
  width: 270px;
  height: 270px;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.65);
  filter: blur(8px);
}

.microscope {
  position: absolute;
  width: 190px;
  height: 280px;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -45%);
}

.microscope-top {
  position: absolute;
  width: 80px;
  height: 115px;
  right: 28px;
  top: 10px;
  border-radius: 30px 30px 12px 12px;
  background: #263e36;
  transform: rotate(23deg);
}

.microscope-body {
  position: absolute;
  width: 75px;
  height: 190px;
  left: 53px;
  top: 55px;
  border-radius: 45px 20px 30px 25px;
  background: #345b4e;
  transform: rotate(22deg);
}

.microscope-base {
  position: absolute;
  width: 190px;
  height: 38px;
  bottom: 0;
  border-radius: 30px 30px 14px 14px;
  background: #263e36;
}

.microscope-lens {
  position: absolute;
  width: 100px;
  height: 22px;
  left: 45px;
  bottom: 66px;
  border-radius: 15px;
  background: #1d302a;
}

.floating-tag {
  position: absolute;
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 9px 13px;
  border-radius: 10px;
  background: rgba(255,255,255,0.88);
  backdrop-filter: blur(8px);
  box-shadow: 0 10px 30px rgba(40, 70, 60, 0.12);
  font-size: 12px;
  font-weight: 700;
}

.floating-tag span {
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: var(--green-light);
}

.tag-one {
  left: 24px;
  top: 75px;
}

.tag-two {
  right: 22px;
  top: 155px;
}

.tag-three {
  left: 30px;
  bottom: 70px;
}

.scan-line {
  position: absolute;
  width: 100%;
  height: 1px;
  top: 50%;
  background: rgba(23, 76, 60, 0.12);
}

.section {
  padding: 95px 0;
  background: white;
}

.section-heading {
  display: flex;
  align-items: end;
  justify-content: space-between;
  gap: 40px;
  margin-bottom: 40px;
}

.section-heading h2,
.about h2 {
  font-size: clamp(32px, 4vw, 48px);
  line-height: 1.05;
  letter-spacing: -2px;
}

.section-heading p {
  max-width: 390px;
  color: var(--muted);
  line-height: 1.6;
}

.tissue-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 16px;
}

.tissue-card {
  position: relative;
  min-height: 290px;
  padding: 26px;
  overflow: hidden;
  border: 1px solid var(--border);
  border-radius: 18px;
  background: var(--cream);
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}

.tissue-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 18px 40px rgba(23, 76, 60, 0.09);
}

.card-number {
  color: #a4b0ab;
  font-size: 12px;
  font-weight: 800;
}

.card-icon {
  margin-top: 48px;
  color: var(--green);
  font-size: 34px;
}

.tissue-card h3 {
  margin-top: 20px;
  font-size: 19px;
}

.tissue-card p {
  margin-top: 8px;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.5;
}

.tissue-card button {
  position: absolute;
  left: 26px;
  bottom: 24px;
  background: transparent;
  color: var(--green);
  font-size: 13px;
  font-weight: 800;
}

.tissue-card button span {
  margin-left: 8px;
}

.about {
  padding: 105px 0;
  background: #eaf2ee;
}

.about-grid {
  display: grid;
  grid-template-columns: 0.8fr 1fr;
  gap: 100px;
}

.about-text > p {
  color: #56645e;
  font-size: 17px;
  line-height: 1.7;
}

.features {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 25px;
  margin-top: 35px;
}

.features div {
  padding-top: 20px;
  border-top: 1px solid #cbdad3;
}

.features strong,
.features span {
  display: block;
}

.features strong {
  font-size: 14px;
}

.features span {
  margin-top: 7px;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.5;
}

.footer {
  padding: 25px 0;
  background: var(--green);
  color: white;
}

.footer .container {
  display: flex;
  justify-content: space-between;
  color: rgba(255,255,255,0.7);
  font-size: 12px;
}

.footer span:first-child {
  color: white;
  font-weight: 700;
}

@media (max-width: 850px) {
  .hero-grid,
  .about-grid {
    grid-template-columns: 1fr;
    gap: 55px;
  }

  .hero {
    padding-top: 65px;
  }

  .hero-visual {
    min-height: 400px;
  }

  .tissue-grid {
    grid-template-columns: 1fr 1fr;
  }
}

@media (max-width: 600px) {
  .container {
    width: min(100% - 32px, 1160px);
  }

  .nav {
    display: none;
  }

  .menu-button {
    display: block;
  }

  .hero h1 {
    font-size: 50px;
    letter-spacing: -3px;
  }

  .hero-actions {
    flex-direction: column;
  }

  .button {
    justify-content: center;
  }

  .section-heading {
    display: block;
  }

  .section-heading p {
    margin-top: 15px;
  }

  .tissue-grid {
    grid-template-columns: 1fr;
  }

  .about-grid {
    gap: 35px;
  }

  .features {
    grid-template-columns: 1fr;
  }

  .footer .container {
    flex-direction: column;
    gap: 8px;
  }
}

.slide-header {
  padding: 55px 0 35px;
  background: white;
  border-bottom: 1px solid var(--border);
}

.back-link {
  display: inline-block;
  margin-bottom: 30px;
  color: var(--muted);
  font-size: 13px;
}

.slide-title {
  display: flex;
  justify-content: space-between;
  align-items: end;
  gap: 40px;
}

.slide-title h1 {
  font-size: clamp(36px, 5vw, 58px);
  letter-spacing: -3px;
  line-height: 1;
}

.slide-title p {
  margin-top: 15px;
  color: var(--muted);
}

.slide-meta {
  display: flex;
  flex-direction: column;
  gap: 8px;
  color: var(--muted);
  font-size: 12px;
}

.viewer-section {
  padding: 40px 0 80px;
  background: #f1f4f2;
}

.viewer-layout {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 340px;
  gap: 24px;
  align-items: start;
}

.viewer-container {
  overflow: hidden;
  border: 1px solid var(--border);
  border-radius: 18px;
  background: #101513;
  box-shadow: 0 20px 50px rgba(0,0,0,0.08);
}

.viewer {
  width: 100%;
  height: 650px;
  background: #101513;
}

.viewer-controls {
  min-height: 58px;
  padding: 10px 14px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 15px;
  background: #18201d;
  color: #aab7b1;
  font-size: 11px;
}

.zoom-buttons {
  display: flex;
  gap: 5px;
}

.zoom-buttons button {
  width: 38px;
  height: 34px;
  border-radius: 7px;
  background: #29332f;
  color: white;
  font-size: 18px;
}

.zoom-buttons button:hover {
  background: var(--green-light);
}

.info-panel {
  padding: 28px;
  border: 1px solid var(--border);
  border-radius: 18px;
  background: white;
}

.panel-label {
  margin-bottom: 15px;
  color: var(--green-light);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 1.7px;
}

.info-panel h2 {
  font-size: 25px;
  line-height: 1.1;
}

.description {
  margin-top: 15px;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.7;
}

.info-section {
  margin-top: 30px;
}

.info-section h3 {
  font-size: 14px;
}

.structure-list {
  margin-top: 15px;
  display: grid;
  gap: 8px;
}

.structure {
  width: 100%;
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px;
  text-align: left;
  border-radius: 10px;
  background: #f4f7f5;
}

.structure:hover {
  background: #e8f1ec;
}

.structure-dot {
  width: 9px;
  height: 9px;
  flex: 0 0 9px;
  border-radius: 50%;
  background: var(--green-light);
}

.structure strong,
.structure small {
  display: block;
}

.structure strong {
  font-size: 12px;
}

.structure small {
  margin-top: 3px;
  color: var(--muted);
  font-size: 10px;
}

.content-button {
  width: 100%;
  margin-top: 25px;
  padding: 14px;
  display: flex;
  justify-content: space-between;
  border-radius: 10px;
  background: var(--green);
  color: white;
  font-size: 12px;
  font-weight: 700;
}

.learning-section {
  padding: 90px 0;
  background: white;
}

.learning-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
}

.learning-card {
  padding: 35px;
  border: 1px solid var(--border);
  border-radius: 18px;
}

.learning-card h2 {
  font-size: 28px;
  letter-spacing: -1px;
}

.learning-card > p {
  max-width: 500px;
  margin-top: 12px;
  color: var(--muted);
  line-height: 1.6;
  font-size: 14px;
}

.video-placeholder {
  height: 190px;
  margin-top: 25px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 10px;
  border-radius: 12px;
  background: #edf2ef;
  color: var(--green);
}

.video-placeholder span {
  font-size: 35px;
}

.video-placeholder small {
  color: var(--muted);
}

.learning-card .button {
  margin-top: 25px;
}

@media (max-width: 900px) {
  .viewer-layout {
    grid-template-columns: 1fr;
  }

  .slide-title {
    display: block;
  }

  .slide-meta {
    margin-top: 25px;
  }

  .learning-grid {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 600px) {
  .viewer {
    height: 450px;
  }

  .viewer-controls {
    flex-direction: column;
    align-items: stretch;
  }

  .zoom-buttons {
    justify-content: center;
  }
}

.card-link {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  margin-top: 15px;
  color: var(--green-light);
  font-size: 12px;
  font-weight: 800;
  text-decoration: none;
}

.card-link span {
  transition: transform 0.2s ease;
}

.card-link:hover span {
  transform: translateX(4px);
}

.tissue-card .eyebrow {
  display: block;
  margin-top: 10px;
}

.auth-page {
  min-height: 100vh;
  display: grid;
  place-items: center;
  padding: 30px;
  background: #f1f4f2;
}

.auth-card {
  width: min(430px, 100%);
  padding: 45px;
  border: 1px solid var(--border);
  border-radius: 20px;
  background: white;
  box-shadow: 0 25px 70px rgba(0,0,0,.08);
}

.auth-logo {
  margin-bottom: 20px;
  font-size: 42px;
}

.auth-card h1 {
  margin-top: 8px;
  font-size: 34px;
}

.auth-card > p {
  margin: 12px 0 30px;
  color: var(--muted);
  line-height: 1.6;
  font-size: 13px;
}

.auth-card form {
  display: grid;
  gap: 18px;
}

.auth-card label {
  display: grid;
  gap: 7px;
  font-size: 12px;
  font-weight: 700;
}

.auth-card input {
  width: 100%;
  padding: 13px 14px;
  border: 1px solid var(--border);
  border-radius: 9px;
  outline: none;
  font: inherit;
}

.auth-card input:focus {
  border-color: var(--green-light);
}

#login-error {
  color: #b43b3b;
  font-size: 12px;
}

.auth-card .back-link {
  margin-top: 25px;
}

.admin-page {
  min-height: calc(100vh - 75px);
  padding: 60px 0 100px;
  background: #f1f4f2;
}

.admin-header {
  display: flex;
  align-items: center;
  gap: 15px;
}

.admin-user {
  color: var(--muted);
  font-size: 11px;
}

.logout-button {
  padding: 8px 13px;
  border: 1px solid var(--border);
  border-radius: 7px;
  background: white;
  font-size: 11px;
}

.admin-title {
  display: flex;
  align-items: end;
  justify-content: space-between;
  gap: 30px;
}

.admin-title h1 {
  margin-top: 8px;
  font-size: 42px;
  letter-spacing: -2px;
}

.admin-title p {
  margin-top: 10px;
  color: var(--muted);
}

.admin-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 15px;
  margin-top: 40px;
}

.admin-stat {
  padding: 25px;
  border: 1px solid var(--border);
  border-radius: 14px;
  background: white;
}

.admin-stat span,
.admin-stat strong,
.admin-stat small {
  display: block;
}

.admin-stat span {
  font-size: 20px;
}

.admin-stat strong {
  margin-top: 12px;
  font-size: 30px;
}

.admin-stat small {
  margin-top: 3px;
  color: var(--muted);
}

.admin-section {
  margin-top: 50px;
}

.admin-section-header h2 {
  margin-top: 6px;
}

.admin-list {
  margin-top: 18px;
  display: grid;
  gap: 8px;
}

.admin-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 18px 20px;
  border: 1px solid var(--border);
  border-radius: 12px;
  background: white;
}

.admin-item strong,
.admin-item small {
  display: block;
}

.admin-item small {
  margin-top: 4px;
  color: var(--muted);
  font-size: 11px;
}

.status {
  padding: 6px 10px;
  border-radius: 20px;
  background: #f0f0f0;
  color: var(--muted);
  font-size: 10px;
}

.status.published {
  background: #e5f3ea;
  color: #267244;
}

.empty-state {
  padding: 60px 20px;
  text-align: center;
  border: 1px dashed var(--border);
  border-radius: 14px;
  background: white;
}

.empty-state span {
  font-size: 35px;
}

.empty-state h3 {
  margin-top: 12px;
}

.empty-state p {
  margin-top: 7px;
  color: var(--muted);
  font-size: 13px;
}

@media (max-width: 700px) {

  .admin-title {
    display: block;
  }

  .admin-title .button {
    margin-top: 20px;
  }

  .admin-grid {
    grid-template-columns: 1fr;
  }

  .admin-user {
    display: none;
  }

  .auth-card {
    padding: 30px;
  }

}

.lamina-form {
  max-width: 900px;
  margin-top: 40px;
}

.form-card {
  margin-bottom: 20px;
  padding: 30px;
  border: 1px solid var(--border);
  border-radius: 16px;
  background: white;
}

.form-card h2 {
  margin-top: 7px;
  margin-bottom: 25px;
}

.form-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
}

.form-grid .full {
  grid-column: 1 / -1;
}

.form-card label {
  display: grid;
  gap: 8px;
  font-size: 12px;
  font-weight: 700;
}

.form-card input,
.form-card textarea {
  width: 100%;
  padding: 13px 14px;
  border: 1px solid var(--border);
  border-radius: 9px;
  outline: none;
  font: inherit;
  resize: vertical;
}

.form-card input:focus,
.form-card textarea:focus {
  border-color: var(--green-light);
}

.form-help {
  color: var(--muted);
  font-size: 13px;
  line-height: 1.6;
  margin-bottom: 20px;
}

.upload-area {
  min-height: 190px;
  display: flex !important;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 7px;
  padding: 30px;
  border: 2px dashed var(--border);
  border-radius: 12px;
  cursor: pointer;
  text-align: center;
}

.upload-area:hover {
  border-color: var(--green-light);
  background: #fafcfb;
}

.upload-area input {
  display: none;
}

.upload-icon {
  font-size: 36px;
}

.upload-area strong {
  font-size: 13px;
}

.upload-area small {
  color: var(--muted);
  font-size: 11px;
}

.image-preview {
  display: block;
  width: 100%;
  max-height: 400px;
  object-fit: contain;
  margin-top: 20px;
  border-radius: 10px;
  background: #111;
}

.publish-option {
  display: flex !important;
  grid-template-columns: auto 1fr;
  grid-template-rows: auto;
  gap: 12px !important;
  align-items: flex-start;
  cursor: pointer;
}

.publish-option input {
  width: auto;
  margin-top: 3px;
}

.publish-option span {
  display: grid;
  gap: 4px;
}

.publish-option small {
  color: var(--muted);
  font-weight: 400;
  line-height: 1.5;
}

.form-actions {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  margin-top: 25px;
}

.success-message {
  padding: 14px 16px;
  margin-top: 20px;
  border-radius: 9px;
  background: #e5f3ea;
  color: #267244;
  font-size: 13px;
}

#form-message:not(.success-message) {
  margin-top: 15px;
  color: #b43b3b;
  font-size: 12px;
}

@media (max-width: 700px) {

  .form-grid {
    grid-template-columns: 1fr;
  }

  .form-grid .full {
    grid-column: auto;
  }

  .form-card {
    padding: 22px;
  }

}

.catalog-page {
  min-height: 100vh;
  background: #f7f8f6;
}

.catalog-hero {
  padding: 80px 0 65px;
  background: white;
  border-bottom: 1px solid var(--border);
}

.catalog-hero h1 {
  margin: 12px 0;
  font-size: clamp(36px, 5vw, 64px);
  letter-spacing: -2px;
}

.catalog-hero p {
  max-width: 650px;
  color: var(--muted);
  font-size: 17px;
  line-height: 1.7;
}

.catalog-content {
  padding: 50px 0 80px;
}

.laminas-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
}

.lamina-card {
  overflow: hidden;
  background: white;
  border: 1px solid var(--border);
  border-radius: 18px;
  transition: transform .2s ease, box-shadow .2s ease;
}

.lamina-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 15px 35px rgba(0,0,0,.08);
}

.lamina-image {
  position: relative;
  height: 230px;
  overflow: hidden;
  background: #111;
}

.lamina-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform .4s ease;
}

.lamina-card:hover .lamina-image img {
  transform: scale(1.06);
}

.zoom-badge {
  position: absolute;
  right: 12px;
  bottom: 12px;
  padding: 8px 11px;
  border-radius: 8px;
  background: rgba(0,0,0,.72);
  color: white;
  font-size: 11px;
  font-weight: 700;
}

.lamina-card-content {
  padding: 22px;
}

.lamina-category {
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 1px;
  text-transform: uppercase;
  color: var(--green-light);
}

.lamina-card h2 {
  margin: 8px 0;
  font-size: 21px;
}

.lamina-card p {
  min-height: 55px;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.6;
}

.explore-lamina {
  width: 100%;
  margin-top: 10px;
}

.loading-state,
.error-state,
.empty-state {
  padding: 60px 20px;
  text-align: center;
  color: var(--muted);
}

.empty-state span {
  display: block;
  margin-bottom: 15px;
  font-size: 40px;
}

.empty-state h3 {
  margin-bottom: 8px;
  color: var(--text);
}

@media (max-width: 900px) {

  .laminas-grid {
    grid-template-columns: repeat(2, 1fr);
  }

}

@media (max-width: 600px) {

  .laminas-grid {
    grid-template-columns: 1fr;
  }

}



/* =========================================================
   LOGO DA PRÉVIA DO EDITOR DE APARÊNCIA
   ========================================================= */

.preview-logo {
  width: 48px;
  height: 48px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  overflow: visible;
}

.preview-logo img {
  width: 42px;
  height: 42px;
  max-width: none;
  max-height: none;
  object-fit: contain;
  display: block;
  transform-origin: center center;
  transition: transform 0.15s ease;
}
 
.logo-preview {
  width: 120px;
  height: 120px;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: visible;
}

.logo-preview img {
  width: 90px;
  height: 90px;
  max-width: none;
  max-height: none;
  object-fit: contain;
  display: block;
  transform-origin: center center;
  transition: transform 0.15s ease;
}


/* =========================================================
   APARÊNCIA — EDITOR MODERNO
   ========================================================= */

#admin-section-aparencia {
  max-width: 1180px;
}

#aparencia-form {
  display: flex;
  flex-direction: column;
  gap: 22px;
}

#aparencia-form .form-card {
  background: #fff;
  border: 1px solid #e4e9e7;
  border-radius: 18px;
  padding: 26px;
  box-shadow: 0 4px 18px rgba(16,47,41,.04);
}

#aparencia-form .form-card-title {
  display: flex;
  align-items: flex-start;
  gap: 14px;
  margin-bottom: 24px;
}

#aparencia-form .form-card-title > span {
  width: 42px;
  height: 42px;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #edf6f2;
  border-radius: 12px;
  font-size: 20px;
}

#aparencia-form .form-card-title h2 {
  margin: 2px 0 5px;
  font-size: 18px;
  color: #17211f;
}

#aparencia-form .form-card-title p {
  margin: 0;
  color: #7a8581;
  font-size: 13px;
}

#aparencia-form .form-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0,1fr));
  gap: 20px;
}

#aparencia-form .form-field.full {
  grid-column: 1 / -1;
}

#aparencia-form .form-field label {
  display: block;
  margin-bottom: 8px;
  color: #34413d;
  font-size: 13px;
  font-weight: 700;
}

#aparencia-form input[type="text"],
#aparencia-form input[type="url"],
#aparencia-form textarea {
  width: 100%;
  box-sizing: border-box;
  border: 1px solid #dce4e0;
  background: #fbfcfc;
  border-radius: 10px;
  padding: 12px 14px;
  color: #17211f;
  font: inherit;
  outline: none;
  transition: .18s ease;
}

#aparencia-form textarea {
  resize: vertical;
  min-height: 95px;
}

#aparencia-form input[type="text"]:focus,
#aparencia-form input[type="url"]:focus,
#aparencia-form textarea:focus {
  background: #fff;
  border-color: #1e6f5c;
  box-shadow: 0 0 0 3px rgba(30,111,92,.10);
}

#aparencia-form input[type="range"] {
  width: 100%;
  accent-color: #1e6f5c;
  cursor: pointer;
}

#aparencia-form #logo-tamanho-valor {
  display: inline-block;
  min-width: 55px;
  padding: 6px 9px;
  background: #edf6f2;
  color: #1e6f5c;
  border-radius: 8px;
  text-align: center;
  font-size: 12px;
  font-weight: 800;
}

#aparencia-form input[type="file"] {
  width: 100%;
  box-sizing: border-box;
  padding: 10px;
  border: 1px dashed #cbd8d3;
  border-radius: 12px;
  background: #f8faf9;
  color: #66736e;
  cursor: pointer;
}

#aparencia-form input[type="color"] {
  width: 52px;
  height: 42px;
  padding: 3px;
  border: 1px solid #dce4e0;
  border-radius: 10px;
  background: #fff;
  cursor: pointer;
}

#logo-preview {
  min-height: 120px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 12px;
  padding: 18px;
  border: 1px solid #e1e7e4;
  border-radius: 14px;
  background: #f7f9f8;
  overflow: hidden;
}

#logo-preview img {
  max-width: 100%;
  max-height: 120px;
  object-fit: contain;
}

#logo-nome {
  display: block;
  margin-top: 8px;
  color: #78837f;
  font-size: 12px;
}

#remover-logo {
  margin-top: 10px;
  border: 0;
  border-radius: 9px;
  padding: 9px 13px;
  background: #fff1f1;
  color: #a33b3b;
  font-size: 12px;
  font-weight: 700;
  cursor: pointer;
}

#remover-logo:hover {
  background: #ffe5e5;
}

.aparencia-preview-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 14px;
}

.preview-live {
  padding: 6px 10px;
  border-radius: 20px;
  background: #e9f7ef;
  color: #14734d;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: .5px;
}

.aparencia-preview {
  overflow: hidden;
  border: 1px solid #dfe6e3;
  border-radius: 18px;
  box-shadow: 0 10px 30px rgba(16,47,41,.08);
}

#aparencia-form .form-actions {
  position: sticky;
  bottom: 18px;
  z-index: 5;
  display: flex;
  justify-content: flex-end;
  padding: 14px 16px;
  background: rgba(255,255,255,.94);
  backdrop-filter: blur(12px);
  border: 1px solid #e1e7e4;
  border-radius: 14px;
  box-shadow: 0 8px 30px rgba(16,47,41,.10);
}

@media (max-width: 750px) {
  #aparencia-form .form-grid {
    grid-template-columns: 1fr;
  }

  #aparencia-form .form-field.full {
    grid-column: auto;
  }

  #aparencia-form .form-card {
    padding: 20px;
  }
}


/* =========================================================
   APARÊNCIA — EDITOR MODERNO
   ========================================================= */

#admin-section-aparencia {
  max-width: 1180px;
}

#aparencia-form {
  display: flex;
  flex-direction: column;
  gap: 22px;
}

#aparencia-form .form-card {
  background: #fff;
  border: 1px solid #e4e9e7;
  border-radius: 18px;
  padding: 26px;
  box-shadow: 0 4px 18px rgba(16,47,41,.04);
}

#aparencia-form .form-card-title {
  display: flex;
  align-items: flex-start;
  gap: 14px;
  margin-bottom: 24px;
}

#aparencia-form .form-card-title > span {
  width: 42px;
  height: 42px;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #edf6f2;
  border-radius: 12px;
  font-size: 20px;
}

#aparencia-form .form-card-title h2 {
  margin: 2px 0 5px;
  font-size: 18px;
  color: #17211f;
}

#aparencia-form .form-card-title p {
  margin: 0;
  color: #7a8581;
  font-size: 13px;
}

#aparencia-form .form-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0,1fr));
  gap: 20px;
}

#aparencia-form .form-field.full {
  grid-column: 1 / -1;
}

#aparencia-form .form-field label {
  display: block;
  margin-bottom: 8px;
  color: #34413d;
  font-size: 13px;
  font-weight: 700;
}

#aparencia-form input[type="text"],
#aparencia-form input[type="url"],
#aparencia-form textarea {
  width: 100%;
  box-sizing: border-box;
  border: 1px solid #dce4e0;
  background: #fbfcfc;
  border-radius: 10px;
  padding: 12px 14px;
  color: #17211f;
  font: inherit;
  outline: none;
  transition: .18s ease;
}

#aparencia-form textarea {
  resize: vertical;
  min-height: 95px;
}

#aparencia-form input[type="text"]:focus,
#aparencia-form input[type="url"]:focus,
#aparencia-form textarea:focus {
  background: #fff;
  border-color: #1e6f5c;
  box-shadow: 0 0 0 3px rgba(30,111,92,.10);
}

#aparencia-form input[type="range"] {
  width: 100%;
  accent-color: #1e6f5c;
  cursor: pointer;
}

#aparencia-form #logo-tamanho-valor {
  display: inline-block;
  min-width: 55px;
  padding: 6px 9px;
  background: #edf6f2;
  color: #1e6f5c;
  border-radius: 8px;
  text-align: center;
  font-size: 12px;
  font-weight: 800;
}

#aparencia-form input[type="file"] {
  width: 100%;
  box-sizing: border-box;
  padding: 10px;
  border: 1px dashed #cbd8d3;
  border-radius: 12px;
  background: #f8faf9;
  color: #66736e;
  cursor: pointer;
}

#aparencia-form input[type="color"] {
  width: 52px;
  height: 42px;
  padding: 3px;
  border: 1px solid #dce4e0;
  border-radius: 10px;
  background: #fff;
  cursor: pointer;
}

#logo-preview {
  min-height: 120px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 12px;
  padding: 18px;
  border: 1px solid #e1e7e4;
  border-radius: 14px;
  background: #f7f9f8;
  overflow: hidden;
}

#logo-preview img {
  max-width: 100%;
  max-height: 120px;
  object-fit: contain;
}

#logo-nome {
  display: block;
  margin-top: 8px;
  color: #78837f;
  font-size: 12px;
}

#remover-logo {
  margin-top: 10px;
  border: 0;
  border-radius: 9px;
  padding: 9px 13px;
  background: #fff1f1;
  color: #a33b3b;
  font-size: 12px;
  font-weight: 700;
  cursor: pointer;
}

#remover-logo:hover {
  background: #ffe5e5;
}

.aparencia-preview-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 14px;
}

.preview-live {
  padding: 6px 10px;
  border-radius: 20px;
  background: #e9f7ef;
  color: #14734d;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: .5px;
}

.aparencia-preview {
  overflow: hidden;
  border: 1px solid #dfe6e3;
  border-radius: 18px;
  box-shadow: 0 10px 30px rgba(16,47,41,.08);
}

#aparencia-form .form-actions {
  position: sticky;
  bottom: 18px;
  z-index: 5;
  display: flex;
  justify-content: flex-end;
  padding: 14px 16px;
  background: rgba(255,255,255,.94);
  backdrop-filter: blur(12px);
  border: 1px solid #e1e7e4;
  border-radius: 14px;
  box-shadow: 0 8px 30px rgba(16,47,41,.10);
}

@media (max-width: 750px) {
  #aparencia-form .form-grid {
    grid-template-columns: 1fr;
  }

  #aparencia-form .form-field.full {
    grid-column: auto;
  }

  #aparencia-form .form-card {
    padding: 20px;
  }
}


/* =========================================================
   APARÊNCIA — EDITOR DE CORES
   ========================================================= */

#aparencia-form input[type="color"] {
  appearance: none;
  -webkit-appearance: none;

  width: 58px;
  height: 46px;

  padding: 4px;

  border: 1px solid #d7e1dd;
  border-radius: 12px;

  background: #fff;

  cursor: pointer;

  transition:
    transform .15s ease,
    border-color .15s ease,
    box-shadow .15s ease;
}

#aparencia-form input[type="color"]:hover {
  transform: translateY(-1px);

  border-color: #1e6f5c;

  box-shadow:
    0 4px 12px rgba(30,111,92,.12);
}

#aparencia-form input[type="color"]:focus {
  outline: none;

  border-color: #1e6f5c;

  box-shadow:
    0 0 0 3px rgba(30,111,92,.12);
}

#aparencia-form input[type="color"]::-webkit-color-swatch-wrapper {
  padding: 0;
}

#aparencia-form input[type="color"]::-webkit-color-swatch {
  border: none;
  border-radius: 8px;
}

#aparencia-form .color-editor {
  display: flex;
  align-items: center;
  gap: 12px;
}

#aparencia-form .color-editor input[type="text"] {
  flex: 1;
  min-width: 0;

  font-family:
    ui-monospace,
    SFMono-Regular,
    Menlo,
    Monaco,
    Consolas,
    monospace;

  font-size: 13px;
  font-weight: 700;

  letter-spacing: .4px;

  text-transform: uppercase;
}

#aparencia-form .color-editor::before {
  content: "HEX";

  display: flex;
  align-items: center;
  justify-content: center;

  height: 36px;

  padding: 0 9px;

  border-radius: 8px;

  background: #f0f4f2;

  color: #687671;

  font-size: 10px;
  font-weight: 800;
  letter-spacing: .6px;
}

#aparencia-form .form-field:has(input[type="color"]) {
  padding: 16px;

  border: 1px solid #e5ebe8;
  border-radius: 14px;

  background: #fbfcfc;

  transition:
    border-color .18s ease,
    background .18s ease,
    box-shadow .18s ease;
}

#aparencia-form .form-field:has(input[type="color"]):hover {
  background: #fff;

  border-color: #d5e1dc;

  box-shadow:
    0 4px 14px rgba(16,47,41,.04);
}

#aparencia-form .form-field:has(input[type="color"]) label {
  margin-bottom: 12px;
}


/* =========================================================
   APARÊNCIA — PREVIEW AO VIVO
   ========================================================= */

#aparencia-preview {
  position: relative;
  overflow: hidden;

  border: 1px solid #dfe7e3;
  border-radius: 20px;

  background: var(--preview-fundo, #ffffff);

  box-shadow:
    0 18px 50px rgba(20, 45, 38, .10);

  transition:
    background .2s ease,
    border-color .2s ease,
    box-shadow .2s ease;
}

/* Barra superior */

#aparencia-preview .preview-header {
  min-height: 58px;

  display: flex;
  align-items: center;
  justify-content: space-between;

  padding: 0 22px;

  background:
    color-mix(
      in srgb,
      var(--preview-fundo, #ffffff) 94%,
      var(--preview-principal, #1e6f5c)
    );

  border-bottom: 1px solid
    color-mix(
      in srgb,
      var(--preview-principal, #1e6f5c) 12%,
      transparent
    );
}

/* Logo */

#aparencia-preview .preview-logo {
  display: flex;
  align-items: center;
  justify-content: center;

  width: 42px;
  height: 42px;

  flex-shrink: 0;

  overflow: hidden;
}

#aparencia-preview .preview-logo img {
  display: block;

  width: 42px;
  height: 42px;

  max-width: none;
  max-height: none;

  object-fit: contain;

  transition:
    width .15s ease,
    height .15s ease;
}

/* Nome */

#aparencia-preview #preview-nome {
  color: var(--preview-texto, #17211f);

  font-size: 14px;
  font-weight: 800;
}

#aparencia-preview #preview-subtitulo {
  color:
    color-mix(
      in srgb,
      var(--preview-texto, #17211f) 60%,
      transparent
    );

  font-size: 11px;
}

/* Conteúdo principal */

#aparencia-preview .preview-hero {
  min-height: 280px;

  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;

  padding: 50px 30px;

  text-align: center;

  background:
    radial-gradient(
      circle at 50% 0%,
      color-mix(
        in srgb,
        var(--preview-principal, #1e6f5c) 13%,
        transparent
      ),
      transparent 58%
    ),
    var(--preview-fundo, #ffffff);
}

/* Título */

#aparencia-preview #preview-titulo {
  max-width: 680px;

  margin: 0;

  color: var(--preview-texto, #17211f);

  font-size: clamp(26px, 4vw, 42px);
  line-height: 1.08;
  font-weight: 850;
  letter-spacing: -.8px;
}

/* Descrição */

#aparencia-preview #preview-descricao {
  max-width: 590px;

  margin: 16px auto 0;

  color:
    color-mix(
      in srgb,
      var(--preview-texto, #17211f) 68%,
      transparent
    );

  font-size: 14px;
  line-height: 1.7;
}

/* Botões */

#aparencia-preview .preview-actions {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-wrap: wrap;

  gap: 10px;

  margin-top: 26px;
}

#aparencia-preview .preview-button {
  min-height: 42px;

  padding: 0 18px;

  border-radius: 10px;

  font-size: 12px;
  font-weight: 800;

  cursor: default;

  transition:
    transform .15s ease,
    box-shadow .15s ease;
}

#aparencia-preview .preview-primary {
  border: 1px solid var(--preview-principal, #1e6f5c);

  background: var(--preview-principal, #1e6f5c);

  color: #fff;

  box-shadow:
    0 7px 18px
    color-mix(
      in srgb,
      var(--preview-principal, #1e6f5c) 25%,
      transparent
    );
}

#aparencia-preview .preview-secondary {
  border: 1px solid
    color-mix(
      in srgb,
      var(--preview-principal, #1e6f5c) 30%,
      transparent
    );

  background: transparent;

  color: var(--preview-principal, #1e6f5c);
}

/* Rodapé */

#aparencia-preview .preview-footer {
  padding: 14px 22px;

  border-top: 1px solid
    color-mix(
      in srgb,
      var(--preview-texto, #17211f) 8%,
      transparent
    );

  color:
    color-mix(
      in srgb,
      var(--preview-texto, #17211f) 55%,
      transparent
    );

  font-size: 10px;

  text-align: center;
}

/* Indicador */

#aparencia-preview::before {
  content: "PRÉ-VISUALIZAÇÃO AO VIVO";

  position: absolute;
  top: 12px;
  right: 14px;

  z-index: 10;

  padding: 5px 8px;

  border-radius: 999px;

  background:
    color-mix(
      in srgb,
      var(--preview-principal, #1e6f5c) 12%,
      #ffffff
    );

  color: var(--preview-principal, #1e6f5c);

  font-size: 8px;
  font-weight: 900;
  letter-spacing: .5px;

  pointer-events: none;
}

/* Responsivo */

@media (max-width: 650px) {

  #aparencia-preview .preview-header {
    padding: 0 14px;
  }

  #aparencia-preview .preview-hero {
    padding: 38px 18px;
  }

  #aparencia-preview .preview-actions {
    flex-direction: column;
    width: 100%;
  }

  #aparencia-preview .preview-button {
    width: 100%;
  }

}


/* COMBO 3 - LAMINA */

/* Visual profissional da página individual */

.slide-header {
  margin-bottom: 28px;
}

.slide-title {
  gap: 24px;
}

.slide-title h1 {
  letter-spacing: -1.5px;
}

.viewer-section {
  margin-top: 10px;
}

.viewer-layout {
  gap: 24px;
  align-items: start;
}

.viewer-container {
  overflow: hidden;
  border-radius: 18px;
  background: #101614;
  box-shadow: 0 18px 45px rgba(0,0,0,.12);
}

.viewer {
  min-height: 520px;
}

.viewer-controls {
  border-radius: 12px;
  padding: 6px;
  backdrop-filter: blur(10px);
}

.viewer-controls button {
  transition:
    transform .18s ease,
    background .18s ease;
}

.viewer-controls button:hover {
  transform: translateY(-2px);
}

.info-panel {
  position: sticky;
  top: 24px;

  border-radius: 18px;

  box-shadow:
    0 10px 30px rgba(0,0,0,.06);

  transition:
    transform .2s ease,
    box-shadow .2s ease;
}

.info-panel:hover {
  transform: translateY(-2px);

  box-shadow:
    0 15px 35px rgba(0,0,0,.09);
}

/* Mobile */

@media (max-width: 900px) {

  .viewer-layout {
    grid-template-columns: 1fr;
  }

  .info-panel {
    position: static;
  }

  .slide-title {
    flex-direction: column;
    align-items: flex-start;
  }

}

@media (max-width: 600px) {

  .slide-header {
    margin-bottom: 20px;
  }

  .slide-title h1 {
    font-size: 2rem;
    letter-spacing: -1px;
  }

  .viewer-container {
    border-radius: 14px;
  }

  .viewer {
    min-height: 330px;
  }

  .info-panel {
    padding: 18px;
    border-radius: 14px;
  }

}


/* ============================================================
   MARCAÇÕES DAS ESTRUTURAS DA LÂMINA
   ============================================================ */

.estrutura-overlay {

  pointer-events: auto;

  overflow: visible;

}


.estrutura-marcacao {

  position: relative;

  width: 100%;

  height: 100%;

  overflow: visible;

}


.estrutura-caixa {

  position: absolute;

  inset: 0;

  border: 3px solid #ff3b30;

  background: rgba(255, 59, 48, 0.10);

  border-radius: 8px;

  box-shadow:
    0 0 0 1px rgba(255,255,255,.8),
    0 0 18px rgba(255,59,48,.45);

}


.estrutura-label {

  position: absolute;

  left: 50%;

  top: -10px;

  transform:
    translate(-50%, -100%);

  padding: 6px 10px;

  border-radius: 7px;

  background: #ff3b30;

  color: white;

  font-size: 13px;

  font-weight: 700;

  line-height: 1.2;

  white-space: nowrap;

  box-shadow:
    0 4px 14px rgba(0,0,0,.28);

  pointer-events: none;

}


.estrutura-marcacao:hover
.estrutura-caixa {

  border-color: #fff;

  background:
    rgba(255, 59, 48, 0.18);

}


.estrutura-marcacao:hover
.estrutura-label {

  background: #c62828;

}


/* Em telas pequenas */

@media (max-width: 700px) {

  .estrutura-label {

    font-size: 11px;

    padding: 4px 7px;

  }

}




/* =====================================================
   MARCADORES DAS ESTRUTURAS HISTOLÓGICAS
   ===================================================== */

.estrutura-overlay {
  pointer-events: none;
  overflow: visible;
  z-index: 50;
}

.estrutura-marcacao {
  position: relative;
  width: 100%;
  height: 100%;
  min-width: 18px;
  min-height: 18px;
  pointer-events: none;
}

.estrutura-ponto {
  position: absolute;
  inset: 0;
  border: 3px solid #ff3b30;
  border-radius: 50%;
  box-sizing: border-box;
  background: rgba(255, 59, 48, 0.08);
  box-shadow:
    0 0 0 2px rgba(255,255,255,0.85),
    0 0 14px rgba(255,59,48,0.65);
}

.estrutura-label {
  position: absolute;
  left: 50%;
  top: -34px;
  transform: translateX(-50%);

  padding: 5px 9px;

  background: rgba(20, 20, 20, 0.92);
  color: white;

  border-radius: 7px;

  font-size: 13px;
  font-weight: 700;

  white-space: nowrap;

  box-shadow:
    0 4px 14px rgba(0,0,0,0.35);

  border: 1px solid rgba(255,255,255,0.15);
}


/* ============================================================
   COMBO 5 - ESTRUTURAS INTERATIVAS
   ============================================================ */

.structure {
  cursor: pointer;
  transition:
    transform 0.2s ease,
    box-shadow 0.2s ease;
}

.structure:hover {
  transform: translateX(3px);
}

.structure.estrutura-selecionada {
  transform: translateX(5px);
  box-shadow:
    0 0 0 2px rgba(255,255,255,0.12),
    0 8px 24px rgba(0,0,0,0.12);
}

.estrutura-overlay {
  cursor: pointer;
}

.estrutura-overlay .estrutura-ponto {
  transition:
    transform 0.2s ease,
    box-shadow 0.2s ease;
}

.estrutura-overlay.estrutura-selecionada .estrutura-ponto {
  transform: scale(1.5);
  box-shadow:
    0 0 0 6px rgba(255,255,255,0.22),
    0 0 22px rgba(255,255,255,0.75);
}

.estrutura-overlay.estrutura-selecionada .estrutura-label {
  transform: scale(1.06);
  transform-origin: left center;
}


/* ============================================================
   COMBO 6 - NAVEGACAO ENTRE ESTRUTURAS
   ============================================================ */

.estrutura-navegacao {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  margin-top: 10px;
  margin-bottom: 10px;
}

.estrutura-nav-button {
  width: 38px;
  height: 34px;
  border: 0;
  border-radius: 8px;
  cursor: pointer;
  font-size: 18px;
  line-height: 1;
  transition:
    transform 0.18s ease,
    opacity 0.18s ease,
    box-shadow 0.18s ease;
}

.estrutura-nav-button:hover:not(:disabled) {
  transform: scale(1.06);
}

.estrutura-nav-button:disabled {
  opacity: 0.35;
  cursor: not-allowed;
}

.estrutura-contador {
  min-width: 125px;
  text-align: center;
  font-size: 13px;
  font-weight: 600;
  white-space: nowrap;
}


/* ============================================================
   COMBO 7 - MODO ESTUDO
   ============================================================ */

.estrutura-estudo-panel {
  position: absolute;
  left: 20px;
  bottom: 20px;
  width: min(360px, calc(100% - 40px));
  z-index: 30;
  padding: 18px;
  border-radius: 14px;
  background: rgba(15, 15, 15, 0.94);
  color: white;
  box-shadow:
    0 15px 45px rgba(0, 0, 0, 0.35);
  backdrop-filter: blur(12px);
}

.estrutura-estudo-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}

.estrutura-estudo-eyebrow {
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 0.12em;
  opacity: 0.65;
}

#estrutura-estudo-fechar {
  width: 30px;
  height: 30px;
  border: 0;
  border-radius: 50%;
  cursor: pointer;
  font-size: 20px;
  line-height: 1;
}

.estrutura-estudo-panel h3 {
  margin: 10px 0 6px;
  font-size: 20px;
}

.estrutura-estudo-panel p {
  margin: 0;
  line-height: 1.55;
  font-size: 14px;
  opacity: 0.8;
}

@media (max-width: 700px) {

  .estrutura-estudo-panel {
    left: 10px;
    right: 10px;
    bottom: 10px;
    width: auto;
  }

}



===== backup-reparador-completo-20260819-212337\src\data\laminas.js =====
export const laminas = [
  {
    id: 'epitelio-teste',
    nome: 'Epitélio de revestimento',
    categoria: 'Tecido Epitelial',

    imagem: '/images/laminas/teste/lamina-teste.png',

    descricao:
      'O tecido epitelial é formado por células muito próximas entre si, formando revestimentos e participando de funções como proteção, absorção e secreção.',

    tecnica: 'Microscopia óptica',
    coloracao: 'Coloração histológica',

    estruturas: [
      {
        nome: 'Núcleo',
        descricao: 'Material genético da célula'
      },
      {
        nome: 'Citoplasma',
        descricao: 'Região entre o núcleo e a membrana'
      },
      {
        nome: 'Membrana celular',
        descricao: 'Limite da célula'
      }
    ],

    video: '',
    quiz: true
  }
]


===== backup-reparador-completo-20260819-212337\src\lib\supabase.backup-admin-20260819-165208.js =====
import { createClient } from '@supabase/supabase-js'

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL
const supabasePublishableKey = import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY

export const supabase = createClient(
  supabaseUrl,
  supabasePublishableKey
)


===== backup-reparador-completo-20260819-212337\src\lib\supabase.js =====
import { createClient } from '@supabase/supabase-js'

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL
const supabasePublishableKey = import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY

export const supabase = createClient(
  supabaseUrl,
  supabasePublishableKey
)

export const ADMIN_USER_ID =
  'a59b0b71-111b-4c2e-9deb-39018d5ffd75'

export async function isAdmin() {

  const {
    data: {
      user
    }
  } =
    await supabase.auth.getUser()

  return !!user &&
    user.id === ADMIN_USER_ID

}



===== backup-reparador-completo-20260819-212337\src\pages\admin-style-import.js =====
import './admin.css'


===== backup-reparador-completo-20260819-212337\src\pages\admin.backup-admin-20260819-165208.js =====
import { renderAparencia, setupAparencia } from './aparencia.js'
import './admin.css'

import { supabase } from '../lib/supabase.js'

export async function renderAdmin() {

  const { data: { user } } =
    await supabase.auth.getUser()

  if (!user) {
    window.location.hash = '#login'
    return ''
  }

  const { data: laminas, error } =
    await supabase
      .from('laminas')
      .select('*')
      .order('created_at', { ascending: false })

  if (error) {
    console.error('Erro ao carregar lâminas:', error)

    return `
      <div class="admin-page">
        <div class="admin-error">
          Não foi possível carregar as lâminas.
        </div>
      </div>
    `
  }

  const lista = laminas || []

  const publicadas =
    lista.filter(lamina => lamina.publicado === true).length

  const ocultas =
    lista.filter(lamina => lamina.publicado !== true).length

  return `
    <div class="admin-page">

      <header class="admin-header">

        <div class="admin-brand">

          <a href="#inicio" class="admin-logo">
            🔬
          </a>

          <div>
            <strong>Atlas Histológico</strong>
            <small>Painel administrativo</small>
          </div>

        </div>

        <div class="admin-user">

          <span>
            ${escapeHtml(user.email || 'Administrador')}
          </span>

          <button id="logout-admin" class="admin-logout">
            Sair
          </button>

        </div>

      </header>


      <div class="admin-layout">

        <aside class="admin-sidebar">

          <button
            class="admin-menu active"
            data-section="laminas"
          >
            🔬
            <span>Lâminas</span>
          </button>

          <button
            class="admin-menu"
            data-section="categorias"
          >
            🗂️
            <span>Categorias</span>
          </button>

          <button
            class="admin-menu"
            data-section="aparencia"
          >
            🎨
            <span>Aparência</span>
          </button>

          <button
            class="admin-menu"
            data-section="configuracoes"
          >
            ⚙️
            <span>Configurações</span>
          </button>

          <div class="admin-sidebar-bottom">

            <a href="#inicio">
              ← Voltar ao site
            </a>

          </div>

        </aside>


        <main class="admin-content">

          <section
            id="admin-section-laminas"
            class="admin-section active"
          >

            <div class="admin-title-row">

              <div>

                <span class="eyebrow">
                  GERENCIAMENTO
                </span>

                <h1>
                  Lâminas
                </h1>

                <p>
                  Cadastre, edite e controle a publicação das
                  lâminas do Atlas.
                </p>

              </div>

              <a
                href="#nova-lamina"
                class="button primary"
              >
                + Nova lâmina
              </a>

            </div>


            <div class="admin-stats">

              <div class="admin-stat">

                <span>🔬</span>

                <div>
                  <strong>${lista.length}</strong>
                  <small>Total</small>
                </div>

              </div>


              <div class="admin-stat">

                <span>🟢</span>

                <div>
                  <strong>${publicadas}</strong>
                  <small>Publicadas</small>
                </div>

              </div>


              <div class="admin-stat">

                <span>⚪</span>

                <div>
                  <strong>${ocultas}</strong>
                  <small>Ocultas</small>
                </div>

              </div>

            </div>


            <div class="admin-toolbar">

              <div class="admin-search">

                🔎

                <input
                  id="admin-search"
                  type="search"
                  placeholder="Buscar lâmina..."
                >

              </div>

              <select id="admin-status-filter">

                <option value="todas">
                  Todas
                </option>

                <option value="publicadas">
                  Publicadas
                </option>

                <option value="ocultas">
                  Ocultas
                </option>

              </select>

            </div>


            <div
              id="admin-laminas-list"
              class="admin-laminas-list"
            >

              ${renderLaminas(lista)}

            </div>

          </section>


          <section
            id="admin-section-categorias"
            class="admin-section"
          >

            <div class="admin-title-row">

              <div>

                <span class="eyebrow">
                  ORGANIZAÇÃO
                </span>

                <h1>
                  Categorias
                </h1>

                <p>
                  Gerencie as categorias utilizadas pelas lâminas.
                </p>

              </div>

            </div>


            <div class="admin-placeholder">

              <div>🗂️</div>

              <h2>
                Gerenciamento de categorias
              </h2>

              <p>
                Essa área será ativada na próxima etapa.
              </p>

            </div>

          </section>


          ${await renderAparencia()}


          <section
            id="admin-section-configuracoes"
            class="admin-section"
          >

            <div class="admin-title-row">

              <div>

                <span class="eyebrow">
                  SISTEMA
                </span>

                <h1>
                  Configurações
                </h1>

                <p>
                  Configurações gerais do Atlas.
                </p>

              </div>

            </div>


            <div class="admin-placeholder">

              <div>⚙️</div>

              <h2>
                Configurações gerais
              </h2>

              <p>
                Essa área será configurável diretamente pelo
                administrador.
              </p>

            </div>

          </section>

        </main>

      </div>

    </div>
  `
}


function renderLaminas(laminas) {

  if (!laminas.length) {

    return `
      <div class="admin-empty">

        <div>
          🔬
        </div>

        <h2>
          Nenhuma lâmina encontrada
        </h2>

        <p>
          Comece cadastrando sua primeira lâmina.
        </p>

        <a
          href="#nova-lamina"
          class="button primary"
        >
          + Nova lâmina
        </a>

      </div>
    `
  }


  return laminas.map(lamina => {

    const publicada =
      lamina.publicado === true


    return `
      <article
        class="admin-lamina-card"
        data-id="${lamina.id}"
        data-nome="${escapeHtml(
          lamina.nome || ''
        ).toLowerCase()}"
        data-publicado="${publicada}"
      >

        <div class="admin-lamina-thumb">

          ${
            lamina.imagem_url

              ? `
                <img
                  src="${escapeHtml(lamina.imagem_url)}"
                  alt="${escapeHtml(lamina.nome || 'Lâmina')}"
                >
              `

              : `
                <div class="admin-no-image">
                  🔬
                </div>
              `
          }

        </div>


        <div class="admin-lamina-info">

          <div class="admin-lamina-top">

            <span class="admin-category">
              ${escapeHtml(
                lamina.categoria || 'Histologia'
              )}
            </span>

            <span
              class="admin-status ${
                publicada
                  ? 'published'
                  : 'hidden'
              }"
            >

              ${
                publicada
                  ? '● Publicada'
                  : '○ Oculta'
              }

            </span>

          </div>


          <h2>
            ${escapeHtml(
              lamina.nome || 'Lâmina sem nome'
            )}
          </h2>


          <p>
            ${escapeHtml(
              lamina.descricao || 'Sem descrição.'
            )}
          </p>


          <div class="admin-tags">

            ${
              lamina.tecnica
                ? `<span>🔬 ${escapeHtml(lamina.tecnica)}</span>`
                : ''
            }

            ${
              lamina.coloracao
                ? `<span>🧫 ${escapeHtml(lamina.coloracao)}</span>`
                : ''
            }

          </div>

        </div>


        <div class="admin-lamina-actions">

          <a
            href="#lamina/${lamina.id}"
            class="admin-action view"
            title="Visualizar"
          >
            👁️
          </a>


          <button
            class="admin-action edit"
            data-action="edit"
            data-id="${lamina.id}"
            title="Editar"
          >
            ✏️
          </button>


          <button
            class="admin-action toggle"
            data-action="toggle"
            data-id="${lamina.id}"
            title="${
              publicada
                ? 'Ocultar'
                : 'Publicar'
            }"
          >
            ${
              publicada
                ? '🙈'
                : '🟢'
            }
          </button>


          <button
            class="admin-action delete"
            data-action="delete"
            data-id="${lamina.id}"
            title="Excluir"
          >
            🗑️
          </button>

        </div>

      </article>
    `
  }).join('')
}


export function setupAdmin() {

  const search =
    document.querySelector('#admin-search')

  const statusFilter =
    document.querySelector('#admin-status-filter')


  const cards =
    () => [
      ...document.querySelectorAll(
        '.admin-lamina-card'
      )
    ]


  function filtrar() {

    const texto =
      (search?.value || '')
        .trim()
        .toLowerCase()

    const status =
      statusFilter?.value || 'todas'


    cards().forEach(card => {

      const nome =
        card.dataset.nome || ''

      const publicada =
        card.dataset.publicado === 'true'


      const textoOK =
        !texto ||
        nome.includes(texto)


      const statusOK =
        status === 'todas' ||
        (
          status === 'publicadas' &&
          publicada
        ) ||
        (
          status === 'ocultas' &&
          !publicada
        )


      card.style.display =
        textoOK && statusOK
          ? ''
          : 'none'

    })
  }


  search?.addEventListener(
    'input',
    filtrar
  )


  statusFilter?.addEventListener(
    'change',
    filtrar
  )


  setupAparencia()

  document
    .querySelectorAll('.admin-menu')
    .forEach(menu => {

      menu.addEventListener(
        'click',
        () => {

          const section =
            menu.dataset.section


          setupAparencia()

  document
            .querySelectorAll('.admin-menu')
            .forEach(item =>
              item.classList.remove('active')
            )


          document
            .querySelectorAll('.admin-section')
            .forEach(item =>
              item.classList.remove('active')
            )


          menu.classList.add('active')


          document
            .querySelector(
              `#admin-section-${section}`
            )
            ?.classList.add('active')

        }
      )

    })


  document
    .querySelectorAll(
      '[data-action="toggle"]'
    )
    .forEach(button => {

      button.addEventListener(
        'click',
        () => togglePublicacao(
          button.dataset.id
        )
      )

    })


  document
    .querySelectorAll(
      '[data-action="delete"]'
    )
    .forEach(button => {

      button.addEventListener(
        'click',
        () => excluirLamina(
          button.dataset.id
        )

      )

    })


  document
    .querySelectorAll(
      '[data-action="edit"]'
    )
    .forEach(button => {

      button.addEventListener(
        'click',
        () => editarLamina(
          button.dataset.id
        )

      )

    })


  document
    .querySelector('#logout-admin')
    ?.addEventListener(
      'click',
      async () => {

        await supabase.auth.signOut()

        window.location.hash =
          '#login'

      }
    )
}


async function togglePublicacao(id) {

  const { data: lamina, error: buscarErro } =
    await supabase
      .from('laminas')
      .select('publicado')
      .eq('id', id)
      .single()


  if (buscarErro || !lamina) {

    alert(
      'Não foi possível localizar a lâmina.'
    )

    return
  }


  const novoEstado =
    lamina.publicado !== true


  const { error } =
    await supabase
      .from('laminas')
      .update({
        publicado: novoEstado,
        updated_at: new Date().toISOString()
      })
      .eq('id', id)


  if (error) {

    console.error(error)

    alert(
      'Erro ao alterar a publicação da lâmina.'
    )

    return
  }


  window.location.hash =
    '#admin'
}


async function excluirLamina(id) {

  const confirmar =
    confirm(
      'Tem certeza que deseja excluir esta lâmina? Esta ação não pode ser desfeita.'
    )


  if (!confirmar) return


  const { error } =
    await supabase
      .from('laminas')
      .delete()
      .eq('id', id)


  if (error) {

    console.error(error)

    alert(
      'Erro ao excluir a lâmina.'
    )

    return
  }


  alert(
    'Lâmina excluída com sucesso.'
  )


  window.location.hash =
    '#admin'
}


function editarLamina(id) {

  window.location.hash =
    `#nova-lamina?editar=${id}`

}


function escapeHtml(value = '') {

  return String(value)

    .replaceAll('&', '&amp;')

    .replaceAll('<', '&lt;')

    .replaceAll('>', '&gt;')

    .replaceAll('"', '&quot;')

    .replaceAll("'", '&#039;')
}






